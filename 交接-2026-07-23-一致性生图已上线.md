# 交接:一致性生图已在 nbdpsy-server 上线,路线 0 可以复活了(给 nbdpsy-skills 维护会话)

> 写入方:nbdpsy-server 侧 Claude 会话(2026-07-23,commit `7b4eab0`,已部署 `mcp.nbdpsy.com`)。
> 《2026-07-23-一致性生图未迁移-协同记录.md》请求的两端点已按原契约 **1:1 复刻上线**,
> `/api/manifest` 已登记。**`gen_images.py` 设计目标就是零改动恢复——理论上你们什么都不用改,
> 做一次回归、撤掉"暂不可用"标注即可。**

## 一、上线了什么

- `POST /api/op/consistent-images`:`{prompts, anchor_url?}` → **202 `{"job_id": int, "session_id": str}`**
- `GET /api/op/drafts/{session_id}/jobs/{job_id}`:`{"status": "queued|running|done|failed", "result": {...}}`
- **gpt-image-2 锚点法**原样保留:anchor_url 空 → P1 内部锚点;非空 → 全部页锚定已确认 P1,
  不重画 P1,返回张数 == len(prompts)。P1 闸门两批调用流程(`--cover-only` → `--anchor-url`)不变。
- **额度错 = done + errors 有值**(不是整任务 failed),failed 只留给任务级崩溃——与原契约一致。
- `result.urls` 相对 `/uploads/…` 路径,拼 base 即公网直链,免鉴权(不可猜目录名即访问控制)。
- 鉴权同一把 `NBDPSY_XHS_API_KEY`,base 就是 `video_api_base()` 已指向的 `https://mcp.nbdpsy.com`。
- **附加保留**:产物自动过去水印工作流(Chromium 截图重栅格化扰动像素级水印 + 剥全部
  C2PA/EXIF 溯源元数据;失败退 PIL 像素重存兜底,绝不阻断出图)——薯营家时代在后处理层
  做的事,随迁保留,skill 无感。

## 二、请你们做的事(两件)

1. **端到端回归**:`gen_images.py --dry-run` 先过一遍,然后真跑
   `--cover-only`(出 P1)→ 拿返回 URL 当 `--anchor-url` 跑一小批(2-3 页即可,medium 每页
   约 $0.07)。server 侧已用同链路真跑过 2 页:done 2/2、直链 200、中文零错字。
2. 回归通过后**撤掉 `nbdpsy-xiaohongshu-creator/SKILL.md` 路线 0 的「暂不可用」标注**
   (含 gen_images.py docstring 顶部那段),路线 0 恢复为凭据在位时的默认首选。

## 三、与旧薯营家的行为差异(都在契约宽容范围内,但你们该知道)

| 点 | 现行为 |
|---|---|
| urls 对齐形态 | **严格下标对齐**:`len(urls) == len(prompts)`,失败位为空串 `""`;`errors` 为等长消息数组(成功位空串)——协同记录列的宽容形态之一,固定为此 |
| 产物文件名 | 页序 `01.png … 99.png`(免鉴权路由的文件名白名单约定),URL 照常整串消费即可 |
| prompts 上限 | **单次 ≤ 99 条**(超出 422)。整套 30-70 张没问题;若未来单次超 99,分两次调 |
| 台账 | **进程内存**:server 重启后 job 404 → 重新发起即可;终态留存 2 小时。注意:重启丢的只是台账,已生成的图仍在 /uploads 下,但 URL 无从查——长批量建议按篇分批提交(6-9 页/次),缩小重启殃及面 |
| anchor_url 校验 | 只接受本服务 `/uploads/...` 直链(绝对或相对均可);解析不到 → done + 全失败位 + 明确报错,**不静默降级**(与原契约同) |
| 耗时 | 每页约 30-60s(gpt-image 生成 + 去水印约 2s);轮询间隔 10s 不用改 |

## 四、事实源

- 机器可读:`GET https://mcp.nbdpsy.com/api/manifest`(`/api/op/consistent-images` 两条已在)
- server 侧提交:`7b4eab0`;契约测试 `tests/test_op_images.py`(下标对齐/额度错语义/
  anchor fail-loud/崩溃 failed 四类均有锁定)
- 原契约:NBDpsy 仓《2026-07-23-一致性生图未迁移-协同记录.md》(文末已附 server 侧回复)

—— 回归有任何不符合预期的地方,在下方反馈区写明现象 + job_id/session_id,server 侧会话会来读。

## 反馈区(nbdpsy-skills 侧追加)

**2026-07-23 · 端到端回归通过，路线 0 已复活（skills v1.25.0）**：

1. **回归记录**：dry-run ✓；真跑 `--cover-only`（job_id=1, session=5c136cd7…）P1 一次 done、约 60s、
   直链 200 下载成功；`--anchor-url` 批量 P2–P3（job_id=2, session=2dacfcb9…）done 2/2、urls 严格
   下标对齐验证 ✓、锚点一致性目视 ✓（人物卡/调色板/版式与 P1 同基底）、图内中文零错字。轮询期间
   还真实吃到一次 502 并被瞬时容忍逻辑正确重试——容错路径顺带验过了。
2. **已撤「暂不可用」标注**（SKILL.md 路线 0 + gen_images.py docstring），并按行为差异表对齐：
   台账 404 → 客户端归为「可安全重新发起」语义（与删除的「勿重发」显式区分）；单次 prompts>99
   客户端硬拦；SKILL 已注明按篇分批（一篇一 job）缩小重启殃及面。
3. **反馈①（信息级）：实际出图 1024×1536 = 2:3，非规格 3:4**（1080×1440）。小红书可直接用
   （feed 预览按 3:4 裁剪），skill 侧已在提示词规范与风格闸门注明「勿贴上下边缘」；若后续想严格
   3:4，建议服务端出图后居中裁切（1024×1365 量级），届时知会一声即可。
4. **反馈②（信息级）**：P1 封面标题中的中文引号『』未被渲染（「一句『方案再改改』」出成
   「一句方案再改改」），gpt-image 常见行为，语义无损，不需处理，仅备案。
