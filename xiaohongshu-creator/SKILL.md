---
name: xiaohongshu-creator
description: 把 seo-artical-creator 产出的心理科普 pillar 长文（或任意一篇官网博客长文）拆分成多篇可直接发小红书的图文笔记——每篇约 300 字正文 + 一套 6–9 页「PPT 式」轮播插图，每页给出页面文字与可直接喂给 Gemini/GPT 的中文绘图提示词（图中要显示的中文文字直接写进提示词里）。只要用户提到「拆成小红书 / 发小红书 / 小红书笔记 / 小红书图文 / 把这篇博客或长文改成小红书 / 出几条小红书 / 小红书配图提示词 / RED 笔记 / 长文拆短文配图」，即使没说「拆分」或「配图」字样，也应使用本 skill。本 skill 是 NBDpsy 专用（绑定其品牌话术、心理科普合规红线与固定视觉品牌基底），承接 seo-artical-creator 的长文产物；不适用于其它站点的泛化小红书写作。
---

# 小红书图文笔记创作（从 pillar 长文拆分）

承接 `seo-artical-creator` 产出的心理科普 pillar 长文，把一篇 4000+ 字的长文拆成 **5–8 条可直接发小红书的图文笔记**。每条笔记 = 一段 **~300 字正文** + 一套 **6–9 页"PPT 式"轮播插图**（每页给出"页面文字"+"中文绘图提示词"，提示词把要显示在图上的中文直接写进去，复制即可喂给 Gemini/GPT 出图）。

**项目背景一句话**：NBDpsy 是咨询师全员北大硕博的纯线上华人心理咨询工作室。官网 pillar 长文负责被搜索引擎/AI 引擎收录；小红书负责触达正在 emo、正在内耗、正在搜"怎么办"的真人。长文重信源与循证，小红书重共情与钩子——本 skill 的使命是把前者的可信内容，翻译成后者的语言，**绝不在翻译中编造新事实**。

## 三条贯穿全程的铁律（理解动机，别只背规则）

1. **不新增未验证事实**。统计数字、专家引语、出处只能沿用 pillar 原文里已核实过的内容。短文里出现的每个数字，都要能在源长文里找到。小红书没有参考文献区，但"编造一个数据被扒出来"对一个心理工作室是致命的——所以宁可少写一个数字，不可造一个。
2. **合规红线高于传播效果**。心理/健康类是小红书重点审核领域：极限词（最/第一/根治/100%/彻底）违广告法、医疗词（治疗/治愈/诊断）非医疗机构禁用、站外导流（微信/二维码/加群）直接限流封号。详见 `references/xiaohongshu-spec.md`，发布前用脚本扫一遍。
3. **小红书无 API，本 skill 只产出草稿**。最终由人工把正文+标签复制进小红书、把每页提示词喂给 Gemini/GPT 出图后发布。所以交付时要把"怎么发"也讲清楚。

多步任务先建 TodoWrite 看板，每步勾掉。

---

## 完整流程（六步，每步都有验证闸门）

```
1. 定位长文 + 规划拆分   → 验证：5–8 篇，每篇一个独立钩子、彼此不重叠、合起来覆盖原文核心
2. 写正文（每篇 ~300 字）→ 验证：count_xhs.sh 字数达标 + 钩子/分点/CTA 齐 + 不新增未验证事实
3. 设计配图轮播（6–9 页）→ 验证：每页复用固定品牌基底 + 图中文字短 + 首页是钩子封面
4. 合规校验             → 验证：check_compliance.sh 全绿 + 危机声明每篇在位 + 语义自检过
5. 落盘                → 验证：一篇一文件写入 seo-geo/content/xiaohongshu/{slug}/，含 00-overview
6. 交付提醒（人工发布） → 验证：给出文件路径 + 发布要点（首图最重要/3:4/标签/危机声明保留）
```

### 第 1 步 · 定位长文 + 规划拆分

**输入**：pillar 的 slug 或文件路径。默认在 `seo-geo/content/drafts/{slug}.md`（也接受任意一篇官网博客长文）。先把全文读完——尤其是 TL;DR、各 H2、对比表、统计块、FAQ，这些是拆分的原料。

**规划 5–8 篇短文**：按 H2 主题**智能聚合**，不是机械每个 H2 一篇。判据：每篇围绕**一个能独立成立的钩子角度**（如「CPTSD 和 PTSD 到底差在哪」「童年情感忽视：那道看不见的伤」「复杂性创伤的 5 个自救步骤」）。太薄的 H2 合并、太厚的拆开。爆款角度参考 `references/xiaohongshu-spec.md` 的"选题口吻"。

把拆分大纲写进 `00-overview.md`（每篇：序号 / 钩子标题 / 对应原文哪些 H2 / 核心 takeaway / 拟用统计或引语）。

**验证**：每篇有独立钩子、彼此不重叠、合起来覆盖原文核心论点；篇数落在 5–8（原文确实太短才可少于 5，并说明理由）。

### 第 2 步 · 写发布文案（正文 ~300 字 + 可见标签）

每篇 post 的第一个内容块就是**复制即发的"发布文案"单元**——这是用户真正粘进小红书输入框的东西。块标题用 `## 发布文案（复制这一段直接发布）`，里面依次是：① 正文主体 ② 危机声明一行 ③ **可见的 `#标签` 行**。

> **标签必须可见**：标签除了写进 frontmatter 的 `hashtags`（供机器读取），**必须同时作为可见文本 `#标签1 #标签2 …` 附在文案末尾**。frontmatter 在网页/编辑器渲染时不显示，只放 frontmatter 等于「看不到标签、也没法复制」。小红书惯例标签贴在正文末尾。

打开 `references/xiaohongshu-spec.md` 对照写。骨架记忆点：

- **首句 3 秒钩子**：用痛点/场景/数字开场（"总觉得自己和别人不一样？"），抓不住人就划走了。
- **总-分-总**：分点用 `·` 或编号，每段 2–4 行、配 1 个克制的 emoji（治愈系不要满屏 emoji）。
- **口吻**：第一人称共情 + 干货清单 + 自救指南，口语化，像朋友说话。**共情先于科普**（这是心理内容区别于普通种草文的关键）。
- **结尾 CTA**：提问引评论 / "收藏不迷路" / "评论区抱抱"。
- **标题 ≤20 字**：数字体 / 痛点 / 悬念 / 身份标签四类钩子任选。
- **标签 5–10 个**：关联（精准品类如 #心理科普 #CPTSD）+ 热度（蹭当下）+ 长尾（细分/身份/地域，如 #留学生心理 #情绪内耗）。同时写进 frontmatter `hashtags` 与文案末尾可见标签行。

**字数默认 300 汉字**（用户指定值，只数正文主体、不含标签）。提醒：小红书爆款正文常在 500–800 字、信息更厚更易被收藏；如想提收藏率可把默认调到 500–600，但默认遵守 300。

**验证**：`bash .claude/skills/xiaohongshu-creator/scripts/count_xhs.sh <post.md> 300` 字数达标；发布文案块含可见标签行；第 1 步规划的钩子有落地；正文里的数字都能在源长文找到出处。

### 第 3 步 · 设计配图轮播（每篇 6–9 页）

每条笔记配一套"像 PPT 一样"的轮播图。**核心规则见 `references/illustration-spec.md`**，记忆点：

- **每页 = 一页 PPT**：① 页面文字（大标题 ≤12 字 + 至多 3 条要点、每条 ≤16 字）② 中文绘图提示词。
- **图中文字直接写进提示词**：你用的 Gemini/GPT 能渲染中文，所以把这页要显示的中文用引号写进提示词，并指定位置与层级。文字越短渲染越准。
- **首页 = 钩子封面**（最重要，决定点击率）；中间页 = 一页一个要点；末页 = 行动建议 + 品牌一句话 + 危机声明。
- **全套复用固定品牌基底**：每页提示词都以同一段"风格基底"开头（莫兰迪低饱和治愈系插画 + 固定主色 + 固定人物卡），保证 6–9 张风格一致、整个号调性统一。

**验证**：每页提示词都含品牌基底；图中文字够短；首页是钩子封面而非内页；页数落在 6–9。

### 第 4 步 · 合规校验（发布前必须全绿）

```bash
# 高置信违禁词扫描（极限词/医疗违禁/站外导流）+ 危机声明在位检查
bash .claude/skills/xiaohongshu-creator/scripts/check_compliance.sh seo-geo/content/xiaohongshu/{slug}/
```

脚本测不出的**语义自检**（见 `references/xiaohongshu-spec.md` 的禁用词替换表）：

- 禁用「治疗/诊断/治愈/医院/医生」做自我描述或承诺 → 改「陪伴/梳理/觉察/自我调节/缓解」。
- 禁极限词「最/第一/根治/100%/彻底/绝对」。
- 禁站外导流：正文、评论、简介、图中都不能出现微信号/二维码/加群/外链（小红书会限流封号）。
- **危机声明每篇在位**（放正文末或末页）：`本文为心理科普，不构成诊断或治疗；如持续困扰请寻求专业帮助，全国统一心理援助热线 12356`。

### 第 5 步 · 落盘

写入 `seo-geo/content/xiaohongshu/{pillar-slug}/`：

```
00-overview.md     拆分大纲（第 1 步产物）
post-01.md         frontmatter + ## 发布文案（正文 + 危机声明 + 可见标签行）+ ## 配图轮播
post-02.md         ...
post-0N.md
```

一篇一文件，方便人工逐条复制去发。单篇文件格式见 `assets/example-xhs-note.md`（黄金范例，对照学习）。

### 第 6 步 · 生成预览页 + 交付提醒（人工发布）

**先生成预览页**——把拆分产物渲染成一个自带一键复制按钮的网页，区分"给人读"与"复制给 AI"两类内容：

```bash
python3 .claude/skills/xiaohongshu-creator/scripts/render_preview.py seo-geo/content/xiaohongshu/{slug}/ seo-geo/content/xiaohongshu/{slug}/preview.html
```

- **发布文案/页面文字** → 暖纸阅读 UI，标签做成 pill，配「复制文案」按钮（整段粘进小红书）。
- **绘图提示词** → 深色等宽面板「保持原样」，每段右上角「复制」按钮（粘给 Gemini/GPT）。
- 给用户这个 `preview.html` 的路径，让他读文案、逐段复制提示词出图——比直接看 markdown 文件顺手得多。

交付时告诉用户：

- 文件路径与篇数 + `preview.html` 路径。
- **怎么发图**：把每页"中文绘图提示词"喂给 Gemini/GPT 出图，导出 **1080×1440（3:4 竖版）**，首页当封面，按 P1→PN 顺序上传。第 1 张定稿后可作参考图喂给后续页，保证风格一致（见 `references/illustration-spec.md`）。
- **发布要点**：首图最关键、标签贴够、危机声明保留、不要在任何位置放微信/二维码导流。

---

## 关键文件

| 用途 | 路径 |
|------|------|
| 笔记结构规格 + 合规红线 + 禁用词替换表 | `references/xiaohongshu-spec.md` |
| 插图提示词体系（固定品牌基底 + 文字入图 + 单页模板 + 多页一致性） | `references/illustration-spec.md` |
| 单篇笔记黄金范例（从 CPTSD pillar 拆出，对照学习） | `assets/example-xhs-note.md` |
| 正文汉字计数 + 区间判定 | `scripts/count_xhs.sh` |
| 高置信违禁词扫描 + 危机声明在位检查 | `scripts/check_compliance.sh` |
| 渲染预览页（发布文案 UI + 提示词一键复制） | `scripts/render_preview.py` |
| 源长文（输入） | `seo-geo/content/drafts/{slug}.md` |

## 红线速记（违反任一即不合格）

1. 不新增未验证事实——数字/引语只沿用源 pillar，短文里的每个数字都能在原文找到。
2. 不用「治疗/诊断/治愈/医院/医生」自我描述；不用极限词；不夸大效果。
3. 任何位置都不放站外导流（微信/二维码/加群/外链）——小红书限流封号红线。
4. 危机声明（全国统一心理援助热线 12356）每篇在位。
5. 全套轮播图复用同一品牌基底，风格一致；图中文字短、不编造专业名词拼写。
