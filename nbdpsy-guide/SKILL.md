---
name: nbdpsy-guide
description: NBDpsy 内容运营工具包的「上手向导 + 客服台」，新运营不知道能干啥、怎么开始时用它。当用户说「教我用 / 怎么用 / 这个能干啥 / 帮我上手 / 新手引导 / 有哪些功能 / help / 我该做什么 / 从哪开始」，或说「更新工具包 / 更新 nbdpsy-skills / 升级 skill / 装最新版」，或问长文怎么做成小红书「拆成小红书还是做文字版 / 这篇适合哪种 / 做成文字版 / 整篇发别拆 / 拆成上中下」，或问小红书运营操作「怎么装插件 / 怎么登录小红书账号 / 我有哪些账号 / 账号还能用吗 / 怎么发小红书 / 发布到小红书 / 怎么看笔记数据 / 拉取分析笔记数据」，或说「我要更新我小红书的风格 / 改一下我的风格 / 换个调性 / 我的风格跟别人不一样 / 我的风格档案 / 回到上个版本的风格 / 风格改回去 / 我想要两套风格 / 图文和文字版风格不一样 / 再建一套风格 / 把这套参考图风格存起来 / 给风格改个名 / 以后默认用文字版那套 / 我有几套风格」，或说「更新默认配置 / 改默认配置 / 把默认风格改成… / 以后新人都用这套 / 新人的风格是哪来的 / 我改了默认配置谁会变 / 默认配置是什么」时，用本 skill。它介绍九个内容创作/运营技能能做什么、怎么串成产线，手把手带做第一个任务，逐条给出 nbdpsy 小红书运营 API 的操作命令（装插件 / 登录 / 看账号 / 验活 / 发布 / 拉数据分析），并管理每个运营自己的风格档案（读取 / 更新 / 版本回退）与全公司的默认配置（运营老大更新）。是非专业兼职运营的第一站；具体创作/发布仍由对应 skill 执行。
---

# NBDpsy 内容运营 · 上手向导

运营对你说「教我用 / 能干啥 / 帮我上手 / 从哪开始」，或问某个具体操作（装插件、登录、发小红书、看数据…）时，
按本向导应对。**你的角色是热情、耐心的向导**：先讲清能干啥，再问运营想先做哪件，然后要么带他做、
要么把对应 skill 接过来执行。运营多半是非技术兼职人员——**别甩命令让他自己敲，命令由你替他跑**，
只在需要他本人动手（扫码、点浏览器）时才一步步指路。

> **路径约定**：下面命令里
> `PUB` = `~/.claude/skills/nbdpsy-xiaohongshu-creator/scripts/publish_note.py`
> `SP` = `~/.claude/skills/nbdpsy-xiaohongshu-creator/scripts/style_profile.py`
> `TV` = `~/.claude/skills/nbdpsy-youtube-transport/scripts/transport_video.py`
> `COMMON` = `~/.claude/skills/nbdpsy-content-pipeline/scripts/nbdpsy_common.py`
> （Windows 把 `~` 换成 `%USERPROFILE%`、`/` 换成 `\`，`python3` 换成 `python`。）

---

## 第 1 步 · 先自检环境（每次上手先跑，10 秒）

同时跑这两条，把结果读给运营听：

```bash
python3 COMMON doctor                 # 本机凭据齐不齐（xhs_ready / 博客 / 豆包）
python3 PUB --self-check              # 小红书 API：连通性 + 身份 + 被授权账号 + 就绪
```

- 都绿 → 告诉运营「**你之前已经配好了，不用再发接入包**，直接开始就行」，进第 2 步问他想干啥。
  （凭据存在本机 `~/.config/nbdpsy/secrets.env`，永久保存、跨对话通用；只有换密钥时才需重发新包，
  重发会自动覆盖旧的。）
- `doctor` 缺 `NBDPSY_XHS_API_KEY` 或 `--self-check` 报 401 → **找系统管理员（在 NBDpsy 管理后台生成配置包的那位）要/重发「运营接入配置包」**，
  导入后重试（导入：把配置包整段存成 creds.txt，跑 `python3 COMMON secret import creds.txt`）。
- `--self-check` 报 `Host not allowed`/超时 → 沙盒拦网：`python3 COMMON sandbox allow` 后**重启 Claude**。
- `doctor` 还会报**本机工具包版本**（形如 `工具包 v1.41.0（b4c5812，装于 2026-07-28）`）：运营说「更新工具包」时先看这行，
  比最新版落后、或显示「版本标记缺失」（旧安装器装的），都是跑一次 `python3 COMMON update` 就好
  （覆盖式安装，秒级，不动凭据；详见下面「更新工具包」节的四条路径与 ⛔ 禁令）。

**不管是本来就全绿、还是刚 `secret import` 完配置包，紧接着做第 1.5 步读他的风格档案**，别跳过——
他的笔记与配图都按那份档案做。

---

## 第 1.5 步 · 读一遍他的风格档案（凭据一确认可用就做）

**每个运营有自己一份风格档案**（底色质感 / 配色 / 人物形象 / 语气 / 每页字数…），
不是全公司共用一套。写小红书、出配图、事后质检都按**他这一份**来，所以上手先认一遍。

**一个人可以有好几套风格**（比如「**图文那套**」画配图时用、「**文字版那套**」把正文排版成文字版时用），
所以**先看他有几套、默认是哪套，再回读默认那套的内容**——两条命令都由你跑：

```bash
python3 SP --list-profiles     # 先看：他有几套、默认哪套
python3 SP --get               # 再读：默认那套的完整内容（不带参数 = 默认那套）
```

**先说「你有几套」**（照下面两种情形开口）：

| `--list-profiles` 列出来 | 你怎么开口 |
|---|---|
| **只有一套**（或一条都没列出来——他还没有自己的档案） | **就当他只有一套，一个字都别提"多套"**——跳过这句，直接照下面回读那几句念 |
| **两套或更多** | 「**你有 {N} 套风格：图文那套、文字版那套（现在默认用图文那套）。我先把默认这套回读给你确认**」 |

> ⚠️ **老运营的档案多半只有一套**（还没分图文 / 文字版）——**这是正常的、不是缺东西**。别主动跟他说
> 「你少了文字版那套、要不要建」，把人吓住；他自己问起、或真要做文字版笔记时再讲第 3.6 步④。
> 套名一律照返回里的名字念（运营自己起的名可能叫「水墨风」「小清新」），别自作主张改叫法。

> ⛔ **返回里那个标形态的英文取值（`carousel` / `typeset`）只写在命令里，绝不念给运营听**——
> 一律翻成「**图文那套**」「**文字版那套**」。他是非技术兼职人员，念英文只会更懵。

> `--list-profiles` **非 0 退出就当他只有一套**，直接跑 `--get` 按下表处置，别为它单独编一套降级话术。
> 写操作要用的 `base_version` 以**读那一套时**的返回为准——**每套各有各的版本号，改哪套就用哪套的**。

**再回读默认那套**：看返回里的 **`exists`**，分三种情况开口。**下面这两句话逐字照说、别自行改写**——
说错会让运营以为默认配置已经是他自己的档案了：

| 情况 | 你怎么开口（照说） | 接着做什么 |
|---|---|---|
| `exists: true` | 「**上次用的是「{套名}」v{N}，这次照旧吗？还是换一套 / 调整？**」 | 先把这套档案**翻成人话**念给他（见下），再等他答。他说「照旧」→ 直接开工；「换一套」→ 第 3.6 步⑥；「不对 / 想改」→ 第 3.6 步① |
| `exists: false` | 「**你还没有自己的风格档案，先用默认配置，可以吗？**」 | 他说「可以」→ 就按默认配置开工；他说「我想要我自己的」→ 第 3.6 步①（先 `--init-sets` 把他现在跟着的风格原样建成他自己的，再改） |
| 命令 **exit 2**（没配 key / 连不上 / 超时） | 「**没连上风格档案服务，本次用 skill 内置的默认风格（默认配置）继续；等下次连上再核对**」 | 照常开工，别卡住产线。⚠️ 这句**必须说出口**，不许闷声降级 |
| 命令 **其它非 0 退出**（`exit 1` = 服务端报错，如 500） | **同上，说同一句话** | 同第 ③ 层处置。⚠️ 500 恰恰是"服务挂了"最常见的形态，别因为它不是 exit 2 就没了话术、自己发挥 |

> ⚠️ **「上次用的」指的是他现在默认那一套**（`--get` 读到的 active 套），**不是**跨批次记下来的
> 「上回你选了什么」——跨批「上次选择」存储（形态 + 套名 + 日期）还没落地。所以 ⛔ 别说成
> 「上次做视频那批你用的是…」，你没有那个数据；只把这一套是什么念清楚，然后问他这次照不照旧。

> `exists:false` 时返回的 `profile` 是**默认配置的值**，不是他的——所以第二句话不能说成
> 「这是你的档案」。没配 `NBDPSY_XHS_API_KEY` 的运营（接入包可选、装的时候能跳过）走第三行，照样能做内容。

> ⚠️ **用着默认配置的运营一定会问「我用的这套以后会不会变」——会。** 默认配置是**实时读的**，
> 不是他第一次读到时的快照：**默认配置一被改，所有还没建自己档案的人下次出图就跟着变**。想稳住就建一份
> 自己的（第 3.6 步①）——**一旦建了个人档案，默认配置再怎么改都影响不到他**。完整说明见第 3.6 步③。

响应里的 **`base_version` 就是下次 `--put` / `--rollback` 该传的那个数，直接用、别自己推**：
server **无条件下发**它（没有个人档案时给 `0`，有档案时给当前版本号），脚本原样透传。

**回读要说人话，别把 JSON 甩给运营。** 比如：

> 「你的风格是：暖米白纸质底 + 莫兰迪三色，人物是短发圆领毛衣女生，每页 150–250 字。对吧？」

⚠️ 这句只是**念法的样子**，里头的值取自**默认配置**。念的时候一律照**他自己 `profile` 里的实际取值**说——
别人的档案可能是完全不同的配色和人物（多号矩阵本来就该各长各的），照着上面这句念就念错了。
对照关系（**档案里没有的字段就别念**，别硬凑）：

| 档案字段 | 念成人话 |
|---|---|
| `visual.texture` / `visual.palette` | 什么底子 + 什么配色（「暖米白纸质底 + 莫兰迪三色」） |
| `visual.character_card` | 图里的人长什么样（「短发圆领毛衣女生」） |
| `visual.cover_layout` / `content_layout` | 封面和内容页大概什么版式 |
| `density.每页文字量` / `density.信息密度档位` | 每页多少字、信息密不密 |
| `tone.*` | 用第几人称、语气、emoji 多不多 |

⚠️ `density` 底下那五个 key 是**中文**的（`信息密度档位` / `每页文字量` / `每页信息点` / `版式档` /
`运营原话`）。念给运营听怎么说都行，**但在任何命令、任何文件里都不许把它们改写成英文**——创作端和
审查端都按这五个中文 key 读写，改名即断链。

---

## 第 2 步 · 这套工具能干啥（介绍给运营）

一句话：**给一个心理科普话题，AI 一条龙做成官网长文 → 小红书图文笔记 → 配图 → 竖屏短视频，
每步先过 AI 质检；也能单独用某一环，还能直接把小红书发出去、把长文发到微信服务号。** 九个技能：

| 想做的事 | 对运营怎么说 | 背后的 skill |
|---|---|---|
| 写一篇官网博客长文并发布 | 「写一篇 XX 主题的科普长文」 | nbdpsy-seo-artical-creator |
| 把长文拆成小红书图文笔记 + 配图提示词 | 「把这篇拆成小红书」 | nbdpsy-xiaohongshu-creator（路线①，默认） |
| **把长文整篇排版成竖版文字版**（不拆、不过 AI 生图，正文本身就是图） | 「**把这篇做成文字版**」 | nbdpsy-xiaohongshu-creator（路线②）｜长文太长可再拆「上/中/下」系列篇 |
| **给某位咨询师写小红书介绍**（推介笔记，从官网资料取材） | 「给 XX 咨询师写小红书介绍」 | nbdpsy-xiaohongshu-creator（咨询师场景）｜要放真人照片先问运营拿本人授权 |
| 把笔记做成带字幕的竖屏短视频 | 「把这篇做成视频」 | nbdpsy-text-to-video |
| **搬运一条 YouTube 视频**（翻译+配音+烧中文字幕，自动带品牌 logo） | 「搬运这个 YouTube 视频」+ 链接 | nbdpsy-youtube-transport |
| **把长文发到微信服务号**（一句话走完全程：写长文 → 改公众号版 → **出封面/插图** → 排版编译 → 发布或定时发；另有群发、菜单装修、台账、数据统计） | 「发公众号」「排版发服务号」「给公众号出封面」 | nbdpsy-fuwuhao-operator（见第 3.7 步） |
| 质检（找茬、查合规） | 自动在每步之间跑，不用单独喊 | nbdpsy-content-reviewer |
| **拆解别人的爆款**（笔记/视频号/新闻/录屏/音频 → 拆解素材单 + 模仿蓝图） | 「拆解这条视频/笔记」+ 链接或文件 | nbdpsy-content-teardown |
| **一句话全套**（最省事） | 「**做一期 XX 的全套内容**」 | nbdpsy-content-pipeline |
| 把做好的笔记**发到小红书** + 管账号 + 看数据 | 见第 3 步「小红书运营手册」 | 本向导 + publish_note.py |

**新手最推荐**：让运营给一个话题，直接触发 `nbdpsy-content-pipeline`（「做一期 XX 的全套内容」），
它会带 checkpoint 一步步跑完，运营只需在出图/发布等节点搭把手。

---

## 第 3 步 · 小红书运营手册（server 工具逐项操作）

发小红书靠自建的 nbdpsy-api（`https://mcp.nbdpsy.com`）+ 一个 Chrome 登录插件。九件事：

> **架构别搞错（2026-08-02 澄清）**：**发布、验活、拉数据、删除等所有操作，全部在后台 nbdpsy-server 上执行，与运营本机、与插件都没有关系**——运营的电脑关机了照样能发。
> **Chrome 插件只干一件事：扫码登录时把 cookie 录进服务端**（第 ①② 步）。录完这一票，插件的活就干完了。
>
> 由此推出三条排障纪律：
> - ⛔ **发布失败不要让运营去开浏览器、装插件、重扫码**——除非 `--check-cookie` 明确回 `invalid`/`captcha`（登录态真过期）。cookie 是 `valid` 却发不出去，问题在服务端，不在运营那台电脑。
> - ⛔ **别把发布失败说成"你本地浏览器没开"**。运营本机有没有开 Chrome、有没有那个无痕窗口，跟发布成败无关。
> - ✅ 服务端侧的问题（超时、未检测到成功标志、页面元素变化等）**只能由后端排查**，运营侧拿不到日志，该写需求给后端而不是继续在运营环境里试。

### ① 下载安装登录插件（只为录 cookie，不参与发布）

```bash
python3 PUB --extension-info          # 拿 download_url + 官方安装步骤 + server_time
```

把返回的 `install_steps` 翻译成人话逐条带运营做（默认口径，以 install_steps 为准）：
下载 `download_url` 的 zip → 解压到固定文件夹 → Chrome 开 `chrome://extensions` → 开「开发者模式」
→「加载已解压的扩展程序」选该文件夹 → 插件弹窗填 **serverUrl=`https://mcp.nbdpsy.com`** 与
**apikey**（接入包里那把）→ 详情页勾「在无痕模式下启用」。**记下这次的 `server_time`，下一步登录要用。**

### ② 登录小红书账号（扫码）

登录没有接口，靠运营本人扫码：让他在插件开的**无痕窗口**里打开小红书、用手机小红书 App 扫码。
**这是插件唯一被用到的场景**——扫完码、cookie 进了服务端，后续发布/验活/拉数据全在后台跑，不再碰这个浏览器。
你这边轮询确认：

```bash
python3 PUB --wait-login --since <上一步的 server_time>     # 登新号
python3 PUB --wait-login --since <server_time> --account-id <id>   # 重登某个旧号
```

`done=true` 即登录完成。新号登好后**让系统管理员在管理后台把它授权给这位运营**（首次导入者自动有权）。

### ③ 看现在有哪些可用账号

```bash
python3 PUB --list-accounts           # 列被授权的号 + cookie_status
python3 PUB --self-check              # 同时给身份 + 就绪判定（更全）
```

想要的号不在列表 = 没授权，找系统管理员在管理后台「调配账号」补授。
> `--self-check` 第一步就打 `whoami`（最便宜的连通 + key 校验），身份 `{name, role}` 已并入其输出，
> 不必再单独跑一条 whoami。

### ④ 检测账号可用性（cookie 验活）

```bash
python3 PUB --check-cookie <账号名或id>
```

六态：`valid`=能发；`unknown`=没验过（可发，稳妥起见发前验一下）；`invalid`/`captcha`=登录态失效，
回 ② 重新扫码；**`error`=检测本身失败 ≠ cookie 失效，别让运营白扫，稍后重测**；
**`restricted`=账号被小红书挂了风控验证墙**（cookie 好好的，回 ② 重扫码治不好）——让运营**用手机
小红书 App 扫码验证身份**后重新检测；若提示「请求太频繁」，先晾一阵别再操作该号。这类号会被
按 `valid` 筛选的逻辑自动排除，属期望行为。

### ⑤ 发布笔记到小红书

笔记与配图由 `nbdpsy-xiaohongshu-creator` 产出（`post-NN.md` + `images/post-NN/`）。
**发布前先和运营确认：发哪个账号、发哪几篇、立即还是定时。** 然后：

```bash
python3 PUB --note <note_dir>/post-01.md --account <账号名或id>
# 定时：加 --schedule "2026-07-16T09:00:00+08:00"（务必带 +08:00 时区）
```

发布是**异步**：命令会轮询到 `published` 并回 `note_url`（发给运营留档）。
`outcome=unknown`（网络抖动）→ **绝不重发**，按提示 `--job <id>` 复查到终态。多篇逐条串行发。

**发布时要把笔记归进对应的合集**（2026-08-07 运营定案）——同议题的笔记聚成一摞，读者点进主页能一次看好几篇，比散着强。做法：

```bash
python3 PUB --notes ... # 先查该号有哪些合集（每批查一次就行）
python3 ~/.claude/skills/nbdpsy-xiaohongshu-creator/scripts/note_ops.py --collections <账号>
# 发布时带上（id 从上一步按合集名匹配，⛔ 别写死——合集会新建会改名）
python3 PUB --note <稿> --account <账号> --collection-id <id> --collection-name <合集名>
```

跟运营说话时要讲清的两件事：

- **「加入合集」不是「提到合集」**：这个参数是把笔记**归拢进**那个合集、成为它的成员。（曾按「引用」理解，误把 52 篇科普笔记塞进了「咨询师简介」。）
- **「咨询师简介」只放咨询师推介笔记**，科普笔记一篇都不进；科普笔记进它自己的议题合集（CPTSD / 原生家庭 / 讨好型人格 / 社恐 / 恋爱脑 / 依恋类型 / 亲密关系 等，以该号平台上实际有的为准）。
- ⚠️ **挂错了没法用工具摘**（server 只有「加入」、没有「移出」），只能运营去创作后台一篇篇取消——所以挂之前核对一遍合集名。

### ⑥ 拉取并分析账号笔记数据

```bash
python3 PUB --notes <账号名或id>            # 读已落库的笔记快照（快，不重新导出）
python3 PUB --notes <账号名或id> --refresh   # 先触发一次导出拉最新数据再读（约 1–2 分钟）
```

拿到数据后，你（Claude）直接**读 JSON 帮运营分析**：哪几篇浏览/点赞/收藏高、选题与钩子的规律、
发布时段效果、给下一批选题的建议。

> ⛔ **`--notes` 返回的 `notes[]` 是「入库顺序」，不是按任何指标排的**（2026-07-27 服务端核实并修正了
> 此前 meta 里"按最新 views 降序"的错误说明）。所以**要找"哪几篇最高"，必须自己按指标排序**——
> 直接取 `notes[:5]` 当 Top5 拿到的是**最早入库的 5 篇**，不会报错、结论却是错的
> （服务端实测：真正的 Top1 排在第 4 位）。
> 要现成排好序的，调 `/note-trends`（它的 `notes` 才是按最新 views 降序）。
- **想拉最新数据**（比如刚发完想看有没有入库）就加 `--refresh`：它先让创作中心重新导出一遍
  （约 1–2 分钟）再读，比直接读快照新。
- 加了 `--refresh` 若返回 `no_data`（`available:false` + `no_data:true`）——这是**当天刚发的笔记**，
  数据看板次日才入数据，**不是故障**。跟运营说：「**今天刚发的笔记，数据明天才能拉到，不用管它**。」
> 说明：笔记数据接口**已上线**（创作中心导出无 note_id，业务主键是 (账号,标题,发布时间) 三元组）。
> 若 `--notes` 直接返回 404，多半是该号还没有导出快照——先跑一次 `--notes <账号> --refresh` 触发导出即可。

#### 📐 分析前必读：先读响应里的字段口径（2026-07-26）

**口径的真源是 API 响应本身**——`meta.field_meta` 里，**逐字段**给出：
`label`（平台列原名）、`desc`（平台官方说明**原文**）、`window`、`unit`、`source`（`platform` 原生列 / `derived` 我们算的）。
**读数前先读它**，别按字段名猜含义，也别照搬本文档里的记忆值——平台口径会变，响应里的才是当下的。

`field_meta` 现在 **`GET /api/accounts/{id}/notes` 与 `GET /api/accounts/{id}/note-trends` 两个端点都有**
（同一份口径块 `meta.{field_meta, field_notes}`，`notes` 的 `trend=daily` 路径同样带上），此前只有 note-trends 有。
`urls` 等既有字段语义不变。

> ⚠️ **`field_meta` 声明的字段比 `/notes` 实际下发的多**（2026-07-26 真机实测）：
> `field_meta` 有 19 个字段，其中 `like_rate` / `collect_rate` / `comment_rate` / `engage_rate` /
> `follow_rate` / `follow_rate_t1` 这几个**派生率只在 `/note-trends` 的 `notes[].rates` 里下发**；
> **`/notes` 的 `notes[]` 只有 15 个原始指标**（likes / collects / comments / danmu / shares / reposts /
> follows / exposure / views / cover_ctr / avg_view_duration 等），**没有 `rates` 这一层**。
> 所以：**要算率值、要 `follow_rate_t1`，就调 `/note-trends`**（`python3 PUB --notes <账号>` 走的是
> `/notes`，只有原始指标）。在 `/notes` 里找不到 `follow_rate_t1` **不是数据缺失、也不是你取值路径写错了**。

`window` 三个取值，**含义各不相同**：

| 取值 | 含义 |
|---|---|
| `T` | **实时**——截止目前 |
| `T-1` | **每天更新**——截至昨日 |
| `unknown` | 口径未核实。⚠️ **这不是默认值，别当成 `T` 或 `T-1` 去推算**（目前只有 `reposts` 是这一类，见硬提醒③） |

**各指标的实际 `window`**（server 真机核实，2026-07-26）：

| 字段 | 实际 `window` |
|---|---|
| `likes` 点赞 / `comments` 评论 / `collects` 收藏 / `shares` 分享 / `danmu` 弹幕 | **`T`** 实时 |
| `follows` 涨粉 / `avg_view_duration` 人均观看时长 | **`T-1`** 截至昨日 |
| `reposts` 转载 | **`unknown`** ——⛔ 不可用，见硬提醒③ |

**这 7 个字段不必再挂"口径未知"的免责声明**——它们早已核实，此前文档里"其余指标时间窗尚未逐个核实"的说法是旧快照，作废。
仍然要**先读响应里的 `field_meta`**、以它为准（平台口径会变），但别再无差别地给每个指标加口径存疑的注脚。

### ⛔ 三条硬提醒

**① 跨 `window` 的字段相除，得不到真实比率**——分子分母不在同一时间窗。已知实例：

- **`views / exposure` 算不出 `cover_ctr`**（`views` 是 T、`exposure` 与 `cover_ctr` 是 T-1）
- **`follow_rate` 在新笔记上不可用**（`follows` 是 T-1、`views` 是 T）——话术见下方「涨粉率怎么读」

**②（已核实·安全）`engage_rate` 没有跨窗问题。** `(likes + collects + comments) ÷ views` **四项同为实时 `T`、口径一致**，
我们之前"它可能踩了和 `cover_ctr` 同一个坑"的推测**已被证伪**。放心用，**别再给它加口径存疑的注脚**。

**③（写死）`reposts` 不可用于任何分析与结论。** 它恒为 0 **不是"没人转载"，是当前导出表压根没有这一列、根本没采到**。
⛔ 绝不能拿这个 0 去横向比较、算比率、下结论（"转载为 0 说明内容不值得转"就是典型误判）。
运营问起转载，直接说「**这项平台导出里没有，拿不到**」。

#### 📈 涨粉率怎么读：`follow_rate` 与 `follow_rate_t1`

`follow_rate = follows(T-1) ÷ views(T)`，分子分母跨窗。两条运营话术（server 用生产数据实测，照着说）：

1. **发布当天/次日这个数不能用**，而且**不是"偏低"是"无意义"**——`follows` 是 T-1 口径、
   还没覆盖到发布日，值必然趋近 0，**绝不要解读成"涨粉能力差"**。
2. **发布约两周以上可直接用**：实测中位偏差 0%、最大 6%。

（server 样本里 0–2 天、3–6 天两档各只有个位数条笔记，**新笔记证据弱**——别把某个单点百分比当结论去引用。）

**`follow_rate_t1` 是同窗口径的涨粉率**：`follows(T-1) ÷ 上一快照日的 views`，分子分母都对齐到昨日，
在 `notes[].rates` 里与 `follow_rate` 并排下发，`field_meta.follow_rate_t1` 有完整口径。要比较涨粉表现优先用它。

- **取不到时为 `null`**（这条笔记没有更早的快照日、或上一快照日 `views ≤ 0`）。这个键**恒定存在、不会时有时无**——
  读到 `null` 就是"这条算不出来"，不是字段缺失，跳过它、别当 0 参与计算。
- **不是精确值，会轻微偏高**：上一快照是昨天*某个时刻*采的，而 `follows` 覆盖到昨日 24:00，分子多覆盖了几小时。
  实测与 `follow_rate` 之差：老笔记 +0.0%~+0.9%，一条 2 天新笔记 +5.6%。
- **快照断档会把偏高放大**（生产上出现过 07-23 → 07-25 隔两天）：分母停在更早那天、分子仍到昨日，偏高按间隔放大。
  **间隔看 `series` 的 `snapshot_date` 与 `delta.days_between`**，发现断档就在结论里说明，别把放大后的值当真实涨幅。
- **只有一个快照日的新接入账号拿不到这个值**（整列 `null`），只能等快照攒够天数。
  ⚠️ 不要试图用 `exposure × cover_ctr` 去凑 T-1 的观看量——server 已实测证伪：该比值随笔记变老一路降到 0.30，
  说明观看包含大量非封面入口，凑不出来，**必须依赖快照历史**。

---

以下是 `cover_ctr` 这个已核实字段的完整成因，作为「跨窗相除会怎样」的样板（2026-07-26 悬停数据看板 ⓘ 抄的官方原文）：

| 字段 | 平台官方说明 | `window` |
|---|---|---|
| `exposure` 曝光 | 该篇笔记**截至昨日**获得的曝光数之和，每天更新 | `T-1` |
| `views` 观看 | 笔记**截止目前**的观看数，实时更新 | `T` |
| `cover_ctr` 封面点击率 | **截至昨日**的封面点击次数 ÷ 封面曝光次数（**视频下滑不计入**），每天更新 | `T-1 ÷ T-1`（内部自洽） |

**互算是可以的，但要知道自己在算什么。** `views/exposure` 与 `cover_ctr` 是两个不同口径的数，三处差叠加：① **时间窗错位**（`views` 是实时 T，`exposure` 与 `cover_ctr` 是 T-1，分子分母不同期）；② **分子定义不同**（CTR 分子是**封面点击次数**，点了秒退计点击、不计观看 → 点击 > 观看）；③ **CTR 排除视频下滑触发的曝光**，`views` 不排除。

所以想看"曝光转化"，**自己用 `views/exposure` 算一个就好，只是别叫它 CTR**，且只在同一批、同期笔记之间横向比。`cover_ctr` 则用于比"哪篇封面更抓人"。

⚠️ **两者对不上是正常的，别据此判定数据有误。** 偏差方向还会相反，很容易被误读成"数据自相矛盾"：
- **还在涨观看的新笔记** → 实算被抬高（实时分子 ÷ 昨日分母），显得 `cover_ctr` 偏低
- **不再涨的老笔记** → 时间窗影响小，但"点了不看"的差额显出来，显得 `cover_ctr` 偏高

`cover_ctr` 是平台原生字段，服务端只做单位归一（值在 (0,1) 时 ×100），不自行计算——列错位、换算、平台数据三种嫌疑已逐项排查排除（服务端 `bf43171`）。

### ⑦ 改定时 / 撤稿（发出去之前还能改）

运营说「改一下定时 / 提前发 / 先别发了 / 撤掉那篇」时：**先列任务确认，再改或撤**。
只有 `pending`（排队中/定时未到期/发布失败等待自动重试）的任务能改能撤；一旦 `publishing`（已在发）或已终态就拦不住了。发布失败还在自动重试、想拦住重试的，就用撤稿（`--cancel`）。

```bash
python3 PUB --list-jobs --status pending          # 先看有哪些待发任务，认准 job_id
python3 PUB --reschedule <job_id> --schedule "2026-07-16T09:00:00+08:00"   # 改定时（务必带 +08:00）
python3 PUB --reschedule <job_id> --schedule now  # 清空定时→立即发
python3 PUB --cancel <job_id>                     # 撤稿（先别发了）
```

- `--list-jobs` 也可加 `--account <号>` 只看某个号、`--status`/`--limit` 过滤。
- 改/撤只带用户要改的那一项（改期就只动定时，不碰标题正文图片）。
- 返回 `ok:false` = 任务已在发或已终态，**改不动了**——需要就重新发一篇新的。

### ⑧ 删除已发布的笔记（不可逆，发出去之后的清理）

运营说「把 XX 那篇删了 / 发重了清一下」时用这个。⑦ 的撤稿是拦**还没发出去**的任务，
本节删的是**已经发到小红书上**的笔记（按标题删，创作中心导出无 note_id，标题就是业务主键）。

> **🔴 铁律：删除不可逆，触发前必须把「哪个账号 + 完整标题 + 删几篇」逐字复述给运营、
> 得到明确确认，才执行。** 删错了**恢复不了**。

```bash
python3 PUB --delete-note --account <账号名或id> --title "完整标题"          # 删 1 篇
python3 PUB --delete-note --account <账号名或id> --title "完整标题" --count N  # 同题多篇删 N 篇
```

- 标题**精确匹配**（容忍卡片末尾的截断省略号）。删前拿不准标题有没有敲对，先 `--notes <账号>` 核对。
- **`--count` 用法（清理重复发布）**：同一篇发重了出现 2–3 篇同题笔记时，`--count` = **多余篇数、留 1 篇**
  （发重了 3 篇 → `--count 2`，删 2 留 1）；上限 10。返回的 `remaining` 是删完还剩几篇同题。
- 命令是**异步**，会自己轮询到终态（约 1–2 分钟）：
  - `outcome=done` → 删成功，`deleted` 是删掉几篇；`remaining>0` 会提示还剩几篇同题。
  - `outcome=failed`：`reason` 含 `note_not_found`=该号没有这个标题的笔记（标题没匹配上，先 `--notes` 核对）；
    含 `need_manual_login`=creator 登录态失效，回②重新扫码后再试。
  - `outcome=unknown` 的处置（共同铁律：**绝不盲目重发**——删了没删不确定，重发可能多删）：
    - **轮询超时** → `python3 PUB --delete-status <deletion_id>` **重查终态**——`deleted`/`remaining`
      是权威判据；删除台账已落库（2026-07-23 起），server 重启也不丢、随时可查。
    - **接口回 `unknown`**（server 重启恰好打断了删除执行的那约一分钟，结果真实未知）→ 让运营人工去
      创作中心看一眼该标题剩几篇，确认后再决定。
    - **404 = deletion_id 不存在**（台账落库后 404 不再意味着「重启丢了」）→ 多半是 ID 敲错或从未发起，
      核对后重查；实在不确定就 `--notes <账号> --refresh` 核对剩几篇（⚠️ 当天刚发的看板次日才有数据）。
- 同一账号的发布/验活/导出/删除**共享一把锁自动串行**——别对同一个号同时发起多件，会排队（不会坏，
  只是轮询显得久）。

### ⑨ 图床上传（可选）

要把本地图片先传成公网直链（比如复用素材、或不想走笔记目录发图）时：

```bash
python3 PUB --upload-images <目录或多个图片路径>    # 1–18 张，返回 batch_id + urls + expires_at
python3 PUB --list-uploads                        # 列自己未过期的批次
```

传目录会自动按文件名排序收图；返回的 `urls` 就是**可直接用于发布的公网直链**，默认 **7 天**后过期。

---

## 第 3.5 步 · 搬运 YouTube 视频（server 新能力）

把一条 YouTube 视频搬成**带中文字幕/配音的成片**——服务端全自动下载→转写→翻译→豆包配音→
烧中文字幕→加 NBDpsy 品牌 logo 与片头版权声明。**用的是同一把运营接入凭据**（`NBDPSY_XHS_API_KEY`），
配过小红书发布就能直接搬，不用另外要凭据。

```bash
python3 TV --url "https://www.youtube.com/watch?v=xxxx"   # 建任务 + 轮询到完成（几分钟级）
python3 TV --job 42            # 查进度/取产物   python3 TV --list  # 列我的任务
python3 TV --retry 42          # 重试失败        python3 TV --delete 42  # 删除
```

- 只认 **youtube.com / youtu.be** 链接；别的站点直接告诉运营「本搬运只支持 YouTube」。
- 完成后 `products` 里是**免鉴权公网链接**：`video_url`（成片）、中文/英文字幕、中英双语逐字稿——
  把 `video_url` 回给运营即可播放/下载；要本地留档加 `--download`。
- `outcome=unknown` 或轮询超时 = **仍在跑，别重新提交**，用 `--job <id>` 复查（重复提交会重复搬运）。
- 配音音色默认服务端牧羊音色，一般不用管；需要换加 `--voice`。
- **再制作（remake）**：运营说「做一版自制的 / 版权更稳的」→ 加 `--mode remake`（画面像素全部自产、
  台词按品牌口吻重写；**仅适合黑底卡片/几何动画类原片**（如 EMDR），真人出镜的别用；全链约
  30–60 分钟，提前告知运营）。发布时简介须附成片 meta 里的 `attribution` 文案，重要成片人工终审。
- **修订成片（revise，可反复迭代）**：运营看完 remake 成片说「XX 那句改得更温暖点 / 结尾加一句祝愿」→
  `python3 TV --revise <任务号> --instructions "他的原话"`。增量重制只重做改动部分（几分钟到十几分钟），
  改完还能对修订版**再修订**，直到满意。⚠️ 只有 remake 成片能修订，普通搬运片不行（要改就重搬或转 remake）。

---

## 第 3.6 步 · 改我的风格 / 退回上个版本 / 更新默认配置 / 多来几套风格

运营说「我要更新我小红书的风格」「改一下我的风格」「换个调性」「回到上周那版」时走 ①②；
**运营老大**说「**更新默认配置**」「把默认风格改成…」「以后新人都用这套」时走 ③；
运营说「**我想要两套风格**」「图文和文字版不一样」「把这套存起来」「给这套改个名」「以后默认用文字版那套」时走 ④⑤⑥。
**命令你替他跑**，他只要用大白话说想改成什么样就行。

### ① 更新我的风格

> **🔴 铁律：存一套风格是「整套覆盖」，不是「改一个字段」。**
> 哪怕只改一个颜色，也必须**先读那一套的全量、在全量上改、再把这一套完整发回去**。
> 只发被改的那几个字段 = 把这套里其余字段全清空。

> ⚠️ **他有好几套风格时，先问清「改哪一套」**：「是改**图文那套**还是**文字版那套**？」
> **只有一套的运营不用问这句**，直接往下。

**一读一写对得上**：`--get` 给的就是那一套、`--put` 发回去的也是那一套，
**多套单套一个写法**——读全量 → 改 → 整份发回去，没有别的花样。

四步走，一步都别省：

**A · 先读那一套的全量**

```bash
python3 SP --list-profiles   # 先看他有几套、默认哪套（只有一套就跳过下面那条 --profile）
python3 SP --get             # 读**默认那套**的全量，并记下响应里的 base_version

# 要改的不是默认那套时，改读那一套（base_version 也用这条读到的）：
python3 SP --get --profile "<套名>"
```

⚠️ 认 `base_version` 这个字段，别去找 `version`，更别自己推：server 无条件下发它
（没档案时给 `0`、有档案时给该套当前版本号），脚本原样透传——响应里那个数就是唯一真源，
照抄，不要自行换算。**每套各有各的版本号，读哪套就配哪套的 `base_version`。**

⚠️ **他还没有自己的档案时（`exists:false`）**：他读到的是**全公司的默认配置**、不是他的，
直接在这上面改是改不动的。要给他建一份自己的，先跑：

```bash
python3 SP --init-sets       # 照默认配置有几套，在他名下原样建齐几套（内容原样继承）
```

建完他就跟默认配置脱钩了（往后老大再改默认配置也影响不到他），然后回 A 重读、照 B→C→D 改。
⛔ 这一步等于给他建档案，**动手前先拿到他明确点头**（怎么开口见 ④C 的 `exists:false` 那条）。

**B · 跟运营确认改哪几项**（先把现在的档案用人话回读一遍，见第 1.5 步）

问他「哪几项要改、改成什么」，然后**逐条复述让他点头**再动手，别替他发挥。比如：

> 「那我改这两项：配色从莫兰迪三色换成低饱和莓果色系；人物从短发圆领毛衣女生换成长发戴眼镜的女生。
> 其他（纸质底、每页 150–250 字、语气）都不动。对吗？」

**C · 整份发上去**

把 A 读到的那一套的**完整内容**（`--get` 输出里 `profile` 那一段）存成一个 json 文件，
只把他点头的字段替换掉、其余原样保留，然后：

```bash
python3 SP --put /tmp/profile-new.json --base-version <A 读到的 base_version> --note "运营原话摘要"

# 改的不是默认那套时，跟 A 读的时候点同一套：
python3 SP --put /tmp/profile-new.json --profile "<套名>" \
  --base-version <A 读到的 base_version> --note "运营原话摘要"
```

⛔ **读哪套就写哪套**，别拿 A 套读到的内容去覆盖 B 套。

**D · 存完先读 `dropped_keys`，再跟运营报喜**

PUT 的响应里有一个 `dropped_keys`，列出**这次整份覆盖把哪些字段冲掉了**（server 替你比对上一版，
比自己在本地猜可靠）：

- **`dropped_keys: []` = 正常，什么都没丢。** 这个键**恒定存在**（首次建档也给 `[]`），无条件读它，
  别当它可能缺失、也别把"没丢"写成"未知"。
- **非空**（如 `["tone", "visual.character_card"]`）= 这次把「语气设定」「人物卡」冲掉了。
  ⛔ **别闷头往下走，当场翻成人话回读给运营确认**：
  > 「这次会丢掉这几项：语气设定、人物卡。确定吗？」

  他说"不是有意的" → 从 `--version <上一版号>`（不是默认那套就再带 `--profile "<套名>"`）把被丢的字段取回来，补进全量里，重新走一遍 C（覆盖已经
  发生了，重新 `--get` 拿到的是刚存进去那份缺字段的新版，光重发救不回来）；他说"就是要删" → 照常往下。
- ⚠️ `dropped_keys` 只比**两层**（顶层 + 二级），顶层整段消失只报顶层名、不展开里头的子项。
  它**只告知、不拦截**——server 已经存进去了，回读确认是你的活，不是它的。

成功后把新版本号告诉他，并说清楚：**从现在起你的笔记和配图都按新档案做**（已经做完的老批次不受影响，
它们各自记着当时用的是哪一版）。

**两种失败照下面处置：**

- **exit 3 / 409（档案在别处被改过了）——绝对不要把同一份 body 再发一遍。** 照实说：
  > 「你的风格档案在别处被改过（v{你读到的} → v{响应里的 `detail.current_version`}），我重新读一遍再改。」

  然后**回到 A 重新 `--get`**，在**新拿到的全量**上重做修改，用**新的** `base_version` 再发一次。
- **exit 2（没配 key / 连不上）**：告诉运营「没连上风格档案服务，这次存不进去，先按现在的风格做，
  等连上了我再帮你存」——**别假装存成功了**。

### ② 退回上一个版本

运营说「回到上个版本 / 上周那版 / 改回去 / 还是原来那样好」时：

**每套风格各有各的版本历史，回退是「退这一套」。** 他有好几套时先问清「退哪一套」，
然后下面每条命令都带上同一个 `--profile "<套名>"`（改的是默认那套就不用带）：

```bash
python3 SP --versions          # ① 列历史版本：把**版本号 + 时间（created_at）+ 备注（note）**念给运营，让他挑一个
                               #    这三样服务端都给全，照念就行，不用含糊成「第几版、记不清什么时候改的」
python3 SP --versions --limit 50 --offset 50   # ①' 版本多时翻页（默认一页 50 条、上限 200，超了自动钳到 200 不报错）
python3 SP --version <N>       # ② 挑中的那版先预览，同样用人话回读，问他「是这版吗」
python3 SP --get               # ③ 拿当前的 base_version（①② 都不产出这个数，必须单独跑一次）
python3 SP --rollback <N> --base-version <③ 拿到的 base_version>   # ④ 他确认了再回退
```

`--versions` 的响应带 `total` / `limit` / `offset` / `has_more`：**`has_more: true` 就是还有更早的版本没取到**，
把 `--offset` 往后加一页再取一次；先跟运营说清「一共 {total} 版，我先念最近这 {limit} 版」，别让他以为只有这些。

⚠️ **`--base-version` 填的是「现在最新是第几版」，不是「要退回到第几版」**——后者是 `--rollback` 那个 `N`。
两个数填反了不会报错，会撞 409，而 409 的提示语是**无条件文案**「你的风格档案在别处被改过」——
明明没人改过，你却会照着这句去跟运营说，绕一圈才发现是自己填错了。所以 ③ 那一步不能省。

**一定要告诉运营这句：回退不会把中间的版本抹掉——它是把老版本的内容再存成一个新版本。
所以退错了还能再退回来，你现在这版也留着、不会丢。** 运营听懂这点才敢放手试。

⚠️ **回退只退你点名的那一套，别的套原样不动**（"默认用哪套"也不受影响）。所以他有好几套时，
动手前把退的是哪一套复述一遍：
> 「这会把你**图文那套**退回那天的样子，文字版那套不动。可以吗？」

回退同样可能撞 409（别处刚改过）：**不重试**，按上面①的说法重新 `--get` 拿当前 `version` 再来。

**回退的响应同样带 `dropped_keys`**，规矩跟 ①D 一模一样：`[]` 就正常往下；**非空**说明退回的那版里没有
现在这版才有的字段（后来加的会被退没），**当场念给运营确认**「退回这版会丢掉：语气设定、文字色，确定吗？」。
他说不是有意的，就别硬退——改成 `--version <N>` 把老版内容取出来、跟当前全量合并后走 ① 的 `--put`。

### ③ 更新默认配置（运营老大才用；但每个运营都该知道它会变）

**触发**：运营老大说「**更新默认配置**」「把默认风格改成…」「以后新人都用这套」「默认那套换个调性」时进这里。

> ⚠️ **先分清他嘴里的「默认」是哪个**：本节的「**默认配置**」是**全公司**没建个人档案的人共用的那一份（只有运营老大能改）；
> ⑥ 的「**默认用哪套**」是**某个人**在自己那几套风格里挑一套当默认（只影响他一个人）。
> 运营说「把默认改一下」时含糊，**问一句**「是改你自己默认用哪套风格，还是改全公司新人的那份默认配置？」再动手。

**默认配置是一份独立的配置**，不是谁的个人档案——**没建个人档案的运营，用的就是这一份**
（第 1.5 步 `exists:false` 那行）：

```bash
python3 SP --admin-default /tmp/default-config.json --note "本次改动说明"
```

- **谁能改**：打的是 `PUT /api/style-profile/admin-default`，**只有运营老大（`role=admin`）能用（`require_admin`），
  一般用户（`role=operator`）调会 403**。**任何一位运营老大都能改它**（它不挂在谁名下，不是某个人的档案）。
  一般用户问起就照说「默认配置由运营老大维护，你改不了；但你可以建一份自己的，建了就归你自己管」。
  ⚠️ 这个 403 脚本回 **exit 1** 并明说「只有运营老大能改默认配置」——**别把它当成"没连上"去念第 1.5 步那句降级话术**，
  服务好好的，只是这个人不是运营老大。
- 同样是**整套覆盖**（先读全量 → 在全量上改 → 整份发回去）、version 自增，但**不带 `--base-version`**：
  它没有乐观锁，也就不会撞 409——正因为如此，两个运营老大同时改会后写覆盖先写，**改之前先招呼一声**。
- **一次改一套**，跟改个人档案一个写法：`--admin-default` 覆盖的是默认配置里**默认那套**的内容。

**动手前先把这三条讲给运营老大听（波及面他得先知道）：**

1. 🔴 **改了它，所有没有自己风格档案的人立刻跟着变——不是只影响之后新建的人。**
   那些人每次 `--get` 都**实时读**最新的默认配置，不是他们建档/首次读到时的快照：
   **你一按下去，他们下一批图就是新样子**。所以别当成"给新人准备的模板"随手改。
   （`--get` 在 `exists:false` 时会带 `admin_default_version`，可以顺口告诉那位运营「你现在用的是默认配置第 N 版」。）
2. **已经建了个人档案的运营各自独立、完全不受影响**——默认配置怎么改都动不了他那份。所以运营担心
   「我的风格会不会哪天被人改掉」，答案是：**建了自己的就不会**。
3. ⚠️ **默认配置不进版本历史表、不可回退**——`--versions` / `--rollback` 对它**无效**（那两个只管个人档案）。
   改坏了没有后悔药。
   **所以改之前先留底，用这条**（服务端 2026-07-27 新增的只读端点，谁都能读、不需要运营老大）：

   ```bash
   python3 SP --get-default > /tmp/默认配置-留底-$(date +%F).json
   ```

   ⛔ **别用 `SP --get` 当留底手段**——它读的是**你自己的档案**：你要是建过个人档案，拿到的就是你那份、
   根本不是默认配置，而命令照样 exit 0、不会报错，等于以为留了底其实留错了。
   （只有在你自己**没建**个人档案、`exists:false` 时，`--get` 读到的才恰好是默认配置——
   半对半错，所以别用它。留底一律用 `--get-default`。）

⚠️ **运营老大改自己的风格 ≠ 改默认配置，这是两份东西。**
用 ① 的 `--put` 改的是**他自己那份个人档案**，跟默认配置毫无关系，没建档案的新人**不会**跟着变；
要让新人也用上他这套，得**再显式更新一次默认配置**：把他那一套 `--get` 出来当全量
（不是默认那套就 `--get --profile "<套名>"`），走本节的 `--admin-default` 发一次。
反过来同理：改了默认配置，他自己的个人档案纹丝不动。

### ④ 再建一套风格（图文一套、文字版一套，各管各的）

**触发**：运营说「我想要两套风格」「图文和文字版不一样」「再建一套」「把我发的这几张参考图的风格存起来以后用」。

**先用一句话讲清这是什么**（别念术语）：

> 「你可以有好几套风格：**图文那套**用来画配图（AI 生图那种），**文字版那套**用来把正文排版成文字版。
> 做哪种笔记就自动用哪套，互不打架。」

> ⚠️ **④⑤⑥ 都是写操作，失败处置与 ① 一模一样**：撞 **exit 3 / 409**（档案在别处被改过）→
> ⛔ 绝不把同一条命令原样再跑一遍，回去重新 `--get` 拿**新的** `base_version` 再来；
> **exit 2**（没配 key / 连不上）→ 照实说「没连上风格档案服务，这次存不进去，先按现在的风格做」，
> **别假装存成功了**。

**A · 先看他现在有几套 + 拿 `base_version`**

```bash
python3 SP --list-profiles     # 现有套名，顺便查重名
python3 SP --get               # 记下 base_version —— 下面原样传它
```

**B · 问清三件事，逐条复述让他点头**

1. **这套叫什么名**——他自己起（「水墨风」「小清新」都行）。**别跟现有的套名撞**，先拿 A 的清单核一遍。
2. **是图文那套还是文字版那套**——做 AI 配图 = 图文那套；把正文精细排版成竖版文字版 = 文字版那套。
3. **内容从哪儿来**，三选一：

| 他说 | 你走哪条 |
|---|---|
| 「先照默认的建一套，回头再慢慢改」 | 不带 `--from` / `--file`，用内置骨架 |
| 「照我现在这套复制一份改」 | `--from "<已有套名>"`（原样复制改个名，之后按 ① 调） |
| 「按我发的这几张参考图做一套」 | `--file <拆解产物.json>`（参考图拆解由 nbdpsy-xiaohongshu-creator 第 0.5 步做，产物存成 json 走这条） |

> ⛔ **没拍板就不存。** 参考图拆解出来的风格，运营没明确说「存成一套、叫 XX」，就只在本批次生效，
> 别自作主张写进他的档案（沿用"没拍板一律按不更新处理"的口径）。

**C · 动手前先说清楚——判据看 A 里 `--get` 的 `exists`**

⛔ **判据是 `exists`，不是「`--list-profiles` 列出几条」**（第 1.5 步的红线：`exists:false` 时
读到的值不是他的）。

- **`exists: true`（他已经有自己的档案）** —— 这次是**在旁边多加一套**，已有的那几套一个字不动。
  ⛔ 别闷声做，先照说：

  > 「你原来那套风格**一个字都不会变**，这次是在旁边多加一套。以后做图文笔记还是走原来那套。」

- **`exists: false`（他还在跟着默认配置走、名下没有档案）** —— 建这一套会**顺带给他建一份自己的档案**，
  从此他就跟默认配置脱钩了。⛔ **这一步必须先拿到他明确点头**（不是通知一声就动手），照说：

  > 「你现在用的是**全公司的默认配置**、还没有自己的档案。建这一套会顺便**给你建一份属于你自己的**：
  > 你现在用的风格会原样存成**你自己的**，再加上你要的新套；
  > 以后运营老大再改默认配置就**不会再影响你**了。要这样吗？」

  他点头 → **先跑一次 `python3 SP --init-sets`**（把他现在跟着的风格原样建到他名下），
  再**回 A 重跑一次 `--get`**（建完档案那个 `base_version` 就变了，别拿建档前的数往下用），然后建新的那套。
  他说「那我先不建 / 我还是跟着默认的」→ **就停手**，别硬建；想改就先按 ① 走。

确认过了再跑：

```bash
python3 SP --new-profile "<他起的名>" --kind <carousel|typeset> \
  [--from "<已有套名>" | --file /tmp/style-new.json] \
  --base-version <A 读到的 base_version>
```

> ⛔ 命令里那两个英文取值（`carousel` = 图文那套、`typeset` = 文字版那套）**只写在命令里，绝不念给运营听**。

**D · 报喜**

建一套是**新增**，不动他已有的任何一套——**没有 ①D 那种"丢字段"的问题，不用去念"这次会丢掉什么"**。
成功后跟他说清三件事：**现在有哪几套、默认是哪套、做哪种笔记会用到哪套**。

### ⑤ 给风格改个名（顺带：删掉不要的那套）

**改名**（运营说「这套名字不好听」「叫水墨风吧」）：

```bash
python3 SP --get                                            # 拿 base_version
python3 SP --rename-profile "<旧名>" "<新名>"
```

- **只改名字，里头的内容一个字不动**——这句要说给运营听，他才不会以为改名会把风格改掉。
- 新名**别跟现有的套名撞**；拿不准先 `--list-profiles` 看一眼现有的名字。

**删掉一套**（运营说「这套不要了 / 删掉」）：

```bash
python3 SP --delete-profile "<套名>"
```

> 🔴 **删一套是把那套内容整个去掉，动手前把套名逐字复述给他、得到明确确认再执行。**

- **删到只剩一套时会被直接拒绝**（不让他把自己清空）。**这不是出错**，照实说：
  > 「你只剩这一套了，删不了。想换风格就直接改它（我带你改）。」
- **删一套 = 这套的内容连同它的版本历史一起没了**，删完跟他回读一遍删掉的是哪一套。
- 删掉的正好是**默认那套**时，跑一次 `--list-profiles` 看默认落到了哪套、念给他听；不是他要的就按 ⑥ 切一下。

### ⑥ 换默认用哪套

**触发**：运营说「以后默认用文字版那套」「换成我那套水墨风」「默认改成 XX」。

「**默认那套**」= 他没点明要哪种形态时的兜底，也是第 1.5 步回读给他听的那套。

```bash
python3 SP --get                                    # 拿 base_version
python3 SP --set-active "<套名>"
```

- **切默认不改任何一套的内容，随时能切回来**——这句说给他听，他才敢试。
- ⚠️ **做文字版笔记时本来就会自动挑「文字版那套」，不受这个默认影响。** 所以运营抱怨
  「我做文字版老是用错风格」，多半**不是默认设错了**，而是那套本身要调（①）或压根还没建（④）——别顺手切默认了事。
- 🔴 **「默认用哪套」跟运营老大的「默认配置」（③）是两码事，别混**：前者是**他自己**几套风格里挑一套当默认，
  只影响他一个人；后者是**全公司**没建个人档案的人共用的那份，只有运营老大能改。他说「默认」时先问清是哪个。

---

## 第 3.7 步 · 发微信服务号（公众号）

运营说「发公众号 / 发到服务号 / 把这篇长文发到公众号 / 排版成公众号样式 / **给公众号出封面/配图** /
定时发公众号 / 推送给粉丝 / 装修菜单 / 公众号数据」时，**触发 `nbdpsy-fuwuhao-operator` skill**。

**一句话能走完全程**：「写 XX 主题发公众号」= 写长文（查证+合规）→ 改公众号版（标题+行文）→
出封面**先给运营过目** → 以封面为锚点出正文插图（整篇调性统一）→ 排版编译（图自动传微信图床）→
建草稿/发布/定时发布。配图这一步会在流程里主动问运营要不要出，**封面没确认不批量出图**。

运营问「**服务号能干啥 / 这个能不能做 / 为什么不行**」时，让那个 skill 读
`references/capabilities.md` 回答——里面有八项能力和一张**「不能做什么」表**（改已发文章、
加群发次数、定时催达走服务号、催付催填），照着答即可，别自己现编也别硬凑。

逐项操作与红线都在它自己的 SKILL.md 里，本向导只做路标 + 三句必须先说清的话：

1. **凭据不一样**：服务号走 `NBDPSY_WECHAT_API_KEY`（不是小红书那把）。没配就先让管理员在
   「博客 → API Keys → 生成凭据配置包」时**勾选微信服务号权限**，把配置包整段发来一键导入。
   微信的 AppSecret **只在生产服务器上**，不会下发到运营电脑（微信换 token 认 IP 白名单）。
2. **群发是高危动作**：每个自然月**只有 4 次**，发出去**撤不回**、粉丝会收到推送。
   触发群发前必须跟运营逐项确认（发哪篇、发给谁、这个月已用几次），得到明确「发」才动手。
3. **微信没有「改」**：已发布文章改错字只能删了重发，**链接会变、阅读量清零**。
   所以发之前的审稿比发之后的补救重要得多。

**服务号还是一条触达通道**（不只是发文）：已关注 + 完成身份绑定的来访者，会在五类事件发生时
收到服务号消息——预约已确认 / 咨询师回复留言 / 咨询师发来问卷 / 预约取消改期 / 开通正式咨询通道。
⛔ 但**「刚发生的事的回执」之外一律不行**：预约前提醒、催付款、催填问卷这类**定时催达是微信的
合规红线**（《模板消息运营规范》明文禁止，处罚到封号），继续走短信与小程序订阅消息。
运营提这类需求时直接说不行 + 给替代通道，别去试。

---

## 第 4 步 · 带做第一个任务（把话头接住）

介绍完就主动问一句，别让运营悬着：

> 「想先从哪件开始？① 写一篇科普长文　② 做一期完整内容（长文+小红书+视频）　③ 把已有笔记发到小红书
> 　④ 教我装插件/登录账号　⑤ 看某个账号的数据　⑥ 搬运一条 YouTube 视频　⑦ 把一篇长文发到微信服务号。
> 　你说一个，我这就带你做。」

运营选了就**直接触发对应 skill 或跑对应命令**，全程替他执行，只在扫码/出图/浏览器操作这类
必须他本人做的地方一步步指路。做完汇报结果 + 下一步能干啥。

---

## 更新工具包（运营说「更新 / 升级 / 装最新版」时）

> ## ⚠️ 先看这条：撞「工具包版本过旧」的运营，就是没更新
>
> **server 已换成原生多套（2026-07-28）。工具包低于 v1.50.0 的运营，改风格时会撞
> 400「工具包版本过旧，请升级后重试」**——**遇到这个报错不是他操作错了**，也别去改他的档案内容，
> 跑一次 `python3 COMMON update` 就好（更新完提醒他重启 Claude Code）。
> 跟运营就说：「**你这台的工具包旧了，我更一下就好，你的风格和账号都不动。**」

**命令由你替他跑**，运营一个字不用敲——他是非技术兼职人员，别让他开终端、别让他背命令。
更新 = 把 7 个 skill 整目录覆盖成 GitHub 最新版（凭据一概不动）。

> ### ⚠️ 但有一件事必须他点头：更新会改写你自己的行为准则
>
> 这 7 个 skill 里的 `SKILL.md` **就是你（agent）之后办事的指令**。更新 = 从网上拉一份新的下来、
> 覆盖掉现在这份——**拉来的内容会直接变成你的行为方式，中间没有任何人看过**。
> 所以这件事**不能你自己决定**，必须运营明确说"更新吧"才动手。
>
> **怎么问**（一句话，别念术语）：
> 「要把工具包更到最新版吗？会把 7 个技能文件覆盖成 GitHub 上的最新版本——
> 你的账号密钥、风格档案都不会动。更新吗？」
> 他说"更新 / 好 / 可以" → 跑 ①。他没明确答应、或在问别的事 → **先别跑**。
>
> ⛔ **不要为了"省事"跳过这一问**：这道闸挡的是"仓库万一被人推了坏东西，所有装了工具包的机器上
> agent 自动照做"——概率低，但代价是所有人的 agent 一起出问题，而问一句的成本几乎为零。
> **例外**：运营自己开口说的就是「更新工具包 / 升级 / 装最新版」——那本身就是明确同意，直接跑，别再追问一遍。

得到同意后，按 ①→②→③ 的顺序试，顺序不许调换：① 由你执行（绝大多数情况到这就完了），
②③ 是 ① 跑不通时才用、需要运营本人动手的退路。

### ① 首选 · 你直接跑 `COMMON update`

```bash
python3 COMMON update                        # git clone 官方仓库最新版 → 覆盖 ~/.claude/skills 与 ~/.agents/skills
python3 COMMON update --source <本地仓库路径>  # 手上已有仓库副本时（测试/内网）跳过 clone
```

- **为什么用它而不是安装脚本**：它是运营机器上**早就装好的**那个工具（跟你天天跑的 `COMMON doctor`、
  `COMMON secret import` 同一个文件），纯 Python、跨系统一条命令（Windows 把 `python3` 换成 `python`），
  没有 zsh / bash 的行为差异——手写 shell 脚本踩过的坑（见下面 ⛔ 禁令）它一个都不会踩。
  它内部每复制完一个 skill 都会校验 `SKILL.md` 真的落地了，**失败立刻报错退出，不会假装成功**。
- 跑完把 `✓ 工具包 v{旧} → v{新}（{commit}）` 这行念给运营听；stdout 的 JSON 里 `skills` 是本次覆盖的清单、
  `dests` 是装到哪两个目录。**没打这行、或某个 skill 报错退出 = 没更新成功**，别当成功汇报。
- 报 **git 不存在 / clone 失败** → 直接转 ② 或 ③，**别自己另想办法**（见下面 ⛔ 禁令）。
- 本机装的还是**旧版、没有 `update` 子命令**（跑出来是「用法: nbdpsy_common.py workspace | doctor | …」）
  → 同样转 ② 或 ③；装过一次新版之后就有了。

### ② 备选 A · 让运营在他**自己的终端**里跑官方安装命令

把命令发给运营，让他在系统终端（macOS「终端」/ Windows PowerShell）里粘贴执行：

```bash
# Linux / macOS：
curl -fsSL https://raw.githubusercontent.com/Buxiulei/nbdpsy-skills/master/install.sh | bash
# Windows：
powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "irm https://raw.githubusercontent.com/Buxiulei/nbdpsy-skills/master/install.ps1 | iex"
```

⚠️ **这条要麻烦运营本人操作，所以只在 ① 跑不通时用**（他多半不熟终端，你得一步步指路：
在哪儿打开终端、粘哪一行、回车之后看什么）。让他把输出念回来：7 个 skill 逐个打 ✓ 即更新成功。

### ③ 备选 B · 会话里让运营自己敲 `!` 前缀命令

运营嫌开终端麻烦，就让他**在 Claude Code 输入框里自己敲**（开头那个 `!` 不能省——
`!` 前缀 = 这条命令由**运营本人**执行，不是 agent 的工具调用）：

```
! curl -fsSL https://raw.githubusercontent.com/Buxiulei/nbdpsy-skills/master/install.sh | bash
```

仓库已经克隆到本地的，就让他敲 `! bash install.sh`。**你不能替他敲这条**，得他本人发出去才算数。

### ⛔ 禁令 · 绝不手写「等效安装脚本」

安装器跑不起来时，**不许**通读 `install.sh` 之后自己手搓一份「功能一样」的复制脚本。真实事故（2026-07-28，
同事 macOS 机器）：agent 手写的等效脚本里写了 `SKILLS="a b c …"` + `for s in $SKILLS`——

- **zsh 不做无引号变量分词**（bash 会，zsh 不会），7 个 skill 名被当成**一整个路径**；
- 循环体里 `cp` 全部失败，但 **`echo "✓ $s"` 照常打印**——**一个 skill 都没复制，屏幕上却全是 ✓**，
  agent 还向运营汇报「更新成功」；
- 同一个循环里的 `rm -rf "$dest/$s"` 拿着同样那个畸形路径删东西，**只是侥幸目标不存在**才没删错文件。

静默零复制 + 假 ✓ + `rm -rf` 误删风险，一次踩齐。所以：**这条路直接封死**。
万一真到了只能手搓的地步（正常情况下不会），三条硬要求：用 **`bash`**（明确写 `#!/usr/bin/env bash`，
不许在 zsh 下裸跑）、用**数组**（`SKILLS=(a b c)` + `"${SKILLS[@]}"`）、**每复制一个就断言
`[ -f "$dest/$s/SKILL.md" ]`**，断言不过立刻 `exit 1`——**绝不允许在失败路径上打 ✓**。

### 更新完照例交代三句

- 凭据**不会**被更新冲掉：博客/小红书/豆包 key 存在用户级 `secrets.env`（仓库目录外），即梦登录态在 `~/.dreamina_cli/`，全都原样保留。
- 提醒运营**重启一下 Claude Code** 再继续——新版 skill 重启后才生效。
- **插件也会更新**：`python3 PUB --extension-info` 返回的 **`extension_version`**（当前 2.1.3）才是插件
  版本（`version` 字段是服务端版本，别拿它比对）；运营 `chrome://extensions` 显示的版本低于它时，
  让 TA 重新下载 `download_url` 的 zip、解压替换后在 `chrome://extensions` 点「重新加载」（或删旧加载新）。
  凭据填在插件里的 serverUrl/apikey 不受影响。

---

## 常见问题速查（运营一句话 → 你怎么接）

| 运营说 | 你做什么 |
|---|---|
| 「能干啥 / 教我用」 | 第 1 步自检 → 第 2 步介绍 → 第 4 步问想先做哪件 |
| 「怎么发小红书」 | 第 3 步⑤，先确认账号与篇目再发 |
| 「我有哪些号 / 账号能用吗」 | 第 3 步③④（`--self-check` 一把梭） |
| 「怎么登录 / 账号掉线了」 | 第 3 步①②（装插件 + 扫码） |
| 「看看数据 / 哪篇火了」 | 第 3 步⑥（`--notes` + 你来分析） |
| 「改一下定时 / 提前发 / 先别发了 / 撤掉那篇」 | 第 3 步⑦（先 `--list-jobs` 认 job_id，再 `--reschedule`/`--cancel`，仅 pending 有效） |
| 「把 XX 那篇删了 / 发重了清一下」 | 第 3 步⑧（**先把账号+完整标题+删几篇复述给运营、确认后再删**；不可逆） |
| 「删错了能恢复吗」 | **不能，删除不可逆**——所以⑧触发前必须把标题复述给运营确认，宁可多问一句 |
| 「怎么发重了 2–3 篇同文」 | 2026-07-23 已根治（点击被接收绝不补点）；只可能是之前旧事，用⑧清理（`--count`=多余篇数、留 1 篇） |
| 「今天刚发的怎么拉不到数据」 | 正常：当天刚发次日才入看板（第 3 步⑥ `--refresh` 遇 `no_data`），跟运营说「明天再拉，不用管」 |
| 「**做成文字版 / 文字版笔记 / 整篇发别拆 / 正文排版发出去 / 照那个文字版做**」 | 触发 nbdpsy-xiaohongshu-creator 走**路线②**（文字版）：整篇长文精细排版成竖版文字版，**不过 AI 生图**、图里就是正文本身。完整口径在那个 skill 的 `references/longform-typeset-spec.md` |
| 「这篇太长了 / **拆成上中下** / 做成系列」 | 路线② 的**系列篇**（`split_longform.py`）：按章节边界拆成 2000 字左右的上/中/下（或一二三四），每篇独立发、第 2 篇起自动带承接段 |
| 「**末页带上 XX 咨询师** / 这条挂某某老师」 | 路线② 出图时加 `--counselor <工号>`：末页追加一页**针对性推介**（要结合本文主题写标题与文案，不是通用名片）。⚠️ 推介谁是**运营的决定**，没点名就问 |
| 「拆小红书还是做文字版 / 这篇适合哪种」 | 那个 skill 第 0.6 步**开口就问运营要哪种**（图文笔记 5–8 条 / 文字版 1 条）；他说拿不准，再按判据说人话给建议：**有完整论证链或必须整体呈现的表格 → 文字版；能切出 5–8 个互不重叠的痛点场景 → 拆笔记**。**产出形态差很多（1 条 vs 5–8 条），最后一律运营拍板** |
| 「把图片传成链接 / 上传图床 / 复用素材」 | 第 3 步⑨（`--upload-images` 得直链，7 天过期） |
| 「把图发我看看 / 链接呢」 | 最终成图在出图收尾时已上传图床，直接把 mcp 直链列表发 TA（点开即看，7 天有效，过期让 AI 重传）；别甩本地目录/服务器路径 |
| 「我的风格跟别人不一样了 / 想换个调性 / 改一下我的风格」 | 第 3.6 步①（`SP --list-profiles` 看几套 → 有多套先问清改哪一套 → `SP --get`（不是默认那套就 `--get --profile "<套名>"`）拿全量 + `base_version` → 逐项跟他确认 → **同一套整份 PUT** 带 `base_version`；**绝不只发改动字段**；**读哪套就写哪套**） |
| 「回到上个版本 / 上周那版 / 风格改回去」 | 第 3.6 步②（`--versions` 让他挑 → `--version N` 预览 → `--rollback N`；告诉他回退也生成新版本、还能再退回来。**有好几套风格的先问清退哪一套——每套各退各的，别的套不动**） |
| 「**我想要两套风格 / 图文和文字版不一样 / 再建一套**」 | 第 3.6 步④（`--list-profiles` 看现有 → 问清**叫什么名 + 图文那套还是文字版那套 + 内容从哪来** → `--new-profile`）。**动手前先看 `--get` 的 `exists`：`true` 就说明"你原来那套内容一个字不变，这次是多加一套"；`false`（他还在跟默认配置）要换一句、且必须先拿到明确点头——点头后先 `--init-sets` 建齐再加新套，从此默认配置再改也影响不到他** |
| 「**把这套存成一套风格** / 参考图这个风格留着以后用」 | 第 3.6 步④ 的 `--file` 那条（参考图拆解产物存成一套）。⛔ **他没明确说"存、叫什么名"就不存**，只在本批次生效 |
| 「**换成我那套水墨风** / 以后默认用文字版那套」 | 第 3.6 步⑥（`--set-active`）。⚠️ 这是**他自己**"默认用哪套"，跟运营老大的「默认配置」（③）是两码事，先问清他说的是哪个 |
| 「给这套改个名 / 这套不要了删掉」 | 第 3.6 步⑤（`--rename-profile` 只改名不动内容 / `--delete-profile` 删前复述确认）。**删到只剩一套会被拒，这是保护不是报错** |
| 「我有几套风格 / 我都有哪些风格」 | 第 1.5 步的 `SP --list-profiles`，把**套名 + 默认哪套**念给他；⛔ 英文形态取值别念，说「图文那套 / 文字版那套」 |
| 「我用的是默认配置，以后会不会变 / 我的风格会被人改掉吗」 | 第 3.6 步③：**用默认配置的会跟着实时变；建了自己的就完全不受影响**（想稳住就走第 3.6 步①建一版） |
| 「更新默认配置 / 把默认风格改成… / 以后新人都用这套」 | 第 3.6 步③（**仅运营老大**，`SP --admin-default` 整份覆盖；先讲清波及面 + 改前留底） |
| 「新人的风格是哪来的 / 我改了默认配置谁会变」 | 第 3.6 步③：新人用的是**默认配置**；改它 → **所有没建个人档案的人实时跟着变**（不只新建的），已建档案的不受影响 |
| 「搬运这个 YouTube / 把这条油管翻译配音」 | 第 3.5 步（`TV --url <链接>`，只收 YouTube） |
| 「这条视频 XX 处改一下 / 再改改」 | 第 3.5 步 revise（`TV --revise <任务号> --instructions "原话"`，仅 remake 成片可修订、可反复迭代） |
| 「**发公众号 / 发到服务号 / 排版成公众号样式 / 定时发公众号**」 | 第 3.7 步（触发 nbdpsy-fuwuhao-operator；先确认配了 `NBDPSY_WECHAT_API_KEY`，那是跟小红书不同的另一把 key） |
| 「**给粉丝群发 / 推送给粉丝**」 | 第 3.7 步的高危动作：**每自然月只有 4 次、发出去撤不回**——发哪篇/发给谁/这月已用几次，逐项确认后才动手 |
| 「公众号那篇改个错字 / 删掉重发」 | 微信**没有「改」**：只能删了重发，**链接会变、阅读量清零**；先把这句告诉运营再问要不要做 |
| 「**服务号能干啥 / 这个能不能做 / 为什么不行**」 | 第 3.7 步：让 fuwuhao skill 读 `references/capabilities.md` 照答（八项能力 + 一张「不能做什么」表），别现编 |
| 「**给公众号出个封面 / 配图**」 | 第 3.7 步：与小红书同一套生图服务，出横版；**先出封面给运营确认再批量出插图**（没确认就批量 = 风格跑偏全废） |
| 「**预约前提醒/催付款也走服务号吧**」 | ⛔ 合规红线，微信明文禁止定时催达（罚到封号）。服务号只做「刚发生的事的回执」；提醒继续走短信与小程序订阅消息 |
| 「提示缺 key / 连不上」 | 第 1 步的两个兜底（要接入包 / sandbox allow） |
| 「改风格时报『**工具包版本过旧，请升级后重试**』」 | **不是他操作错了**，是他这台的工具包旧了（server 已换成原生多套）：跑 `python3 COMMON update`（见「更新工具包」节开头那条），完了提醒重启 Claude Code，再重来一遍刚才的改动。⛔ 别去动他的档案内容"绕开"这个报错 |
| 「更新工具包 / 升级 / 装最新版」 | 上面「更新工具包」节：运营这么开口本身就是同意，**直接 `python3 COMMON update`**（①）；跑不通再按 ②③ 退（要他本人动手）。完了提醒重启 Claude Code。⛔ 绝不手写等效安装脚本 |
| 「插件怎么用 / 帮助在哪」 | 让 TA 点插件弹窗底部「**使用帮助**」（2.1.3 起内置离线指南：配置/扫码/账号状态/FAQ） |
| 「插件是不是旧版」 | 比对 `--extension-info` 的 `extension_version` ↔ TA 的 chrome://extensions 显示版本；旧了按「更新工具包」节换新包 |
| 「装不了 / 你说没有本机执行能力」 | 提醒他用 **Claude Desktop 的「Code」标签页**，别用 Chat/网页/手机版 |
