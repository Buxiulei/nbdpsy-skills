# 交接：即梦视频生成已服务化（nbdpsy-server）——skill 侧开发指南

> 写入方：nbdpsy-server 侧会话（2026-08-05，server commit `4e6459e` 已部署 `mcp.nbdpsy.com`，
> skill 侧 `jimeng_gen.py` 双后端 v1.60.0→v1.61.3 已同步接好并实测切换）。
> 本文是给 **nbdpsy-text-to-video 维护会话**的开发交接：契约参考 + 金钱语义 + 待开发项。
> 事实源优先级：`GET /api/manifest`（机器可读，随部署更新）> 本文 > 记忆。

## 一、总览：什么搬走了，什么没动

十步产线里只有第 5–6 步（即梦 submit/fetch）搬进了 nbdpsy-server。分镜/TTS/BGM/ffmpeg
合成/审查全部原样在 skill 侧。`jimeng_gen.py` 已是双后端薄客户端：

```
auto 探测（默认，进程内一次）：
  有 NBDPSY_XHS_API_KEY → GET /api/dreamina-status
    200 且 logged_in=true  → server 后端（运营免装 CLI、免扫码、排队不挂会话）
    其他（404/未登录/网络） → 本机 dreamina CLI 兜底（行为与服务化前逐字一致）
强制：--backend server|local / 环境变量 NBDPSY_JIMENG_BACKEND
```

五个子命令（credits/gen/submit/fetch/batch）stdout JSON 契约不变，只增键
（`backend`/`client_ref`/`video_url`/`expires_at`/`batch_id` 等）。**运营与上层 skill 文档
（SKILL.md 十步流程）零改动**——第 5-6 步的命令行一个字没变，变的只是背后走哪条路。

## 二、server 端点契约速查（详见 /api/manifest）

鉴权一律 `Authorization: Bearer $NBDPSY_XHS_API_KEY`（与小红书发布同一把，无新 scope），
base = `video_api_base()`（默认 `https://mcp.nbdpsy.com`）。

| 端点 | 要点 |
|---|---|
| `POST /api/video-clips` | 202 `{clip_id, warning?}`；`client_ref` 命中时 `{clip_id, status, reused:true}` 零新建。`image` 只收 http(s) 直链或裸 `/uploads/...` 路径（内网地址一律拒，SSRF 闸含重定向逐跳）。image2video 带 `ratio` → 422 |
| `GET /api/video-clips/{clip_id}` | `status: queued\|submitted\|querying\|done\|error`；`last_poll_error`（查询侧异常，**与任务失败分列**——非终态）；`queued_seconds` 在飞时如实累计；done 后 `video_url`（拼 base 即免鉴权直链）+ `credit_count` + `expires_at`（TTL 14 天）；TTL 清理后 `expired:true` 且 status 仍是 done（**别按失败处理**） |
| `POST /api/video-clip-batches` | `{shots:[...]}` ≤50 → 202 `{batch_id, clip_ids[]}` 与 shots 等长同序；逐镜独立不连坐；纯重放批 `batch_id` 可为 null（定位以 clip_ids 为准） |
| `GET /api/video-clip-batches/{batch_id}` | 按批内序逐镜 + summary |
| `GET /api/video-credits` | `{credit, low_threshold_hit, logged_in}` |
| `GET /api/dreamina-status` | `{logged_in, credit, compliance_confirmed_models, error}`（合规列表=有 done 记录的模型，观测近似） |

错误形态：400/401/403/404 键是 `error`；409/422 键是 `detail`——`_api_error()` 已双键兼容。

## 三、金钱语义（改任何相关代码前先读这节）

即梦的成本模型：**submit 即占队列位、success 即扣积分、排队中无法取消**。两端代码里所有
“看起来绕”的设计都是为这个服务的，改动前对照：

1. **submit 每镜恰好一次**。server 侧歧义结局（CLI 超时/无 submit_id）判 error 并写明
   「是否已入队未知」，**绝不自动重提**；重提是运营决策（换 `client_ref` 新 POST，通常换
   fast_vip 抢队列）。skill 侧同理没有任何自动重跑。
2. **`client_ref` 幂等键**（uuid4，每次 submit 生成一次）：网络异常重发**必须复用同一 ref**
   ——单镜与批量都已开启重发（批量的逐镜去重 2026-08-05 已活体验收：同 refs 重放回原
   clip_ids 零新增零扣分）。**重新生成 ref = 新任务 = 双倍扣分**，测试
   `test_batch_post_network_error_resends_once_with_same_refs` 钉死了这条。
3. **重放不过闸**：server 端登录闸/积分闸只对真要新建的镜生效——低积分/掉登录时重放
   照样返回原 clip_id（否则「响应丢失→重发被 409→运营重跑」就是双倍烧钱链）。
4. **复活语义**：从未碰过即梦队列的失败行（参考图物化失败，`submit_id` 与 `submitted_at`
   均空）同 ref 重放会原地复活重试（同 clip_id）；碰过队列的行绝不复活。
5. **fetch 超时/查询失败 ≠ 任务失败**：`last_poll_error` 与 `error` 分列；skill 侧超时话术
   保留 submit_id 引导稍后 fetch，绝不引导重跑 gen。
6. fetch 的 auto 分派：先免费问 server 认领（GET 一次），不认领才按 16-hex 形态兜底
   ——`clip_id` 形态已钉死 `vc_`+10hex 不会与本机 submit_id 碰撞。

## 四、模型面（2026-08-05 起默认 seedance2.5）

CLI a857341 起模型全集：`seedance2.0 / 2.0fast / 2.0fast_vip / 2.0_vip / 2.0mini / 2.5`。

| 模型 | 时长 | 分辨率 | 积分(5s 实测) | 说明 |
|---|---|---|---|---|
| **seedance2.5（默认）** | **4–30s** | 480p/720p（我们统一 720p） | **130（已实测）** | VIP-only；**秒级出片无排队**（2026-08-05 生产实测 vc_3e1260f8ce，maestro 会员，未撞合规墙）；注意其 submit_id 是 **UUID 形态**非 16-hex（不影响任何分派逻辑——clip 级判据用 `vc_` 前缀，仅记档） |
| seedance2.0fast | 4–15s | 720p | 25 | 排队长（实测队列可 15h+），夜间错峰用 |
| seedance2.0fast_vip | 4–15s | 720p | 55 | 加速通道 ~3min 出片，急用/投放首选 |
| seedance2.0 / 2.0_vip / 2.0mini | 4–15s | 720p（_vip 另有 1080p/4k） | 未实测 | |

时长校验两端都按模型分档（非 2.5 传 >15s 会被 422/本地校验拦下，文案指明「仅 seedance2.5
支持到 30s」）。

## 五、登录态运维（⚠️ 2026-08-05 CLI 升级是 breaking change）

- CLI `a857341` 起登录换成 **OAuth Device Flow**：`dreamina login` 打印 `verification_uri`
  ——**任何设备打开授权即可**，不再有 127.0.0.1 回调死结（skill 里 `dreamina_login.py` 的
  find_login_port 那套已过时，对新 CLI 无效，待重写为 device flow：起 login → 抓
  verification_uri → 给用户开页/发链接 → 等进程退出判成败；码约 10 分钟过期）。
- 新凭据在 `~/.local/share/dreamina/byted_cli_user_token.json`（**不再是**
  `~/.dreamina_cli/credential.json`）——升级 CLI 会导致登录态一次性失效，需重扫。
- server 侧登录失效时的降级链（已实测）：`/api/dreamina-status.logged_in=false` → skill
  auto 探测回落本机 CLI；server 的 POST 明确 503；在飞任务只记 `last_poll_error` 不判死，
  重新登录后自动恢复轮询，submit_id 不丢。

## 六、skill 侧代码地图（v1.61.3）

`jimeng_gen.py` 关键位置：顶部 `EP_*/K_*/SRV_*` 常量块（server 字段唯一适配点）；
`probe_server/resolve_backend`（auto 探测，进程内缓存）；`_post_idempotent`（唯一提交出口，
网络异常复用 ref 重发一次，4xx/5xx 绝不重发）；`_server_fetch`（轮询+`.part` 原子下载+
非 JSON 计瞬时失败）；`_shot_server_capable`（本机媒体整镜回落 local 的判据）。
测试 `tests/test_jimeng_gen.py` 全离线（伪 requests 注入），64+ 用例，改代码必跑全量：
`cd nbdpsy-skills && <主项目venv>/python -m pytest tests/ -q`（显式取退出码）。

## 七、待开发（按优先级）

1. **image2video 摆脱本机回落**（主力图生流程目前 server 模式下整镜回落本机 CLI）：
   分镜 2.5 步收的参考图在运营本机 → 改为先 `POST /api/uploads/images`（multipart，与
   gen_images.py 同图床）拿 `/uploads/...` 直链，再作为 `image` 提交 server。
   改动点：`_shot_server_capable` 对本机路径不再回落而是走「先传后提交」；上传失败才回落。
   注意图床 7 天过期 vs clip 独立生命周期——server 收到后会**复制**进 clip 目录，无需担心。
2. **`dreamina_login.py` 重写为 device flow**（见第五节）——本机 CLI 兜底路径还需要它。
3. ~~seedance2.5 首跑回填~~：已完成（130/5s，见第四节表格；server 价格表与 SKILL.md 均已回填）。
4. `check_env.py` 无需改动（server 模式检查已自动生效）。

## 八、事实源与验证记录

- 机器可读契约：`GET https://mcp.nbdpsy.com/api/manifest`
- server：`4e6459e`（86 项离线测试 + 生产 E2E：fast_vip 全链 ~2.5min 出片、直链 200、
  credit_count=55 对账精确；验收 8 条全过）；回执
  《NBDpsy/文档/2026-08-05-server回执-即梦视频生成服务化.md》
- skill：v1.60.0（双后端）/ v1.61.3（批量重发开启）
- 已知观察项：验收期间公司号多扣 25 积分来源未明（疑网页版人工消耗），clip 级对账精确

—— 联调/回归有任何不符合预期，在下方反馈区写现象 + clip_id，server 侧会话会来读。

## 反馈区（nbdpsy-skills 侧追加）
