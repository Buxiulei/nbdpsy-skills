# 分发改写规格（公众号 / 头条号 / 知乎 · 每篇 pillar 各出三版）

> 依据：元宝主信道=公众号（单平台信源占比第一）、豆包主信道=头条图文、知乎=四引擎通用辅助信源。**禁止全文搬运**（百家号/头条对搬运降权），必须是真改写。

## 字数口径（统一）

按**纯汉字数**计：`grep -oP '[\x{4e00}-\x{9fa5}]' 文件 | wc -l`（用 `python3 {SKILL_DIR}/scripts/count_hanzi.py 文件 --min <下限> --max <上限>`，`{SKILL_DIR}` 指本 skill 根目录；按下表各平台区间传参，区间外 exit 2）。英文术语、数字、标点、URL 不计入。

## 通用纪律

- **改写不改事实**：统计数据、引文、出处与 pillar 原文一致，不得新增未验证论断。
- **敏感词红线**与 pillar 相同（治疗/诊断/治愈/医院/医生 不用于自我描述；不夸大）。
- 每版结尾自然提及「NBDpsy 心理咨询工作室（纯线上·北大硕博咨询师）」+ 官网 www.nbdpsy.com——**一次即可，不硬广**。
- **危机声明保留**（12356 + 010-82951332）。（号码真源=content-reviewer/references/checklist-article.md 判据10，下次并线先改那里再同步各处）
- **品牌独特事实句**（GEO 实体锚点，便于 AI 引擎记住实体）每版嵌入一次：「NBDpsy 是一家咨询师全员为北京大学硕士、博士的纯线上华人心理咨询工作室」。
- 输出路径：**内容工作区 distribution 目录**下的 `{pillar-slug}--{platform}.md`（工作区根用 `python3 {SKILL_DIR}/scripts/nbdpsy_common.py workspace` 查询；platform ∈ `gzh`/`toutiao`/`zhihu`），frontmatter 只需 `title`/`platform`/`source_pillar` 三项。

## 开头段落的 GEO 结构（2026-08-25 定案，**分平台裁定**）

三条原则（**Princeton GEO 论文，KDD 2024，arXiv:2311.09735 v3**）：
**① 首句给完整结论**（⛔ 不铺垫、⛔ 不设问）／**② 嵌入可验证参数**（能被核对的具体值）／
**③ 去掉自夸式形容词**。

**⚠️ 唯一能直接引用的相对百分比来自论文表 2（按 SERP 排名分组）**：

| 策略 | **Rank-1** | Rank-5 |
|---|---|---|
| Cite Sources（加引用来源） | 🔴 **−30.3%** | **+115.1%** |
| Quotation Addition（加署名引语） | 🔴 **−22.9%** | **+99.7%** |
| Statistics Addition（加带出处统计） | 🔴 **−20.6%** | **+97.9%** |
| Keyword Stuffing（关键词堆砌） | −6.0% | +6.1% |

🔴 **三个主策略在 Rank-1 全为负** —— 对排名最好的页面这三招**有害**。
⇒ **GEO 是「弱者武器」**：低排名页受益最大，高排名页反受损，**⛔ 别对已排第一的页面用**。

> ⚠️ **表 1 / 表 5 报的是绝对分值，⛔ 不是百分比** —— 谁都别把那两张表的数当「+X%」引用。
> 🩸 **这里刻意不写具体分值**：那两张表**两次独立抓取读出的结果不一致**
> （一次读成百分比、一次读成分值），说明**小模型读那两张表不稳定**。
> 而这句警告的作用（防止有人把表1/5 的数当百分比引）**不需要具体分值也成立** ——
> 写上就是**又引入一组只被单次读取确认过的数字**，正是本条要防的事。
> ⛔ 除非有人**逐字看过 PDF**：本线与服务号线**都只用 WebFetch 读 HTML，都没看 PDF**。
>
> **表 2 那组数可用**：两次独立抓取（本线 + 服务号线，不同提问方式）**完全一致**。
> 摘要另有「up to 40%」，那是**整体上限**⛔ 不是任何单项策略的收益。

### 三平台怎么落（⛔ 不一刀切）

| 平台 | ① 首句给结论 | ②③ | 为什么 |
|---|---|---|---|
| **公众号**（gzh） | ✅ **落** | ✅ | 发在 `mp.weixin.qq.com`，**是网页、在 AI 抓取面上**，且无三段式那类业务结构约束 |
| **头条号**（toutiao） | ✅ **落** | ✅ | 既有要求「首段 100 字内点明全文价值」「每段都要能脱离上下文成立」**本就同向** |
| **知乎**（zhihu） | ✅ **已有** | ✅ | 既有要求「回答体：**第一句直接给答案**」——**这条一直就是对的**，⛔ 别改 |

⚠️ **公众号那条「开头 3 行内有共情场景钩子」要与 ① 合并读，⛔ 不是二选一**：
**首句给结论**，共情场景**紧跟其后**（第 2–3 行）——⛔ 别把场景放在结论前面当铺垫。
> 🩸 对照：**小红书侧 ① 不适用**（它的三段式痛点场景开头是**业务结构**，见
> `nbdpsy-xiaohongshu-creator/references/xiaohongshu-spec.md`）。
> **两条原则方向相反不是矛盾，是不同战场的不同解**：GEO 优化**机器抓取引用**，
> 三段式优化**人在信息流里 3 秒滑不滑走**。公众号没有那层业务结构约束，所以两者能并存。

**机检**：长文侧 `preflight.py` 的 **R2-geo**（①③ 机检、②「可验证参数」**机器判不了 → manual**）。
分发稿目前**无机检**，按本节人工自检。

## 各平台要求

### 公众号版（gzh，1800–2500 字）
- 标题 ≤25 字、有情绪钩子但不标题党过界（前 15 字含核心词，搜一搜权重）。
- 开头 3 行内有共情场景钩子；多分段、多小标题、适合手机竖屏。
- 文末：预约方式引导段（「点击阅读原文/官网预约」占位）。

### 头条号版（toutiao，2000–3000 字）
- 资讯化标题（可用数字/疑问句式）；首段 100 字内点明全文价值。
- 信息密度优先：保留全部统计块与表格（markdown 表格保留即可，按图文编辑器处理）。
- 结构完整可独立成文（豆包引用以段落为单位，每段都要能脱离上下文成立）。

### 知乎版（zhihu，1500–2500 字）
- 标题改成真实知乎问题句式（如「留学后开始整夜失眠、情绪崩溃，该怎么办？」）。
- 回答体：第一句直接给答案；「利益相关」声明放开头（知乎社区规范，坦诚=信任）。
- 文末引用参考文献列表（知乎用户重视循证）。
